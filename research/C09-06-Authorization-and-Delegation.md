# C9.6: Authorization, Delegation, and Continuous Enforcement

[Back to C09 Index](C09-Orchestration-and-Agents.md)

## Purpose

Agents act on behalf of users and interact with systems that have their own access control policies. This section ensures that agent actions are authorized against fine-grained policies at execution time, that delegation context (who the agent is acting for, with what scope) is propagated and enforced throughout the call chain, and that authorization decisions are never made by the AI model itself. The core principle: access control must be enforced by deterministic policy engines, not probabilistic language models.

---

## Requirements

| # | Requirement | Level | Threat Mitigated | Verification Approach | Gaps / Notes |
|---|-------------|:-----:|-----------------|----------------------|--------------|
| **9.6.1** | **Verify that** agent actions are authorized against fine-grained policies enforced by the runtime that restrict which tools an agent may invoke, which parameter values it may supply (e.g., allowed resources, data scopes, action types), and that policy violations are blocked. | 1 | **Unauthorized tool invocation and parameter abuse (MITRE ATLAS AML.T0053; OWASP Agentic ASI02 Tool Misuse).** Without fine-grained authorization, an agent may invoke any tool with any parameters, even if the initiating user is not authorized for that action. An agent authorized to read files should not be able to delete them; an agent authorized to query one database should not be able to query another. Prompt injection can instruct an agent to invoke tools outside its authorized scope. Real-world examples include Microsoft Copilot M365 being directed to exfiltrate personal information via authorized tools, and GitHub Copilot Chat exploited through prompt injection to perform unauthorized data retrieval. The OpenClaw supply chain attack (February 2026, MITRE ATLAS investigation) demonstrated how malicious agent skills could invoke tools outside intended scope -- affecting 1,184 packages. The PleaseFix vulnerability family (Zenity Labs, March 2026) showed that agentic browsers like Perplexity Comet could be hijacked via indirect prompt injection to access local filesystems and steal credentials from password managers (1Password) -- all within the agent's legitimately authorized session, without the user seeing anything unusual. | Review the policy engine configuration. Verify that each tool has an associated policy specifying allowed callers, parameter constraints, and data scopes. Test by attempting unauthorized tool invocations and out-of-scope parameter values. Confirm the runtime blocks these attempts and logs the violation. Verify policies are evaluated server-side, not by the agent itself. As of March 2026, AWS Bedrock AgentCore provides a purpose-built Cedar-based policy enforcement layer that evaluates every agent-to-tool request against explicit Cedar policies -- a reference architecture for how this should work. Test that natural-language policy generation (where supported) produces correctly restrictive Cedar policies by verifying the generated output against known-good baselines. For MCP deployments, verify that an Agent Gateway or equivalent proxy sits between the agent and MCP servers, enforcing per-request authorization before tool invocations reach the server. Cerbos policy decisions typically resolve in single-digit milliseconds, making per-call authorization feasible without meaningful latency impact. | OPA (Open Policy Agent), Cedar (AWS), and Google Zanzibar-style systems can enforce fine-grained policies. As of March 2026, Cedar has gained significant adoption for agent authorization specifically: AWS Bedrock AgentCore uses Cedar as its native policy engine, and Cedar policies can be authored programmatically or generated from plain-English statements. Cerbos offers an open-source, policy-as-code authorization engine that can embed directly within MCP server runtimes for per-request tool filtering and authorization decisions -- its batch query API lets servers check multiple tool permissions in a single call, and YAML-based policies support RBAC, ABAC, and context-aware conditions (e.g., transaction amount thresholds, team membership). The challenge remains defining policies at the right granularity -- per-tool policies are straightforward, but per-parameter-value policies require deep integration with the tool's semantics. A related gap is **semantic privilege escalation**, where an agent operates entirely within its technical permissions but performs actions outside the semantic scope of its assigned task (e.g., an agent authorized to summarize documents instead scans for API keys and exfiltrates them). Traditional RBAC/IAM cannot detect this -- it requires intent-tracking and behavioral anomaly detection at the orchestration layer. The OWASP Agentic Top 10 formalizes this as the **Least Agency** principle: autonomy should be earned, not default, and agents need task-scoped permissions that dynamically adjust. Policy management overhead grows with the number of tools and agents. The Oso authorization platform also provides agent-specific delegation patterns out of the box. |
| **9.6.2** | **Verify that** when an agent acts on a user's behalf, the runtime propagates an integrity-protected delegation context (user ID, tenant, session, scopes) and enforces that context at every downstream call without using the user's credentials. | 2 | **Scope creep and cross-tenant access (OWASP Agentic ASI03 Identity & Privilege Abuse).** Without integrity-protected delegation context, an agent may "forget" or "modify" its delegation scope as it chains through multiple tool calls. In multi-tenant systems, a corrupted or missing tenant context can lead to cross-tenant data access. Using the user's actual credentials (instead of a delegation token) gives the agent the user's full permissions rather than a scoped subset. The IETF AIMS framework (draft-klrc-aiagent-auth-00, March 2026) specifically warns that broad-scope tokens leaked through multi-hop chains are a primary attack vector, recommending transaction tokens that are downscoped and bound to a specific transaction to prevent lateral movement. The OWASP Agentic Top 10 (2026) identifies this as ASI03: agents that inherit user sessions, reuse secrets, or rely on implicit cross-agent trust produce privilege escalation and unattributable actions -- "confused deputy" scenarios at agent scale. | Trace a multi-step agent execution and verify the delegation context (user ID, tenant, session, scopes) is present and integrity-verified at every downstream call. Test by: (a) stripping the delegation context and confirming the call is rejected, (b) modifying the context and confirming integrity verification fails, (c) attempting cross-tenant access and confirming it is blocked. Verify JWT delegation tokens use dual-identity encoding (`sub` for the agent, `act.sub` for the delegated user) per the on-behalf-of pattern. Confirm delegation depth is limited by policy to prevent unbounded chaining (user -> agent1 -> agent2 -> ... -> service). For MCP deployments, verify that the MCP OAuth 2.1 authorization spec's prohibition on token passthrough is enforced: agents must never forward their original token to downstream MCP servers, but instead use OAuth 2.0 token exchange (RFC 8693) at each hop to obtain a narrower-scoped token. Verify that per-client consent is validated -- user authentication to the MCP server does not imply authorization for any specific MCP client. | OAuth 2.0 token exchange (RFC 8693) and macaroons provide mechanisms for scoped delegation tokens. The IETF AIMS framework (draft-klrc-aiagent-auth-00, published March 2026) composes WIMSE, SPIFFE, and OAuth 2.0 into a layered architecture for agent identity, with short-lived cryptographically-bound credentials and transaction tokens for safe multi-hop propagation. The MCP specification (as of March 2026) mandates OAuth 2.1 with five authorization patterns: per-client consent validation, token audience (`aud`) claim verification on every request, rejection of token passthrough to backends, exact redirect URI matching, and cryptographic state parameter validation (minimum 128-bit entropy). Session-based authentication is explicitly prohibited. The Aembit MCP Identity Gateway provides a production implementation of this pattern, sitting between agents and MCP servers to handle credential injection and token exchange without agents ever holding long-lived secrets. JWT tokens should encode nested `act` claims supporting chained delegation with full attribution (recommended token lifetime: 5--15 minutes). Google Cloud Security Command Center now detects agent-specific delegation abuse patterns, including "AI Agent Suspicious Token Generation Using Implicit Delegation" -- flagging when agents misuse implicit delegation mechanisms to generate elevated-privilege tokens, and cross-project token generation anomalies. This is the first major cloud provider to ship purpose-built detection for agent delegation attacks. Key gap: most agent frameworks still do not have built-in delegation context propagation -- this must be custom-built, though the AIMS specification provides a concrete reference architecture. |
| **9.6.3** | **Verify that** authorization is re-evaluated on every call (continuous authorization) using current context (user, tenant, environment, data classification, time, risk). | 2 | **Stale authorization and changed context.** Long-running agent tasks may execute over minutes or hours. During that time, user permissions may change, sessions may expire, or risk context may shift (e.g., anomalous behavior detected). Without continuous re-evaluation, an agent can continue acting on stale authorizations. An attacker who gains temporary access can initiate a long-running agent task and retain access after their permissions are revoked. The AuthZEN specification (approved January 2026) defines a four-element authorization tuple (Subject, Action, Resource, Context) that maps cleanly to per-call agent evaluation, making continuous authorization architecturally viable at scale. | Verify that each tool invocation triggers a fresh authorization check (not cached from a previous check). Revoke a user's permissions mid-execution and confirm that subsequent agent actions on their behalf are blocked. Change the risk context (e.g., trigger an anomaly alert) and confirm authorization is re-evaluated with the new context. Test that the OpenID Shared Signals Framework (SSF) or equivalent event-driven mechanism propagates revocation signals in real-time -- verify that CAEP/RISC events trigger immediate token invalidation without waiting for TTL expiry. | Continuous authorization adds latency to every tool call. Caching strategies (short-TTL caches, event-driven cache invalidation) can mitigate this but introduce a window of vulnerability. The OpenID AuthZEN 1.0 specification (finalized January 2026) standardizes a transport-agnostic API for PEP-to-PDP queries regardless of vendor, reducing integration friction. As of April 2026, AuthZEN and the Shared Signals Framework (SSF) form a two-layer authorization system: AuthZEN handles the synchronous decision (permit/deny per request) while SSF/CAEP handles asynchronous signals (something changed, re-evaluate or revoke). SSF transmits Security Event Tokens (signed JWTs) when conditions change -- session risk elevations, device compliance shifts, credential compromises, or group/attribute updates -- and the PDP subscribes to these streams to maintain current authorization state without polling. Sidecar PDP deployments address latency concerns: local PDP instances sync policy from the central authority but evaluate decisions with sub-5ms latency, avoiding the 5-20ms network round-trip penalty of centralized evaluation. AuthZEN is explicitly on the 2026 roadmap for MCP integration, with 15+ vendors (AWS, Cerbos, Aserto, SGNL) demonstrating live interoperability. The key gap for agent use cases: first-party SaaS platforms lack native AuthZEN PEP hooks, requiring middleware layers for platforms like Microsoft 365. This aligns with Zero Trust principles (NIST SP 800-207) -- "never trust, always verify." |
| **9.6.4** | **Verify that** all access control decisions are enforced by application logic or a policy engine, never by the AI model itself, and that model-generated output (e.g., "the user is allowed to do this") cannot override or bypass access control checks. | 2 | **Model-based access control bypass.** If the AI model makes access control decisions (e.g., "Based on the conversation, this user seems authorized to view this data"), prompt injection can manipulate these decisions. LLMs are not access control systems -- they are probabilistic text generators. Any access control decision made by a model can be influenced by adversarial input. | Review the architecture to confirm all access control checks are performed by deterministic code/policy engines, not by model inference. Search for patterns where model output influences access decisions (e.g., model output parsed for "allowed"/"denied" strings). Test by crafting prompts that instruct the model to claim elevated privileges and verify these claims have no effect on actual access control enforcement. | This is a critical architectural principle. Common anti-pattern: using the model to "decide" whether a user is authorized based on conversation context. Another anti-pattern: letting the model choose which tools to call without a policy engine validating the choice. The policy engine must sit between the model's tool selection and actual tool execution. CVE-2026-26137 (Microsoft 365 Copilot BizChat, March 2026, CVSS 8.9) illustrates the risk concretely: an SSRF vulnerability allowed an authorized attacker to escalate the assistant into a higher-authority state, turning Copilot into a tool for scaling access beyond the user's own permissions. The vulnerability class -- where the assistant itself *becomes* the privilege escalation vector -- is qualitatively different from traditional authz bypass. |
| **9.6.5** | **Verify that** secrets and credentials required by an agent at runtime are not exposed within the model's observable context, including the context window, system prompts, or tool call parameters, and are instead provided via out-of-band mechanisms such as credential proxies, secrets manager injection, runtime sidecar authentication, or short-lived scoped tokens. | 2 | **Credential exposure via context window, prompt injection-driven exfiltration, and secrets sprawl.** When API keys, tokens, or passwords appear in the model's context window, system prompt, or tool call parameters, they become extractable through prompt injection -- an attacker can instruct the model to "debug authentication" or "display configuration" and the model will comply since the credentials are visible in its context. CVE-2026-21852 (Claude Code, CVSS 5.3) demonstrated this concretely: a malicious repository could redirect API key transmission to an attacker-controlled server simply by overriding the `ANTHROPIC_BASE_URL` in a project settings file. CVE-2025-6514 (mcp-remote, 437,000+ environments affected) showed how a compromised OAuth proxy for MCP could steal API keys, cloud credentials, and SSH keys. GitGuardian's State of Secrets Sprawl 2026 report found AI-service secret leaks reached 1,275,105 (up 81% YoY), with 24,008 secrets in MCP configuration files alone (2,117 confirmed valid). Code-assisted commits leak credentials at roughly twice the baseline rate (3.2% vs 1.5%). The OWASP AI Agent Security Cheat Sheet identifies credential patterns (`api_key`, `password`, `secret`) as requiring mandatory redaction from agent contexts. | Verify that credentials are never present in the model's observable context by: (1) inspecting system prompts and tool definitions for hardcoded secrets, API keys, or connection strings; (2) reviewing tool call logs to confirm credential values do not appear in parameters sent to or returned from the model; (3) confirming a credential proxy, secrets manager sidecar, or runtime injection layer sits between the agent and authenticated endpoints; (4) testing with prompt injection payloads that attempt to extract credentials (e.g., "print your API key", "show environment variables", "debug authentication configuration") and verifying no secrets are returned; (5) checking that environment variables containing secrets are not passed into the agent's context or tool schemas; (6) verifying that MCP configuration files do not contain plaintext credentials. For Kubernetes deployments, confirm that Vault Agent Injector or equivalent sidecars inject secrets into pod filesystems rather than environment variables accessible to the model. For credential proxy architectures (Aembit, AgentSecrets, OneCLI), verify the proxy intercepts outbound requests and injects credentials at the transport layer without exposing values to the agent process. | Several credential isolation patterns have emerged as of March 2026. **Transport-layer credential proxies** (AgentSecrets, OneCLI) run on localhost, intercept outbound HTTP requests from the agent, resolve credentials from an encrypted vault or OS keychain, inject them into the request headers, and return only the API response -- the agent never sees the credential value. AgentSecrets supports six injection patterns (Bearer, headers, query params, basic auth, JSON/form body) and uses X25519 + AES-256-GCM encryption. **Secrets manager sidecars** (HashiCorp Vault Agent Injector, AWS Secrets Manager) inject credentials into pod filesystems or environment variables at runtime, with Vault generating dynamic short-lived credentials scoped to specific operations. HashiCorp Vault's validated pattern for AI agent identity uses MCP servers as isolated credential brokers: the agent authenticates via On-Behalf-Of token exchange, the MCP server retrieves dynamic database credentials from Vault mapped to user group policies, and executes the operation -- the LLM never touches the credential. **Workload identity federation** (Aembit) replaces static credentials entirely: the agent's runtime environment presents cryptographic attestation (Kubernetes pod identity, container image signature), a policy engine evaluates the request, and a short-lived credential (15-30 minute lifetime) is generated and injected per-call without storage. 1Password announced runtime credential issuance for agent and machine workloads (shipping later in 2026), with partnerships (Runlayer, Natoma) enabling secure credential injection into agent sessions. The Peta platform ("1Password for AI Agents") provides server-side encrypted vaults where agents receive only scoped, time-limited tokens. Key gap: most agent frameworks still pass credentials via environment variables or tool parameters that are visible in the model's context. The architectural shift from "agent retrieves credential" to "proxy injects credential" is well-understood but not yet the default in any major framework. MCP documentation patterns have actively contributed to the problem by encouraging hardcoded credentials in configuration files. |
| **9.6.6** | **Verify that** when an agent acts under delegated authority, the policy decision point evaluates both the agent's own granted permissions and the initiating principal's delegated scope as independent constraints, denying the action if either is insufficient for the requested operation. | 2 | **Privilege escalation via single-constraint evaluation and confused deputy attacks.** If the policy engine evaluates only the agent's permissions (ignoring the delegating user's scope) or only the user's scope (ignoring agent-level restrictions), the effective access becomes the *union* rather than the *intersection* of both. An agent with broad tool access acting on behalf of a limited user could access resources the user cannot reach; conversely, a highly-privileged user delegating to a restricted agent could have the agent's restrictions silently bypassed. CVE-2026-32213 (Azure AI Foundry, CVSS 10, April 2026) demonstrated catastrophic failure of this pattern: an improper authorization check allowed an unauthorized attacker to elevate privileges over the network (AV:N/AC:L/PR:N/UI:N/S:C), achieving full confidentiality, integrity, and availability impact across changed scope -- exactly the blast radius that dual-constraint intersection enforcement is designed to prevent. The GCP Vertex AI "Double Agents" research (Unit 42, March 2026) showed that the default Per-Project Per-Product Service Agent (P4SA) had overprivileged permissions granted automatically, enabling an attacker who compromised the agent to extract P4SA credentials and gain unrestricted read access to all Cloud Storage buckets in the project -- a direct failure of intersection enforcement where the agent's execution context inherited broader permissions than the task warranted. The CSA Agentic Trust Framework identifies "delegation without scope attenuation" as a primary vector for cross-agent privilege escalation: each handoff multiplies access unless both the delegator's scope and the delegate's own permissions are independently verified. | Review the policy engine configuration to confirm that authorization evaluations take two independent inputs: (1) the agent's own granted permissions and (2) the delegating principal's scoped authority. The action should only be permitted if **both** constraints are satisfied (intersection, not union). Test by: (a) creating an agent with broad permissions acting on behalf of a restricted user -- verify the restricted user's scope limits the effective access; (b) creating a restricted agent acting on behalf of a privileged user -- verify the agent's own permission limits are enforced; (c) attempting to invoke a tool that the agent is allowed but the delegating user is not, and vice versa -- both cases must be denied. For Microsoft Entra Agent ID deployments, verify that blocked high-privilege roles (Global Administrator, Privileged Role Administrator, User Administrator, and blocked Graph permissions like `Application.ReadWrite.All`, `RoleManagement.ReadWrite.All`, `Directory.AccessAsUser.All`) cannot be assigned to agent identities, even when the delegating user holds those roles. For Cedar-based deployments, verify that delegation records are structured as data inputs to the policy engine (the "delegation as data" pattern), with distinct `Agent` and `SubAgent` principal types evaluated against both global policies and delegation-specific constraints. For AuthZEN-compliant systems, confirm the four-element tuple (Subject, Action, Resource, Context) includes delegation scope in the Context element and that the PDP evaluates it as an independent constraint. | Microsoft Entra Agent ID (GA as of April 2026) provides the most mature production implementation of this pattern: agent identities are explicitly blocked from receiving high-privilege directory roles (Global Admin, Privileged Role Admin, User Admin) and high-risk Graph permissions (`Application.ReadWrite.All`, `RoleManagement.ReadWrite.All`, `User.ReadWrite.All`, `Directory.AccessAsUser.All`). Agents operate under a principle of limited scope where even administrator consent cannot override blocked permissions -- enforcing the intersection at the identity platform level. The Microsoft Authorization Fabric architecture extends this with a centralized PEP/PDP that evaluates OAuth scopes (agent capability layer) and RBAC/ABAC policies (runtime authorization layer) as independent constraints, with deterministic ordering where tenant isolation and classification rules are evaluated before approval injection. The "delegation as data" pattern using Cedar policies (described by Phil Windley, March 2026) provides an elegant implementation: the parent agent creates an explicit delegation record encoding bounded capabilities, the PEP injects delegation attributes into the Cedar request context, and Cedar evaluates the action against both the global policy set and delegation constraints simultaneously. Distinct `Agent` and `SubAgent` principal types prevent accidental privilege escalation. Key gap: as of April 2026, the RSAC 2026 analysis found that all five shipped agent identity frameworks (CrowdStrike, Cisco, Palo Alto, Microsoft, Cato CTRL) can verify agent identity but none can track delegation chains between agents or confirm what agents do post-authentication. The delegation chain tracking problem remains unsolved at scale. |
| **9.6.7** | **Verify that** agent-to-agent task delegation is restricted by an explicit peer authorization policy (e.g., an approved agent registry or allowlist) so that even authenticated agents can only delegate to or accept delegations from pre-approved peers, with delegation attempts from unlisted agents rejected by default. | 2 | **Unauthorized inter-agent delegation, agent session smuggling, and cross-agent privilege escalation.** Without an explicit peer authorization policy, any authenticated agent can delegate tasks to any other agent in the system. This enables several attack patterns identified by CSA (March 2026): **agent session smuggling**, where a compromised sub-agent embeds hidden commands in its responses that the parent agent executes without detection (e.g., a research agent injecting unauthorized financial transactions into responses passed back to an orchestrator); and **cross-agent privilege escalation**, where one compromised agent reconfigures peers by rewriting their runtime configurations (e.g., `.mcp.json` files), creating self-reinforcing control loops. The RSAC 2026 "Slack swarm" incident illustrates this concretely: over 100 agents collaborated autonomously, Agent 12 committed code, and after the fact nobody could reconstruct which agent proposed the fix, which agents reviewed it, which delegated the commit, or why Agent 12 executed it -- complete loss of delegation chain accountability. The GCP Vertex AI "Double Agents" research (Unit 42, March 2026) showed how an agent's overprivileged default service account could be extracted and used to access restricted Google Artifact Registry repositories containing proprietary container images, reasoning engine code, and internal infrastructure documentation -- demonstrating that agent-to-service trust without explicit verification enables lateral movement from agent execution context into cloud infrastructure. The A2A protocol specification acknowledges this risk by requiring agents to declare authentication requirements in Agent Cards, but the protocol itself does not enforce peer allowlisting -- each enterprise must define its own governance framework for which agents can call which others. | Verify that the system maintains an explicit peer authorization registry (allowlist) specifying which agents may delegate to or accept delegations from which other agents. Test by: (a) deploying a new agent that is not in the registry and attempting delegation -- confirm the request is rejected by default; (b) removing an agent from the registry mid-execution and confirming subsequent delegation attempts from that agent are blocked; (c) attempting delegation between two agents where only one-way delegation is authorized -- confirm the reverse direction is denied; (d) verifying that delegation scope narrows at each hop (a parent agent with read+write cannot delegate write access if the peer is only approved for read). For A2A protocol deployments, verify that Agent Cards include cryptographic signatures for identity verification and that the receiving agent validates the sender's Agent Card against the approved registry before accepting any task. For Microsoft Entra Agent ID deployments, verify that agent identity blueprints require an assigned sponsor (human owner) and that inter-agent token exchange uses the `jwt-bearer` grant type with explicit scope constraints. Confirm that agent-to-agent delegation events are logged with full attribution (delegating agent, receiving agent, delegated scope, task context) in a tamper-evident audit trail. For Google Cloud deployments, verify that custom service accounts (BYOSA) replace default P4SA agents, with each agent's OAuth scopes explicitly restricted to its declared capabilities. | The Google Agent2Agent (A2A) protocol provides the most explicit framework for inter-agent communication: agents declare capabilities via Agent Cards (which support cryptographic signatures for tamper-proof identity verification), authenticate using OAuth 2.0/OIDC with PKCE, and interact only through declared capabilities without accessing each other's internal state. However, A2A does not enforce peer allowlisting at the protocol level -- each deploying organization must build its own governance layer specifying which agents can call which others, what scopes they need, and what data they can exchange. Microsoft Entra Agent ID provides a managed agent registry (the Agent Registry and Agent Collections blades in the Entra admin center, though these are being retired May 2026 in favor of the newer Agent ID platform). Agent identity blueprints support one-to-many relationships, require an assigned sponsor (human owner), and generate agent-specific audit entries. The Microsoft Agent Governance Toolkit (open-source, April 2026) adds policy engines, trust scoring, and SRE patterns for multi-agent systems. The CSA Agentic Trust Framework (February 2026) recommends a **trust decay model** where an agent's trust score decreases over time without positive signals, requiring ongoing demonstrated safe behavior rather than one-time grants, and **scope narrowing** at each delegation hop -- a parent agent with read+write can delegate only read access to a child agent, never escalate. The IETF AIMS framework (draft-klrc-aiagent-auth-01) defines three authorization scenarios for inter-agent trust: user-delegated (authorization code), agent self-authorized (client credentials), and agent-as-resource-server, with SPIFFE IDs and mutual TLS providing cryptographic peer identity at the transport layer. Key gap: the RSAC 2026 analysis identified delegation chain tracking as one of three critical unsolved problems -- all five major agent identity frameworks shipped at RSAC 2026 can verify agent identity but none can reconstruct the full delegation chain after the fact, making post-incident forensics extremely difficult in multi-agent systems. Building a comprehensive agent registry (business justification, human owner, credentials held, systems accessed, approved delegation paths) remains a manual governance exercise in most organizations. |

---

## Implementation Guidance

### The Authorization Crisis for Agentic AI (2025--2026)

Traditional IAM frameworks were designed for human users and static service accounts. Research and industry analysis in 2025--2026 identifies seven critical failures when applying these frameworks to AI agents (ISACA, 2025):

1. **Coarse-grained permissions:** Agents need fine-grained, task-specific permissions that dynamically change based on context, not static role-based access with long-lived tokens.
2. **Single-entity model limitations:** Current systems cannot track complex nested delegations where agents spawn sub-agents or represent multiple principals simultaneously.
3. **Context blindness:** Access decisions lack awareness of runtime context, agent intent, or risk level -- permissions remain static throughout sessions.
4. **Scalability pressures:** Hundreds or thousands of transient agents generate overwhelming token management demands (issuance, validation, revocation).
5. **Inadequate inter-agent authentication:** OAuth and SAML rely on hierarchical trust models unsuitable for decentralized agent networks that require peer-to-peer trust.
6. **Secret sprawl:** Each agent's multiple API integrations create a multiplicative factor in secrets requiring rotation and storage.
7. **Revocation complexity:** Revoking access in one place does not automatically cut off access elsewhere across distributed ephemeral sessions.

A 2026 survey (Grantex) found that 93% of popular AI agent projects use unscoped API keys, and fewer than half of companies deploying agents had comprehensive governance in place.

### Emerging Standards and Frameworks

- **NIST AI Agent Standards Initiative (February 2026):** NIST's Center for AI Standards and Innovation (CAISI) launched the AI Agent Standards Initiative alongside a concept paper on "Accelerating the Adoption of Software and Artificial Intelligence Agent Identity and Authorization." The initiative proposes NCCoE lab demonstrations exploring agent identification (distinguishing agents from humans), authorization (OAuth 2.0/2.1 extensions and policy-based access control), and access delegation (linking user identities to agents for accountability). The concept paper recommends ABAC over RBAC for agent permissions and explicitly calls for transitive trust limits preventing unbounded permission cascading. Standards under consideration include MCP, OAuth 2.0/2.1, OpenID Connect, SPIFFE/SPIRE, SCIM, and Next Generation Access Control. The public comment period closed on April 2, 2026; community input will inform a draft project description, and CAISI began sector-specific listening sessions in April 2026 focused on barriers to confident AI agent adoption.
- **IETF AIMS Framework (draft-klrc-aiagent-auth-00, March 2026):** The Agent Identity Management System composes WIMSE, SPIFFE, and OAuth 2.0 into a layered architecture. It defines five layers: identifiers (SPIFFE IDs), credentials (short-lived X.509/JWT bound to private keys), attestation (TEE evidence, binary validation, Kubernetes PSAT), authentication (mTLS + WIMSE Workload Proof Tokens), and authorization (OAuth 2.0 with transaction tokens). Three authorization scenarios are defined: user-delegated (authorization code grant), agent self-authorized (client credentials), and agent-as-resource-server. The framework explicitly requires audit logs recording authenticated agent ID, delegated subject, action, decision, and attestation state.
- **Agent Relationship-Based Identity and Authorization (ARIA):** Treats delegation relationships as explicit, cryptographically verifiable entities in a graph. Every delegation is a distinct relationship that enables surgical permission revocation and full auditability. ARIA integrates OAuth 2.0 Rich Authorization Requests, OAuth 2.0 token exchange (RFC 8693), and OpenID AuthZEN for context-aware policy evaluation.
- **OpenID AuthZEN 1.0 (Approved January 2026):** Now a Final Specification, AuthZEN standardizes a transport-agnostic API where any Policy Enforcement Point queries any Policy Decision Point regardless of vendor. The information model is a four-element tuple (Subject, Action, Resource, Context) that maps cleanly to agent tool invocations. This enables fine-grained, context-aware policies without abandoning installed-base OAuth infrastructure -- applying constraints such as geo-fences, budget thresholds, and temporal windows.
- **Agent Identity Protocol (AIP, March 2026):** A proposed protocol for verifiable delegation across MCP and A2A boundaries. AIP uses Decentralized Identifiers (DIDs) and delegatable credentials to create cryptographic chains of authorization -- each delegation step is signed, producing non-repudiable evidence of who authorized what, with explicit controls on transitivity (whether a delegate can further delegate received permissions). The protocol addresses the delegation chain tracking gap identified at RSAC 2026 by making every handoff inspectable without requiring a central authority.
- **Zero Trust Identity with DIDs:** Combines Decentralized Identifiers, Verifiable Credentials, Agent Naming Services, and zero-knowledge proofs for privacy-preserving compliance verification in agent identity.
- **IETF Transaction Tokens for Agents (draft-oauth-transaction-tokens-for-agents-06, April 2026):** An active Internet-Draft (authored by Amazon) extending OAuth Transaction Tokens specifically for AI agent workloads. Introduces three new claims: `act` (identifies the AI agent performing the action), `sub` (the principal on whose behalf work occurs), and an optional `agentic_ctx` claim carrying agent attributes (type, version, intent, allowed actions, environment constraints) relevant to authorization and auditing. The critical security property is the `actchain` claim -- a cryptographic trace of the delegation path as an ordered array of previous agents, solving the delegation chain tracking gap identified at RSAC 2026. During token replacement flows, the TTS must not modify the `act` field, principals and transaction IDs remain immutable, and a delegator cannot request a replacement token with broader permissions -- preventing privilege escalation by design. Supports Rich Authorization Request (RAR) propagation for fine-grained, user-consented permissions flowing through multi-hop service chains.
- **IETF OAuth Scope Aggregation (draft-jia-oauth-scope-aggregation-00, February 2026):** Proposes a scope-aggregated authorization pattern where an AI agent pre-computes the scopes required across a multi-step workflow and initiates a single authorization procedure for the aggregated scope. This reduces repeated user consent prompts and authorization round-trips in complex agentic workflows while maintaining explicit user approval of the full scope set.
- **IETF OAuth Multi-Agent Collaboration (draft-song-oauth-ai-agent-collaborate-authz-01):** Extends OAuth 2.0 with additional fields in token and message flows to support task-oriented multi-agent collaboration. Sub-agents can execute tasks as a group under a unified authorization framework while maintaining compatibility with existing OAuth 2.0 workflows.

### Practical Authorization Patterns

- **Scopes for coarse-grained limits, claims for fine-grained context:** Use OAuth scopes to define broad permission boundaries and token claims to carry runtime context (user identity, tenant, data classification, risk score) that policy engines evaluate per-call. URI-style scope naming (e.g., `urn:infra:monitoring:read`) improves cross-service compatibility.
- **On-behalf-of delegation with dual-identity tokens:** OAuth 2.0 token exchange (RFC 8693) cryptographically binds an actor (the agent) to a delegator (user or service), preserving the chain of responsibility without exposing the user's credentials. JWT tokens should encode the agent as `sub` and the delegated user as `act.sub`, with nested `act` claims supporting multi-level delegation chains (user -> agent1 -> agent2 -> service). Recommended token lifetime is 5--15 minutes.
- **Policy engines at the enforcement point:** OPA, Cedar, and Zanzibar-style systems enforce fine-grained policies. The policy engine must sit between the model's tool selection and actual tool execution -- never after. As of March 2026, AWS Bedrock AgentCore demonstrates this pattern concretely: every agent-to-tool request is evaluated against Cedar policies before execution, with policies definable programmatically or generated from natural-language descriptions.
- **Just-in-time credential issuance:** Request credentials at runtime for each tool invocation and discard them immediately after task completion. This eliminates standing access vulnerabilities inherent in long-lived tokens. The Oso platform implements this pattern with delegated access where the agent inherits the user's identity via token exchange and the downstream service enforces the user's current permissions in real-time -- if the user loses access, so does the agent immediately.
- **Short-lived, single-use credentials:** Replace long-lived API keys with ephemeral tokens scoped to individual tasks. This limits blast radius and eliminates the stale-authorization window. The Grantex 2026 survey found 93% of popular agent projects still use unscoped API keys -- this is the single most common authorization failure in deployed agent systems.
- **Human-in-the-loop approval gates:** For sensitive or destructive operations (healthcare data access, record deletion, compliance actions), require manual approval. Grant access to action *types* rather than individual operations to keep the approval burden manageable.

### Recent Incidents and Attack Patterns

Several real-world incidents in 2025--2026 demonstrate why authorization enforcement matters for agents:

- **MITRE ATLAS AML.T0053 -- AI Agent Tool Invocation:** Formally catalogued as a technique where adversaries exploit an agent's authorized tool access to perform unauthorized actions. Microsoft Copilot M365 and GitHub Copilot Chat have both been exploited through this vector, with agents directed to exfiltrate data or perform actions outside intended scope.
- **OpenClaw Malicious Skills (February 2026):** The largest confirmed supply chain attack targeting agent infrastructure. MITRE ATLAS investigators confirmed 1,184 malicious skills across the ClawHub ecosystem (roughly one in five packages), using direct and indirect prompt injection combined with unauthorized tool invocation.
- **Langflow Authentication Bypass (2025):** CVE-2025-34291 demonstrated how permissive CORS settings combined with credential forwarding created a complete authentication bypass in an agent workflow platform. Default configurations allowed cross-origin requests with credentials from any origin.
- **Git MCP Server Exploit Chain (January 2026):** Three CVEs (CVE-2025-68143, CVE-2025-68144, CVE-2025-68145) targeting Anthropic's Git MCP server demonstrated path traversal, argument injection, and repository scoping bypass -- all of which are authorization boundary failures.
- **Microsoft 365 Copilot BizChat Privilege Escalation (March 2026):** CVE-2026-26137 (CVSS 8.9) -- an SSRF vulnerability in Copilot BizChat that allowed an authorized attacker to escalate privileges over the network. The vulnerability is notable because the AI assistant itself becomes the privilege escalation vector: if an attacker can move the assistant into a higher-authority state, it becomes a tool for scaling access across the organization.
- **MS-Agent Shell Injection (2026):** CVE-2026-2256 -- the MS-Agent framework for autonomous AI workflow automation failed to sanitize user-controlled or AI-generated inputs passed through its built-in Shell tool, enabling prompt injection to trigger arbitrary command execution with the agent's privileges. This is a textbook example of why tool parameter validation must happen at the policy engine level, not in the agent itself.
- **PleaseFix Agentic Browser Hijack (March 2026):** Zenity Labs disclosed PleaseFix, a family of critical vulnerabilities in agentic browsers including Perplexity Comet. Two exploit chains were demonstrated: (1) a zero-click compromise where attacker-controlled content (e.g., a calendar invite) triggers autonomous agent execution, granting access to the local filesystem and exfiltrating data while the agent continues returning expected results to the user; (2) credential theft where the agent's authorized access to password managers (1Password) is abused to steal stored credentials or enable full account takeover. Neither exploit directly attacks the password manager -- both operate entirely within the agent's legitimately granted permissions, making this a real-world demonstration of authorization scope being technically correct but semantically exploitable. Perplexity patched the browser-side execution issue before public disclosure.
- **Claude Code API Key Exfiltration (January 2026):** CVE-2026-21852 (CVSS 5.3) -- a malicious repository could override the `ANTHROPIC_BASE_URL` via project settings, redirecting all API requests (including the API key) to an attacker-controlled server before any trust prompt was shown. Fixed in Claude Code 2.0.65. Because Anthropic Workspaces allow multiple API keys to share project files, a single exposed key could scale to team-wide compromise. CVE-2025-59536 (CVSS 8.7) in the same disclosure showed how malicious Hooks in `.claude/settings.json` could achieve RCE when a developer opens a repository.
- **mcp-remote OAuth Proxy Command Injection (2025):** CVE-2025-6514 -- a critical OS command injection vulnerability in mcp-remote, a popular OAuth proxy for MCP, affected over 437,000 environments. The vulnerability turned unpatched installs into supply chain backdoors capable of stealing API keys, cloud credentials, SSH keys, and repository contents.
- **MCP Configuration Secrets Exposure (2026):** GitGuardian's State of Secrets Sprawl 2026 report documented 24,008 secrets in MCP configuration files on public GitHub repositories, with 2,117 confirmed valid credentials capable of authenticating against production services. The root cause was MCP documentation patterns that encouraged hardcoding credentials in configuration files.
- **Azure AI Foundry Privilege Escalation (April 2026):** CVE-2026-32213 (CVSS 10) -- an improper authorization vulnerability in Azure AI Foundry allowing an unauthorized attacker to elevate privileges over the network with no prerequisites (no authentication, no user interaction, low attack complexity). The changed scope meant a successful exploit could breach confidentiality, integrity, and availability across tenant boundaries. This is one of the highest-severity authorization failures disclosed in an AI platform to date.
- **GCP Vertex AI "Double Agents" (March 2026):** Unit 42 researchers demonstrated how the default Per-Project Per-Product Service Agent (P4SA) in Vertex AI had overprivileged permissions granted automatically. By extracting P4SA credentials from a deployed agent's execution context, attackers could gain unrestricted read access to all Cloud Storage buckets in the project, access restricted Google Artifact Registry repositories containing proprietary reasoning engine images and internal infrastructure documentation, and leverage non-editable OAuth 2.0 scopes that theoretically extended access into Google Workspace (Gmail, Calendar, Drive). Google recommended Bring Your Own Service Account (BYOSA) as the mitigation.
- **FastGPT Cross-Tenant Access (April 2026):** CVE-2026-40252 -- a Broken Access Control vulnerability (IDOR/BOLA) in FastGPT prior to 4.14.10.4 allowed any authenticated team to access and execute applications belonging to other teams by supplying a foreign `appId`. The API validated the team token but did not verify the requested application belonged to the authenticated team, enabling cross-tenant data exposure and unauthorized execution of private AI workflows.
- **RSAC 2026 Delegation Chain Gaps:** Analysis of five agent identity frameworks shipped at RSAC 2026 (CrowdStrike, Cisco, Palo Alto, Microsoft, Cato CTRL) found that none could track delegation chains between agents, detect an agent rewriting its own security policy, or confirm what agents did after authentication. In one cited incident, a CEO's AI agent autonomously removed a security policy restriction that was preventing task completion. In the "Slack swarm" incident, over 100 agents collaborated and Agent 12 committed code, but the full decision chain (which agent proposed the fix, which reviewed it, which delegated the commit) could not be reconstructed.
- **OpenClaw Authorization Collapse (March 2026):** The OpenClaw AI agent framework -- which lets agents act on a user's behalf across Slack, Discord, APIs, and local files -- accumulated 138 CVEs over five months, including 7 critical. Three authorization failures stand out: CVE-2026-32922 (CVSS 9.9) in `device.token.rotate` failed to constrain new token scopes to the caller's existing scope set, allowing a caller with only `operator.pairing` scope to mint a fully privileged `operator.admin` token; CVE-2026-22172 (CVSS 9.9) allowed WebSocket clients to self-declare administrator scopes, completely bypassing authentication; and CVE-2026-33579 (CVSS 9.8) omitted scope transmission in the `/pair approve` path, enabling unauthorized privilege escalation. SecurityScorecard's STRIKE team identified 135,000+ publicly exposed OpenClaw instances in February 2026, with 63% running without any authentication. The root cause across all three CVEs was the same classic pattern: authorization validation logic existed in the codebase (`scopesAllow`) and was correctly used in some paths but missing from others.
- **Knostic MCP Server Authentication Scan (2025--2026):** Knostic scanned 1,862 internet-exposed MCP servers and found every single one lacked authentication -- all 119 manually verified samples responded to unauthenticated requests and exposed their full tool lists. By early 2026, a Zuplo survey found 17,000+ MCP server listings across directories, and even though the percentage without authentication dropped from 100% to 41%, the absolute number of exposed servers increased by an order of magnitude. Backslash Security independently confirmed similar results in a separate scan of another 2,000 servers.
- **Docker AuthZ Bypass via Oversized Request Bodies (March 2026):** CVE-2026-34040 (CVSS 8.8) -- an incomplete fix for CVE-2024-41110 left Docker Engine vulnerable to authorization bypass: HTTP request bodies larger than 1 MB are silently dropped before reaching AuthZ plugins, but the Docker daemon processes them normally. An attacker pads a legitimate container creation request to exceed 1 MB, the AuthZ plugin receives an empty body and approves it, and the daemon creates a privileged container with full host filesystem access. All authorization plugins (OPA, Prisma Cloud, Casbin) are equally vulnerable because none receive the request body for inspection. The AI agent attack chain is particularly concerning: an autonomous coding agent (such as OpenClaw) running inside a Docker-based sandbox can be tricked via prompt injection concealed in a GitHub repository into constructing a padded request that exploits this bypass -- the agent can discover the size boundary on its own when legitimate requests fail, then craft the exploit autonomously. Once the sandbox is breached, the agent can extract cloud credentials and SSH keys from the host. Fixed in Docker Engine 29.3.1 and Docker Desktop 4.66.1. This affects an estimated 92% of enterprise container deployments.
- **Red Hat OpenShift AI Token Disclosure (April 2026):** CVE-2026-5483 (CVSS 8.5) -- a flaw in the `odh-dashboard` component of Red Hat OpenShift AI allows disclosure of Kubernetes Service Account tokens via a NodeJS endpoint. An attacker who obtains these tokens gains unauthorized access to Kubernetes resources, potentially compromising the entire cluster. This is significant for agent authorization because AI platforms running on Kubernetes often use service account tokens for agent-to-infrastructure authentication -- if the dashboard exposes them, the credential isolation boundary is broken regardless of how well the agent itself is locked down. Red Hat issued multiple security advisories (RHSA-2026:7404, RHSA-2026:7403, RHSA-2026:7397, RHSA-2026:7398) to address the vulnerability.
- **Qualys ETM Rogue Agent Detection (April 2026):** Qualys published a detailed incident analysis where their External Threat Management platform detected an unauthorized OpenClaw agent disguised as a routine package on a Windows Server 2025 Datacenter EC2 instance. The detection correlated four distinct signals: an unregistered agent binary, anomalous outbound WebSocket connections, the presence of CVE-2026-25253 (a Control UI flaw that trusts the `gatewayUrl` parameter without validation, leaking authentication tokens to attacker-controlled endpoints), and unexpected Docker API calls. This represents an emerging detection pattern: organizations need to actively scan for and identify unauthorized AI agents running in their infrastructure, not just secure the agents they know about.
- **Azure MCP Server Authentication Bypass (April 2026):** CVE-2026-32211 (CVSS 9.1) -- a missing authentication vulnerability in Azure MCP Server instances where certain endpoints lack proper credential verification before granting access. Attackers need only the correct endpoint URLs or request formats to access protected resources, potentially exposing API keys, authentication tokens, and configuration details. This is particularly concerning for agent authorization because MCP servers are the primary tool integration layer -- a compromised MCP server bypasses all upstream agent authorization controls regardless of how well the agent itself is locked down.
- **Azure Kubernetes Service Authorization Bypass (April 2026):** CVE-2026-33105 (CVSS 10) -- an improper authorization vulnerability in AKS allowing an unauthenticated attacker to elevate privileges over the network. The weakness in AKS RBAC implementation can move an attacker from limited workload access to broader cluster control. For AI agent deployments, this is significant because many agent orchestration platforms run on AKS -- if the underlying cluster's RBAC can be bypassed, agent-level authorization controls become irrelevant.
- **2026 Incident Volume:** Industry tracking reports 520 confirmed tool misuse and privilege escalation incidents involving AI agents in 2026 alone, making it the most common reported agent security incident category.

### Semantic Privilege Escalation

A distinct class of authorization failure is emerging that traditional access control cannot address: **semantic privilege escalation**. In this attack, the agent never exceeds its technical permissions -- every API call, every tool invocation is individually authorized. But the *sequence* of actions falls outside the semantic scope of the assigned task.

Consider: a user asks an agent to summarize a document. The document contains hidden prompt injection (white text, invisible Unicode). The agent, following the injected instructions, scans the user's drive for API keys and emails them to an external address. Each individual action (read document, search files, send email) may be within the agent's technical authorization. The escalation is in *intent*, not in *permissions*.

Defending against semantic privilege escalation requires capabilities beyond traditional IAM:

1. **Intent capture and tracking** -- Record what the user actually asked for and maintain it as a reference throughout execution.
2. **Intent-action alignment evaluation** -- At each step, compare the proposed action against the captured intent. Does "summarize this document" semantically justify "search for files matching *.key"?
3. **Behavioral anomaly detection** -- Flag action sequences that deviate from typical patterns for the task type, even if each action is individually authorized.
4. **Security-annotated execution traces** -- Log not just what happened, but the semantic justification chain, enabling post-hoc analysis of whether the agent stayed within scope.

As of April 2026, no production authorization framework fully addresses semantic privilege escalation. GitGuardian's analysis of enterprise MCP deployments identifies this as a "sequence-level risk" -- individual operations each pass authorization checks, but their combination produces unauthorized outcomes. The multi-hop MCP architecture (user -> AI host -> MCP client -> multiple servers) concentrates risk not in individual requests but in the sequences agents orchestrate and the gaps between operations where context or credentials might leak. Gateway-mediated authorization (see MCP Agent Gateways below) partially addresses this by maintaining cross-request context at the enforcement point, but reliable real-time detection of semantic escalation without unacceptable false-positive rates remains an unsolved problem.

### Cloud-Native Agent Authorization Detection

Cloud providers are beginning to ship purpose-built detection for agent authorization abuse. Google Cloud Security Command Center (SCC) now includes a dedicated category of AI agent threat findings that map to MITRE ATT&CK tactics:

- **AI Agent Suspicious Token Generation Using Implicit Delegation** -- Flags when agents misuse implicit delegation to generate elevated-privilege tokens (maps to T1528: Steal Application Access Token).
- **AI Agent Suspicious Cross-Project Access Token Generation** -- Detects agents attempting to generate tokens for resources outside their authorized project scope.
- **AI Agent Suspicious Token Generation Using signJwt** -- Catches agents using the `signJwt` API to forge unauthorized JWT credentials.
- **AI Agent Unauthorized Service Account API Call** -- Identifies API calls exceeding the agent's assigned permissions.
- **AI Agent Excessive Permission Denied Actions** -- Tracks repeated failed authorization attempts, indicating reconnaissance or brute-force probing of privilege boundaries.

These detections are significant because they represent the first shift from generic workload monitoring to agent-specific authorization threat models. Organizations deploying agents on GCP should enable these findings and route them to their SIEM/SOAR pipeline.

### MCP-Specific Authorization Patterns

The Model Context Protocol (MCP) introduces specific authorization challenges because MCP servers currently expose all tools to connected agents by default -- there is no built-in selective disclosure mechanism. This creates both context window bloat (the agent sees tools it should not use) and security risk (the agent can invoke tools outside its authorization scope). As of April 2026, the MCP specification mandates OAuth 2.1 compliance (RFC 9700) with PKCE, explicitly prohibits session-based authentication, requires server-side token validation on every request, and now mandates Resource Indicators (RFC 8707) to prevent token mis-redemption attacks. The resource indicator requirement means clients must include a `resource` parameter in both authorization and token requests, explicitly naming the target MCP server -- tokens that omit this or target a different server must be rejected. This closes a practical attack where a malicious MCP server could replay a token it received against a different MCP server the user had authorized. Clutch Security research reports 86% of enterprise MCP servers have adopted RFC 8707 resource indicators as of early 2026. The spec also supports Dynamic Client Registration (DCR), enabling agents to self-register with authorization servers to reduce administrative overhead in environments with many transient agent clients.

A notable enterprise-readiness critique (Posta, April 2026) argues the current MCP authorization spec couples two distinct OAuth roles by requiring MCP servers to function as both resource servers *and* authorization servers simultaneously -- a departure from established enterprise patterns where stateless resource servers delegate token issuance to centralized Identity Providers. The spec's requirement that each MCP server expose its own `/authorize`, `/token`, and `/register` endpoints (or delegate to a third-party IdP while maintaining token mappings) creates operational complexity and fragments security infrastructure. Community proposals are exploring `WWW-Authenticate`-based discovery to redirect clients to centralized authorization servers, which would better align with enterprise SSO deployments. Organizations deploying MCP at scale should consider API gateways for centralized token validation rather than distributing authorization server responsibilities across individual MCP servers.

Recommended patterns for MCP authorization:

- **Selective tool disclosure:** MCP servers should implement tool filtering, revealing only tools the connecting agent/user is authorized to use. This reduces both the attack surface and the risk of the model selecting inappropriate tools. Retrieval-based approaches that dynamically surface relevant tools per query (rather than exposing the full tool manifest upfront) are gaining traction as context window efficiency measures that double as security controls.
- **Per-request authorization at the MCP boundary:** Rather than relying on the MCP client (the agent) to self-police, embed a Policy Decision Point within or in front of each MCP server. Cerbos provides an open-source policy-as-code engine that can run as a sidecar or library for this purpose, with batch query support for checking multiple tool permissions in a single call and YAML-based policies supporting RBAC, ABAC, and context-aware conditions.
- **Token downscoping at each hop:** When an MCP client calls an MCP server which itself calls downstream services, apply OAuth 2.0 token exchange (RFC 8693) at each boundary to narrow credentials. The MCP spec explicitly prohibits token passthrough -- never forward the client's original token to backend services. Token audience (`aud`) claims must be verified to match the MCP server's identifier on every request.
- **SPIFFE identities for MCP workloads:** Assign SPIFFE IDs to MCP servers and clients, enabling mutual TLS authentication and cryptographically verified identity at every connection. This is more robust than static API keys and aligns with the IETF AIMS framework's identity layer.
- **Per-client consent validation:** The MCP spec requires that user authentication does not imply client authorization. Each MCP server must maintain a consent registry mapping users to approved MCP clients with specifically granted scopes. A valid user token from an unapproved client must be rejected.

### MCP Agent Gateways

A maturing pattern as of March 2026 is the **Agent Gateway** -- a dedicated proxy that sits between agents and the MCP servers (or other tool endpoints) they connect to. The gateway serves as a centralized policy enforcement point:

- **Aembit MCP Identity Gateway:** Sits between agents and tools, handling credential injection and token exchange at both the agent-to-gateway and gateway-to-tool legs. Agents never hold long-lived secrets; the gateway injects the correct credential for each downstream call. Demonstrated at RSA 2026 in partnership with Netskope.
- **Agent Gateway (open-source):** An agentic proxy supporting MCP authentication with OAuth 2.0 protection, JWT token validation, and protected resource metadata (RFC 9728). Acts as a resource server validating incoming tokens before routing to downstream MCP servers.
- **Enterprise MCP Registry (Azure API Center):** Microsoft's internal MCP governance model registers every MCP server in Azure API Center for centralized discovery, then places every remote MCP server behind an API gateway for authentication, authorization, rate limiting, and logging. Every tool call carries a correlation ID from client through gateway to server and back, with prompts, tool selections, authorization decisions, and resource access logged in a consistent schema.

The gateway pattern addresses the "scattered MCP server" problem: without centralized governance, each MCP server implements its own authorization (or none), creating inconsistent enforcement. A gateway consolidates policy decisions, credential management, and audit logging in one place. An important security property of the gateway pattern is **token transformation**: the gateway strips incoming access tokens from the agent and issues short-lived, narrowly-scoped assertions to backend MCP servers, reducing exposure of long-lived credentials to LLM-facing components. The `mcp-oauth-gateway` open-source project demonstrates this pattern, adding OAuth 2.1 authorization to any MCP server without code modification. As of April 2026, gateway adoption is growing but raises legitimate concerns about creating a new single point of failure and potential for gateway sprawl analogous to the API gateway fragmentation enterprises experienced in the 2010s.

### Inter-Agent Delegation and the A2A Protocol (9.6.7)

Agent-to-agent delegation introduces trust boundary challenges distinct from user-to-agent delegation. When a primary agent decomposes a task and delegates subtasks to specialized peer agents, each hop crosses a trust boundary and inherits some form of the original authority. Without explicit peer authorization, delegation becomes a vector for lateral movement, session smuggling, and scope escalation.

The Google Agent2Agent (A2A) protocol (initially introduced April 2025, actively developed through 2026) is the most explicit framework for inter-agent communication. Key security properties:

- **Agent Cards as trust anchors:** Each agent declares its capabilities, authentication requirements, and identity in a structured Agent Card. Cards support cryptographic signatures for tamper-proof identity verification and versioned capability declarations that clients validate before interaction. Authenticated clients may receive extended Agent Cards with additional capabilities unavailable to public requesters.
- **Capability-based authorization:** A2A implements authorization at the operation level (tasks, push notifications require capability declarations) and data level (clients must be authorized to access specific tasks). The protocol explicitly maintains the **opaque execution principle** -- agents interact only through declared capabilities without accessing each other's internal state, memory, or tools.
- **Standard authentication:** A2A supports API keys, HTTP authentication, OAuth 2.0 (authorization code, client credentials, device code), OpenID Connect, and mutual TLS. The remote agent verifies tokens against its policy, checking client ID, scopes, and optional audience fields.

However, A2A does not enforce peer allowlisting at the protocol level. Each deploying organization must build a governance layer defining which agents can call which others, what scopes they need, and what data they can exchange.

The CSA delegation security research (March 2026) identifies two critical attack patterns specific to inter-agent delegation:

1. **Agent session smuggling:** A compromised sub-agent embeds hidden commands in its responses that the parent agent executes without detection. For example, a financial assistant delegates to a research agent, which injects unauthorized stock trade instructions invisible to the user -- the parent agent executes them because the response appears to be legitimate research output.
2. **Cross-agent privilege escalation:** One compromised agent reconfigures peers by rewriting their runtime configurations (e.g., `.mcp.json` files), creating self-reinforcing control loops that deepen intrusion scope. The delegation relationship itself becomes the attack surface.

Recommended defenses for inter-agent delegation include scope narrowing at each delegation hop (a parent with read+write can only delegate read), cryptographic lineage tracking to verify task alignment, trust decay models where agent trust scores decrease without positive behavioral signals, and observable execution logs preventing silent command injection.

### OWASP Agentic Top 10 (2026) Mapping

The OWASP Top 10 for Agentic Applications (2026), developed with over 100 industry experts, introduces the **Least Agency** principle: autonomy is a feature that should be earned, not a default setting. Three of the ten risks map directly to C9.6 requirements:

- **ASI01 Agent Goal Hijack** maps to 9.6.4 -- attackers manipulate agent objectives through natural-language injection, and if the agent participates in access control decisions, the hijacked goal can override authorization. Policy engines external to the model prevent this.
- **ASI02 Tool Misuse and Exploitation** maps to 9.6.1 -- agents misuse legitimate tools (email, CRM, web browser, DNS, internal APIs) in risky ways, often staying within granted permissions but performing destructive or exfiltrating actions. Fine-grained per-tool and per-parameter policies are the primary defense.
- **ASI03 Identity and Privilege Abuse** maps to 9.6.2 and 9.6.3 -- agents inherit user sessions, reuse secrets, or rely on implicit cross-agent trust, leading to privilege escalation and unattributable actions. Short-lived credentials, task-scoped permissions, and continuous authorization enforcement are the prescribed mitigations.

### CSA Agentic Trust Framework

The Cloud Security Alliance published the Agentic Trust Framework (ATF) in February 2026, applying Zero Trust governance specifically to AI agents. The ATF organizes controls around five pillars: identity (authentication, authorization, session management), behavior (observability, anomaly detection, intent analysis), data governance (input validation, PII protection, output controls), segmentation (access boundaries, resource limits, policy enforcement), and incident response (circuit breakers, kill switches, containment). A distinctive feature of the ATF is its **progressive autonomy model** -- agents are deployed with minimal autonomy and earn increased autonomy through demonstrated safe behavior, with continuous monitoring at every level. This contrasts with traditional "grant and forget" access models.

### Credential Isolation for Agents (9.6.5)

The fundamental principle is straightforward: if a credential exists anywhere in the model's observable context, prompt injection can extract it. This applies to system prompts, tool parameters, environment variables that are exposed to the model, and any data that flows through the context window. The defense is architectural -- credentials must never enter the model's observable state.

As of March 2026, three credential isolation patterns have production implementations:

1. **Transport-layer credential proxies.** A local proxy intercepts the agent's outbound HTTP requests and injects the real credential at the network layer. The agent sends requests with a placeholder or key name; the proxy resolves the actual credential from an encrypted vault or OS keychain, swaps it into the request headers, and forwards the response. AgentSecrets and OneCLI implement this pattern. The critical property is that the credential value never appears in the agent's process memory, context window, or logs. AgentSecrets takes this further with zero-knowledge encryption (X25519 key exchange + AES-256-GCM) and audit logs that are structurally incapable of recording credential values.

2. **Secrets manager sidecar injection.** In containerized deployments, a sidecar (HashiCorp Vault Agent Injector, AWS Secrets Manager agent) authenticates with the secrets manager on behalf of the workload and renders secrets into files on a shared volume. The agent's code reads from the file at the moment of use, and the secret never transits through the model's context. Vault's dynamic secrets engine generates credentials on-the-fly with configurable TTLs (typically minutes to hours), so even if a credential is somehow captured, it expires quickly.

3. **Workload identity federation with per-call credential generation.** The agent's runtime environment presents cryptographic proof of its identity (Kubernetes service account, pod metadata, container image signature) to a trust provider. A policy engine evaluates whether the agent is authorized for the specific API call, and if approved, generates a short-lived credential (15-30 minute lifetime) that is injected directly into the outbound request without being stored. Aembit implements this for AI agent workloads, supporting multiple LLM providers transparently.

4. **Platform-managed credential vaults with structural isolation.** Anthropic's Managed Agents (public beta, April 2026) implements a "brain/hands/session" zero-trust architecture where three components do not trust each other: the brain (Claude and the routing harness), the hands (disposable Linux containers for code execution), and the session (an append-only event log outside both). Credentials live in a separate Credential Vault component -- the sandbox execution environment has zero access to long-lived credentials by design. MCP tool calls route through a dedicated proxy that fetches credentials from the Vault only when needed, injects them at the transport layer, and returns only tool results to the agent. OAuth token renewal is handled automatically, and tokens never appear in sandbox code or environment variables. A security audit (hi120ki, April 2026) identified two residual weaknesses: (a) a confused deputy problem where workspace-level credential sharing means anyone with API key access can use any vault credential in a session, and (b) no process-level monitoring inside the sandbox (no Falco or eBPF-based tooling), so malicious package backdoors could execute undetected system calls. OAuth scope narrowing is also not supported at the Vault level -- if a stored token holds `channels:read` and `chat:write`, both apply even if only one is needed for the task.

5. **V8 isolate-based sandboxing with credential interception.** Cloudflare Dynamic Workers (open beta, April 2026) provide V8 isolate-based sandboxing that starts approximately 100x faster and uses 10-100x less memory than containers. The security-relevant property for credential isolation is that isolates intercept outbound HTTP requests at the security boundary and inject authentication tokens transparently -- agent-generated code never directly accesses secrets. Communication uses Cap'n Proto RPC bridges across the sandbox boundary. Because isolates are cheap enough to create and destroy per-request, there is no need for warm container reuse across tasks, eliminating cross-session credential leakage. Multi-layered defenses include automatic V8 security patch deployment within hours, a custom second-layer sandbox with dynamic risk-based tenant cordoning, and hardware-level MPK (Memory Protection Keys) protections.

The HashiCorp Vault validated pattern for AI agents demonstrates how these approaches compose: the LLM orchestrates task logic but never directly accesses sensitive resources. When a tool call requires authenticated access, the MCP server (not the agent) authenticates to Vault using an On-Behalf-Of token, retrieves dynamic credentials scoped to the user's group policies, executes the database or API query, and returns only the result to the agent. The credential never enters the LLM's context.

**The scale of the problem:** GitGuardian's State of Secrets Sprawl 2026 report found 1,275,105 AI-service secrets leaked publicly (81% YoY increase), with LLM infrastructure secrets (orchestration, RAG, vector storage) growing five times faster than core model provider keys. MCP configuration files alone contained 24,008 secrets, of which 2,117 were confirmed valid. This is partly attributable to MCP documentation patterns that encouraged hardcoded credentials in configuration files -- a problem the community is actively correcting.

### Anti-Patterns to Avoid

- Letting the AI model make access control decisions based on conversation context (prompt injection can manipulate these).
- Using the end user's actual credentials for agent actions instead of scoped delegation tokens.
- Caching authorization decisions for long-running agent tasks without event-driven invalidation.
- Granting agents broad "admin" or "full-access" scopes because their task is open-ended.
- Using static API keys for agent identity -- agents are workloads that should use short-lived, cryptographically-bound credentials (the IETF AIMS framework explicitly labels static API keys as an anti-pattern).
- Allowing unlimited delegation depth in multi-agent chains without policy-enforced limits on transitive trust.
- Passing API keys, database passwords, or service credentials as tool parameters or including them in system prompts where the model can observe and potentially leak them via prompt injection.
- Hardcoding credentials in MCP configuration files -- GitGuardian found 24,008 secrets in MCP configs on public GitHub, with 2,117 confirmed valid.
- Relying on environment variables for credential storage when those variables are accessible to the model's context (the agent process can read them, and prompt injection can instruct the model to output them).

---

## Related Standards & References

- [NIST SP 800-207: Zero Trust Architecture](https://csrc.nist.gov/pubs/detail/sp/800-207/final) -- continuous verification and least-privilege principles
- [NIST AI Agent Standards Initiative (Feb 2026)](https://www.nist.gov/caisi/ai-agent-standards-initiative) -- CAISI initiative for interoperable and secure agent standards
- [NIST Concept Paper: AI Agent Identity and Authorization (Feb 2026)](https://www.nccoe.nist.gov/sites/default/files/2026-02/accelerating-the-adoption-of-software-and-ai-agent-identity-and-authorization-concept-paper.pdf) -- NCCoE concept paper with proposed demonstrations
- [IETF AIMS Framework (draft-klrc-aiagent-auth-00, March 2026)](https://dev.to/kanywst/ai-agent-authentication-authorization-deep-dive-reading-draft-klrc-aiagent-auth-00-5d1) -- Agent Identity Management System composing WIMSE, SPIFFE, and OAuth 2.0
- [OpenID AuthZEN 1.0 (Approved Jan 2026)](https://openid.net/specs/authorization-api-1_0-04.html) -- transport-agnostic authorization API specification
- [OAuth 2.0 Token Exchange (RFC 8693)](https://www.rfc-editor.org/rfc/rfc8693) -- delegation token mechanism
- [Open Policy Agent (OPA)](https://www.openpolicyagent.org/) -- general-purpose policy engine
- [AWS Cedar](https://www.cedarpolicy.com/) -- policy language and evaluation engine
- [AWS Bedrock AgentCore Policy (March 2026)](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/policy.html) -- Cedar-based fine-grained agent-to-tool authorization
- [MITRE ATLAS AML.T0053: AI Agent Tool Invocation](https://ttps.ai/technique/ai_agent_tool_invocation.html) -- adversarial technique for unauthorized tool use via agents
- [MITRE ATLAS OpenClaw Investigation (Feb 2026)](https://www.mitre.org/sites/default/files/2026-02/PR-26-00176-1-MITRE-ATLAS-OpenClaw-Investigation.pdf) -- supply chain attack analysis
- [OWASP LLM06:2025 Excessive Agency](https://genai.owasp.org/llmrisk/llm062025-excessive-agency/) -- excessive permissions and lack of authorization
- [The Looming Authorization Crisis: Why Traditional IAM Fails Agentic AI (ISACA)](https://www.isaca.org/resources/news-and-trends/industry-news/2025/the-looming-authorization-crisis-why-traditional-iam-fails-agentic-ai) -- analysis of IAM failures for agents
- [State of AI Agent Security 2026 (Grantex)](https://grantex.dev/report/state-of-agent-security-2026) -- survey data on agent security practices
- [On-Behalf-Of Authentication for AI Agents (Scalekit)](https://www.scalekit.com/blog/delegated-agent-access) -- delegation token patterns and implementation
- [Setting Permissions for AI Agents (Oso)](https://www.osohq.com/learn/ai-agent-permissions-delegated-access) -- delegated access patterns
- [Best Practices of Authorizing AI Agents (Oso)](https://www.osohq.com/learn/best-practices-of-authorizing-ai-agents) -- practical implementation guidance
- [AI Agents Are Becoming Authorization Bypass Paths (Hacker News)](https://thehackernews.com/2026/01/ai-agents-are-becoming-privilege.html) -- real-world exploitation patterns
- [A New Identity Playbook for AI Agents in 2026 (Strata)](https://www.strata.io/blog/agentic-identity/new-identity-playbook-ai-agents-not-nhi-8b/) -- agentic identity architecture
- [CSA Agentic Trust Framework (Feb 2026)](https://cloudsecurityalliance.org/blog/2026/02/02/the-agentic-trust-framework-zero-trust-governance-for-ai-agents) -- Zero Trust governance framework for AI agents
- [Google Cloud SCC AI Threat Findings](https://docs.cloud.google.com/security-command-center/docs/ai-threats) -- cloud-native detection for agent authorization abuse
- [Cerbos: MCP and Zero Trust for AI Agents](https://www.cerbos.dev/blog/mcp-and-zero-trust-securing-ai-agents-with-identity-and-policy) -- policy-as-code authorization for MCP servers
- [CyberArk: Zero Trust for AI Agents](https://developer.cyberark.com/blog/zero-trust-for-ai-agents-delegation-identity-and-access-control/) -- delegation, identity, and JIT/JEA patterns
- [Semantic Privilege Escalation (Acuvity)](https://acuvity.ai/semantic-privilege-escalation-the-agent-security-threat-hiding-in-plain-sight/) -- intent-action misalignment in authorized agents
- [CVE-2026-26137: Copilot BizChat Privilege Escalation](https://windowsforum.com/threads/cve-2026-26137-copilot-bizchat-privilege-escalation-risk-msrc-confidence-guide.405874/) -- SSRF-based privilege escalation via AI assistant
- [SPIFFE vs OAuth for Nonhuman Identities (Security Boulevard)](https://securityboulevard.com/2026/03/spiffe-vs-oauth-access-control-for-nonhuman-identities/) -- comparison of identity frameworks for agent workloads
- [Red Hat: Zero Trust for Autonomous Agentic AI (Feb 2026)](https://next.redhat.com/2026/02/26/zero-trust-for-autonomous-agentic-ai-systems-building-more-secure-foundations/) -- zero trust foundations for agentic systems
- [Unit 42: Navigating Security Tradeoffs of AI Agents](https://unit42.paloaltonetworks.com/navigating-security-tradeoffs-ai-agents/) -- Palo Alto Networks research on agent security
- [OWASP Top 10 for Agentic Applications 2026](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/) -- ASI01 Goal Hijack, ASI02 Tool Misuse, ASI03 Identity & Privilege Abuse
- [PleaseFix Vulnerability: Agentic Browser Hijack (Zenity Labs, March 2026)](https://zenity.io/company-overview/newsroom/company-news/zenity-labs-discloses-pleasefix-perplexedagent-vulnerability) -- zero-click agent compromise and credential theft via indirect prompt injection
- [MCP Authentication and Authorization Patterns (Aembit)](https://aembit.io/blog/mcp-authentication-and-authorization-patterns/) -- five mandatory MCP OAuth 2.1 authorization patterns
- [Aembit MCP Identity Gateway](https://docs.aembit.io/ai-guide/mcp/identity-gateway/setup-mcp-gateway/) -- credential injection and token exchange proxy for MCP
- [Agent Gateway (Open Source)](https://github.com/agentgateway/agentgateway) -- agentic proxy with MCP OAuth 2.0 and JWT validation
- [Microsoft Internal MCP Security and Governance](https://www.microsoft.com/insidetrack/blog/protecting-ai-conversations-at-microsoft-with-model-context-protocol-security-and-governance/) -- API gateway pattern, Azure API Center as MCP registry
- [Cerbos MCP Authorization: Fine-Grained Access Control](https://www.cerbos.dev/blog/mcp-authorization) -- per-request policy decisions, batch queries, YAML-based RBAC/ABAC policies
- [CVE-2026-21852: Claude Code API Key Exfiltration (Check Point Research)](https://research.checkpoint.com/2026/rce-and-api-token-exfiltration-through-claude-code-project-files-cve-2025-59536/) -- project settings override redirecting API keys to attacker infrastructure
- [State of Secrets Sprawl 2026 (GitGuardian)](https://blog.gitguardian.com/the-state-of-secrets-sprawl-2026/) -- AI-service secrets up 81% YoY, 24K secrets in MCP configs
- [Securing AI Agents Without Static Credentials (Aembit)](https://aembit.io/blog/securing-ai-agents-without-secrets/) -- workload identity federation and credential proxy patterns
- [Secure AI Agent Authentication Using HashiCorp Vault Dynamic Secrets](https://developer.hashicorp.com/validated-patterns/vault/ai-agent-identity-with-hashicorp-vault) -- MCP server as credential broker with Vault dynamic secrets
- [AgentSecrets: Zero-Knowledge Credential Platform](https://dev.to/the_seventeen/agentsecrets-the-complete-zero-knowledge-credential-platform-for-ai-teams-33g2) -- transport-layer proxy with X25519 + AES-256-GCM
- [1Password Unified Access for AI Agent Security (March 2026)](https://1password.com/press/2026/mar/1password-unified-access) -- runtime credential issuance for agent workloads
- [OWASP AI Agent Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/AI_Agent_Security_Cheat_Sheet.html) -- credential redaction patterns and memory security guidance
- [Microsoft Entra Agent ID: Authorization](https://learn.microsoft.com/en-us/entra/agent-id/identity-professional/authorization-agent-id) -- blocked high-privilege roles, dual-layer agent authorization, delegated permission model
- [Agent2Agent (A2A) Protocol Specification](https://a2a-protocol.org/latest/specification/) -- Agent Cards, cryptographic signatures, capability-based authorization for inter-agent communication
- [CSA: Fixing AI Agent Delegation for Secure Chains (March 2026)](https://cloudsecurityalliance.org/blog/2026/03/25/control-the-chain-secure-the-system-fixing-ai-agent-delegation) -- agent session smuggling, cross-agent privilege escalation, scope narrowing
- [Microsoft Authorization Fabric for AI Agents (April 2026)](https://techcommunity.microsoft.com/blog/microsoft-security-blog/authorization-and-governance-for-ai-agents-runtime-authorization-beyond-identity/4509161) -- PEP/PDP architecture, dual-constraint enforcement, runtime authorization beyond identity
- [Delegation as Data: Applying Cedar Policies to Subagents (March 2026)](https://www.technometria.com/p/delegation-as-data-applying-cedar) -- Cedar-based delegation records, distinct principal types, intersection enforcement
- [RSAC 2026: Five Agent Identity Frameworks, Three Critical Gaps (VentureBeat)](https://venturebeat.com/security/rsac-2026-agent-identity-frameworks-three-gaps) -- delegation chain tracking gap, action governance gap, policy modification detection gap
- [Double Agents: Exposing Security Blind Spots in GCP Vertex AI (Unit 42)](https://unit42.paloaltonetworks.com/double-agents-vertex-ai/) -- overprivileged default service agents, P4SA credential extraction, BYOSA recommendation
- [CVE-2026-32213: Azure AI Foundry Privilege Escalation (CVSS 10)](https://cve.threatint.eu/CVE/CVE-2026-32213) -- improper authorization in AI platform enabling full privilege escalation
- [CVE-2026-40252: FastGPT Cross-Tenant Access (IDOR/BOLA)](https://github.com/labring/FastGPT/security/advisories/GHSA-gc8m-w37w-24hw) -- cross-tenant application access via missing ownership validation
- [IETF AIMS Framework draft-01 (draft-klrc-aiagent-auth-01)](https://datatracker.ietf.org/doc/draft-klrc-aiagent-auth/) -- updated agent identity management with inter-agent authorization scenarios
- [Microsoft Agent Governance Toolkit (Open Source, April 2026)](https://techcommunity.microsoft.com/blog/linuxandopensourceblog/agent-governance-toolkit-architecture-deep-dive-policy-engines-trust-and-sre-for/4510105) -- policy engines, trust scoring, SRE patterns for multi-agent systems
- [Ping Identity Runtime Identity Standard for Autonomous AI (March 2026)](https://press.pingidentity.com/2026-03-24-Ping-Identity-Defines-the-Runtime-Identity-Standard-for-Autonomous-AI) -- runtime identity for autonomous agents
- [MCP Authorization Specification (2026-03-15)](https://modelcontextprotocol.io/specification/draft/basic/authorization) -- OAuth 2.1 resource server classification, RFC 8707 resource indicators, dynamic client registration
- [OAuth for MCP: Emerging Enterprise Patterns (GitGuardian)](https://blog.gitguardian.com/oauth-for-mcp-emerging-enterprise-patterns-for-agent-authorization/) -- gateway-mediated authorization, token transformation, sequence-level risk analysis
- [MCP Authorization with Resource Indicators (RFC 8707)](https://dasroot.net/posts/2026/04/mcp-authorization-specification-oauth-2-1-resource-indicators/) -- token mis-redemption prevention, resource parameter requirements
- [Agent Identity Protocol (AIP): Verifiable Delegation Across MCP and A2A](https://arxiv.org/pdf/2603.24775) -- DID-based delegatable credentials with cryptographic chain of authorization
- [Knostic: Mapping MCP Servers Across the Internet](https://www.knostic.ai/blog/mapping-mcp-servers-study) -- 1,862 servers scanned, all lacked authentication
- [OpenClaw Security 2026: 138 CVEs (CVEfind)](https://www.cvefind.com/en/blog/openclaw-compromise-ai-agents.html) -- CVE-2026-32922 (CVSS 9.9) token scope bypass, CVE-2026-22172 (CVSS 9.9) WebSocket admin scope bypass
- [mcp-oauth-gateway (Open Source)](https://github.com/atrawog/mcp-oauth-gateway) -- OAuth 2.1 authorization server wrapper for unauthenticated MCP servers
- [A2A Protocol Security: Authenticating Agent-to-Agent Communication (SecureW2)](https://securew2.com/blog/a2a-protocol-security) -- mutual TLS, signed agent cards, PKI-backed machine identities
- [CVE-2026-34040: Docker AuthZ Bypass via Oversized Request Bodies (Cyera)](https://www.cyera.com/blog/cyera-research-discovers-docker-authorization-bypass-that-silently-disables-security-policies) -- 1MB body padding silently disables all AuthZ plugins, enabling AI agent sandbox escape
- [CVE-2026-5483: Red Hat OpenShift AI Token Disclosure (CSO Online)](https://www.csoonline.com/article/4067305/red-hat-openshift-ai-weakness-allows-full-cluster-compromise-warns-advisory.html) -- Kubernetes service account token leakage in AI platform dashboard
- [Anatomy of an Autonomous AI Agent Risk: Qualys ETM on OpenClaw (April 2026)](https://blog.qualys.com/product-tech/2026/04/13/anatomy-autonomous-ai-agent-risk-qualys-etm-openclaw) -- unauthorized agent detection and CVE-2026-25253 token exfiltration
- [AuthZEN + Shared Signals Framework: Continuous Authorization (Andrew Doering, 2026)](https://andrewdoering.org/blog/2026/authzen-shared-signals-framework-part-1-fundamentals/) -- two-layer synchronous/asynchronous authorization with CAEP event-driven revocation
- [MCP Authorization Spec Enterprise Critique (Christian Posta, April 2026)](https://blog.christianposta.com/the-updated-mcp-oauth-spec-is-a-mess/) -- analysis of resource server / authorization server coupling and enterprise misalignment
- [IETF Transaction Tokens for Agents (draft-oauth-transaction-tokens-for-agents-06, April 2026)](https://datatracker.ietf.org/doc/draft-oauth-transaction-tokens-for-agents/) -- agent-aware transaction tokens with `act`, `actchain`, and `agentic_ctx` claims for delegation chain provenance
- [IETF OAuth Scope Aggregation for Multi-Step AI Agent Workflows (draft-jia-oauth-scope-aggregation-00)](https://datatracker.ietf.org/doc/draft-jia-oauth-scope-aggregation/) -- single aggregated authorization for multi-step agentic workflows
- [IETF OAuth Multi-Agent Collaboration (draft-song-oauth-ai-agent-collaborate-authz-01)](https://datatracker.ietf.org/doc/draft-song-oauth-ai-agent-collaborate-authz/) -- OAuth 2.0 extension for task-oriented multi-agent authorization
- [IETF OAuth On-Behalf-Of User Authorization for AI Agents (draft-oauth-ai-agents-on-behalf-of-user-02)](https://datatracker.ietf.org/doc/draft-oauth-ai-agents-on-behalf-of-user/) -- `requested_actor` and `actor_token` parameters for three-party delegation
- [Anthropic Managed Agents: Decoupling Brain from Hands (April 2026)](https://www.anthropic.com/engineering/managed-agents) -- zero-trust brain/hands/session architecture with credential vault isolation
- [Security Analysis of Claude Managed Agents (hi120ki, April 2026)](https://hi120ki.github.io/blog/posts/20260413/) -- confused deputy problem in workspace credential sharing, sandbox observability gaps
- [Cloudflare Dynamic Workers: Isolate-Based Agent Sandboxing (April 2026)](https://www.infoq.com/news/2026/04/cloudflare-dynamic-workers-beta/) -- V8 isolate-based sandboxing with transport-layer credential interception
- [CVE-2026-32211: Azure MCP Server Missing Authentication (CVSS 9.1)](https://windowsnews.ai/article/cve-2026-32211-critical-azure-mcp-server-authentication-flaw-exposes-sensitive-data-cvss-91.409622) -- unauthenticated access to MCP server endpoints exposing sensitive data
- [CVE-2026-33105: Azure Kubernetes Service Privilege Escalation (CVSS 10)](https://www.thehackerwire.com/azure-kubernetes-service-critical-privilege-escalation-cve-2026-33105/) -- improper authorization in AKS RBAC enabling unauthenticated privilege escalation
- AISVS C05 (Access Control) -- general AI access control; C09.6 addresses agent-specific delegation and continuous enforcement

---

## Open Research Questions

- How do you define "minimum necessary scope" for delegation tokens when the agent's task is open-ended (e.g., "research and summarize")? The NIST concept paper recommends ABAC but does not yet provide concrete policy templates.
- Can authorization policies for agent actions be auto-generated from tool manifests and user role definitions? AWS Bedrock AgentCore's natural-language-to-Cedar generation is an early attempt, but the correctness of generated policies has not been independently validated at scale.
- What are the performance implications of per-call authorization in high-throughput agent systems, and what caching strategies are safe? AuthZEN's standardized PEP-to-PDP API may reduce integration overhead, but latency benchmarks for agent-scale workloads are not yet published.
- How should authorization work for agents that operate across organizational boundaries (federated agent systems)? The IETF AIMS framework defines identity chaining and cross-domain flows, but real-world deployments are still in pilot stage.
- The NIST AI Agent Standards Initiative comment period closed April 2, 2026, and CAISI is now conducting sector-specific listening sessions. How will the resulting draft project description shape mandatory requirements for agent authorization in federal and regulated environments, and will it establish concrete guidance on delegation chain limits and ABAC policy templates that the concept paper recommended but left unspecified?
- Can the ARIA graph-based delegation model and the AIMS layered architecture converge into a unified standard, or will organizations need to choose between them?
- What attestation mechanisms (TEE evidence, binary validation, supply-chain provenance) are practical for agent identity in cloud-native environments, and how do they interact with existing SPIFFE/SPIRE deployments?
- How can semantic privilege escalation be detected in real-time when each individual agent action is technically authorized? Intent-tracking and behavioral anomaly detection are proposed approaches, but no production system has demonstrated reliable detection without unacceptable false-positive rates.
- As cloud providers ship agent-specific authorization detections (Google Cloud SCC's AI threat findings being the first), will these become a de facto standard that other providers must match? What coverage gaps exist in current detections?
- How should MCP authorization evolve? The March 2026 spec mandates OAuth 2.1 and prohibits session auth and token passthrough, but still lacks built-in selective tool disclosure. Should dynamic tool filtering become a protocol-level primitive, or should it remain delegated to agent gateways and individual server implementations?
- Will agent gateways (Aembit, Agent Gateway, Azure API Center) converge on a standard architecture, or will enterprises face gateway sprawl analogous to the API gateway fragmentation of the 2010s? The gateway pattern solves immediate authorization gaps but introduces a new single point of failure and policy management surface.
- The PleaseFix vulnerability demonstrated that technically-authorized agent actions within an authenticated browser session can be exploited for credential theft and file exfiltration. How should authorization frameworks model the boundary between "agent has permission" and "agent is acting on attacker-controlled instructions within those permissions"? This is the practical manifestation of the semantic privilege escalation problem.
- Credential proxy architectures (AgentSecrets, OneCLI, Aembit) keep secrets out of the agent's context, but adoption remains low -- most frameworks still pass credentials via environment variables or tool parameters. What would it take for credential proxies to become the default pattern in major agent frameworks (LangChain, CrewAI, AutoGen)? Is framework-level integration or a protocol-level primitive (e.g., an MCP credential injection extension) the more viable path?
- GitGuardian found that code-assisted commits leak secrets at roughly twice the baseline rate (3.2% vs 1.5%). As agents increasingly write and execute code, how should credential isolation extend to agent-generated code that may inadvertently embed secrets in source files, logs, or configuration?
- The A2A protocol provides capability-based authorization via Agent Cards with cryptographic signatures, but does not enforce peer allowlisting at the protocol level. Should peer authorization be a protocol-level primitive in A2A (and MCP), or is it inherently an enterprise governance concern that varies too much across deployments?
- The OpenClaw CVE pattern (138 vulnerabilities in five months, with authorization validation logic present but inconsistently applied across code paths) suggests that agent frameworks face a systematic code coverage problem for authorization checks. Is this best addressed through mandatory authorization middleware that intercepts all tool invocations by default (deny-unless-checked), through static analysis tooling that detects unguarded code paths, or through protocol-level enforcement where the runtime rejects any tool call that hasn't passed through a registered policy decision point?
- With 41% of 17,000+ MCP servers still running without authentication as of early 2026 (down from 100% in mid-2025 per Knostic's scan), is the MCP ecosystem converging on authentication fast enough, or will agent gateways need to treat all MCP servers as untrusted by default and enforce authentication at the gateway layer regardless of server-side implementation? The mcp-oauth-gateway pattern of wrapping unauthenticated servers in an OAuth 2.1 layer suggests the community expects significant tail of unprotected servers.
- CVE-2026-32213 (Azure AI Foundry, CVSS 10) and the Vertex AI "Double Agents" research both demonstrate that cloud AI platforms can ship with catastrophically overprivileged default service accounts. Should cloud providers be required to default to least-privilege agent identities (like Google's BYOSA recommendation), or does this create too much friction for initial adoption?
- CVE-2026-34040 (Docker AuthZ bypass) showed that agents running in Docker-based sandboxes can autonomously discover and exploit infrastructure authorization flaws to escape containment -- the agent figured out the 1MB size boundary on its own. How should agent sandbox architectures account for the fact that the agent itself is an adversarial intelligence capable of probing and bypassing authorization boundaries, not just a passive workload? Is defense-in-depth (multiple independent authorization layers) sufficient, or do we need fundamentally different isolation models for agentic workloads?
- The RSAC 2026 analysis found that none of the five shipped agent identity frameworks can reconstruct delegation chains after the fact. The IETF Transaction Tokens for Agents draft (draft-oauth-transaction-tokens-for-agents-06, April 2026) proposes `actchain` claims for cryptographic delegation lineage -- is this the right approach, or does the JWT size growth from deeply nested agent calls (the draft permits TTS-imposed depth limits and oldest-entry truncation) make it impractical at scale?
- The Anthropic Managed Agents "brain/hands/session" architecture and Cloudflare Dynamic Workers both demonstrate that platform-managed credential isolation (where the sandbox structurally cannot access credentials) is production-viable. Will this pattern converge into a standard isolation model for all agent platforms, or will the diversity of deployment environments (cloud, on-prem, edge) prevent standardization? The confused deputy problem in Managed Agents (workspace-level credential sharing without per-user vault scoping) suggests the model still has gaps to address.
- Four new IETF drafts target agent-specific OAuth extensions: transaction tokens for agents, scope aggregation, multi-agent collaboration authorization, and on-behalf-of user authorization. Will these converge into a coherent agent authorization profile, or will the proliferation of overlapping drafts fragment implementation and delay adoption?

---

## Related Pages

- [C05-02-Authorization-Policy.md](C05-02-Authorization-Policy.md) -- Covers the foundational RBAC/ABAC enforcement and externalized policy engine patterns that C9.6 applies specifically to agent tool invocations, including JIT privileged access and temporal scoping.
- [C05-06-Autonomous-Agent-Authorization.md](C05-06-Autonomous-Agent-Authorization.md) -- Covers the identity-layer counterpart to C9.6: scoped capability tokens, default-deny for high-risk actions, and session-bound credential patterns that feed into the delegation and policy enforcement mechanisms discussed here.
- [C05-Access-Control.md](C05-Access-Control.md) -- Hub page for identity, authorization, and multi-tenant isolation controls; C9.6 extends these patterns to the agent-specific challenges of delegation chains and continuous enforcement.

---
