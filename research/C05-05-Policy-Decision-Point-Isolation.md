# C5.5: Policy Decision Point Isolation

> [Back to C05 Index](C05-Access-Control.md)
> **Last Researched:** 2026-04-16 (updated with Kyverno policy-engine CVEs, nginx-ui MCPwn, Azure MCP Server missing-auth, OAuth RAR agent extensions)

## Purpose

Ensure that the authorization decision infrastructure for AI agents is protected from compromise or manipulation by the agents it governs. A policy decision point (PDP) that shares an execution surface with the agent runtime cannot be trusted to enforce authorization, because a compromised agent can influence evaluation inputs, alter policy state, or observe decision logic in ways that a segregated PDP would not permit. This section covers the runtime isolation boundary between agent execution and the PDP that evaluates each agent action, complementing the agent-specific authorization controls in C9.6.

> **Scope note:** Agent authorization policy design (scoped capabilities, delegation, continuous evaluation) lives in C9.6. Single-system agent identity is in C9.4.1. This section focuses narrowly on the runtime isolation of the PDP itself so that the surface a compromised agent can reach does not include the decision engine.

---

## Requirements

| # | Requirement | Level | Threat Mitigated | Verification Approach | Gaps / Notes |
|---|-------------|:-----:|-----------------|----------------------|--------------|
| 5.5.1 | **Verify that** the policy decision point for agent authorization is isolated from the agent's execution environment such that a compromised agent runtime cannot influence or bypass evaluation. | 3 | MITRE ATLAS techniques added in the October 2025 Zenity collaboration and the February 2026 v5.4.0 release cover this attack surface directly: `AML.T0073 Modify AI Agent Configuration`, `AML.T0074 Escape to Host`, `AML.T0075 Publish Poisoned AI Agent Tool`, and the long-standing `AML.T0051 LLM Prompt Injection` (direct and indirect). MITRE's own ATLAS OpenClaw investigation (published February 9, 2026 by MITRE's Center for Threat-Informed Defense) uncovered seven new techniques unique to agent frameworks and documented a one-click RCE chain (CVE-2026-25253) where cross-site request forgery modified the agent's configuration to disable user confirmation, then escaped the sandbox — the exact failure mode an isolated PDP is meant to prevent. Concrete 2025–2026 incidents reinforce the need for a process boundary: Microsoft 365 Copilot's **EchoLeak** (CVE-2025-32711, CVSS 9.3, disclosed June 2025 by Aim Security — the first known zero-click attack on a production AI agent) exfiltrated data by injecting instructions into emails that Copilot later retrieved, a failure that Aim described as an "LLM Scope Violation" — exactly the class of attack a scope-isolated PDP is meant to contain. Trail of Bits' October 2025 "prompt-injection to RCE" series named three agents with authorization-allowlist bypasses via argument injection, with coordinated disclosure covering Claude Code (CVE-2025-54795), Cursor (GHSA-534m-3w6r-8pqr), and Amazon Q; the common thread is that "safe" allowlisted commands like `find`, `grep`, and `go test -exec` still embed arbitrary code execution when the authorization layer only gates the command name and not its typed arguments. Check Point Research disclosed an additional Claude Code chain in February 2026 (CVE-2025-59536 for hooks/MCP consent bypass and **CVE-2026-21852** for RCE + API-token exfiltration through project files) where two repository-controlled settings, `enableAllProjectMcpServers` and `enabledMcpjsonServers`, could auto-approve MCP servers before the user saw the trust dialog — a case study in why an in-agent consent prompt is not a PDP. Docker's **CVE-2026-34040** (disclosed April 2026) showed that even the container host can have authorization bypasses that let an attacker escape to the host, reinforcing why the PDP's isolation boundary should not end at the container layer alone. An April 2026 cluster of agent-framework sandbox escapes illustrates the same lesson: **CrewAI** (CVE-2026-2275/2285/2286/2287, disclosed April 2, 2026 by ThaiCERT) chained a Docker-unavailable fallback to `SandboxPython`, an SSRF in the RAG search tool, and a missing JSON path check into a prompt-injection-driven RCE; **PraisonAI** saw three critical bypasses of its AST-based Python sandbox within one week (CVE-2026-34938 CVSS 10.0 on April 3 via a `str` subclass with an overridden `startswith()` that disabled the guard list, CVE-2026-39888 CVSS 9.9 on April 8 using frame-traversal attributes through a caught exception to reach the real `builtins` dict, and CVE-2026-40158 CVSS 8.6 on April 10); **Terrarium** (CVE-2026-5752, CVSS 9.3, April 14, 2026) escalated a sandbox escape to root-level code execution; and **ByteDance Deer-Flow** (CVE-2026-34430) joined the list alongside **n8n** (CVE-2026-1470, Orca Security). The common shape: if the PDP lives inside the agent's Python/Node runtime, every one of these bypasses ends inside the PDP too. The policy-engine layer itself is not immune — two **Kyverno** disclosures show the PDP can be the attack vector even when properly out-of-process: **CVE-2026-22039** (CVSS 10.0, disclosed February 4, 2026, fixed in 1.16.3 / 1.15.3) let any authenticated user with namespaced Policy creation rights smuggle variable substitutions into the `apiCall.Fetch` `urlPath` field, causing the admission controller's cluster-wide ServiceAccount to read or mutate resources across namespaces; **CVE-2026-4789** (published March 30, 2026 by Orca's Igor Stepansky, still unpatched as of PR #15789 merged April 6, 2026) lets the same class of attacker abuse unrestricted CEL `http.Get()` functions in namespaced policies to perform SSRF from the Kyverno pod into internal cluster services or the cloud metadata endpoint, with the response exfiltrated back through `messageExpression` in the admission denial. Both CVEs reinforce that PDP isolation includes **hardening the PDP's own request path** — a PDP that evaluates user-authored policy and speaks to the cluster API with elevated credentials is itself a high-value target. The MCP layer is seeing parallel incidents: **CVE-2026-33032 "MCPwn"** (CVSS 9.8, actively exploited in the wild as of April 15, 2026; patched in nginx-ui 2.3.4, March 15, 2026) exposed nginx-ui's `/mcp_message` endpoint without the `AuthRequired()` middleware applied and with a default-empty IP allowlist that the middleware treated as "allow all", letting any network-reachable attacker invoke the twelve MCP tools — including config writes with automatic nginx reload — without authentication. **CVE-2026-32211** (CVSS 9.1, disclosed April 3, 2026, no patch at time of writing) is a missing-authentication disclosure in Microsoft's `@azure-devops/mcp` npm package that exposed Azure DevOps work items, repositories, pipelines, and pull-request tools to unauthenticated callers. Both confirm that an MCP server cannot be treated as its own PDP — a dedicated PDP must mediate every tool call, and the transport layer must fail closed on default configuration. The OWASP Top 10 for Agentic Applications 2026 (relabeled from the Black Hat Europe draft to the final **ASI01-ASI10** scheme) now lists **ASI01 Agent Goal Hijack** and **ASI03 Identity and Privilege Abuse** as the dominant authorization-relevant risks, noting that agents inheriting user sessions, reusing secrets, or relying on implicit cross-agent trust produces privilege escalation that cannot be cleanly attributed. Microsoft's April 2026 Agent Governance Toolkit whitepaper reports 26.67% bypass rates when authorization is enforced only through prompt-level safety, dropping to 0.00% when policy evaluation is moved to an isolated, deterministic enforcement layer. | Auditors should walk the request path from agent to decision: confirm the PDP runs out-of-process (separate container, separate VM, or separate trust zone), authenticates to the PEP with a workload identity the agent cannot mint, and rejects requests that do not carry that identity. For cloud deployments, inspect network policy or service-mesh rules (Istio AuthorizationPolicy, Cilium CiliumNetworkPolicy, AWS VPC endpoint policies) that restrict ingress to the PDP from PEPs only, not from the agent sandbox. For AWS Bedrock AgentCore deployments (AgentCore Policy reached GA on March 3, 2026 across 13 regions — see `docs.aws.amazon.com/bedrock-agentcore`), confirm that all agent tool calls traverse the AgentCore Gateway and that Cedar policies are stored and versioned outside the agent's own IAM role; the Gateway injects `context.toolName` at runtime from the OpenAPI `operationId` and applies forbid-wins semantics so a single matching `forbid` overrides any `permit`. For Microsoft-stack deployments, inspect that the Azure Functions/App Service endpoint is behind Entra authentication with "Require authentication" set for all requests, plus IP restrictions, Private Endpoints, or API Management as documented in the April 2026 Microsoft Security blog — the four decision outputs (ALLOW / DENY / REQUIRE_APPROVAL / MASK) must be deterministic and the PEP must not coerce REQUIRE_APPROVAL into ALLOW on its own. Red-team the boundary by running a rogue agent that tries to (a) call the PDP directly with a forged action, (b) mutate Rego/Cedar policy files mounted into the PDP pod, (c) exhaust the PDP with a denial-of-service, and (d) poison the context store the PDP reads from. None of these paths should succeed. SuperClaw (Superagentic AI, open-sourced February 21, 2026) provides a reusable harness for this kind of exercise: it generates adversarial "tool-bypass" scenarios through its Bloom engine, captures the agent's tool calls as evidence, and scores results against behavior contracts — a close fit for 5.5.1 test cases where the question is whether a prompt-injected agent can coerce a policy-allowed tool name into a denied action (cybersecuritynews.com, February 2026). Review audit logs: every authorization decision should be attributable to a specific PEP identity, not the agent. Cerbos v0.51.0 (February 2026) added explicit policy lifecycle management and scopes that make this kind of audit trail easier to verify (cerbos.dev blog, 2026). For long-running agents, pair the isolated PDP with a Shared Signals Framework (SSF/CAEP) stream so the PDP can revoke active sessions mid-task when an IdP emits a credential-compromise or risk-elevation Security Event Token; the April 2026 AuthZEN + SSF write-up frames this as "AuthZEN handles the synchronous decision, SSF/CAEP handles the asynchronous signal to re-evaluate or revoke," with universal-logout propagation back through the IdP preventing a cached `permit` from surviving a compromise (andrewdoering.org, 2026). Sidecar-vs-out-of-process is a live design tension: Cerbos' own benchmark (cerbos.dev/blog/from-opa-to-our-own-engine-cerbos) reports sub-millisecond embedded evaluation and up to 17× improvement over their previous OPA-based engine, and OPA documentation (openpolicyagent.org/docs/deploy/k8s, aws.amazon.com/blogs/opensource ECS sidecar guide) acknowledges that a shared cluster service introduces 1–5 ms of latency and becomes a single point of failure if misconfigured — the right answer is usually "sidecar deployed with a workload identity the agent cannot forge," not "in-process library call from the agent." Detection can be stitched together with existing exposure-management tooling: the April 13, 2026 Qualys ETM + OpenClaw walk-through (blog.qualys.com) shows a Windows Server host where four individually-benign signals (an unsigned `openclaw.exe` masquerading as a routine package, outbound connections to an unknown gateway, a WebSocket handshake carrying a token, and CPU-anomaly spikes from an LLM call) correlated into a high-priority incident — the kind of weak-signal fusion a PDP alone cannot produce and that an SIEM/XDR feed must backstop. | No widely deployed reference architecture yet exists, though the landscape is converging quickly. The Microsoft Agent Governance Toolkit (opensource.microsoft.com, April 2026) ships an in-process policy engine using LangChain callbacks and CrewAI decorators, justified by a sub-0.1 ms p99 latency requirement and layered "execution rings" rather than process boundaries. This is a legitimate design choice for low-latency tool mediation but shifts the trust boundary into the same process as the agent; pair it with out-of-band enforcement for high-risk actions. AWS moved the other direction with Bedrock AgentCore Policy (GA March 2026), which intercepts every tool call at an external gateway and evaluates it against Cedar policies before execution, with role-based tiers (e.g., read-only "junior" agents, capability-bounded "senior" agents, and fully scoped "admin" agents) encoded directly as Cedar policy — a concrete example of the out-of-process pattern this control requires. The OpenClaw CVE wave of March 18–21, 2026 (nine CVEs in four days including CVE-2026-22172 at CVSS 9.9 for WebSocket scope-binding failure and CVE-2026-32922 for privilege escalation) showed what happens when a `scopesAllow` validation function exists in the codebase but is only wired into some request paths — isolation of the PDP means nothing if multiple PEPs disagree on when to call it. Apple's August 2025 hiring of the OPA maintainers and the planned sunset of Styra's enterprise OPA offerings has injected roadmap uncertainty for Rego-based deployments (osohq.com, styra.com). Cedar (AWS Verified Permissions, and the open-source Cedar Agent + OPAL stack from permit.io) keeps gaining ground because its type system and determinism make the PDP easier to isolate and formally reason about (goteleport.com benchmark, 2025). Honest gap: most open-source agent frameworks (LangChain, LlamaIndex, AutoGen, CrewAI) still default to in-process guardrails; teams have to build the PDP boundary themselves. |
| 5.5.2 | **Verify that** the policy decision point receives structured action descriptions (e.g., action type, target resource, parameters) rather than the agent's raw reasoning context. | 3 | Passing raw chain-of-thought or prompt text to the PDP re-exposes the decision to prompt injection — the same class of attack (`AML.T0051`) the PDP is supposed to contain. MITRE ATLAS' 2025 additions `AI Agent Context Poisoning` and `Memory Manipulation` show how adversary-controlled text embedded in tool outputs, documents, or long-term memory can steer an agent's reasoning; if that reasoning is what the PDP sees, the PDP inherits the compromise. OWASP's 2025 LLM Top 10 places Prompt Injection (LLM01) at #1, noting it appears in over 73% of audited production AI deployments (obsidiansecurity.com, genai.owasp.org). The Trail of Bits October 2025 disclosures (Claude Code, Cursor, Amazon Q) all trace back to the same root cause: the enforcement layer saw only a command name like `find` or `grep` — not the full typed argument tree — so prompt-injected flags such as `-exec 'bash -c …'` rode through the allowlist. The Authensor gateway pattern (authensor.com, 2026) and the Cerbos MCP guide (cerbos.dev, 2025) both emphasize that the PDP must see a typed action envelope — never free-form text — with undeclared actions defaulting to DENY. The October 2025 AgentSentry paper (arxiv.org/abs/2510.26212, SaTS @ CCS 2025) goes further, arguing that even typed envelopes are insufficient if the allowed action set is static; it proposes task-centric access control that derives a temporary, minimal policy per task and revokes it when the task completes. The February 4, 2026 IETF individual draft **draft-chen-oauth-rar-agent-extensions-00** (expires August 8, 2026) targets exactly this point at the protocol layer by extending OAuth 2.0 Rich Authorization Requests (RFC 9396) with two new `authorization_details` members: `policy_context` (so a client can demand a specific assurance level — e.g., `"financial_grade_v1"` — preventing a prompt-injected agent from silently opting into a weaker policy set) and `lifecycle_binding` (so the token is tied to an external task state and revoked the moment the task completes, fails, or is cancelled, closing the window where a leaked token outlives the work it was granted for). The companion `draft-brossard-oauth-rar-authzen-03` (July 2024) uses AuthZEN as the on-the-wire shape for `authorization_details`, making the RAR payload the same structured Subject-Action-Resource-Context envelope the PDP already consumes. The OWASP Agentic Top 10 2026 (**ASI03 Identity and Privilege Abuse**) specifically calls out agents that reuse user sessions or self-assert identity in authorization calls; the PEP must construct the subject from a workload identity, not trust the agent's serialized output. | Auditors should capture a sample of PDP request payloads and confirm they conform to a typed schema: a subject (agent identity), an action with a discrete `name` plus typed `properties`, a resource with `type` and `id`, and a context object with PEP-supplied environment attributes. The AuthZEN Authorization API 1.0 reached Final on January 11, 2026 and was published on the Standards Track on April 16, 2026 (openid.github.io/authzen) — it is now the vendor-neutral shape for PDP payloads. A dedicated **AuthZEN Profile for MCP Tool Authorization** (`profiles/authzen-mcp-profile-1_0.md` in the openid/authzen repo) introduces the `coazMapping` extension to an MCP tool's `inputSchema`, mapping tool arguments and session tokens to the Subject-Action-Resource-Context model so MCP clients and proxies can perform parameter-level authorization before invoking a tool (openid/authzen, 2026; see also modelcontextprotocol/ext-auth issue #15 on parameter-level mapping). Verify AuthZEN compliance against the official spec editors' reference implementations (Aserto/Topaz, Axiomatics, SGNL); PDPs such as Cerbos (v0.51.0, February 2026 — see cerbos.dev/authzen), Axiomatics, Styra, AWS Verified Permissions, and PlainID now ship AuthZEN-compatible endpoints, and the OPA community has an open tracking issue (open-policy-agent/opa#8449) plus a community plugin that turns OPA into an AuthZEN-compatible PDP. Validate via JSON Schema on the PEP side that no free-form reasoning, system prompt, or raw user utterance leaks into the payload. For OPA-based stacks, review the Rego input contract and confirm `input.action` is an enum or tagged union, not a string pulled from the LLM's output; run `opa test` with adversarial inputs. For Cedar, the schema file enforces this at compile time — AWS Bedrock AgentCore Policy (GA March 2026) uses this exact pattern to bind tool calls to typed Cedar actions before dispatch, injecting `context.toolName` from the OpenAPI `operationId` so the agent cannot rename the action in its output. Fuzz the PEP with prompt-injected agent outputs and confirm the PEP refuses to forward anything that does not match the schema; the Trail of Bits post-mortem explicitly recommends argument separators (`--`), disabling shell invocation, and drastically reducing allowlists over regex-based filtering. | Many homegrown agent frameworks still serialize the model's tool-call JSON directly to the PDP, which works when the framework parses the tool call strictly but fails when it forwards the raw `function_call.arguments` string, trusting the model to have produced valid structure. OpenAI and Anthropic function-calling produces structured JSON, but agents that synthesize tool calls via free-form text (ReAct-style) are at higher risk. AuthZEN 1.0 is now Final (January 2026) / Standards Track (April 2026), the MCP profile draft addresses parameter-level mapping, and the February 2026 `draft-chen-oauth-rar-agent-extensions-00` begins filling the agent-attribute gap at the OAuth layer with `policy_context` and `lifecycle_binding` — but the spec ecosystem remains deliberately agnostic about delegation chains, model identifiers, and capability tokens; those go in the extensible `properties` object for now and are a likely extension point for a future AuthZEN Agent Profile. No current tool automatically detects that a PEP is leaking raw reasoning into the PDP — this is still a code-review finding. Tool recommendations for the PEP boundary: `pydantic` or `zod` for the typed envelope, `jsonschema` on the PDP side, and `promptfoo`, `DeepTeam`, or `Garak` to red-team the boundary with MITRE ATLAS-aligned prompt-injection payloads. |

---

## Community Notes

A recurring point of confusion in 2025–2026 architecture reviews: teams conflate "the PDP is in a separate file" with "the PDP is isolated." Running a Rego policy inside the agent's Python process, or evaluating Cedar policies via an SDK linked into the agent runtime, gives the agent read and write access to the policy store, the decision cache, and often the PDP's memory. That is not isolation in the sense this control requires — a compromised agent can mutate policy before the next call, tamper with cached decisions, or simply return ALLOW from the in-process client stub. The bar for 5.5.1 is a separate trust zone: a different process with a distinct workload identity, a network boundary, and a PEP that mediates all agent-to-PDP traffic.

A second recurring issue: using the agent's own output tokens as the authorization request body. If the agent is what constructs `{"action": "transfer_funds", "amount": 50000}` and the PEP forwards that verbatim, then a prompt-injected agent that invents an `action` string the policy silently allows (or a numeric edge case the policy didn't consider) bypasses the PDP even though the PDP "ran." The PEP, not the agent, must construct the request payload from validated inputs — the agent's role is to propose, the PEP's role is to describe the proposal to the PDP in typed form.

A third pattern, surfaced by the April 2026 CrewAI and PraisonAI CVE clusters: a fallback path that silently re-enables an unsafe execution mode. CrewAI's code interpreter fell back from Docker to an in-process Python sandbox when Docker was unavailable (CVE-2026-2287), and PraisonAI's AST allowlist could be defeated by a `str` subclass with an overridden `startswith()` (CVE-2026-34938). Both are PDP-shaped failures even when no explicit PDP is wired in: the "policy" was the sandbox's allowlist, and the enforcement layer shared a process with the attacker-controlled code it was meant to constrain. Treat any in-process allowlist as advisory only — the authoritative decision must live behind a process boundary the agent cannot rewrite.

A fourth pattern, driven home by the Kyverno disclosures of February–April 2026: the PDP itself is a privileged workload and must be hardened the same way any other privileged component is. CVE-2026-22039's policy `apiCall` authorization bypass and CVE-2026-4789's unrestricted CEL `http.Get()` both let an attacker turn the PDP's own admission controller into an SSRF or cross-namespace pivot — the decision engine's broad RBAC, intended for evaluating cluster-wide policy, became the attacker's capability. "Isolated" therefore means more than "separate process": the PDP's own request path needs its own authorization model, its outbound network calls need an egress policy, and user-authored policy inputs (CEL, Rego, Cedar fragments) must be treated as untrusted data evaluated inside a sandbox, not as configuration the engine will faithfully execute with elevated credentials. Similarly, the April 2026 nginx-ui MCPwn incident (CVE-2026-33032) and the Azure MCP Server disclosure (CVE-2026-32211) are reminders that an MCP server cannot double as its own PDP; pair MCP with an external PDP and verify every tool route carries the auth middleware, not just the headline one.

---

## Implementation Guidance

**Deployment pattern for 5.5.1 (isolated PDP):**

1. Run the PDP as a dedicated workload — a Kubernetes Deployment in its own namespace with a distinct ServiceAccount, an Azure Function behind Entra as in the Microsoft reference (techcommunity.microsoft.com, April 2026), or an AWS Verified Permissions policy store accessed via IAM-scoped API calls. Do not co-locate with the agent.
2. Apply network isolation: Istio or Linkerd AuthorizationPolicy that allows only the PEP's ServiceAccount to reach the PDP port; deny-all default for everything else. For cloud-native deployments, use VPC Service Controls (GCP), PrivateLink (AWS), or Private Endpoints (Azure).
3. Use workload identity (SPIFFE/SPIRE, Azure Managed Identity, AWS IRSA, GCP Workload Identity) for PEP-to-PDP authentication. The agent must not hold this credential.
4. Store policy as immutable, signed artifacts (OCI images, Git-backed with commit signing, or AWS Verified Permissions policy templates). The agent's filesystem must be read-only with respect to policy. Use OPAL or Styra DAS for controlled policy distribution.
5. Log every decision with attribution to the PEP identity and the structured action, not the agent's reasoning. Ship to an append-only store (S3 Object Lock, Azure immutable blobs, Loki behind write-only credentials).

**Deployment pattern for 5.5.2 (structured action envelopes):**

1. Define a schema. AuthZEN 1.0 (openid.github.io/authzen) gives a vendor-neutral shape — `subject`, `action`, `resource`, `context` — that works across Cerbos, Axiomatics, Styra, AWS Verified Permissions, and PlainID. For Cedar, define an action-entity schema. For OPA, publish a Rego input contract plus a JSON Schema validated at the PEP boundary.
2. Construct the envelope in the PEP from validated fields: the tool name (from a closed enum of declared tools), the resource identifier (canonicalized, not a free-form URL), the action parameters (typed and range-checked), and agent identity (workload token, not a self-asserted string).
3. Reject any PEP input that does not match the schema. Do not coerce. Do not forward extra fields from the agent's output.
4. Default-deny undeclared actions. Cerbos, Cedar, and the Authensor gateway pattern (authensor.com, 2026) all recommend a final DENY rule catching anything not explicitly allowed.
5. Red-team with MITRE ATLAS-aligned payloads. `promptfoo`, `DeepTeam`, and `Garak` each carry prompt-injection suites; route their outputs through your agent and verify the PEP refuses malformed envelopes.

**Engine selection notes (as of April 2026):**

- **Cedar / AWS Verified Permissions** — strongest isolation story, deterministic, type-safe, formally verifiable. Good fit when the authorization model is primarily RBAC/ABAC.
- **OPA / Rego** — most flexible, widest ecosystem, but Apple's August 2025 acquisition of the maintainers and Styra's enterprise sunset have created roadmap uncertainty; factor that in. Still the right choice for complex cross-cutting policy.
- **Oso Cloud / Polar** — opinionated for ReBAC and relationship-based agent delegation; pairs well with multi-tenant agent platforms.
- **OpenFGA / Zanzibar-style** — best when authorization is primarily about relationship graphs (agent-owns-resource, user-delegates-to-agent).
- **Cerbos** — purpose-built PDP, ships with AuthZEN support, strong MCP integration story (cerbos.dev, 2025).

---

## Related Standards & References

- OWASP AISVS 1.0 C5.5 — source requirements for this section.
- OWASP AISVS 1.0 C9.6 — agent authorization policy design (scoped capabilities, delegation, continuous evaluation).
- OWASP Top 10 for LLM Applications 2025, LLM01 Prompt Injection — https://genai.owasp.org/llmrisk/llm01-prompt-injection/
- OWASP Top 10 for Agentic Applications, December 2025 — https://www.promptfoo.dev/docs/red-team/owasp-agentic-ai/ and https://owasp.org/www-project-agentic-skills-top-10/
- OWASP Top 10 for Agentic Applications 2026 (ASI01 Agent Goal Hijack, ASI03 Identity and Privilege Abuse) — https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/
- MITRE ATLAS (v5.4.0, February 2026) — https://atlas.mitre.org/ — techniques including `AML.T0051` (LLM Prompt Injection), `AML.T0073` (Modify AI Agent Configuration), `AML.T0074` (Escape to Host), `AML.T0075` (Publish Poisoned AI Agent Tool), plus the October 2025 Zenity agent-focused additions.
- AuthZEN Authorization API 1.0 — Final January 11, 2026; Standards Track publication April 16, 2026 — https://openid.github.io/authzen/ — vendor-neutral PDP request/response shape.
- AuthZEN Profile for MCP Tool Authorization (draft, 2026) — https://github.com/openid/authzen/blob/main/profiles/authzen-mcp-profile-1_0.md — `coazMapping` extension mapping MCP tool arguments and session tokens to the SARC model for parameter-level authorization.
- MCP parameter-level authorization mapping (ext-auth issue #15) — https://github.com/modelcontextprotocol/ext-auth/issues/15
- Cerbos, OpenID AuthZEN Is Official (2026) — https://www.cerbos.dev/blog/openid-authzen-is-official-cerbos-is-ready
- Cerbos PDP v0.51.0 release notes (February 2026) — https://www.cerbos.dev/blog/cerbos-pdp-v0-51-0-policy-lifecycle-management-audit-enhancements-and-scopes
- OPA AuthZEN tracking issue — https://github.com/open-policy-agent/opa/issues/8449
- AgentSentry: Task-Centric Access Control for AI Agents (arXiv 2510.26212, SaTS @ CCS 2025) — https://arxiv.org/abs/2510.26212
- Trail of Bits, Prompt Injection to RCE in AI Agents (October 2025) — https://blog.trailofbits.com/2025/10/22/prompt-injection-to-rce-in-ai-agents/ — covers CVE-2025-54795 (Claude Code), GHSA-534m-3w6r-8pqr (Cursor), and Amazon Q argument-injection bypasses of command allowlists.
- EchoLeak / CVE-2025-32711 (Microsoft 365 Copilot, CVSS 9.3, disclosed June 2025) — https://thehackernews.com/2025/06/zero-click-ai-vulnerability-exposes.html and https://arxiv.org/abs/2509.10540 — first publicly documented zero-click prompt-injection exploit in a production AI agent.
- AWS Bedrock AgentCore + Cedar for AI Agent Authorization — https://byteiota.com/aws-policy-agentcore-cedar-language-secures-ai-agents/
- Amazon Bedrock AgentCore Policy GA (March 3, 2026) — https://aws.amazon.com/bedrock/agentcore/ and authorization flow at https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/policy-authorization-flow.html ; example policies at https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/example-policies.html
- MITRE ATLAS OpenClaw Investigation (February 9, 2026) — https://ctid.mitre.org/blog/2026/02/09/mitre-atlas-openclaw-investigation/ and whitepaper at https://www.mitre.org/sites/default/files/2026-02/PR-26-00176-1-MITRE-ATLAS-OpenClaw-Investigation.pdf — seven new agent-framework techniques, including CVE-2026-25253 one-click RCE via configuration modification.
- Check Point Research, Caught in the Hook (CVE-2025-59536, CVE-2026-21852) — https://research.checkpoint.com/2026/rce-and-api-token-exfiltration-through-claude-code-project-files-cve-2025-59536/ — MCP consent bypass via `enableAllProjectMcpServers` and `enabledMcpjsonServers`.
- OpenClaw March 2026 CVE wave (CVE-2026-22172 WebSocket scope bypass CVSS 9.9, CVE-2026-32922 privilege escalation) — https://www.thehackerwire.com/openclaw-critical-websocket-authorization-bypass-cve-2026-22172/ and https://www.armosec.io/blog/cve-2026-32922-openclaw-privilege-escalation-cloud-security/
- Docker CVE-2026-34040 authorization bypass — https://thehackernews.com/2026/04/docker-cve-2026-34040-lets-attackers.html — relevant when the PDP's isolation boundary ends at the container runtime.
- Permit.io, Scaling Authorization with Cedar and OPAL — https://www.permit.io/blog/scaling-authorization-with-cedar-and-opal and https://github.com/permitio/opal-cedar
- NIST AI 600-1, Generative AI Profile — https://nvlpubs.nist.gov/nistpubs/ai/NIST.AI.600-1.pdf
- NIST AI Agent Interoperability Profile, planned Q4 2026 (announced via CAISI, February 2026) — https://www.nist.gov/itl/ai-risk-management-framework
- Cloud Security Alliance, NIST AI RMF Agentic Profile v1 — https://labs.cloudsecurityalliance.org/agentic/agentic-nist-ai-rmf-profile-v1/
- Microsoft Security, Authorization and Governance for AI Agents (April 2026) — https://techcommunity.microsoft.com/blog/microsoft-security-blog/authorization-and-governance-for-ai-agents-runtime-authorization-beyond-identity/4509161
- Microsoft Agent Governance Toolkit (April 2026) — https://opensource.microsoft.com/blog/2026/04/02/introducing-the-agent-governance-toolkit-open-source-runtime-security-for-ai-agents/ and architecture deep-dive at https://techcommunity.microsoft.com/blog/linuxandopensourceblog/agent-governance-toolkit-architecture-deep-dive-policy-engines-trust-and-sre-for/4510105
- Cerbos, MCP Security & AI Agent Authorization — https://www.cerbos.dev/blog/mcp-security-ai-agent-authorization-a-ciso-and-architects-guide
- Natoma, MCP Access Control — OPA vs Cedar — https://natoma.ai/blog/mcp-access-control-opa-vs-cedar-the-definitive-guide
- Teleport, Benchmarking Policy Languages (Rego, Cedar, OpenFGA) — https://goteleport.com/blog/benchmarking-policy-languages/
- Oso, OPA vs Cedar vs Zanzibar — https://www.osohq.com/learn/opa-vs-cedar-vs-zanzibar
- Open Policy Agent project — https://www.openpolicyagent.org/ and https://github.com/open-policy-agent/opa
- AWS Cedar / Verified Permissions — https://www.styra.com/knowledge-center/opa-vs-cedar-aws-verified-permissions/
- Promptfoo MITRE ATLAS red-team suite — https://www.promptfoo.dev/docs/red-team/mitre-atlas/
- DeepTeam MITRE ATLAS framework — https://www.trydeepteam.com/docs/frameworks-mitre-atlas
- SuperClaw open-source red-team framework for AI coding agents (Superagentic AI, February 21, 2026) — https://cybersecuritynews.com/superclaw-red-team-ai-agent/ — exercises tool-policy enforcement, detects tool-bypass via aliases, ambiguous descriptions, and weak policies.
- AuthZEN + Shared Signals Framework deep-dive (Andrew Doering, 2026) — https://andrewdoering.org/blog/2026/authzen-shared-signals-framework-part-1-fundamentals/ — synchronous AuthZEN decisions paired with asynchronous SSF/CAEP revocation for long-running agents.
- IETF draft-chen-oauth-rar-agent-extensions-00 (February 4, 2026) — https://datatracker.ietf.org/doc/draft-chen-oauth-rar-agent-extensions/ — `policy_context` and `lifecycle_binding` extensions for OAuth 2.0 Rich Authorization Requests targeting AI-agent delegation, policy-downgrade attacks, and task-scoped token revocation.
- IETF draft-brossard-oauth-rar-authzen-03 (Axiomatics/Aserto/3Edges, 2024) — https://datatracker.ietf.org/doc/draft-brossard-oauth-rar-authzen/ — AuthZEN request/response profile for `authorization_details`, letting an AS forward RAR payloads to an AuthZEN-conformant PDP.
- Kyverno CVE-2026-22039 authorization bypass (CVSS 10.0, disclosed February 4, 2026; fixed in 1.16.3 / 1.15.3) — https://github.com/kyverno/kyverno/security/advisories/GHSA-8p9x-46gm-qfx2 and write-up at https://www.minimus.io/post/cve-2026-22039-how-kyvernos-critical-authorization-bypass-breaks-kubernetes-namespace-isolation — Policy `apiCall.Fetch` variable-substitution flaw that let the admission controller's ServiceAccount perform cross-namespace reads and writes.
- Kyverno CVE-2026-4789 SSRF via unrestricted CEL `http.Get()` (Orca Security, published March 30, 2026; PR #15789 merged April 6, 2026) — https://orca.security/resources/blog/kyverno-ssrf-vulnerability-cve-2026-4789/ and advisory at https://advisories.gitlab.com/pkg/golang/github.com/kyverno/kyverno/CVE-2026-4789/ — namespaced-policy author can SSRF from the Kyverno pod to cluster-internal services or the cloud metadata endpoint.
- nginx-ui MCPwn CVE-2026-33032 (CVSS 9.8, actively exploited as of April 15, 2026; fixed in 2.3.4 on March 15, 2026) — https://pluto.security/blog/mcp-bug-nginx-security-vulnerability-cvss-9-8/ and https://www.picussecurity.com/resource/blog/cve-2026-33032-mcpwn-how-a-missing-middleware-call-in-nginx-ui-hands-attackers-full-web-server-takeover — `/mcp_message` endpoint missing `AuthRequired()` middleware; default-empty IP allowlist treated as allow-all.
- Azure MCP Server missing authentication CVE-2026-32211 (CVSS 9.1, disclosed April 3, 2026) — https://www.thehackerwire.com/azure-mcp-server-critical-information-disclosure-via-missing-authentication/ and tracking at https://cvefeed.io/vuln/detail/CVE-2026-32211 — `@azure-devops/mcp` npm package exposed tools without authentication.
- Qualys ETM correlation of OpenClaw signals (April 13, 2026) — https://blog.qualys.com/product-tech/2026/04/13/anatomy-autonomous-ai-agent-risk-qualys-etm-openclaw — four-signal weak-evidence fusion (unsigned binary masquerade, unknown gateway outbound, WebSocket token exchange, LLM CPU spike) as PDP-complementary detection.
- Cerbos, From OPA to Our Own Engine (2026) — https://www.cerbos.dev/blog/from-opa-to-our-own-engine-cerbos — sub-millisecond custom-engine benchmark and design rationale for embedded vs service-mode PDP deployment.
- AWS Open Source Blog, OPA sidecar on ECS — https://aws.amazon.com/blogs/opensource/deploying-open-policy-agent-opa-as-a-sidecar-on-amazon-elastic-container-service-amazon-ecs/ — reference pattern for sidecar PDP deployment with per-application policy configuration.
- CrewAI prompt-injection sandbox escape cluster (CVE-2026-2275, CVE-2026-2285, CVE-2026-2286, CVE-2026-2287, disclosed April 2, 2026 by ThaiCERT) — https://www.thaicert.or.th/en/2026/04/02/multiple-vulnerabilities-in-crewai-allow-sandbox-escape-and-remote-code-execution-via-prompt-injection/ — SandboxPython fallback + SSRF + path-traversal chain driven through direct and indirect prompt injection.
- PraisonAI AST sandbox bypass to RCE (CVE-2026-34938 CVSS 10.0 April 3, 2026; CVE-2026-39888 CVSS 9.9 April 8, 2026; CVE-2026-40158 CVSS 8.6 April 10, 2026) — https://www.thehackerwire.com/critical-praisonai-sandbox-escape-rce-cve-2026-39888/ and https://www.thehackerwire.com/praisonai-critical-rce-via-sandbox-bypass-cve-2026-34938/ — three separate bypasses of the same AST-based allowlist in one week.
- Terrarium sandbox escape to root RCE (CVE-2026-5752, CVSS 9.3, April 14, 2026) — https://www.thehackerwire.com/terrarium-sandbox-escape-root-level-code-execution-cve-2026-5752/
- n8n agent RCE via sandbox escape (CVE-2026-1470, Orca Security, 2026) — https://orca.security/resources/blog/cve-2026-1470-n8n-rce-sandbox-escape/
- ByteDance Deer-Flow sandbox escape RCE (CVE-2026-34430) — https://www.thehackerwire.com/bytedance-deer-flow-sandbox-escape-rce-cve-2026-34430/
- Amazon Bedrock AgentCore Policy + Cedar deep-dive (Tolgahan Demirbaş, March 2026) — https://medium.com/@tolghn/amazon-bedrock-agentcore-policy-cedar-control-what-your-ai-agents-can-do-3313e6f3a2db — worked example of tool-call authorization via an external Cedar-evaluating gateway.
- Controlling Agent Tool Access with Bedrock AgentCore Policy and Cedar (Shinya Aizawa, March 15, 2026) — https://shinyaz.com/en/blog/2026/03/15/bedrock-agentcore-policy-cedar-authorization
- Cedar Policy Language 2026 complete guide (StrongDM) — https://www.strongdm.com/cedar-policy-language
- AuthZEN Authorization API 1.0 deep dive (dev.to kanywst, 2026) — https://dev.to/kanywst/authzen-authorization-api-10-deep-dive-the-standard-api-that-separates-authorization-decisions-1m2a
- OPA AuthZEN plugin (dev.to kanywst, 2026) — https://dev.to/kanywst/i-built-an-opa-plugin-that-turns-it-into-an-authzen-compatible-pdp-i81
- Gravitee MCP Resource Server v2 with AuthZEN PDP integration (2026) — https://www.gravitee.io/blog/elevating-mcp-security-with-gravitees-mcp-resource-server-v2
- Gartner IAM 2026 insights on AI agent authorization (Axiomatics) — https://axiomatics.com/blog/insights-from-gartner-iam-2026-in-london
- XACML 3.0 Profile of OpenID AuthZEN 1.0 (OIDF WG) — https://hackmd.io/@oidf-wg-authzen/xacml-to-authzen — bridge spec for teams migrating legacy XACML PDPs to AuthZEN-compatible AI agent authorization.
- Plurilock PDP glossary entry (April 2026) — https://plurilock.com/glossary/policy-decision-point/

---

## Open Research Questions

- [ ] What is the right latency budget for out-of-process PDP evaluation on an agent hot path? Microsoft's in-process toolkit targets <0.1 ms p99; an isolated PDP typically runs 1–10 ms. Where does the security/latency tradeoff land for different agent classes (synchronous chat vs long-horizon autonomous)?
- [ ] How should PDPs handle delegation chains? If agent A spawns agent B, does B carry A's authorization token, a derived capability, or re-authenticate from scratch? AuthZEN 1.0 has no agent-delegation primitives yet.
- [ ] What is the correct threat model when the PDP itself runs a policy authored (or suggested) by an LLM? "AI-generated policy" is appearing in vendor demos but has no audit guidance.
- [ ] Is there a reference architecture for PDP evaluation against long-term agent memory stores (vector DBs, episodic memory) where the data the policy reads may itself be poisoned (MITRE ATLAS `AI Agent Context Poisoning`)?
- [ ] How do organizations reconcile the MCP model (where the MCP server is often itself the PEP) with out-of-process PDP evaluation? MCP-native authorization patterns are still converging.

---

## Related Pages

- [C5.6 Autonomous Agent Authorization](C05-06-Autonomous-Agent-Authorization.md) — the policy content (scoped capabilities, per-action evaluation) the isolated PDP actually evaluates.
- [C5.3 Query-Time Security Enforcement](C05-03-Query-Time-Security-Enforcement.md) — the PEP-side story for RAG and data-store reads, counterpart to the PDP isolation discussed here.
- [C8.1 Access Controls for Memory and RAG](C08-01-Access-Controls-Memory-RAG.md) — authorization against vector stores and retrieval indices the agent reads from.
- [C8.5 Scope Enforcement for User Memory](C08-05-Scope-Enforcement-User-Memory.md) — pre-filter and post-filter scope enforcement that complements the typed-envelope contract in 5.5.2.
- [C9.2 High-Impact Action Approval](C09-02-High-Impact-Action-Approval.md) — the REQUIRE_APPROVAL branch of a PDP decision, covering nonce-protected human-in-the-loop gates.

---
