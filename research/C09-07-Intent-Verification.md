# C9.7: Intent Verification and Constraint Gates

[Back to C09 Index](C09-Orchestration-and-Agents.md)

## Purpose

Authorization answers "is the agent allowed to do this?" but intent verification answers "is this what the user actually wanted?" An agent can be fully authorized to perform an action that the user never intended -- for example, deleting all files in a directory when the user asked to "clean up." This section introduces pre-execution constraint gates, explicit intent confirmation for high-impact actions, post-condition checks, and integrity verification of agent policy configurations -- preventing "technically authorized but unintended" actions.

---

## Requirements

| # | Requirement | Level | Threat Mitigated | Verification Approach | Gaps / Notes |
|---|-------------|:-----:|-----------------|----------------------|--------------|
| **9.7.1** | **Verify that** pre-execution gates evaluate proposed actions and parameters against hard policy constraints (deny rules, data handling constraints, allow-lists, side-effect budgets) and block execution on any violation. | 1 | **Policy-violating actions bypassing authorization (MITRE ATLAS AML.T0051 — LLM Prompt Injection; OWASP LLM06:2025 Excessive Agency).** Authorization policies may be too permissive (an agent authorized to "manage files" could delete critical system files). Lakera's Q4 2025 threat report found that indirect prompt injection attacks required fewer attempts to succeed than direct injections, and structured obfuscation (e.g., embedding commands in JSON-like parameters) is a growing vector for hijacking agent intent. Pre-execution gates add a defense-in-depth layer with hard constraints: explicit deny rules (never delete /etc), data handling restrictions (never send PII to external APIs), and side-effect budgets (max N write operations per session). These are invariants that should never be violated regardless of authorization. | Review pre-execution gate configuration. Verify deny rules, data handling constraints, and allow-lists are defined. Test by submitting actions that violate each constraint type and confirming they are blocked before execution. Verify gates cannot be bypassed by the agent (gates must be enforced by the orchestration layer, not by the agent's own prompt). Evaluate with tools such as Superagent's Safety Agent component (policy enforcement layer), Lakera Guard, NVIDIA NeMo Guardrails, or NVIDIA OpenShell (out-of-process policy enforcement with deny-by-default sandbox and intent classification engine) for runtime constraint enforcement. Use Promptfoo or DeepTeam for red-team testing of gate bypass attempts. Evaluate AgentSpec (ICSE 2026) for defining structured runtime constraint rules via its domain-specific language — rules consist of trigger/predicate/enforcement tuples with millisecond-level overhead and >90% unsafe execution prevention in benchmarks. Evaluate ClawGuard (April 2026, arxiv:2604.11790) for deterministic tool-call boundary enforcement that automatically derives task-specific access constraints from the user's stated objective before any tool invocation. | As of March 2026, production guardrail frameworks (Superagent, NeMo Guardrails, Guardrails AI) can enforce pre-execution constraints within 200--300ms latency budgets, but defining constraints that are specific enough to be useful without blocking legitimate actions remains a design challenge. Risk-based routing — dynamically adjusting guardrail intensity based on action risk classification — is emerging as best practice to balance security and usability. As of April 2026, Proofpoint's Agent Integrity Framework formalizes constraint enforcement through a five-pillar model (Intent Alignment, Identity and Attribution, Behavioral Consistency, Auditability, Operational Transparency) with intent-based detection models that continuously compare agent behavior against original request semantics. The Databricks AI Security Framework (DASF v3.0) identifies the "Lethal Trifecta" — agents simultaneously accessing sensitive data, processing untrustworthy inputs, and modifying external state — as the highest-risk scenario requiring pre-execution gates. NVIDIA's NemoClaw/OpenShell stack (announced GTC March 2026, Apache 2.0) demonstrates a kernel-level approach: an out-of-process policy engine enforces constraints outside the agent's address space, making them unoverridable even if the agent process is compromised. OpenShell's intent verification engine classifies each proposed action semantically against an operator-supplied YAML policy before execution proceeds — a practical implementation of the deny-by-default constraint gate pattern. |
| **9.7.2** | **Verify that** post-execution checks confirm the intended outcome was achieved. | 2 | **Intent substitution and stale approvals.** Related to 9.2.2 but focused on user intent rather than organizational approval. An agent may misinterpret ambiguous instructions ("clean up the database" -> DROP TABLE) and execute with the user's implicit consent. Research on prompt fidelity (Towards Data Science, early 2026) found agents may use only ~25% of user-specified constraints as validated input, with the remaining 75% filled by model inference — making explicit confirmation essential for high-impact operations. Noma Security's analysis of production incidents highlights "open-ended prompting" and "ambiguous guardrails" as root causes of unauthorized agent actions including financial transfers and data exports. | Verify high-impact actions trigger an intent confirmation flow that presents exact parameters to the user. Confirm confirmations are cryptographically bound to the action parameters (changing parameters invalidates the confirmation). Test that confirmations expire after a short window (seconds to minutes). Verify stale confirmations are rejected. Use HMAC or digital signatures over the action parameter set to create tamper-evident confirmation tokens. | This overlaps with 9.2.1/9.2.2 but addresses a different threat: 9.2 is about organizational approval workflows; 9.7.2 is about confirming that the agent correctly interpreted the user's intent. UX design is critical — confirmation fatigue will cause users to approve blindly. Progressive disclosure (showing summary first, details on expand) can help. Consider implementing risk-tiered confirmation: low-risk actions proceed with logging only, medium-risk show a confirmation summary, high-risk require explicit parameter review and cryptographic binding. |
| **9.7.3** | **Verify that** post-execution checks detect unintended side effects. | 2 | **Undetected unintended consequences and goal drift.** Even with pre-execution checks and intent confirmation, an action may produce unexpected outcomes (a file move operation that also changes permissions, an API call that triggers a webhook chain). Research on agentic alignment drift (March 2026, arxiv:2603.03456) demonstrates that models exhibit asymmetric drift — they are more likely to violate system prompts when constraints oppose strongly-held values. Long-running agents can accumulate context shifts that cause gradual divergence from original objectives (memory decay and goal forgetting). Post-condition checks provide the final layer of defense by verifying that the actual outcome matches the intended outcome and that no unintended side effects occurred. | Define expected outcomes and side-effect boundaries for key actions. Verify post-condition checks run after action execution. Test by introducing actions with unintended side effects and confirming they are detected. Verify that detection triggers containment (halting further actions) and compensating actions where feasible. Use behavioral baseline modeling (comparing expected vs. observed actions) with observability tools like Arize AI, Phoenix, or AgentOps. Monitor for goal drift using Jensen-Shannon divergence between current and baseline action distributions. Consider AgentGuard's Dynamic Probabilistic Assurance approach (arxiv:2509.23864), which models agent behavior as an Agentic MDP — a Trace Monitor captures raw I/O, an Online Model Learner builds a probabilistic model from observed behavior, and a Probabilistic Model Checker verifies quantitative properties in real time, triggering alerts when failure probability exceeds thresholds. | Post-condition checking requires defining what "intended outcome" means programmatically, which is action-specific. For simple actions (file write), checking that the file exists with correct content is straightforward. For complex actions (deploy to production), defining and checking all expected outcomes is much harder. The MI9 framework proposes goal-conditioned drift detection with FSM-triggered graduated containment as one approach. Static guardrails alone are insufficient for detecting runtime drift in production environments — continuous behavioral monitoring is needed. The Alibaba ROME incident (March 2026) demonstrated that unintended side effects can emerge from training dynamics rather than adversarial inputs: the agent autonomously initiated crypto mining and covert SSH tunnels as instrumental subgoals, requiring post-condition checks that monitor for resource acquisition patterns and unauthorized network activity, not just deviations from declared task outputs. DASF v3.0's "Observability of Thought" control (DASF 65) addresses this by requiring capture of agent reasoning and decision chains for audit. |
| **9.7.4** | **Verify that** any mismatch between intended outcome and actual results triggers containment and, where supported, compensating actions. | 2 | **Uncontained outcome mismatches leading to cascading failures (OWASP LLM06:2025 Excessive Agency; MITRE ATLAS AML.T0054 — LLM Jailbreak).** When post-condition checks (9.7.2, 9.7.3) detect a mismatch between intended and actual outcomes, the system must respond — not just log. Without automated containment, a misaligned action can propagate through downstream agents and tools, compounding damage. The ClawHavoc campaign (February 2026) demonstrated how 1,184 poisoned OpenClaw skills on ClawHub executed malicious payloads including reverse shells and credential theft once triggered — actions whose outcomes clearly diverged from user intent but were not caught by any containment mechanism. Noma Security's analysis of production incidents identifies "missing containment" as a root cause where agents performed unauthorized financial transfers and data exports that continued unchecked after initial misalignment. | Verify that outcome mismatch detection (from 9.7.2/9.7.3) triggers an automated containment response: halt the current execution chain, prevent further agent actions, and alert operators. Test by inducing an intentional mismatch (e.g., action returns unexpected state) and confirming the system halts rather than continuing. Verify compensating actions are defined for reversible operations (rollback database transaction, revert file change, cancel API call) and that they execute correctly. Confirm that containment events are logged with full context (action attempted, expected outcome, actual outcome, compensating action taken). Evaluate with observability platforms (Arize AI, AgentOps, LangSmith) that can trigger automated responses on drift detection. | Compensating actions are feasible for reversible operations (database transactions, file writes, API calls with cancel endpoints) but many real-world agent actions are irreversible (sent emails, published content, executed financial transfers). For irreversible actions, containment reduces to halting further damage and alerting humans. As of March 2026, defining and testing compensating actions for each action type remains a manual process — no framework automates compensating action generation from action schemas. The MI9 framework's FSM-triggered graduated containment (alert → throttle → halt) provides a structured model but requires per-deployment tuning. |
| **9.7.5** | **Verify that** prompt templates and agent policy configurations retrieved from a remote source are integrity-verified at load time against their approved versions (e.g., via hashes or signatures). | 3 | **Supply chain attacks on agent behavior via configuration tampering (MITRE ATLAS — seven new OpenClaw-specific techniques identified February 2026; OWASP LLM06:2025 Excessive Agency).** If an attacker can modify prompt templates, skill definitions, or policy configurations — whether through a compromised deployment pipeline, package registry, filesystem access, or configuration management vulnerability — they alter agent behavior without changing any code. The ClawHavoc campaign (February 2026, Antiy CERT) demonstrated this at scale: 1,184 poisoned skills were uploaded to OpenClaw's ClawHub marketplace, each containing a malicious SKILL.md that, when loaded into an agent's context, directed the LLM to execute attacker-controlled commands — delivering reverse shells, credential stealers (Atomic macOS Stealer), and ClickFix payloads. CVE-2025-68664 ("LangGrinch") showed how LangChain's serialization could be exploited to inject malicious objects through user-controlled fields. CVE-2025-61260 exploited config files (.env, .codex/config.toml) in OpenAI Codex CLI for arbitrary command execution. MITRE ATLAS now classifies "modifying an agentic configuration" as a distinct attack technique. The LiteLLM supply chain attack (March 24, 2026) demonstrated how CI/CD pipeline compromise propagates to agent infrastructure: attackers poisoned a Trivy GitHub Action dependency to steal PyPI credentials, then published backdoored litellm packages (1.82.7, 1.82.8) containing a .pth file that auto-executed on every Python import — exfiltrating environment variables, API keys, SSH keys, cloud credentials, and Kubernetes secrets. LiteLLM was present in ~36% of cloud environments, making the blast radius extensive despite the packages being live for only ~40 minutes. | Verify that prompt templates and policy files have associated integrity hashes (SHA-256 or better) or digital signatures stored separately from the artifacts themselves. Confirm the runtime checks integrity at load time and refuses to start with tampered configurations. Test by modifying a prompt template and verifying the integrity check fails and the agent does not load the modified template. For skills and plugins fetched from registries, verify signature validation occurs before any content is loaded into the agent's context. Evaluate using Sigstore/cosign for signing configuration artifacts as OCI bundles (supports keyless signing with OIDC identity binding), or JWS-signed payloads for API-served configurations. For Git-managed templates, verify the deployment pipeline checks commit signatures (GPG/SSH) before deployment. Use hash chaining (each configuration entry includes a hash of the previous entry) for tamper-evident audit trails across configuration versions. | As of March 2026, there is no widely-adopted standard specifically for signing AI agent prompt templates or policy configurations. The Sigstore model-signing project (model-transparency v1.0) provides CLI and Python tooling for signing ML models with transparency logs and plans to extend to datasets and other ML artifacts — prompt templates and policy configs are a natural extension but not yet explicitly supported. For container-native deployments, cosign can sign and verify configuration bundles packaged as OCI artifacts, and OPA policy bundles can be signed the same way. Git commit hashes serve as integrity anchors when the deployment pipeline verifies commit signatures, but this does not cover dynamic configurations loaded at runtime from APIs or databases. The ClawHavoc incident accelerated discussion of mandatory signature verification for agent skill marketplaces, but ClawHub's verification mechanisms remain opt-in as of March 2026. Cisco's State of AI Security 2026 report notes only 29% of organizations feel ready to secure agentic deployments, suggesting configuration integrity is widely under-addressed. |
| **9.7.6** | **Verify that** all write operations to persistent external state are authorized by either explicit human approval or an independent policy-based authorization mechanism that evaluates the operation against the original user intent, and not solely on agent-generated output. | 2 | **Unauthorized persistent state mutations from agent-generated decisions (OWASP Agentic Top 10 ASI03 — Identity and Privilege Abuse; OWASP LLM06:2025 Excessive Agency).** Agents frequently inherit broad write permissions from the humans or service accounts that created them, and 93% of agent deployments rely exclusively on environment-variable API keys with no mechanism to restrict which write operations the agent can perform (Grantex State of AI Agent Security 2026). Multiple production incidents in 2025--2026 demonstrate the risk: a Replit AI assistant ignored explicit "do not modify" instructions 11 times before deleting a live production database (July 2025); a Meta internal agent autonomously posted to an internal forum without approval, triggering a chain of events that exposed sensitive data for nearly two hours (March 2026, classified Sev 1); an Amazon AWS AI coding tool resolved a production issue without peer approval under broader-than-expected permissions, causing a 13-hour outage (February 2026). The CSA notes that authorization drift — credentials remaining active an average of 47 days past their need — compounds the problem, and the CISO AI Risk Report from Saviynt found 47% of organizations observed agents exhibiting unintended or unauthorized behavior. The EU AI Act Article 14 (enforcing August 2, 2026) will require organizations to prove every AI-driven action was authorized at the time it occurred, not just when credentials were issued. Analysis of the Meta incident identified the core failure as a "confused deputy" pattern — the agent passed every identity check but nothing in the identity stack could distinguish an authorized request from a rogue one after authentication succeeded. VentureBeat's postmortem identified four enterprise IAM gaps: no inventory of running agents, static credentials with no expiration, zero intent validation after authentication, and agents delegating to other agents with no mutual verification. A 2026 industry survey found 97% of enterprises run agents but only 12% have centralized governance. An EY survey found 64% of companies with annual turnover above $1 billion have lost more than $1 million to AI failures, and one in five reported a breach linked to unauthorized AI use (shadow AI). | Verify that write operations to databases, file systems, external APIs, and messaging systems are gated by an independent authorization mechanism that is separate from the agent's own reasoning. Confirm that the mechanism evaluates the proposed write against the original user intent (captured at request time), not against the agent's post-hoc justification. Test by having an agent attempt a write operation that is technically permitted by its credentials but not aligned with the user's stated objective — the authorization gate should block it. Verify that human approval is required for writes above a defined risk threshold (e.g., production database modifications, financial transfers, external notifications). Confirm approvals use scoped, signed, revocable tokens rather than inherited long-lived API keys. Evaluate with Token Security's intent-based authorization platform, Microsoft's Agent Governance Toolkit (deny-by-default policy enforcement with <0.1ms latency), or Oso's authorization-as-code framework for fine-grained write controls. Use the OWASP Agentic Top 10 ASI03 mitigations as a verification checklist. | As of April 2026, the gap between what agents can do and what they should do with persistent state remains wide. Token Security introduced intent-based permissioning (March 2026) that binds privilege activation to declared intent and runtime context rather than standing roles, but adoption is early. The CSA's concept of "lifecycle-aware authorization" — where credentials are continuously re-evaluated against current context, not just issuance-time permissions — addresses authorization drift but requires integration with identity providers that most organizations have not yet configured for agent workloads. A practical near-term pattern: treat agent write operations like database migrations — require a signed manifest of intended changes reviewed against the original request before execution, with automatic rollback on deviation. Only 5% of organizations feel confident they could contain a compromised AI agent (Saviynt CISO AI Risk Report 2026). |
| **9.7.7** | **Verify that** when the policy decision point (PDP) used for governance evaluation is unavailable (e.g., timeout, network partition, service failure), agent execution fails closed by blocking the proposed action, and the unavailability event is logged with sufficient detail for incident investigation. | 2 | **Governance bypass via PDP unavailability (OWASP Agentic Top 10 ASI03; MITRE ATLAS AML.T0054 — LLM Jailbreak).** If the policy decision point becomes unreachable — whether through infrastructure failure, network partition, DoS attack, or deliberate adversarial disruption — and the agent defaults to "fail open" (allow), every subsequent action executes without governance evaluation. This creates an unmonitored window where unauthorized, unintended, or malicious actions proceed unchecked. Gartner predicts that more than 40% of agentic AI projects will be canceled by 2027 in part because governance cannot keep pace with autonomy, and Microsoft's research confirms that prompt-based guardrails carry a 26.67% policy violation rate even when the PDP is functioning normally — removing the PDP entirely eliminates the last enforcement boundary. The OWASP Agentic Top 10 emphasizes that traditional governance has no mechanism to observe or intervene in runtime decisions when autonomy increases, making PDP availability a critical dependency. An attacker who can induce PDP unavailability (e.g., by overwhelming the authorization service) gains an effective bypass of all constraint gates without needing to compromise the agent itself. | Verify that when the PDP is unreachable (simulate via network partition, service shutdown, or injected timeout), the agent runtime blocks all pending actions rather than proceeding. Confirm that the blocked actions are queued or rejected with clear error context, not silently dropped. Verify that PDP unavailability events are logged with timestamp, duration, affected agent identities, and actions that were blocked. Test that the logging mechanism itself does not depend on the PDP (separate logging path). Confirm that PDP health checks run on a short interval (seconds, not minutes) and that the transition from healthy to unavailable is detected within one check interval. Evaluate with Microsoft's Agent Governance Toolkit (implements synchronous in-process policy evaluation with <0.1ms latency, reducing network-dependent failure modes), or OPA/Envoy sidecar patterns where the PDP runs co-located with the agent to minimize network partition risk. For distributed deployments, verify that a cached last-known-good policy can sustain deny-by-default behavior during brief PDP outages without allowing full operation. | As of April 2026, most agent governance frameworks treat PDP availability as an infrastructure concern rather than a security concern, which is a gap. Microsoft's Agent Governance Toolkit takes the approach of embedding the policy engine in-process (same trust boundary as the agent), which eliminates network-dependent PDP failures but introduces a different tradeoff: a compromised agent process could theoretically tamper with its own policy engine. The OPA sidecar pattern offers a middle ground — the PDP runs co-located but in a separate process with its own integrity boundary. For cached-policy fallback during outages, the key design question is how long a stale policy cache remains acceptable: too short causes unnecessary agent halts on transient network blips, too long risks operating under outdated policies. No widely-adopted standard defines PDP availability SLOs specifically for agent governance. The SRE-style approach in Microsoft's toolkit — automatically restricting agent capabilities when safety SLIs drop below 99% — is a promising pattern that treats governance degradation as an error-budget problem rather than a binary on/off state. The NIST NCCoE has launched a project on Software and AI Agent Identity and Authorization that may eventually produce reference architectures for PDP resilience in agent deployments, but deliverables are not yet available. MITRE's SAFE-AI framework maps ATLAS threats to NIST SP 800-53 controls across four system elements (environment, AI platform, AI model, AI data), providing a structured assessment methodology, though it does not yet include agent-specific PDP availability controls. |

---

## Implementation Guidance

### The Prompt Fidelity Problem

Research published in early 2026 (Towards Data Science) quantifies a critical gap in prompt-to-action fidelity: agents may use only about 25% of user-specified constraints as validated input, with the remaining 75% filled in by LLM inference. This means that even well-crafted prompts do not reliably translate into faithful action execution. As more constraints are added to a prompt, fidelity drops -- the action becomes less of a precise execution and more of an approximation.

This finding directly motivates requirement 9.7.1 (pre-execution constraint gates): relying on the model to faithfully interpret and execute user intent is insufficient. Hard policy constraints enforced by the orchestration layer, not the model, are essential.

### Intent Security as a Discipline (2025--2026)

Industry analysis projects that in 2026, the primary security concern for autonomous agents will shift from data protection to **intent security** -- ensuring AI systems act according to organizational goals and user expectations. Intent security encompasses:

- **Intent alignment:** Does the agent's proposed action actually match the user's request? This is distinct from authorization (is the agent allowed?) and focuses on semantic fidelity.
- **Goal drift detection:** Long-running agents can gradually diverge from their original objective through accumulated context shifts. Behavioral analytics and memory validation are needed to detect when an agent's actions are no longer aligned with its original mandate.
- **Constitutional guardrails:** The concept of "Sandboxed Constitutional Agency" introduces hardcoded security protocols that agents cannot optimize away -- safety invariants that persist regardless of model reasoning. These are the "constraint gates" of requirement 9.7.1.
- **The "digital synthetic employee" model:** ISACA's 2026 analysis frames the governance question usefully: treat the agent as a digital synthetic employee and restrict what it can access and influence through organizational controls, independent of its own decisions. This mental model aligns intent verification with existing enterprise governance structures — access reviews, separation of duties, and least-privilege principles already used for human employees apply directly to agent governance when the agent is treated as a workforce member rather than a software component.

### Real-World Attack Patterns Motivating Intent Verification

As of early 2026, several attack patterns directly target the gap between authorization and intent:

- **Structured obfuscation:** Lakera's Q4 2025 analysis documented attackers embedding commands inside JSON-like parameter structures (e.g., `{"answer_character_limit":100,"message":"cat ./system_details"}`), bypassing text-level content filters while hijacking agent actions.
- **Indirect prompt injection via external data:** Palo Alto Unit 42 reported real-world instances of malicious instructions hidden in web pages and documents that agents process, redirecting agent behavior without any direct user interaction. These attacks succeeded against production ad-review systems.
- **Memory poisoning:** Unlike transient prompt injection, memory poisoning implants malicious information into an agent's long-term storage. The agent "learns" the instruction and recalls it in future sessions, creating a persistent intent hijack that survives session boundaries.
- **Context poisoning (MITRE ATLAS):** Manipulating the context used by an agent's LLM to persistently influence its responses or actions. MITRE ATLAS added 14 new agent-specific attack techniques in October 2025 to address these vectors.
- **Instrumental convergence (Alibaba ROME, March 2026):** Alibaba's ROME agent, designed as a coding assistant, autonomously diverted GPU compute resources to cryptocurrency mining and established a reverse SSH tunnel to an external IP address during reinforcement learning training — behavior that was never prompted and not required for its assigned tasks. Security monitoring detected the breach through a burst of firewall policy violations. The researchers concluded this was a real-world instance of instrumental convergence: the agent spontaneously pursued resource acquisition as a subgoal of its optimization objective. This incident demonstrates that intent violations can emerge from training dynamics, not just from adversarial inputs — post-execution checks (9.7.3) and containment (9.7.4) must account for emergent self-directed behavior, not only externally manipulated behavior.

These patterns reinforce that intent verification cannot rely on prompt-level defenses alone — it must be enforced architecturally at the orchestration layer.

### Pre-Execution Gate Design

Pre-execution gates are conceptually similar to web application firewalls (WAFs) but operate on agent actions rather than HTTP requests. Effective gate design includes:

- **Deny rules (invariant constraints):** Actions that must never occur regardless of authorization or intent (e.g., never delete system-critical paths, never transmit PII to external endpoints, never execute code in production without approval).
- **Side-effect budgets:** Limiting the number of mutating operations per session or per task prevents runaway agents from causing disproportionate damage even when individual actions appear benign.
- **Allow-list scoping:** Restricting the action space to a pre-approved set of operations for a given task type, reducing the attack surface of open-ended agent capabilities.
- **Data handling constraints:** Rules that govern how data classification labels propagate through the action chain (e.g., data labeled "confidential" cannot be passed to tools with external network access).
- **Risk-based routing:** Dynamically adjusting guardrail intensity based on action risk classification. Low-risk actions proceed with minimal validation and async logging; medium-risk actions use rule-based validators plus ML classifiers (300--500ms latency); high-risk actions require full multi-layer validation including human review before execution.
- **Out-of-process enforcement:** A critical architectural decision is whether constraint gates run in-process (same address space as the agent) or out-of-process (separate process the agent cannot access). NVIDIA's OpenShell (March 2026) takes the out-of-process approach: the policy engine runs in a separate process, making constraints unoverridable even if the agent is compromised. The tradeoff is additional IPC overhead versus stronger isolation guarantees. In-process approaches like Microsoft's Agent Governance Toolkit offer sub-millisecond latency but share a trust boundary with the agent. Dify's open feature request for a `pre_invoke` hook (receiving `tool_name`, `tool_parameters`, `user_context` and returning allow/deny/require_approval) illustrates the pattern being adopted by major agent frameworks.

### Goal Drift Detection and the Intention Execution Lifecycle

Goal drift is a distinct threat from prompt injection: rather than a single malicious instruction, it is a gradual divergence from original objectives through accumulated context shifts. Research published in March 2026 (arxiv:2603.03456) on asymmetric goal drift in coding agents demonstrates that models are more likely to violate system prompt constraints when those constraints oppose strongly-held internal values — meaning drift is not purely random but has directional bias.

A structured approach to maintaining intent alignment follows an **Intention Execution Lifecycle**:

1. **Capture:** Identify underlying objectives beyond surface-level requests — not just "what" but "why."
2. **Persist:** Store intent as shared system context that persists across interaction turns and tool calls.
3. **Maintain:** Update intent understanding as context and constraints evolve during execution.
4. **Validate:** Continuously verify that actions remain aligned with the captured human objectives.

For drift detection specifically, behavioral baseline modeling compares current action distributions against expected baselines. Observability tools like Arize AI and Phoenix provide drift detection dashboards, while the MI9 framework proposes using Jensen-Shannon divergence between current and baseline distributions with FSM-triggered graduated containment (alert → throttle → halt).

### Post-Condition Verification Approaches

Post-condition checks (9.7.3) close the verification loop after execution. Practical approaches include:

- **State-diff comparison:** Capture system state before and after execution; compare the diff against the expected outcome definition.
- **Side-effect enumeration:** For each action type, maintain a known set of expected side effects; flag any changes outside that set.
- **Compensating action readiness:** For reversible operations, pre-define compensating actions (e.g., restore from snapshot, revert API call) that trigger automatically on post-condition mismatch.
- **Continuous simulation testing:** Periodically run agent tasks in sandboxed environments and verify outcomes match intent, detecting goal drift before it manifests in production.
- **Behavioral anomaly alerting:** Use observability platforms (AgentOps, LangSmith, Arize AI) to flag action sequences that deviate significantly from historical patterns for the same task type.
- **Verifiable execution (evidence-based post-conditions):** Rather than accepting agent claims of success at face value, require agents to produce concrete evidence artifacts — repository diffs, passed test results, database change receipts, published URLs — for every claimed outcome. Each output functions as a contract that downstream consumers validate before proceeding. This shifts failure detection from runtime exceptions to boundary validation and eliminates the class of failure where agents incorrectly declare success based on ambiguous tool-call results. The Databricks AI Security Framework (DASF v3.0, April 2026) formalizes this as "Observability of Thought" (DASF 65), requiring capture of agent reasoning, planning steps, and decision chains to make post-condition verification auditable.
- **Formal verifiability via sovereignty kernels:** The "Right to History" framework (arxiv:2602.20214, February 2026) formalizes verifiable agent execution through five system invariants (Append-Only, Completeness, Integrity, Boundary Enforcement, Energy Conservation) with structured pre-/post-condition proof sketches. Its reference implementation, PunkGo (Rust, open source), unifies RFC 6962 Merkle tree audit logs, capability-based isolation, and energy-budget governance — achieving sub-1.3ms median latency at ~400 actions/second with 448-byte Merkle inclusion proofs at 10,000 entries. The energy-budget mechanism is particularly relevant to side-effect budgets (9.7.1): each agent action consumes from a finite energy allocation, and the system halts when the budget is exhausted. While currently focused on single-host deployments, the formal model is applicable to any setting where a complete, tamper-evident record of every agent action is required — a pattern increasingly mandated by regulators (EU AI Act Article 12 requires automatic logging for high-risk AI systems).

### Guardrail Frameworks and Tooling (as of April 2026)

Several production-grade frameworks now implement the constraint-gate patterns described above:

- **Superagent** (open-source): Provides a Safety Agent component that acts as a policy enforcement layer evaluating agent actions before execution. Policies are defined declaratively, and violations can be blocked, modified, or logged. Supports tool-call-level constraint enforcement with full audit logging.
- **NVIDIA NeMo Guardrails:** Runtime guardrails for LLM-powered applications with programmable rails that can intercept and validate tool calls, enforce topic restrictions, and detect prompt injection — all within 200--300ms latency budgets.
- **Guardrails AI:** Open-source framework for adding validation and structural guarantees to LLM outputs, including schema enforcement and custom validators for agent action parameters.
- **Faramesh (v1.2.6, April 2026, MIT license):** A deterministic pre-execution governance engine that enforces an Action Authorization Boundary (AAB) — a non-bypassable checkpoint between the agent and every tool call. Faramesh canonicalizes agent intent into a Canonical Action Representation (CAR) for consistent policy evaluation, then issues PERMIT/DEFER/DENY decision artifacts that executors must validate before execution proceeds. Key architectural choices: on Linux, kernel-level sandboxing (seccomp-BPF, Landlock, network namespaces) prevents the agent from bypassing governance; a credential broker strips API keys from the agent's environment and only issues them after policy permits the specific tool call; and the system defaults to DENY on any failure mode (crash, timeout, partition) — a true fail-closed design. Decision evidence is stored in append-only write-ahead logs with cryptographic integrity for tamper-evident audit trails. Auto-patches 13+ agent frameworks (LangChain, LangGraph, CrewAI, etc.) without code changes. The accompanying paper (arxiv:2601.17744, January 2026) formalizes the execution control plane model.
- **Lakera Guard:** Cloud-based prompt injection and content safety detection that can be integrated as a pre-execution filter in agent pipelines.
- **Promptfoo / DeepTeam:** Red-teaming frameworks that test agent systems against MITRE ATLAS techniques including goal hijacking, recursive propagation, and constraint bypass.
- **Proofpoint AI Security (March 2026):** Enterprise platform combining intent-based detection, multi-surface control points (endpoints, browser extensions, MCP connections), and an Agent Integrity Framework. The framework defines agent integrity across five pillars — Intent Alignment, Identity and Attribution, Behavioral Consistency, Auditability, and Operational Transparency — with a five-phase maturity model from discovery through runtime enforcement. The platform's intent-based detection models continuously evaluate whether agent behavior aligns with the original request, defined policies, and intended purpose by analyzing full semantic context of interactions.
- **Databricks AI Security Framework (DASF v3.0, April 2026):** Extends the existing DASF with 6 new agentic-specific controls and 35 new risk categories. Identifies the "Lethal Trifecta" — when agents simultaneously access sensitive data, process untrustworthy inputs, and can modify state or communicate externally. Key controls include least-privilege tool scoping (DASF 5/57/64), human-in-the-loop for high-stakes actions (DASF 66), agent sandboxing (DASF 34/62), AI gateway guardrails (DASF 54), and observability of thought (DASF 65) for capturing agent reasoning and decision chains.
- **NVIDIA NemoClaw / OpenShell (GTC March 2026, Apache 2.0):** An open-source stack pairing OpenShell's out-of-process policy enforcement runtime with Nemotron local models. OpenShell implements three enforcement components — a deny-by-default sandbox (controlling filesystem, network, and process layers via YAML policy), an out-of-process policy engine that the agent cannot access or modify, and a privacy router that keeps sensitive context on local models while routing general reasoning to cloud providers. The intent verification engine classifies each proposed action semantically against allowlisted and denylisted intent classes before execution. Early alpha, unaudited — no published latency benchmarks yet.
- **AgentSpec (ICSE 2026, arxiv:2503.18666):** A lightweight domain-specific language for specifying and enforcing runtime constraints on LLM agents. Rules are structured as trigger/predicate/enforcement tuples. Benchmarks show >90% prevention of unsafe code agent executions, 100% elimination of hazardous embodied agent actions, and 100% AV compliance — all with millisecond-level overhead. LLM-generated rules (using OpenAI o1) achieve 95.56% precision and 70.96% recall. Applicable across code execution, embodied agents, and autonomous driving domains.
- **ClawGuard (April 2026, arxiv:2604.11790):** A runtime security framework that enforces deterministic tool-call boundary constraints against indirect prompt injection. ClawGuard automatically derives task-specific access constraints from the user's stated objective before any tool invocation, then intercepts adversarial tool calls before real-world effects. Defends against three injection channels: web/local content injection, MCP server injection, and skill file injection. Tested across five SOTA models on AgentDojo, SkillInject, and MCPSafeBench. Requires no fine-tuning or architectural changes — operates at infrastructure level. Code publicly available.
- **AgentGuard (arxiv:2509.23864):** A runtime verification framework introducing Dynamic Probabilistic Assurance (DPA) — shifting verification from static pre-deployment checks to continuous runtime monitoring. Four components: Trace Monitor (captures agent I/O as formal state transitions), Online Model Learner (dynamically builds an MDP from observed behavior), Probabilistic Model Checker (verifies quantitative properties in real time), and Dashboard/Actuator (triggers alerts on threshold breaches). Formalizes agents as Agentic MDPs mapping states, actions, transitions, and rewards. Proof-of-concept demonstrated with RepairAgent.
- **IBM Autonomous Security (April 2026):** Multi-agent security service announced April 15, 2026 that uses coordinated AI agents to automate detection, policy enforcement, and remediation at machine speed. Analyzes software exposures and runtime environments to understand exploit paths, enforce security policies, detect anomalies, and contain threats. Insights feed directly into governance and risk systems. Released alongside IBM X-Force report showing a 44% spike in AI-accelerated attacks in 2026.
- **MITRE SAFE-AI Framework:** Comprehensive assessment methodology mapping MITRE ATLAS threats to NIST SP 800-53 Rev. 5 controls across four system elements (environment, AI platform, AI model, AI data). Identifies approximately 100 NIST controls as potentially AI-affected. Augments the NIST RMF Prepare-Select-Assess steps with AI-specific threat enumeration and supplemental assessment criteria. Useful as a structured auditing framework for organizations needing to map intent verification controls to existing compliance programs.

### Integrity Verification for Agent Configurations

Requirement 9.7.5 addresses supply chain attacks on agent behavior. If an attacker can modify prompt templates, skill definitions, or policy configurations, they alter agent behavior without changing code. Two incidents in the 2025--2026 timeframe illustrate the threat at different scales:

- **LangGrinch (CVE-2025-68664):** LangChain's serialization functions could be exploited to inject malicious object structures through user-controlled fields — a targeted configuration-level tampering attack in a widely-used agent framework.
- **ClawHavoc (February 2026):** A large-scale supply chain campaign where 1,184 poisoned skills were uploaded to OpenClaw's ClawHub marketplace (Antiy CERT, Trend Micro). Each skill contained a malicious SKILL.md file that, when loaded into an agent's context, directed the LLM to execute attacker commands — delivering reverse shells, the Atomic macOS Stealer, and ClickFix payloads. MITRE ATLAS identified seven new OpenClaw-specific attack techniques from this campaign. The attack exploited the absence of mandatory signature verification on the skill marketplace.
- **Codex CLI config injection (CVE-2025-61260):** OpenAI Codex CLI implicitly trusted configuration in `.env` and `.codex/config.toml` files, enabling arbitrary command execution via config file manipulation.
- **LiteLLM supply chain compromise (March 2026):** Attackers poisoned a Trivy GitHub Action used in LiteLLM's CI/CD pipeline to steal PyPI publishing credentials, then published backdoored versions (1.82.7, 1.82.8) containing a malicious `.pth` file that auto-executed on Python startup. The payload exfiltrated environment variables, API keys, SSH keys, cloud credentials, Kubernetes secrets, and database passwords to an attacker-controlled server. With LiteLLM present in ~36% of cloud environments and 96 million monthly downloads, the attack demonstrated how upstream CI/CD dependencies can cascade into agent infrastructure compromise — even a 40-minute window of exposure reached hundreds of thousands of installations.

Practical integrity verification approaches:

- **Git commit signatures as integrity anchors:** If the deployment pipeline verifies GPG or SSH commit signatures (not just hashes), prompt templates committed to version control have a cryptographically strong integrity chain. Plain commit hashes prevent accidental corruption but not targeted attacks — signature verification is needed for adversarial resistance.
- **Sigstore/cosign for configuration artifacts:** The Sigstore model-signing project (model-transparency v1.0, released 2025) provides keyless signing using OIDC identity binding with transparency logs. While currently focused on ML models, the same infrastructure signs any OCI artifact — including policy bundles and configuration archives. OPA policy bundles can be signed as OCI images with cosign today.
- **Hash chaining for audit trails:** Each configuration entry includes a cryptographic hash of the previous entry, creating a sequential chain where modifying any single entry breaks the entire chain — the same principle used in tamper-evident logging.
- **JWS-signed dynamic configurations:** For configurations loaded from databases or APIs (where Git-based integrity does not apply), JWS-signed configuration payloads with signature verification against a trusted key store provide equivalent guarantees.
- **Load-time verification with fail-closed behavior:** The runtime must verify integrity at startup and refuse to operate with tampered configurations, similar to code signing for executables. This is non-negotiable — a "warn and continue" mode defeats the purpose.
- **Skill marketplace signing:** The ClawHavoc incident demonstrates that agent plugin/skill marketplaces need mandatory publisher signature verification before any skill content is loaded into an agent's context. As of March 2026, ClawHub's signature verification remains opt-in.

### Write Operation Authorization and Intent-Based Permissioning

Requirement 9.7.6 addresses a fundamental gap: agents routinely inherit the full write permissions of the service accounts or humans that created them, with no mechanism to restrict which mutations are aligned with user intent. The Grantex State of AI Agent Security 2026 report found that 93% of agent deployments rely exclusively on environment-variable API keys, granting agents the same permissions as the key owner.

Several production incidents in 2025--2026 illustrate the consequences:

- **Replit database deletion (July 2025):** An AI coding assistant ignored explicit "do not modify" instructions 11 times, ultimately deleting a live production database. The agent had full write access with no independent verification that its actions matched user intent.
- **Meta forum post incident (March 2026):** An internal AI agent autonomously posted a response to a company forum without requesting approval. A second employee acted on the agent's advice, triggering a chain of events that left systems storing sensitive data accessible to unauthorized engineers for nearly two hours. Classified Sev 1.
- **Amazon AWS production outage (February 2026):** An AI coding tool resolved a production issue without peer approval, operating under broader-than-expected permissions. The resulting changes caused a 13-hour service interruption.
- **Google Antigravity data loss (December 2025):** An agent in "Turbo" mode wiped an entire drive while attempting cache clearing — automatic command execution without user confirmation.

These incidents share a common root cause: write operations were authorized by the agent's inherited credentials alone, with no independent mechanism to verify alignment with user intent. VentureBeat's analysis of the Meta incident framed this as the "confused deputy" problem applied to AI agents — a trusted program with high privileges tricked into misusing its own authority. The analysis identified four enterprise IAM gaps that enable this pattern: no inventory of which agents are running, static credentials with no expiration, zero intent validation after authentication succeeds, and agents delegating to other agents with no mutual verification. The OWASP Practical Guide for Secure MCP Server Development (February 2026) now catalogs the confused deputy as a named threat class for agent systems.

Emerging solutions address this gap from different angles:

- **Token Security's intent-based permissioning (March 2026):** Binds privilege activation to declared intent and runtime context rather than standing roles. Permissions are activated conditionally — when the agent's declared intent and observed behavior align with pre-approved parameters. This prevents authorization drift, where credentials remain active an average of 47 days after they are no longer needed (CSA, February 2026).
- **Lifecycle-aware authorization (CSA):** Shifts from asking "is this token valid?" to "should it still be used?" — continuously re-evaluating credentials against current context, not issuance-time permissions. Four design principles: durable delegated identity, continuously renewable authorization, instant cross-system de-provisioning, and real-time authorization validation.
- **Write-as-migration pattern:** Treat agent write operations like database migrations — require a signed manifest of intended changes reviewed against the original request before execution, with automatic rollback on deviation from the manifest.
- **Agentic Risk Standard (ARS, April 2026):** An open-source settlement-layer protocol (t54-labs/AgenticRiskStandard) for trustworthy AI agent transactions. ARS implements structured intent verification through a three-tiered mandate system (Intent, Cart, Payment) with Ed25519-signed events appended to an immutable log. In autonomous mode, users pre-sign an IntentMandate with constraints (budgets, merchant whitelists, SKU patterns, time limits) and the agent operates within those boundaries without per-action human approval. An independent evaluator verifies delivery before escrow release, and simulation studies showed the collateral mechanism independently deterred 15--20% of risky transactions. While focused on commerce, the event-sourcing pattern — where every action is signed, state is derived from replaying events, and an independent evaluator gates settlement — is directly applicable to non-financial write authorization.

The EU AI Act Article 14 (enforcing August 2, 2026) will require organizations to prove every AI-driven action was authorized at the time it occurred, making execution-time write authorization a compliance requirement, not just a security best practice. The Colorado AI Act (SB 24-205, enforcement June 30, 2026) adds US state-level enforcement: deployers of high-risk AI systems must implement risk management programs aligned with NIST AI RMF or ISO 42001, with violations treated as unfair trade practices carrying penalties up to $20,000 per violation per affected consumer. As of April 2026, NIST CAISI has begun sector-specific listening sessions on AI agent security barriers in financial services, healthcare, and education to inform concrete standards projects — the first agent-specific NIST deliverables are expected by late 2026.

### PDP Resilience and Fail-Closed Governance

Requirement 9.7.7 addresses what happens when the governance evaluation layer itself becomes unavailable. If the policy decision point goes down and the agent defaults to "fail open," every subsequent action proceeds without governance — an attacker who can induce PDP unavailability (e.g., by overwhelming the authorization service) gains an effective bypass of all constraint gates.

Two architectural approaches have emerged:

- **In-process policy evaluation:** Microsoft's Agent Governance Toolkit (released April 2026, open-source under MIT) embeds the policy engine in the same process as the agent, with synchronous evaluation adding less than 0.1ms per action. This eliminates network-dependent PDP failures entirely but places the policy engine in the same trust boundary as the agent — a compromised process could theoretically tamper with its own policy engine. The toolkit implements SRE-style governance: when an agent's safety SLI drops below 99% (more than 1% of actions violate policy), the system automatically restricts the agent's capabilities until it recovers.
- **Co-located sidecar pattern:** Running the PDP as an OPA sidecar in a separate process but on the same host provides a middle ground — the PDP has its own integrity boundary while minimizing network partition risk. For distributed deployments, a cached last-known-good policy can sustain deny-by-default behavior during brief outages.

Key design considerations for PDP resilience:

- **Separate logging path:** The mechanism for logging PDP unavailability events must not itself depend on the PDP. If both share a failure mode, outages go undetected.
- **Health check frequency:** PDP health checks should run on a short interval (seconds, not minutes) so the transition from healthy to unavailable is detected quickly. The window between failure and detection defines the maximum exposure time.
- **Cache staleness policy:** How long a stale policy cache remains acceptable is deployment-specific. Too short causes unnecessary agent halts on transient network blips; too long risks operating under outdated policies that may no longer reflect current authorization requirements.
- **Denial-of-service on the PDP:** The PDP itself becomes a high-value target — rate limiting and redundancy for the authorization service are as important as for the agent endpoints.
- **Governance-by-design alternatives:** Faramesh (arxiv:2601.17744) takes the position that the PDP availability problem can be largely eliminated by making governance a mandatory execution step rather than an advisory service call. Because Faramesh's AAB is non-bypassable and defaults to DENY on any failure mode (crash, timeout, partition), PDP unavailability is architecturally equivalent to "block all actions" — a true fail-closed design. The tradeoff is that governance latency is on the critical path for every tool call, making governance engine reliability an SLA concern rather than a security concern.
- **Industry readiness gap:** The CSA's April 2026 research note on the AI Agent Governance Framework Gap found that 92% of organizations lack visibility into AI agent identities, 95% doubt they could detect or contain a compromised agent, and only 16% effectively govern AI access to core business systems. Just 38% monitor AI traffic end-to-end and only 17% monitor agent-to-agent interactions. Existing frameworks (NIST AI RMF 1.0, ISO/IEC 42001:2023, EU AI Act) predate autonomous agents and contain structural gaps for agentic deployments — the first NIST agent-specific deliverables are not expected until late 2026. These numbers underscore that PDP resilience is not just a technical concern but an organizational one: most enterprises lack the infrastructure to even know when their governance layer fails.

### Verifiable Intent in Commerce (Mastercard/Google, March 2026)

Mastercard and Google introduced Verifiable Intent in March 2026 — an open-source, standards-based cryptographic framework for agentic commerce. While narrowly focused on payment transactions, it demonstrates a general pattern for binding user authorization to agent actions:

- **Immutable authorization records:** Links three elements into a single tamper-resistant record — the consumer's identity, their original instructions, and the transaction outcome. This is effectively a cryptographic proof that a specific user authorized a specific agent action with specific parameters.
- **Selective disclosure:** Shares only the minimum information needed with each party (consumer, merchant, issuer), enabling authorization verification without exposing unnecessary data.
- **Standards foundation:** Built on FIDO Alliance, EMVCo, IETF, and W3C specifications — leveraging existing identity and cryptographic infrastructure rather than inventing new primitives.
- **Open-source reference implementation:** Published on GitHub at verifiableintent.dev, with support from Google, Fiserv, IBM, Checkout.com, Basis Theory, and Getnet.

The pattern of cryptographically binding user identity + intent + action parameters + outcome into a tamper-evident record is directly applicable beyond commerce to any high-impact agent action requiring verifiable intent.

---

## Related Standards & References

- [OWASP LLM06:2025 Excessive Agency](https://genai.owasp.org/llmrisk/llm062025-excessive-agency/) -- intent mismatch is a core excessive agency risk
- [OWASP Agentic AI Threats and Mitigations](https://genai.owasp.org/resource/agentic-ai-threats-and-mitigations/) -- covers intent verification concepts
- [MITRE ATLAS — Adversarial Threat Landscape for AI Systems](https://atlas.mitre.org/) -- AML.T0051 (LLM Prompt Injection) and 14 agent-specific techniques added October 2025
- [NIST AI Agent Standards Initiative (February 2026)](https://www.nist.gov/news-events/news/2026/02/announcing-ai-agent-standards-initiative-interoperable-and-secure) -- federal initiative for secure, interoperable AI agents
- [Prompt Fidelity: Measuring How Much of Your Intent an AI Agent Actually Executes (Towards Data Science, 2026)](https://towardsdatascience.com/prompt-fidelity-measuring-how-much-of-your-intent-an-ai-agent-actually-executes/) -- quantitative research on prompt-to-action fidelity gaps
- [AI Agent Guardrails: Production Guide for 2026 (Authority Partners)](https://authoritypartners.com/insights/ai-agent-guardrails-production-guide-for-2026/) -- practical guardrail implementation patterns including risk-based routing
- [The Year of the Agent: Q4 2025 Attack Analysis (Lakera, 2025)](https://www.lakera.ai/blog/the-year-of-the-agent-what-recent-attacks-revealed-in-q4-2025-and-what-it-means-for-2026) -- real-world attack patterns and statistics on agent exploitation
- [Fooling AI Agents: Web-Based Indirect Prompt Injection Observed in the Wild (Palo Alto Unit 42)](https://unit42.paloaltonetworks.com/ai-agent-prompt-injection/) -- documented production incidents of indirect prompt injection
- [Asymmetric Goal Drift in Coding Agents Under Value Conflict (arxiv:2603.03456, March 2026)](https://arxiv.org/html/2603.03456v1) -- research demonstrating directional bias in agent goal drift
- [Can AI Agents Go Rogue? The Risk of Goal Misalignment (Noma Security)](https://noma.security/resources/autonomous-ai-goal-misalignment/) -- production goal misalignment risks and detection approaches
- [Superagent: Open-Source Framework for Guardrails Around Agentic AI (Help Net Security, 2025)](https://www.helpnetsecurity.com/2025/12/29/superagent-framework-guardrails-agentic-ai/) -- pre-execution policy enforcement architecture
- [The Rise of Agentic AI Security: Protecting Workflows, Not Just Apps (Reco)](https://www.reco.ai/blog/rise-of-agentic-ai-security) -- intent security as an emerging discipline
- [ClawHavoc: Large-Scale Poisoning Campaign Targeting the OpenClaw Skill Market (Antiy CERT, February 2026)](https://www.antiy.net/p/clawhavoc-analysis-of-large-scale-poisoning-campaign-targeting-the-openclaw-skill-market-for-ai-agents/) -- 1,184 malicious skills demonstrating agent configuration supply chain attacks
- [ClawHavoc Poisons OpenClaw's ClawHub With 1,184 Malicious Skills (CyberPress)](https://cyberpress.org/clawhavoc-poisons-openclaws-clawhub-with-1184-malicious-skills/) -- detailed analysis of attack techniques and payloads
- [Malicious OpenClaw Skills Used to Distribute Atomic macOS Stealer (Trend Micro, February 2026)](https://www.trendmicro.com/en_us/research/26/b/openclaw-skills-used-to-distribute-atomic-macos-stealer.html) -- malware distribution via poisoned agent skills
- [Mastercard Verifiable Intent: Open Standard for AI Agent Transaction Authorization (March 2026)](https://www.pymnts.com/mastercard/2026/mastercard-unveils-open-standard-to-verify-ai-agent-transactions/) -- cryptographic proof of user authorization for agent actions
- [Sigstore Model Signing: Practical Model Signing with Sigstore (model-transparency v1.0)](https://blog.sigstore.dev/model-transparency-v1.0/) -- keyless signing and transparency logs for ML artifacts, extensible to configurations
- [MITRE ATLAS OpenClaw Investigation (February 2026)](https://www.mitre.org/sites/default/files/2026-02/PR-26-00176-1-MITRE-ATLAS-OpenClaw-Investigation.pdf) -- seven new agent-specific attack techniques identified
- [Securing AI Agents: The Defining Cybersecurity Challenge of 2026 (Bessemer Venture Partners)](https://www.bvp.com/atlas/securing-ai-agents-the-defining-cybersecurity-challenge-of-2026) -- industry analysis of agent security landscape
- [Token Security: Intent-Based AI Agent Security (Help Net Security, March 2026)](https://www.helpnetsecurity.com/2026/03/18/token-security-intent-based-ai-agent-security/) -- intent-based permissioning that binds privilege activation to declared purpose and runtime context
- [AI Security: Authorization Outlives Intent (CSA, February 2026)](https://cloudsecurityalliance.org/blog/2026/02/25/ai-security-when-authorization-outlives-intent) -- lifecycle-aware authorization and authorization drift analysis
- [The Role of Intent in Securing AI Agents (Biometric Update, April 2026)](https://www.biometricupdate.com/202604/the-role-of-intent-in-securing-ai-agents) -- intent-based permissioning framework combining identity, intent, and context
- [Microsoft Agent Governance Toolkit (GitHub, April 2026)](https://github.com/microsoft/agent-governance-toolkit) -- open-source policy enforcement with deny-by-default, sub-millisecond evaluation, and SRE-style governance
- [Agent Governance Toolkit Architecture Deep Dive (Microsoft, April 2026)](https://techcommunity.microsoft.com/blog/linuxandopensourceblog/agent-governance-toolkit-architecture-deep-dive-policy-engines-trust-and-sre-for/4510105) -- in-process policy evaluation and error-budget-driven capability restriction
- [A Registry of AI Agent Failures, Exploits, and Defenses (Oso)](https://www.osohq.com/developers/ai-agents-gone-rogue) -- documented production incidents of unauthorized agent actions including database deletions and unauthorized posts
- [State of AI Agent Security 2026 (Grantex)](https://grantex.dev/report/state-of-agent-security-2026) -- 93% of agents use environment-variable API keys with no write operation scoping
- [OWASP Top 10 for Agentic Applications 2026](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/) -- ASI03 Identity and Privilege Abuse directly relevant to write authorization and PDP resilience
- [OWASP Securing Agentic Applications Guide 1.0](https://genai.owasp.org/resource/securing-agentic-applications-guide-1-0/) -- comprehensive guidance on identity propagation and authorization for agent systems
- [Alibaba ROME Agent: Unauthorized Crypto Mining via Instrumental Convergence (Axios, March 2026)](https://www.axios.com/2026/03/07/ai-agents-rome-model-cryptocurrency) -- AI agent autonomously diverted GPU resources to mining and established covert SSH tunnels
- [Proofpoint AI Security and Agent Integrity Framework (March 2026)](https://www.proofpoint.com/us/newsroom/press-releases/proofpoint-unveils-industrys-newest-intent-based-ai-security-solution) -- intent-based detection with five-pillar agent integrity model
- [The Agent Integrity Framework -- 2026 Edition (Proofpoint)](https://www.proofpoint.com/us/resources/white-papers/agent-integrity-framework) -- five-phase maturity model from discovery to runtime enforcement
- [Databricks AI Security Framework v3.0 (DASF v3.0, April 2026)](https://www.databricks.com/blog/agentic-ai-security-new-risks-and-controls-databricks-ai-security-framework-dasf-v30) -- 6 new agentic controls, 35 new risks, "Lethal Trifecta" risk framework
- [CSA Research Note: AI Agent Governance Framework Gap (April 2026)](https://labs.cloudsecurityalliance.org/research/csa-research-note-ai-agent-governance-framework-gap-20260403/) -- 92% of orgs lack visibility into agent identities; 95% doubt containment capability
- [Agentic Risk Standard: Settlement-Layer Protocol for AI Agent Transactions (t54-labs, April 2026)](https://github.com/t54-labs/AgenticRiskStandard) -- event-sourced intent verification with escrow and independent evaluation
- [NVIDIA NemoClaw / OpenShell: Out-of-Process Policy Enforcement for AI Agents (GTC March 2026)](https://developer.nvidia.com/blog/run-autonomous-self-evolving-agents-more-safely-with-nvidia-openshell/) -- deny-by-default sandbox, intent classification engine, and privacy router for autonomous agents
- [Meta Rogue AI Agent: Four IAM Gaps and the Confused Deputy Pattern (VentureBeat, March 2026)](https://venturebeat.com/security/meta-rogue-ai-agent-confused-deputy-iam-identity-governance-matrix) -- post-authentication intent validation gap analysis
- [NIST NCCoE: Software and AI Agent Identity and Authorization Project](https://www.nccoe.nist.gov/projects/software-and-ai-agent-identity-and-authorization) -- federal reference architecture for agent identity and authorization
- [MITRE SAFE-AI: A Framework for Securing AI-Enabled Systems](https://atlas.mitre.org/pdf-files/SAFEAI_Full_Report.pdf) -- maps ATLAS threats to NIST SP 800-53 controls across four system elements
- [Agentic AI Governance Is the CIO's Most Urgent Blind Spot (TechHQ, April 2026)](https://techhq.com/news/agentic-ai-governance-enterprise-gap/) -- 97% of enterprises run agents, only 12% have centralized control
- [Microsoft Runtime Authorization and Governance for AI Agents (April 2026)](https://techcommunity.microsoft.com/blog/microsoft-security-blog/authorization-and-governance-for-ai-agents-runtime-authorization-beyond-identity/4509161) -- runtime authorization beyond identity at scale
- [Faramesh: A Protocol-Agnostic Execution Control Plane for Autonomous Agent Systems (arxiv:2601.17744, January 2026)](https://arxiv.org/abs/2601.17744) -- formalizes non-bypassable Action Authorization Boundary with fail-closed DENY defaults
- [Faramesh Core: Runtime Intervention and Containment Layer for AI Agents (GitHub, MIT license)](https://github.com/faramesh/faramesh-core) -- deterministic pre-execution governance with kernel-level sandboxing and credential broker
- [Right to History: A Sovereignty Kernel for Verifiable AI Agent Execution (arxiv:2602.20214, February 2026)](https://arxiv.org/abs/2602.20214) -- formal model for tamper-evident agent action logging with five system invariants and Merkle audit proofs
- [Colorado AI Act (SB 24-205): Enforcement June 30, 2026](https://leg.colorado.gov/bills/sb24-205) -- state-level enforcement requiring risk management for high-risk AI systems, $20K per violation per consumer
- [ISACA: Agentic AI Evolution and the Security Claw (2026)](https://www.isaca.org/resources/news-and-trends/isaca-now-blog/2026/agentic-ai-evolution-and-the-security-claw) -- "digital synthetic employee" governance model and NemoClaw analysis
- [AgentSpec: Customizable Runtime Enforcement for Safe and Reliable LLM Agents (ICSE 2026, arxiv:2503.18666)](https://arxiv.org/abs/2503.18666) -- lightweight DSL for specifying constraint rules with trigger/predicate/enforcement tuples, >90% unsafe execution prevention
- [ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection (April 2026, arxiv:2604.11790)](https://arxiv.org/abs/2604.11790) -- deterministic tool-call boundary enforcement deriving task-specific constraints from user intent
- [AgentGuard: Runtime Verification of AI Agents (arxiv:2509.23864)](https://arxiv.org/abs/2509.23864) -- Dynamic Probabilistic Assurance via Agentic MDP modeling for continuous runtime verification
- [LiteLLM Supply Chain Attack: CI/CD Pipeline Compromise via Poisoned Trivy Dependency (March 2026)](https://www.herodevs.com/blog-posts/the-litellm-supply-chain-attack-what-happened-why-it-matters-and-what-to-do-next) -- backdoored PyPI packages exfiltrated credentials from ~36% of cloud environments
- [IBM Announces Cybersecurity Measures to Confront Agentic Attacks (April 2026)](https://www.prnewswire.com/news-releases/ibm-announces-new-cybersecurity-measures-to-help-enterprises-confront-agentic-attacks-302742526.html) -- multi-agent Autonomous Security service for machine-speed detection and containment
- AISVS C09.2 (High-Impact Action Approval) -- organizational approval workflows complement intent verification
- AISVS C02 (User Input Validation) -- input validation is the first layer; intent verification is the last layer before execution

---

## Open Research Questions

- Can intent verification be partially automated using a secondary model that compares the user's original request with the proposed action?
- How do you define "intended outcome" formally enough for automated post-condition checking across diverse action types?
- What is the right expiration window for intent confirmations? Too short causes failures; too long enables replay.
- How should constraint gates handle novel action types that were not anticipated when the constraints were defined?
- Given that agents use only ~25% of prompt constraints as validated input, what architectural patterns can close the fidelity gap without requiring model-level changes?
- How should intent security frameworks handle ambiguous or underspecified user instructions where multiple reasonable interpretations exist?
- Given the asymmetric nature of goal drift (directional bias toward certain values), can drift detection be tuned to watch for domain-specific drift directions rather than generic distribution shifts?
- How should integrity verification extend to dynamically-generated prompt templates (e.g., templates assembled at runtime from multiple fragments)?
- What is the right balance for risk-based routing thresholds — how do you prevent attackers from deliberately crafting actions that score just below the high-risk threshold?
- After ClawHavoc, what level of mandatory signature verification should agent skill marketplaces enforce — and should unsigned skills be blocked entirely or sandboxed with reduced privileges?
- Can the Mastercard Verifiable Intent pattern (cryptographic binding of identity + instructions + outcome) be generalized to non-commerce agent actions, and what is the minimum viable implementation for open-source agent frameworks?
- What is the appropriate PDP availability SLO for agent governance — and should the SLO vary by action risk tier (e.g., 99.99% for high-risk write operations vs. 99.9% for read-only operations)?
- How should intent-based write authorization handle emergent multi-step workflows where the agent's write operations are individually aligned with intent but collectively produce an unintended outcome?
- Given that in-process policy engines share a trust boundary with the agent, what integrity verification mechanisms can detect tampering with the embedded PDP at runtime?
- The Alibaba ROME incident shows that intent violations can emerge from training dynamics (instrumental convergence) rather than adversarial inputs — how should post-condition checking frameworks distinguish between externally induced and internally emergent misalignment, and does the distinction matter for containment?
- Can the ARS event-sourcing pattern (Ed25519-signed immutable action logs with independent evaluator gates) scale to high-throughput agent deployments where individual action latency budgets are under 100ms?
- OpenShell's out-of-process policy enforcement eliminates the agent-tampering risk of in-process engines but has no published latency data — at what IPC overhead does out-of-process enforcement become impractical for latency-sensitive agent workflows, and can a hybrid model (in-process for low-risk, out-of-process for high-risk) provide the best tradeoff?
- With 97% of enterprises running agents but only 12% having centralized governance, what minimum viable governance architecture can bridge the gap without requiring a full organizational transformation?
- Can kernel-level governance approaches (Faramesh's seccomp-BPF/Landlock sandbox, PunkGo's capability-based isolation) provide stronger non-bypassability guarantees than process-level enforcement (OPA sidecar, OpenShell) — and what is the practical portability tradeoff across Linux, macOS, and container runtimes?
- As the Colorado AI Act and EU AI Act begin enforcement in mid-2026, what evidence of intent verification will regulators actually accept as compliance — and will Merkle-auditable action logs (as in the Right to History framework) become a de facto standard for demonstrating post-hoc that an agent operated within authorized bounds?

---
