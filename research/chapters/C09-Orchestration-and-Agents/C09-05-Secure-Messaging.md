# C9.5: Secure Messaging and Protocol Hardening

[Back to C09 Index](C09-Orchestration-and-Agents.md)

## Purpose

Agent outputs often become another agent's input, plan, memory update, or tool argument. C9.5 focuses on the semantic layer of that exchange: a message can be perfectly valid JSON and still be unsafe if it changes the task intent, swaps the subject, smuggles instructions, changes units, invents approvals, or violates business invariants.

This section should be read narrowly. Transport encryption, authentication, replay protection, and protocol-specific schema validation remain important prerequisites, but they are covered by adjacent controls such as C09.4 and C10. C9.5 asks whether the receiving agent validates that the content it is about to trust still makes sense for the task, actor, authority, state, and downstream impact.

---

## Requirements

| # | Requirement | Level | Threat Mitigated | Verification Approach | Gaps / Notes |
|---|-------------|:-----:|-----------------|----------------------|--------------|
| **9.5.1** | **Verify that** agent outputs propagated to downstream agents are validated against semantic constraints (e.g., value ranges, logical consistency) in addition to schema validation. | 2 | **Semantic message manipulation.** A compromised or misled agent can send schema-valid content that changes downstream behavior: negative prices, stale approval state, a different tenant ID, hidden instructions in text fields, mismatched task IDs, unsupported delegation, or a summary that reframes the original user intent. This maps closely to OWASP Agentic ASI07 Insecure Inter-Agent Communication and ASI06 Memory and Context Poisoning, and to MITRE ATLAS patterns including AML.T0051 LLM Prompt Injection, AML.T0080.000 AI Agent Context Poisoning: Memory, AML.T0011.002 Poisoned AI Agent Tool, AML.T0085.001 AI Agent Tools (Collection), and the early-2026 additions AML.T0099 AI Agent Tool Data Poisoning and AML.T0100 AI Agent Clickbait. The May 2026 Five Eyes guidance on agentic AI services names this failure class "structural risk" and states it plainly: no agent should extend implicit trust to another agent's output merely because both belong to the same system. The threat does not even require an attacker - Sefz's semantic fuzzing of 402 marketplace agent skills (May 2026) found 29.9% violated their own documented guardrails on benign inputs because the guardrails had no machine-enforceable semantics. | Review every agent-to-agent and agent-to-tool contract that can influence decisions, memory writes, or tool calls. For each field and free-text region, verify documented invariants: allowed ranges, units, currencies, actor and tenant binding, task/session binding, source provenance, approval status, temporal ordering, mutually exclusive states, and intent alignment with the original task anchor. Test with payloads that satisfy the schema but violate meaning: out-of-range values, role swaps, stale state, impossible combinations, hidden directives, prompt-injection phrasing, contradictory summaries, and cross-tenant identifiers. Replay tool-poisoning corpora such as MCPTox so receivers must defeat real attack patterns, not just synthetic schemas, and exercise sequence-aware checks against multi-step exfiltration traces of the kind Praetor catches. Add benign-input semantic fuzzing in the style of Sefz so guardrail gaps surface without crafted attacks. Per NSA's May 2026 MCP guidance, log and inspect tool output before it propagates onward, not only the inputs. Confirm the receiver rejects, quarantines, or routes these outputs to review before adding them to context, memory, plans, or tool parameters. | Semantic validation is domain-specific. JSON Schema, Protocol Buffers, Pydantic, and Zod can enforce structure and many invariants, but free-text intent and logical consistency still require task-specific checks, policy engines, guardrail services, replayable test cases, and human review for high-impact flows. LLM-as-judge checks are useful as secondary signals but should not be the only enforcement boundary; the 2026 MCPTox benchmark shows even capable models can be coaxed into following poisoned tool metadata, so the receiver-side checks cannot rely on the model alone to spot drift. April 2026's OX Security "mother of all AI supply chains" advisory and the actively exploited nginx-ui MCPwn flaw (CVE-2026-33032) underline that schema-valid messages routinely cross trust boundaries that the upstream protocol does not enforce on their behalf, and VIPER-MCP's May 2026 automated audit of ~39,900 open-source MCP servers (106 confirmed zero-days, 67 CVEs assigned) showed natural-language input still reaches shell, file, and network sinks unchecked at ecosystem scale. Embedding-similarity intent checks are also bypassable on their own: "Attack the Messages, Not the Agents" (arXiv:2508.03125) tampers with quantitative relations in inter-agent messages while preserving embeddings, reaching over 60% attack success. |

---

## Notable Incidents and Research

| Date | Finding | Relevance | Link |
|------|---------|-----------|------|
| Apr 2025 | Invariant Labs disclosed MCP tool poisoning patterns where hidden instructions in tool descriptions or outputs influence agent behavior. | Tool metadata and tool responses are downstream inputs. The receiver needs semantic checks that treat these fields as untrusted content, not trusted instructions. | [Invariant Labs](https://invariantlabs.ai/blog/mcp-security-notification-tool-poisoning-attacks) |
| Oct 2025 | Unit 42 described agent session smuggling in A2A systems, where a malicious peer injects covert instructions during a stateful multi-turn session. | The recommended task-anchor defense is directly aligned with 9.5.1: each received message should be checked against the original task intent and rejected when it drifts. | [Unit 42](https://unit42.paloaltonetworks.com/agent-session-smuggling-in-agent2agent-systems/) |
| Dec 2025 | OWASP released the Top 10 for Agentic Applications 2026. ASI07 explicitly covers insecure inter-agent communication, including missing semantic validation. | Gives auditors a current standard vocabulary for semantic manipulation, descriptor forgery, replayed delegation, and agent-in-the-middle scenarios. | [OWASP](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/) |
| Jan 2026 | MCP-ITP showed implicit tool poisoning can induce a legitimate high-privilege tool to perform malicious operations, reaching up to 84.2% attack success rate in experiments while evading many detectors. | Demonstrates why receivers cannot rely on whether the poisoned tool was invoked; all tool metadata and propagated outputs need semantic review before they influence planning. | [arXiv:2601.07395](https://arxiv.org/abs/2601.07395) |
| Feb 2026 | Microsoft reported recommendation poisoning that manipulates assistant memory and mapped it to MITRE ATLAS AML.T0051 and AML.T0080.000. | Memory writes are downstream propagation. Semantic constraints should block attempts to mark attacker-controlled content as trusted, authoritative, or persistent. | [Microsoft](https://www.microsoft.com/en-us/security/blog/2026/02/10/ai-recommendation-poisoning/) |
| Feb-Apr 2026 | A comparative threat model for MCP, A2A, Agora, and ANP identified protocol-level risks across trust assumptions, lifecycle behavior, and validation or attestation gaps. | Reinforces that semantic validation must survive mixed-protocol deployments where agents negotiate capabilities and route work across independent systems. | [arXiv:2602.11327](https://arxiv.org/abs/2602.11327) |
| Apr 2026 | A large MCP server study evaluated 1,899 open-source MCP servers and found MCP-specific tool poisoning in 5.5% of servers. | Public tool ecosystems already contain poisoned metadata. Production systems should pin, diff, scan, and validate tool descriptions before exposing them to downstream agents. | [arXiv:2506.13538](https://arxiv.org/abs/2506.13538) |
| Sep 2025 | A squatted `postmark-mcp` npm package introduced a one-line backdoor in version 1.0.16 that BCC'd outbound mail to an attacker domain after fifteen clean releases, with conservative estimates of 3,000-15,000 stolen emails per organization per day across ~500 organizations. | First widely documented malicious MCP server in the wild. Reinforces that tool identity, package provenance, and post-install description diffs are all part of the C9.5 receiver story; the call site looked schema-valid. Maps to MITRE ATLAS AML.T0011.002 Poisoned AI Agent Tool. | [The Hacker News](https://thehackernews.com/2025/09/first-malicious-mcp-server-found.html) |
| Mar 2026 | ModelScope disclosed CVE-2026-2256, a command-injection issue in MS-Agent's Shell tool where regex-based safety filters could be bypassed by prompt-derived input, letting peer messages drive arbitrary OS commands. | Demonstrates how schema-valid peer-agent or tool messages can still smuggle imperative content past content-blocklist defenses, and why semantic validation belongs before the tool layer instead of inside it. | [SentinelOne CVE-2026-2256](https://www.sentinelone.com/vulnerability-database/cve-2026-2256/) |
| Apr 2026 | CVE-2026-35568 disclosed a DNS-rebinding flaw in the MCP Java SDK prior to 1.0.0: missing Origin validation let a malicious web origin call local MCP tools as if it were a local AI client. | Origin and Host checks are upstream of C9.5, but the receiver still has to assume any tool call may have arrived through a confused-deputy boundary; semantic validation, tenant binding, and approval gates limit what such a confused call can achieve. | [GHSA-8jxr-pr72-r468](https://github.com/modelcontextprotocol/java-sdk/security/advisories/GHSA-8jxr-pr72-r468) |
| Apr-May 2026 | The MCPTox benchmark probed 20 LLM agents across 45 live MCP servers, 353 real tools, and 1,312 malicious test cases across ten poisoning categories; capable models such as o1-mini reached a 72.8% attack success rate and even Claude-3.7-Sonnet refused less than 3% of attempts. | Confirms that LLM judges and instruction-following alone are not safe enforcement boundaries for inter-agent and tool messages. Receivers need deterministic semantic checks, allowlists, and human review for high-impact actions. | [arXiv:2508.14925](https://arxiv.org/abs/2508.14925) |
| May 2026 | Red Hat published a three-layer defense pattern for A2A: SPIFFE-based workload identity bound to the Agent Card, JWS-signed cards with X.509 chains for tamper evidence, and mesh-level authorization on every call. | Operationalizes Agent Card verification as a deployment artifact rather than a metadata blob, and pairs it with runtime checks - exactly the upstream signals C9.5 validators need to trust source identity before reasoning about content. | [Red Hat Emerging Tech](https://next.redhat.com/2026/05/13/securing-agent-to-agent-communication/) |
| Feb 2026 | MITRE ATLAS v5.4.0 added agent-focused techniques including AML.T0099 (AI Agent Tool Data Poisoning), AML.T0100 (AI Agent Clickbait, targeting agentic browsers with manipulated UI and hidden instructions), and AML.T0098 (AI Agent Tool Credential Harvesting). | Gives auditors a consistent vocabulary for the receiver-side failures C9.5 must catch: poisoned content placed where an agent will read it, UI-level lures aimed at autonomous browsers, and credential exfiltration disguised as a routine tool call. | [MITRE ATLAS](https://atlas.mitre.org/) |
| Mar 2026 | Microsoft patched CVE-2026-26118, an SSRF in Azure MCP Server that let an authorized attacker submit a malicious URL to an MCP tool and capture the server's managed-identity token from the outbound request. | A textbook example of how schema-valid input to a tool reroutes its credentials; receiver-side allowlists, parameter validation, and credential isolation matter even when upstream auth is intact. | [Sentinel One CVE-2026-26118](https://www.sentinelone.com/vulnerability-database/cve-2026-26118/) |
| Apr 2026 | OX Security's "mother of all AI supply chains" advisory disclosed ten-plus MCP STDIO command-injection CVEs across Anthropic's official SDKs (CVE-2025-65720, CVE-2026-30615 through 30625, CVE-2026-33224, CVE-2026-40933, and others), with arbitrary code execution confirmed on LangFlow, LiteLLM, Flowise, Upsonic, Windsurf, and Claude/Cursor IDE configurations; researchers estimated 200,000 exposed servers and identified four exploitation families including allowlist bypass via `npx -c` and zero-click prompt-injection-to-RCE. | Confirms that MCP messages routinely cross from text-shaped input to subprocess argument with no semantic boundary. Receivers must treat tool parameters and config-modifying messages as untrusted regardless of upstream SDK guarantees, and pin/diff config before agents can apply it. | [OX Security advisory](https://www.ox.security/blog/the-mother-of-all-ai-supply-chains-critical-systemic-vulnerability-at-the-core-of-the-mcp/) |
| Apr 2026 | CVE-2026-33032 ("MCPwn"), an unauthenticated authentication-bypass in nginx-ui's MCP integration, was actively exploited against 2,600+ exposed instances; the `/mcp_message` endpoint only enforced an IP allowlist that defaulted to "allow all," letting any network-adjacent attacker invoke 12 MCP tools (including `nginx_config_add` with auto-reload) in two HTTP requests. | First in-the-wild mass exploitation of an MCP endpoint. Validates that downstream agents/clients should not assume the protocol layer mediates trust, and that capability invocation must be gated by independent authorization plus semantic policy. | [The Hacker News](https://thehackernews.com/2026/04/critical-nginx-ui-vulnerability-cve.html) |
| May 2026 | Praetor (Van Lang University, arXiv:2604.26274) trained a parameterized DFA from ~400 clean tool-call traces and used it as a behavioral firewall for structured-workflow agents; the system cut attack success rate from 12.8% (Aegis baseline) to 2.2% and fully blocked the evaluated multi-step exfiltration chains while remaining a constant-time runtime check. | Demonstrates that sequence-aware semantic validation - not just per-message schema and intent checks - is a practical defense for benign-but-chained tool calls that single-message validators miss. | [Praetor preprint](https://arxiv.org/abs/2604.26274) |
| Jan 2026 | NeuralTrust's "Generative Application Firewall" (GAF, arXiv:2601.15824) defined a five-layer model - network, syntactic, semantic, context, and data - for protecting LLM applications and agents, with the semantic layer dedicated to detecting jailbreaks and prompt manipulation and the context layer killing streams mid-generation when multi-turn escalation is detected. | Reframes the receiver-side validator as a shared enforcement layer rather than ad-hoc per-agent code, and codifies the multi-turn escalation that C9.5 must defeat for stateful peer agents. | [arXiv:2601.15824](https://arxiv.org/abs/2601.15824) |
| May 2026 | CISA, NSA, and Five Eyes partners released "Careful Adoption of Agentic AI Services," the first joint government guidance on agentic AI. It defines structural risk - cascading failures where compromised outputs propagate through multi-agent networks - and states that no agent should extend implicit trust to another agent's output by virtue of co-membership in the same system. | Government-grade backing for 9.5.1's core claim: identity and transport controls are necessary but insufficient, and each receiving agent must validate propagated content itself. | [CISA](https://www.cisa.gov/news-events/news/cisa-us-and-international-partners-release-guide-secure-adoption-agentic-ai) |
| May 2026 | NSA's AI Security Center published a 17-page CSI on MCP security design (U/OO/6030316-26). It recommends validating inputs beyond well-formed schemas, logging and inspecting MCP tool output before passing it onward, signing messages with expiration and replay protection, and explicitly warns that pattern-based scanning misses semantic-equivalence attacks. | The most direct standards statement of the schema-plus-semantics two-layer model to date: tool results are untrusted content that must be inspected before downstream propagation. | [NSA CSI](https://www.nsa.gov/Portals/75/documents/Cybersecurity/CSI_MCP_SECURITY.pdf) |
| May 2026 | VIPER-MCP (arXiv:2605.21392) ran automated taint-style audits across 39,884 open-source MCP server repositories using feedback-driven prompt evolution, confirming 106 zero-days with end-to-end exploit traces; 67 had CVE IDs assigned by publication. | Quantifies the receiver-side gap at ecosystem scale: natural-language tool arguments routinely reach shell, file, and network sinks with no semantic or range validation between schema check and execution. | [arXiv:2605.21392](https://arxiv.org/abs/2605.21392) |
| May 2026 | Sefz (arXiv:2605.13044) semantically fuzzed 402 real-world marketplace agent skills with benign inputs and found guardrail violations in 120 (29.9%), including 26 previously unknown exploitable violations in deployed systems - documents deleted, credentials leaked, funds moved - with no attack payload involved. | Shows semantic failure does not require an adversary. Skills violate their own documented limits on ordinary inputs because the limits lack machine-enforceable semantics, which is precisely what 9.5.1 asks receivers to enforce. | [arXiv:2605.13044](https://arxiv.org/abs/2605.13044) |
| May 2026 | SKILL.md supply-chain research (arXiv:2605.11418) manipulated natural-language metadata in agent skills across the skill lifecycle: 86% pairwise win rate in retrieval manipulation, malicious skills selected in 77.6% of paired trials, and semantic evasion dodging blocking verdicts in 36.5-100% of cases. | Skill and tool descriptors are operational text that biases downstream agent decisions. Schema validation cannot see this; receivers need semantic consistency checks over descriptor content versus declared behavior. | [arXiv:2605.11418](https://arxiv.org/abs/2605.11418) |
| May 2026 | Akamai disclosed a trio of data-infrastructure MCP server flaws including CVE-2025-66335, an unauthenticated SQL injection in Apache Doris MCP Server (fixed in 0.6.1) that bypassed its query validation, plus an Alibaba RDS MCP metadata-exfiltration issue and an Apache Pinot MCP takeover one vendor declined to patch. | Textbook agent-to-tool semantic gap: schema-valid query parameters carried attacker logic straight past the tool's own validation layer into the database. | [GHSA-qhfq-gvvc-5q6q](https://github.com/advisories/GHSA-qhfq-gvvc-5q6q) |
| May 2026 | MITRE CTID's Secure AI v2 release added 45+ techniques and sub-techniques, 10+ mitigations, and 20+ case studies to ATLAS with expanded agentic coverage, introduced a Technique Maturity filter, and moved ATLAS to a monthly release cadence with a Rapid Response Report process. | Keeps the auditor vocabulary for inter-agent and tool-message attacks current; the maturity filter helps teams prioritize which receiver-side failures to test first. | [MITRE CTID](https://ctid.mitre.org/blog/2026/05/06/secure-ai-v2-release) |
| Mar 2026 | Cross-Agent Semantic Flow reconstruction (arXiv:2603.04469) rebuilt fragmented multi-agent operational primitives into contiguous behavioral trajectories and used a supervisor model to flag data-flow violations, control-flow deviations, and intent inconsistencies, reaching 85.3% node-level F1 across 10+ compound attack vectors. | A concrete runtime architecture for the "logical consistency" half of 9.5.1: validating propagated outputs as flows across agents rather than as isolated messages. | [arXiv:2603.04469](https://arxiv.org/abs/2603.04469) |

## Implementation Maturity

| Control Area | Maturity | Notes |
|--------------|:---:|-------|
| Structured contracts with semantic validators | Mature | Pydantic field validators, Zod `refine` / `superRefine`, JSON Schema custom formats, and Protocol Buffers plus application validators can enforce ranges, enums, units, required relationships, and state transitions. They work best when contracts are versioned and treated as deploy artifacts rather than model prompts. |
| Runtime guardrails for agent outputs | Emerging | Guardrails AI supports Pydantic-backed structured output validation and custom validators. NVIDIA NeMo Guardrails v0.20.0 (January 2026) added IORails for parallel input/output rail execution and dedicated tool-input and tool-output rails; v0.21.0 (March 2026) added a standalone `check()`/`check_async()` API returning PASSED/MODIFIED/BLOCKED verdicts plus a LangChain `GuardrailsMiddleware` that wraps every model call in an agent loop; v0.22.0 (May 2026) extended IORails with streaming output-rail support and OpenTelemetry tracing. All of these give receivers a place to run semantic checks before peer-agent or tool messages enter context, but they still require domain-specific policies. |
| Policy-as-code checks | Mature | Open Policy Agent/Rego, Cedar, or equivalent policy engines can validate claims that are easier to express as authorization or business rules: actor may approve action, tenant IDs match, transfer amount is below the delegated limit, and workflow state permits the transition. |
| In-path gateway enforcement | Emerging | Dedicated agent gateways are becoming a practical place to host receiver-side checks outside agent code. agentgateway (a CNCF-aligned proxy, v1.2-1.3 as of May 2026) evaluates CEL-based `mcpAuthorization` policies over MCP tool-call arguments and caller identity on every invocation, supports per-tool targeting and schema deduplication, and snapshots request bodies for inspection. Stacklok's ToolHive (v0.29.0, June 2026) pairs a managed MCP runtime with a registry whose entries carry sigstore provenance fields and claim-based authorization. Gateways centralize policy but still need the same domain-specific semantic rules as in-process validators. |
| Protocol metadata validation | Emerging | A2A Agent Cards and MCP tool definitions are executable trust inputs for the receiver. Treat names, descriptions, examples, schemas, auth declarations, and capability lists as untrusted metadata until they are fetched from an approved source, authenticated where the protocol supports it, pinned or versioned, diff-reviewed, and checked for instruction-shaped content. |
| Natural-language intent validation | Emerging | Task-anchor comparison, intent-diffing, embedding similarity, classifier checks, and LLM-based review can flag drift from the original task. They are useful for free-text summaries and peer-agent messages, but false positives and evasions remain common. Use them to gate review, not to silently bless high-impact actions. |
| Tool description and output scanning | Emerging | Static checks can flag imperative phrases, invisible Unicode, base64 blobs, references to unrelated tools, unexpected URLs, or description changes after install. Open-source scanners are starting to specialize for this: Cisco's MCP Scanner (4.7.x, May-June 2026) runs Yara, an LLM-judge, behavioral code analysis, a vulnerable-dependency analyzer, and a DAST-style protocol analyzer across tool definitions, descriptions, and implementations, with LLM-powered alignment checking that compares declared tool semantics against actual behavior; Cisco's companion A2A Scanner (1.0.1, February 2026, Apache 2.0) combines YARA, A2A spec validation, heuristics, an LLM analyzer, and a live endpoint analyzer that checks HTTPS, security headers, Agent Card presence, and protocol compliance against running agents; Invariant Labs' mcp-scan continues under Snyk as `snyk-agent-scan` (0.5.x, May-June 2026), discovering and scanning agents, MCP servers, and skills across Claude, Cursor, Windsurf, and Gemini CLI hosts for 15+ risk classes while keeping the proxy mode that enforces guardrail policies on live tool-call traffic; Proximity enumerates a server's prompts, tools, and resources and rates their risk; Pipelock (May 2026) wraps the agent in an inbound/outbound firewall that fingerprints tool descriptions on first use and alerts on mid-session "rug-pull" changes. OWASP's MCP tool poisoning guidance recommends constrained response formats, server allowlists, least privilege, and approval for sensitive operations. |
| Agent identity attestation for cards | Emerging | A2A v1.0 (March 2026, with a v1.0.1 patch in May) ships Signed Agent Cards as a production spec feature, so receivers can verify a card was issued by the domain owner; the earlier JWS-over-JCS (RFC 8785) signing model carries forward, and prototypes such as sigstore-a2a issue short-lived X.509 certificates from ambient OIDC claims with Rekor transparency log entries and SLSA provenance. Red Hat's May 2026 pattern binds signed cards to SPIFFE workload identity, and Microsoft's Entra Agent ID reached GA in May 2026 for directory-managed agent identities. Adoption is still mostly opt-in; receivers should fail closed when signatures are absent for high-trust deployments. |
| Sequence-aware behavioral firewalls | Emerging | Per-message validators miss attacks that chain benign-looking calls into exfiltration. Praetor (May 2026, arXiv:2604.26274) learns a parameterized DFA from clean tool-call traces and blocks deviations in constant time, cutting attack success from 12.8% to 2.2% on structured workflows while degrading on less-structured agents. NeuralTrust's Generative Application Firewall (January 2026) generalizes the same idea with explicit network, syntactic, semantic, context, and data layers, including a context layer that terminates streaming mid-generation when multi-turn escalation is detected. DeepContext (February 2026, arXiv:2602.16935) tracks sequences of intent embeddings across turns and detects multi-turn adversarial drift at 0.84 F1 in under 20ms, and Cross-Agent Semantic Flow reconstruction (March 2026, arXiv:2603.04469) extends the idea across agents by validating reconstructed behavioral trajectories instead of single hops. Pipelock's egress-side firewalling complements this with DLP and SSRF controls on agent outbound traffic. The category is young; sequence models still require representative clean traces, and adversaries are already studying denial-feedback leakage, chained-call laundering, and embedding-preserving message tampering (arXiv:2508.03125) that defeats similarity-based drift checks. |

## Verification Playbook

1. **Trace propagation paths.** List every place an agent output becomes another agent's context, memory, plan, routing decision, or tool argument. Include summaries, errors, tool descriptions, retrieved documents, and async status updates.
2. **Define semantic invariants.** For each path, document the facts that must remain true: actor, tenant, task ID, authorization state, units, limits, ordering, provenance, allowed intent, and permitted downstream actions.
3. **Bind messages to task context.** Require task/session identifiers and compare each received message against the original user request or task anchor. Reject messages that introduce unrelated goals, credential prompts, or unexpected delegation.
4. **Validate before model exposure.** Run schema and semantic checks before placing peer-agent or tool output into the LLM context, memory, or planner state. A rejected message should not be summarized into trusted context.
5. **Test schema-valid attacks.** Add regression cases for negative or extreme values, tenant swaps, stale approvals, impossible status transitions, hidden directives, instruction-shaped metadata, prompt-injection phrasing, and contradictory summaries. Pair adversarial cases with benign-input semantic fuzzing, since roughly a third of marketplace agent skills violate their own documented guardrails on ordinary inputs.
6. **Log decisions.** Record accepted, rejected, quarantined, and human-reviewed messages with the validator version, policy decision, source agent identity, and downstream consumer. Logs should support incident review without storing unnecessary sensitive payloads.

## Cross-Chapter Links

| Related Chapter | Overlap Area | Notes |
|-----------------|--------------|-------|
| [C09.4 Agent Identity & Audit](C09-04-Agent-Identity-and-Audit.md) | Source identity and attribution | C9.5 assumes the receiver can identify the sending agent. C9.4 supplies cryptographic identity and tamper-evident audit so semantic failures can be attributed. |
| [C09.7 Intent Verification](C09-07-Intent-Verification.md) | Task-anchor checks | C9.7 validates proposed actions against the user's intent. C9.5 applies the same idea to messages and outputs before they influence downstream agents. |
| [C09.8 Multi-Agent Isolation](C09-08-Multi-Agent-Isolation.md) | Blast-radius control | Semantic validation reduces the chance of bad propagation; isolation limits the impact when a peer agent still sends unsafe content. |
| [C10.4 MCP Schema Validation](../C10-MCP-Security/C10-04-Schema-Message-Validation.md) | Protocol-specific schema enforcement | C10.4 handles MCP schemas and tool definition change detection. C9.5 adds business meaning and intent validation after the message is structurally valid. |
| [C13.6 Proactive Security Behavior Monitoring](../C13-Monitoring-and-Logging/C13-06-Proactive-Security-Behavior-Monitoring.md) | Detection and drift monitoring | Logs from semantic validators become evidence for detecting tool poisoning, context drift, and abnormal peer-agent behavior. |
| [C11 Adversarial Robustness](../C11-Adversarial-Robustness/C11-Adversarial-Robustness.md) | Malicious or misleading outputs | C11 covers broader adversarial robustness. C9.5 focuses on validating the content being handed from one agent boundary to the next. |

## Protocol Notes

- **A2A:** Agent Cards and task messages make cross-agent collaboration explicit, but stateful sessions create room for hidden instruction drift. A2A reached v1.0 under the Linux Foundation in March 2026 (v1.0.1 patch, May 2026): Signed Agent Cards are now a production spec feature so receivers can verify a card was issued by the domain owner, the weaker OAuth implicit and password grants were removed in favor of device-code and PKCE flows, and the spec corrected its TaskStatus state model. The earlier JWS-over-canonicalized-card model (RFC 7515 plus RFC 8785 JCS) carries forward. C9.5 adds the content-level check that a properly authenticated, properly signed peer is still staying within the delegated task.
- **MCP:** Tool descriptions, parameter descriptions, errors, and tool results can become model-facing context. MCP security guidance treats server outputs as untrusted and recommends strong authorization, SSRF protection, session hygiene, scope minimization, and careful trust-boundary handling. C9.5 adds the semantic check before those outputs reach downstream reasoning. The April 2026 OX Security advisory (10+ STDIO command-injection CVEs across Anthropic's official SDKs) and the actively exploited nginx-ui MCPwn flaw (CVE-2026-33032) both show that protocol-layer hardening cannot be assumed: receivers should validate every tool argument independently and treat config-modifying or transport-controlling messages as high-impact actions that demand approval, not silent execution. The MCP 2026-07-28 specification release candidate (locked May 2026) helps on the schema side - tool input and output schemas gain full JSON Schema 2020-12 expressiveness including `oneOf`/`anyOf`/`allOf` composition and conditionals, implementations must not auto-dereference external `$ref` URIs, and authorization adds RFC 9207 `iss` validation against mix-up attacks - but `structuredContent` remains arbitrary JSON, so the semantic layer above the schema is still the receiver's job. NSA's May 2026 CSI on MCP adds the operational baseline: inspect tool output before passing it onward, sign messages with expiration and replay protection, and assume pattern-based scanning misses semantic-equivalence attacks.
- **Mixed-protocol deployments:** Agents often bridge A2A, MCP, custom REST, queues, and shared memory. Validate at the receiving boundary rather than assuming the previous protocol hop preserved meaning.
- **Free-text summaries:** Summaries are high-risk propagation artifacts because they compress evidence and can quietly change intent. Require source links, confidence, omitted-field disclosure, and comparison against the original evidence for high-impact workflows.

## Protocol-Specific Verification Checks

| Boundary | Auditor Checks | Failure Examples |
|----------|----------------|------------------|
| A2A Agent Card intake | Confirm Agent Cards are fetched over HTTPS, authenticated when extended cards are used, JWS-verified against an approved key set when signatures are present (with JCS canonicalization per RFC 8785), bound to a workload identity such as a SPIFFE ID, and stored with version/hash history. Review before new capabilities, examples, URLs, or security schemes are exposed to planning, and compare public and authenticated cards for unexpected capability expansion. | A peer updates a capability description to request credentials, adds a lookalike high-privilege capability, ships an unsigned card claiming a known provider, or changes its endpoint after approval. |
| A2A task/session messages | Verify each received message carries task/session binding and is compared against the original task anchor, allowed capabilities, current workflow state, and user-approved impact level. Red-team with multi-turn requests that slowly shift from the delegated task to credential collection or tool execution. | A remote research agent asks the client to reveal tool schemas, replay prior conversation history, or execute an unrelated purchase during a stateful session. |
| MCP tool metadata | Inspect how the client pins, diffs, and scans tool names, descriptions, parameter descriptions, annotations, and examples. Require review when a server changes metadata after install or when descriptions contain imperatives, invisible Unicode, encoded blobs, unrelated tool references, or external callback URLs. | A benign calculator tool includes hidden instructions to read local configuration, or a server changes descriptions after a trusted initial review. |
| MCP tool results and async events | Validate tool results, errors, resumed stream events, and `tools/list_changed` notifications before they enter model context. Check session IDs are not treated as authentication and that every inbound request is authorized independently. | A hijacked session queues a malicious event that changes offered tools, or an error message contains instructions that cause a privileged follow-up tool call. |
| Shared memory, queues, and summaries | Require provenance, tenant binding, source links, expiry, sensitivity labels, and contradiction checks before propagated output becomes memory or a queue message consumed by another agent. | A schema-valid summary drops caveats, changes the actor or amount, marks attacker-provided content as authoritative, or writes stale approval state into memory. |

## Hardening Recommendations

1. **Use typed envelopes for every propagated output.** Include sender, receiver, task ID, schema version, provenance, confidence, intended use, and expiry. Free-text should be carried as data inside the envelope, not as instructions.
2. **Separate instruction space from data space.** Quote or tag peer-agent content so it cannot be spliced into the system or developer instruction region. Do not let tool descriptions or outputs modify agent policy.
3. **Pin and diff agent and tool metadata.** Store hashes of tool descriptions, parameter descriptions, schemas, and agent capability cards on first use. Block or re-review changes before exposing the new metadata to planning, and treat any mid-session description change as a possible rug-pull. Scanners such as Cisco's MCP Scanner, Proximity, and agent firewalls like Pipelock can automate the fingerprinting and alerting; verify Agent Card JWS signatures and bind the card to a workload identity (e.g., SPIFFE) before accepting it.
4. **Reject semantic drift.** Compare incoming messages to the task anchor and current workflow state. Unexpected goals, unrelated credential prompts, privilege changes, or task subject changes should terminate or quarantine the session.
5. **Treat memory writes as high-impact propagation.** Validate proposed memories for source, scope, sensitivity, trust level, expiry, and contradiction with existing policy before committing them.
6. **Gate high-impact downstream actions.** A schema-valid message should not be enough to trigger financial, identity, production, deletion, or disclosure actions. Require independent policy checks and human approval where required by C09.2 and C14.
7. **Red-team with schema-valid payloads.** Include poisoned tool metadata, contradictory summaries, invisible Unicode, stale authorization state, tenant swaps, prompt-injection phrasing, hidden instructions in error messages, and STDIO/argument-injection payloads of the kind documented in the April 2026 OX Security advisory.
8. **Validate sequences, not just messages.** Pair per-message semantic checks with sequence-aware controls so chained tool calls cannot launder exfiltration past one-shot validators. Behavioral firewalls such as Praetor (pDFA over clean traces) or context-aware enforcement layers like the Generative Application Firewall give receivers a place to detect multi-step drift even when each call is individually well-formed.
9. **Monitor validator bypass attempts.** Alert on repeated semantic validation failures, sudden shifts in peer-agent messages, tool description changes, unexpected routing, and a sender that repeatedly asks downstream agents to use unrelated high-privilege tools.

---

## Related Standards & References

- [OWASP Top 10 for Agentic Applications 2026](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/) -- ASI07 includes missing semantic validation as an inter-agent communication failure
- [OWASP MCP Tool Poisoning](https://owasp.org/www-community/attacks/MCP_Tool_Poisoning) -- attack description and prevention guidance for poisoned MCP tool outputs
- [MCP Security Best Practices](https://modelcontextprotocol.io/docs/tutorials/security/security_best_practices) -- MCP trust-boundary, authorization, SSRF, session hijacking, and local server compromise guidance
- [A2A Protocol Specification](https://a2a-protocol.org/latest/specification/) -- v1.0 Agent Card discovery, Signed Agent Cards, authentication, authorization, TLS, and declared security requirements
- [Agent Session Smuggling in Agent2Agent Systems](https://unit42.paloaltonetworks.com/agent-session-smuggling-in-agent2agent-systems/) -- A2A multi-turn semantic drift and task-anchor defense
- [MCP-ITP: Automated Implicit Tool Poisoning](https://arxiv.org/abs/2601.07395) -- implicit tool poisoning benchmark and attack results
- [Model Context Protocol at First Glance](https://arxiv.org/abs/2506.13538) -- large-scale MCP server security and maintainability study
- [Security Threat Modeling for Emerging AI-Agent Protocols](https://arxiv.org/abs/2602.11327) -- comparative analysis of MCP, A2A, Agora, and ANP
- [Microsoft: AI Recommendation Poisoning](https://www.microsoft.com/en-us/security/blog/2026/02/10/ai-recommendation-poisoning/) -- memory manipulation mapped to MITRE ATLAS AML.T0051 and AML.T0080.000
- [MCPTox Benchmark](https://arxiv.org/abs/2508.14925) -- 1,312 malicious test cases across 45 live MCP servers; up to 72.8% attack success against capable models
- [Postmark MCP Backdoor Disclosure](https://thehackernews.com/2025/09/first-malicious-mcp-server-found.html) -- first widely documented malicious MCP server in the wild
- [CVE-2026-2256 (ModelScope MS-Agent)](https://www.sentinelone.com/vulnerability-database/cve-2026-2256/) -- command injection through prompt-derived input in the agent shell tool
- [CVE-2026-35568 (MCP Java SDK)](https://github.com/modelcontextprotocol/java-sdk/security/advisories/GHSA-8jxr-pr72-r468) -- DNS-rebinding Origin validation failure exposing local MCP tools
- [Red Hat: Securing Agent-to-Agent Communication](https://next.redhat.com/2026/05/13/securing-agent-to-agent-communication/) -- three-layer defense pattern combining SPIFFE identity, JWS-signed Agent Cards, and mesh-level authorization
- [sigstore-a2a](https://github.com/sigstore/sigstore-a2a) -- prototype keyless signing of A2A Agent Cards with Sigstore and SLSA provenance
- [Cisco MCP Scanner](https://blogs.cisco.com/ai/securing-the-ai-agent-supply-chain-with-ciscos-open-source-mcp-scanner) -- open-source contextual and semantic analysis of MCP tool definitions
- [Cisco A2A Scanner](https://github.com/cisco-ai-defense/a2a-scanner) -- open-source A2A scanner combining YARA, spec validation, heuristics, an LLM analyzer, and live endpoint checks for Agent Card and protocol compliance
- [OX Security: Mother of All AI Supply Chains](https://www.ox.security/blog/the-mother-of-all-ai-supply-chains-critical-systemic-vulnerability-at-the-core-of-the-mcp/) -- April 2026 systemic MCP STDIO command-injection advisory spanning Anthropic's official SDKs and ~200,000 exposed servers
- [CVE-2026-33032 (nginx-ui MCPwn)](https://thehackernews.com/2026/04/critical-nginx-ui-vulnerability-cve.html) -- actively exploited unauthenticated MCP authentication-bypass enabling full nginx takeover in two HTTP requests
- [CVE-2026-26118 (Azure MCP Server SSRF)](https://www.sentinelone.com/vulnerability-database/cve-2026-26118/) -- Microsoft-patched SSRF in Azure MCP Server that leaks managed-identity tokens through schema-valid tool input
- [Praetor: Behavioral Firewall for Structured-Workflow Agents](https://arxiv.org/abs/2604.26274) -- May 2026 parameterized DFA over clean traces; reduces attack success from 12.8% to 2.2% on structured agents
- [NeuralTrust Generative Application Firewall](https://arxiv.org/abs/2601.15824) -- January 2026 five-layer (network, syntactic, semantic, context, data) firewall model for LLM applications and agents
- [Proximity MCP Security Scanner](https://www.helpnetsecurity.com/2025/10/29/proximity-open-source-mcp-security-scanner/) -- enumerates prompts, tools, and resources for risk evaluation
- [Pipelock Agent Firewall](https://www.helpnetsecurity.com/2026/05/04/pipelock-open-source-ai-agent-firewall/) -- open-source inbound/outbound agent firewall with tool fingerprinting and rug-pull detection
- [NeMo Guardrails Release Notes](https://docs.nvidia.com/nemo/guardrails/latest/about/release-notes.html) -- v0.20.0 IORails, tool-input/output rails, and `check_async` for standalone validation
- [MITRE ATLAS Framework](https://atlas.mitre.org/) -- AI/ML adversarial techniques including AML.T0011.002 (Poisoned AI Agent Tool, Execution), AML.T0085.001 (AI Agent Tools, Collection), AML.T0051 (LLM Prompt Injection), and AML.T0080.000 (AI Agent Context Poisoning: Memory)
- [Guardrails AI Structured Data Validation](https://guardrailsai.com/guardrails/docs/how-to-guides/generate_structured_data) -- Pydantic-backed structured output validation and validators
- [Guardrails AI Custom Validators](https://guardrailsai.com/guardrails/docs/how-to-guides/custom_validators) -- custom validators for domain-specific checks
- [Pydantic AI Structured Output](https://pydantic.dev/docs/validation/latest/examples/pydantic_ai/) -- output type validation with Pydantic field validators
- [Pydantic AI Output Validators](https://pydantic.dev/docs/ai/core-concepts/output/) -- validation context and output validators for structured outputs and tool arguments
- [NVIDIA NeMo Guardrails Overview](https://docs.nvidia.com/nemo/guardrails/latest/about/overview.html) -- programmable guardrails, tool input/output validation, and agentic security rails
- [Open Policy Agent Documentation](https://www.openpolicyagent.org/docs) -- policy-as-code decisions over structured JSON inputs
- [Cedar Policy Validation](https://docs.cedarpolicy.com/policies/validation.html) -- schema-backed validation for authorization policies and request expectations
- [Careful Adoption of Agentic AI Services](https://www.cisa.gov/news-events/news/cisa-us-and-international-partners-release-guide-secure-adoption-agentic-ai) -- May 2026 Five Eyes joint guidance defining structural risk and the no-implicit-trust rule for agent outputs
- [NSA CSI: MCP Security Design Considerations](https://www.nsa.gov/Portals/75/documents/Cybersecurity/CSI_MCP_SECURITY.pdf) -- May 2026 guidance on tool-output inspection, message signing, replay protection, and semantic-equivalence attack limits of pattern scanning
- [MITRE CTID Secure AI v2](https://ctid.mitre.org/blog/2026/05/06/secure-ai-v2-release) -- May 2026 ATLAS expansion with 45+ agentic techniques, technique maturity ratings, and a monthly release cadence
- [MCP 2026-07-28 Release Candidate](https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/) -- full JSON Schema 2020-12 tool output schemas, external `$ref` restrictions, and RFC 9207 authorization hardening
- [VIPER-MCP](https://arxiv.org/abs/2605.21392) -- May 2026 automated taint-style audit of 39,884 MCP server repos; 106 confirmed zero-days, 67 CVEs assigned
- [Sefz: Semantic Fuzzing of Agent Skills](https://arxiv.org/abs/2605.13044) -- benign-input fuzzing of 402 marketplace skills; 29.9% violated their own guardrails
- [SKILL.md Semantic Supply-Chain Attacks](https://arxiv.org/abs/2605.11418) -- May 2026 lifecycle manipulation of skill metadata biasing downstream agent selection and evading scanners
- [Attack the Messages, Not the Agents](https://arxiv.org/abs/2508.03125) -- embedding-preserving tampering of inter-agent messages that defeats similarity-based drift checks
- [Cross-Agent Semantic Flow Reconstruction](https://arxiv.org/abs/2603.04469) -- execution-aware detection of data-flow, control-flow, and intent violations across agent trajectories
- [DeepContext](https://arxiv.org/abs/2602.16935) -- stateful multi-turn intent-drift detection over sequences of intent embeddings
- [CVE-2025-66335 (Apache Doris MCP Server)](https://github.com/advisories/GHSA-qhfq-gvvc-5q6q) -- unauthenticated SQL injection through the MCP query-execution interface, fixed in 0.6.1
- [agentgateway](https://agentgateway.dev/) -- open-source agent gateway with CEL-based MCP tool-call authorization policies and per-invocation inspection
- [Snyk agent-scan](https://pypi.org/project/snyk-agent-scan/) -- continuation of Invariant Labs mcp-scan; scans agents, MCP servers, and skills with a runtime guardrail proxy mode
- [NIST COSAiS](https://csrc.nist.gov/Projects/cosais) -- in-development SP 800-53 control overlays including dedicated single-agent and multi-agent AI system overlays
- AISVS C10 (MCP Security) -- protocol-specific MCP controls that complement C9.5's semantic validation focus

---

## Open Research Questions

- How should teams specify semantic contracts for free-text peer-agent outputs without reducing every interaction to rigid forms?
- Which intent-diffing approaches reliably catch task drift without generating too many false positives for normal collaboration?
- How should validator decisions be composed across protocol boundaries when A2A, MCP, queues, and shared memory all participate in one workflow?
- Can tool and agent metadata pinning become a common client feature, and what should be re-approved when descriptions or schemas change?
- What evidence is enough for an auditor to conclude that semantic validation occurred before an output reached memory, planning, or tool execution?
- How should organizations measure whether LLM-based semantic checks are improving detection rather than adding another bypassable reasoning step?

## Related Pages

- [C11.8 Agent Security Self-Assessment](../C11-Adversarial-Robustness/C11-08-Agent-Security-Self-Assessment.md) -- Self-assessment gives teams a place to prove that message validators, metadata review, and high-impact action gates are exercised before an agent acts.
- [C02.1 Prompt Injection Defense](../C02-User-Input-Validation/C02-01-Prompt-Injection-Defense.md) -- Prompt injection controls cover the upstream attack patterns that C9.5 must catch again when peer-agent and tool outputs cross a trust boundary.
- [C11.7 Security Policy Adaptation](../C11-Adversarial-Robustness/C11-07-Security-Policy-Adaptation.md) -- Adaptive policy work is closely related to keeping semantic validators, allowlists, and protocol metadata rules current without silently widening trust.
- [C09.9 Data Flow Isolation and Origin Enforcement](C09-09-Data-Flow-Isolation-Origin-Enforcement.md) -- Origin-aware data-flow controls help C9.5 validators decide which propagated facts, summaries, and tool results are allowed to influence downstream agents.
- [C02.2 Pre-Tokenization Input Normalization](../C02-User-Input-Validation/C02-02-Pre-Tokenization-Input-Normalization.md) -- Normalization checks catch invisible Unicode, encoded content, and metadata smuggling before semantic validators reason over the message body.

---
