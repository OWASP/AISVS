# C10.1: Component Integrity & Supply Chain Hygiene

> **Parent:** [C10 MCP Security](C10-MCP-Security)
> **Requirements:** 3 | **IDs:** 10.1.1–10.1.3

## Purpose

This section ensures that MCP server and client software is obtained from trusted sources, that configuration artifacts do not leak credentials, and that only approved MCP servers are permitted in production. MCP components are typically distributed as npm packages, Python packages, Docker images, or standalone binaries — all of which are susceptible to supply-chain attacks including typosquatting, dependency confusion, and build-time tampering. Because MCP servers execute arbitrary code with access to sensitive resources (databases, APIs, file systems), compromised components have outsized blast radius compared to typical library dependencies.

The severity of these risks was demonstrated in real-world incidents throughout 2025-2026, and the late-April 2026 wave made the threat model explicit: supply-chain compromises now deliberately target MCP infrastructure as a persistence and exfiltration channel rather than treating MCP exposure as collateral damage. The Mini Shai-Hulud campaign (April 29-30, 2026) compromised SAP Cloud Application Programming packages (`mbt`, `@cap-js/sqlite`, `@cap-js/postgres`, `@cap-js/db-service` — ~572K weekly downloads), the `intercom-client` npm SDK (v7.0.4 and v7.0.5, April 30, 2026), and PyTorch Lightning 2.6.2/2.6.3 in a coordinated wave. The payload writes `.claude/settings.json` containing a `SessionStart` hook that re-executes the credential stealer every time the developer launches Claude Code, drops `.vscode/tasks.json` with `"runOn": "folderOpen"` for VS Code re-execution on project open, and harvests AI-tooling-specific paths including `~/.claude.json`, `~/.claude/mcp.json`, and `~/.kiro/settings/mcp.json` for MCP server credentials, API keys, and database connection strings. SecurityWeek tracked roughly 1,800 victim repositories and reported credential exfiltration to public GitHub repos named with Dune-universe terminology — converting MCP configuration files from passive credential stores into both the harvest target and the persistence mechanism. The Postmark MCP supply chain breach (2025) saw attackers backdoor an npm package used in AI automation pipelines, inserting a single line of malicious code that directed compromised MCP servers to BCC every outgoing email to an attacker-controlled address. The Smithery supply chain attack (October 2025) affected 3,000+ hosted applications and their API tokens. Research using the MCPTox benchmark showed that tool poisoning attacks — where malicious instructions are embedded in tool metadata invisible to human users but processed by AI models — pass seamlessly into agent contexts with alarming frequency. A 2026 survey of 2,614 MCP implementations found 82% use file operations vulnerable to path traversal, two-thirds have code injection risks, and over a third are susceptible to command injection. AgentSeal's April 2026 scan of 1,808 MCP servers (sourced from GitHub repositories, npm and PyPI packages, and public registries including Smithery and MCP.run) found 66% had at least one security finding — 43% involved shell or command injection, 20% targeted tooling infrastructure, 13% were authentication bypasses, and 10% exploited path traversal. These findings underscore why component integrity and supply-chain hygiene are foundational to MCP security. The ecosystem's governance is also maturing: the Linux Foundation launched the Agentic AI Foundation in December 2025 to provide open governance over MCP standard development, and the MCP specification's November 2025 revision (2025-11-25) introduced formal governance structures including working groups and SDK tiering — though as of April 2026 the spec still stops short of mandating integrity verification or supply-chain controls for servers, and Ox Security's April 15, 2026 disclosure of a systemic STDIO-execution flaw in the official MCP SDKs (Python, TypeScript, Java, Rust — covering roughly 150 million cumulative downloads and an estimated 200,000 vulnerable instances) demonstrated that architectural assumptions baked into the reference SDKs can inherit through to every downstream server, with Anthropic declining to patch on the grounds that STDIO is a "secure default" and sanitization is the developer's responsibility. The downstream consequence arrived almost immediately: LiteLLM tracked the issue as [CVE-2026-30623](https://docs.litellm.ai/blog/mcp-stdio-command-injection-april-2026) and shipped a fix in [PR #25343](https://github.com/BerriAI/litellm/pull/25343) (merged April 8, 2026, stable in v1.83.7) introducing an `MCP_STDIO_ALLOWED_COMMANDS` constant that restricts STDIO launches to a seven-binary allowlist (`npx`, `uvx`, `python`, `python3`, `node`, `docker`, `deno`) with optional extension via `LITELLM_MCP_STDIO_EXTRA_COMMANDS`. The pattern — a small, hardcoded launcher allowlist enforced both at request validation and at client instantiation — is now the practical reference design for any MCP host that cannot rely on the SDK to sanitize STDIO commands, and the same design has been independently proposed for OpenAI Codex via [issue #15814](https://github.com/openai/codex/issues/15814) (March 2026) which adds an optional `sha256` field to the `identity` section of `requirements.toml` so that allowlist entries pin a verified binary hash before execution rather than relying on command-name matching alone. Late April 2026 reinforced why pre-install gating beats post-facto scanning: between April 21 and 30, four independent supply-chain campaigns hit the AI/MCP-adjacent ecosystem within ten days — the [CanisterSprawl](https://socket.dev/blog/namastex-npm-packages-compromised-canisterworm) self-propagating npm worm (16+ malicious package versions across `@automagik/genie`, `pgserve`, `@fairwords`, `@openwebconcept`, harvesting ~40 credential categories and using an Internet Computer Protocol canister as decentralized C2), the [xinference PyPI](https://blog.gitguardian.com/three-supply-chain-campaigns-hit-npm-pypi-and-docker-hub-in-48-hours/) compromise (three consecutive credential-stealing releases on April 22), the [Bitwarden CLI v2026.4.0 cascade](https://blog.ibvl.in/index.php/2026/04/27/teampcp-supply-chain-campaign-update-008-26-day-pause-ends-with-three-concurrent-compromises-checkmarx-kics-bitwarden-cli-cascade-xinference-pypi-canistersprawl-npm-worm-identified-and-tier-1/) (poisoned via Dependabot pulling a trojanized Checkmarx KICS image, the first documented cascade compromise through trusted automation in the TeamPCP campaign), and the [PyTorch Lightning 2.6.2/2.6.3](https://socket.dev/blog/lightning-pypi-package-compromised) compromise (April 30, 11 MB Bun-runtime payload that runs a daemon thread on import, harvesting GitHub/npm/AWS/Azure/GCP credentials and 20+ CI environment variables, with on-import execution making it especially dangerous in agentic workflows where Claude Code or Cursor run `pip install` autonomously). Socket detected the Lightning payload eighteen minutes after publication — useful but too late for any developer who installed during that window — which is why the conversation has decisively shifted from "scan after install" to "verify before install."

---

## Requirements

| # | Requirement | Level | Threat Mitigated | Verification Approach | Gaps / Notes |
|---|-------------|:-----:|-----------------|----------------------|--------------|
| **10.1.1** | **Verify that** MCP server and client components are obtained only from trusted sources and verified using signatures, checksums, or secure package metadata, rejecting tampered or unsigned builds. | 1 | **Supply-chain compromise / trojanized MCP servers.** An attacker publishes a malicious MCP server package (e.g., via typosquatting on npm or PyPI) that exfiltrates data or injects malicious tool definitions. CVE-2025-6514 demonstrated this concretely: a critical command-injection bug in the `mcp-remote` npm package (437,000+ downloads) turned its OAuth proxy into an RCE vector — malicious OAuth endpoints were passed directly to system shell handlers, enabling full credential theft and repository exfiltration. The Smithery path-traversal attack (October 2025) exposed Docker credentials controlling 3,000+ hosted MCP servers. Without integrity verification, compromised components are silently adopted into production. The TeamPCP campaign (March 2026) demonstrated this at scale: attackers weaponized the Trivy scanner and cascaded across five package ecosystems, ultimately compromising LiteLLM (a common transitive dependency in MCP servers) and impacting 1,000+ SaaS environments. The March 31, 2026 Axios compromise (CVE pending) delivered the same pattern through a transitive dependency most MCP servers never explicitly declare: attackers (attributed by Google Threat Intelligence to the North Korea–nexus Sapphire Sleet group) hijacked a maintainer account and published axios 1.14.1 and 0.30.4 — a library with over 100 million weekly installs — that pulled a hidden `plain-crypto-js` dependency carrying the WAVESHAPER.V2 cross-platform RAT (recon, arbitrary command execution, in-memory PE injection on Windows, filesystem enumeration). The malicious versions self-erased from `node_modules` post-execution, so `npm audit` returned clean, and the attack window lasted 179 minutes — a detection budget that is only defensible with pre-install provenance gating, not post-facto scanning. Relates to MITRE ATLAS AML.T0010 (ML Supply Chain Compromise). | Review package installation procedures for checksum or signature verification. Check CI/CD pipelines for integrity gates (e.g., `npm audit signatures`, `pip hash-checking mode`, Docker Content Trust). Verify that MCP server binaries or images are pulled from pinned, authenticated sources. Evaluate adoption of [MCPS (MCP Secure)](https://mcp-secure.dev/) for cryptographic identity and signed tool definitions — it provides ECDSA P-256 "Agent Passports" and signed JSON-RPC message envelopes with nonce/timestamp replay protection. Run [Snyk agent-scan](https://github.com/snyk/agent-scan) (v0.4+, March 2026) to auto-discover MCP servers across Claude, Cursor, VS Code, and other agents and scan for prompt injection, tool poisoning, tool shadowing, and toxic flow vulnerabilities. Deploy MCP servers through [ToolHive](https://toolhive.dev/) (Stacklok) for container-based isolation with built-in Sigstore and GitHub Attestation verification — ToolHive extracts container image fingerprints, validates cryptographic signatures against trusted certificate authorities, and confirms build provenance matches expected source code and workflows before execution, with three enforcement levels (disabled, warn, enabled). The April 13, 2026 ToolHive Registry Server update adds claim-based authorization — read/write/admin operations are gated on JWT claims so "you only see and manage the resources your identity grants access to," with per-entry access control for Kubernetes sources, immutable entry visibility (once a version is published with specific claims, later versions cannot broaden them), a `/me` endpoint for identity/role introspection, and per-user/per-tool rate limiting on `tools/call` (HTTP 429 with `Retry-After`, Redis-backed counters, fail-open if Redis is unreachable) — closing a gap between registry allowlisting and runtime enforcement that gateways have historically handled separately. Evaluate [Credence](https://credence.securingthesingularity.com/) for pre-install trust scoring — it performs static analysis, dependency CVE scanning, author provenance verification (detecting cloned repos and fake maintainers), and MCP-specific checks via adversarial AI deliberation, producing cryptographically signed attestations pinned to exact commit hashes. Sandbox-test third-party MCP servers before production use; inspect source code, verify maintainers, and run `npm audit` / Snyk scans to catch typosquatting and malicious exfiltration logic. Generate SBOMs for MCP server dependencies using tools like Syft or Trivy (CycloneDX/SPDX formats) — traditional manifest parsing often misses build-time dependencies that execute during installation. Pair SBOMs with VEX (Vulnerability Exploitability eXchange) documents to distinguish exploitable CVEs from false positives. For broader visibility, consider BuildGuard for AI-assisted SBOM generation that captures actual dependency chains beyond manifest parsing. For containerized deployments, the [Docker MCP Gateway](https://www.docker.com/blog/mcp-horror-stories-the-supply-chain-attack/) enforces cryptographically signed container images and provides container isolation as a security boundary. [Lasso Security](https://www.lassosecurity.com/) offers real-time MCP server reputation scoring with automatic blocking of servers that fail integrity checks. For a formalized assessment framework, evaluate [Proofpoint's Agent Integrity Framework](https://www.helpnetsecurity.com/2026/03/17/proofpoint-ai-security/) (March 2026), which defines a five-phase maturity model for verifying that AI agents — including their MCP tool integrations — operate within authorized boundaries. Run [MCPShield](https://github.com/mcpshield/mcpshield) to scan MCP configuration files (e.g., `claude_desktop_config.json`) for typosquats, suspicious code patterns, malicious dependencies, and known CVEs — MCPShield tracks verified servers in `mcp.lock.json` using SHA-512 hashes for immutable integrity verification, validates GitHub namespaces to prevent hijacking, and supports policy enforcement via `policy.yaml`. Deploy [MCPTrust](https://github.com/mcptrust/mcptrust) as a deny-by-default runtime security proxy that intercepts MCP server communications — it enforces lockfile-pinned capabilities (`mcp-lock.json`), performs drift detection categorizing changes as critical/moderate/informational, supports Ed25519 and Sigstore keyless signing for artifact verification, and evaluates CEL (Common Expression Language) governance policies with baseline/strict presets. MCPTrust integrates with Claude Desktop, Claude Code, LangChain, AutoGen, and CrewAI, with three enforcement modes (enforce, filter-only, audit-only) and OpenTelemetry tracing for observability. For enterprise-scale registry governance, evaluate the [JFrog Universal MCP Registry](https://jfrog.com/press-room/jfrog-unveils-universal-mcp-registry-for-ai-software-supply-chain/) (March 2026) — part of the JFrog AI Catalog, it provides a centralized system of record for MCP servers with native security blocking of malicious or non-compliant servers before execution, treating MCP servers with the same governance rigor as software packages and binaries. Run [Cisco MCP Scanner](https://github.com/cisco-ai-defense/mcp-scanner) (open-source, October 2025) for pre-deployment vulnerability assessment — it employs three independent scanning engines (YARA signature-based detection, LLM-as-judge semantic analysis, and Cisco AI Defense integration) to identify tool poisoning, rug-pull attacks, and over-privileged permissions before MCP servers are integrated into agent environments. Evaluate [ToolTrust](https://glama.ai/mcp/servers/AgentSafe-AI/tooltrust-scanner) (AgentSafe) for CI/CD-integrated MCP scanning — it detects prompt injection, data exfiltration, privilege escalation, and known-compromised packages (including TeamPCP-affected LiteLLM/Trivy versions) via OSV CVE integration and an offline blacklist for zero-latency detection; deployable as an MCP server, CLI tool, GitHub Action, or Git pre-commit hook. Use [mcpscan.ai](https://mcpscan.ai/) for repository-level MCP security scanning with LLM-based tool poisoning classification, covering command/code injection, path traversal, authentication deficiencies, and IDOR vulnerabilities. For STDIO-launched MCP servers, mirror the [LiteLLM CVE-2026-30623 mitigation pattern](https://github.com/BerriAI/litellm/pull/25343): enforce a hardcoded launcher allowlist (e.g., `MCP_STDIO_ALLOWED_COMMANDS` restricted to `npx`, `uvx`, `python`, `python3`, `node`, `docker`, `deno`) at both request-validation time and client-instantiation time, and confine any test/preview endpoints to admin roles only — this closes the SDK-level RCE gap that Anthropic declined to patch upstream. For pre-install binary verification, follow the [OpenAI Codex `requirements.toml` SHA-256 proposal](https://github.com/openai/codex/issues/15814) (March 2026): augment allowlist entries with an optional `sha256` field so the host computes and verifies the binary hash before spawning the server process, rejecting any binary whose hash does not match the pinned value — this catches PATH-based typosquatting, post-installation tampering, and unauthorized auto-updates that command-name matching misses. After the Mini Shai-Hulud campaign demonstrated agent-config injection at scale, monitor the developer's MCP configuration paths for unauthorized writes as a first-class control: file-integrity monitoring on `~/.claude.json`, `~/.claude/settings.json`, `~/.claude/mcp.json`, `~/.kiro/settings/mcp.json`, `.vscode/tasks.json`, and the equivalent paths for Cursor, Continue, and Windsurf — alert on any addition of `SessionStart` hooks, `runOn: folderOpen` tasks, or new MCP server entries. Pair with auditd/Falco on Linux, EndpointSecurity framework on macOS, or Sysmon on Windows to catch the underlying file-write events; commit `.claude/` and `.vscode/` directories to version control so unauthorized modifications surface as unexpected diffs in `git status`. Evaluate gateway-class tools that sit between agents and MCP servers: [MCP Manager](https://mcpmanager.ai/) provides RBAC, SSO, SCIM, and end-to-end logging across MCP traffic; [Golf.dev](https://golf.dev/) centralizes MCP server deployment behind an authentication-gated master server with prompt-injection mitigation and MCP-specialized rate limiting; [Obot MCP Gateway](https://obot.ai/) ships project sharing, power-user roles, and governance upgrades for enterprise MCP fleets; [TrueFoundry MCP Gateway](https://www.truefoundry.com/mcp-gateway) layers federated login (Okta, Azure AD), RBAC, and OAuth 2.0 token brokering; [MintMCP](https://www.mintmcp.com/) provides a SOC 2 Type II certified gateway with malicious-command blocking, file-access restrictions, and LLM monitoring. | As of March 2026, the MCPS project offers the first MCP-specific signing layer (five trust levels L0–L4, from unsigned through audited), but adoption is early-stage. General package manager features (npm provenance, PyPI attestations, Sigstore) remain the primary verification mechanism. The MCP spec itself does not enforce audit, sandboxing, or verification — enforcement is left to implementers. A scan of 39 agent frameworks found 13 failed baseline security tests. Research by Invariant Labs found 5.5% of MCP servers exhibit tool poisoning characteristics — malicious instructions hidden in tool descriptions that are invisible to users but processed by AI models. Chained vulnerabilities in Anthropic's own `mcp-server-git` (CVE-2025-68145, CVE-2025-68143, CVE-2025-68144) demonstrated that even vendor-maintained components lack hardening, achieving full RCE via malicious `.git/config` files. Tool shadowing — where a malicious server registers a tool with the same name as a trusted one, exploiting naming collisions in the absence of namespace isolation — is an emerging class of supply-chain attack; CVE-2026-30856 in the Tencent WeKnora framework demonstrated this concretely, enabling tool execution hijacking through identifier collisions until patched with UUID-based tool identifiers in v0.3.0. Academic work on Secure Tool Manifests (2026) proposes ECDSA-signed manifests with Merkle-tree transparency logs for tamper detection, demonstrating near-linear scalability across 50,000 manifest instances with under 5% cryptographic overhead — though this remains a research prototype rather than a deployable standard. The "NeighborJack" exposure pattern (2026) revealed hundreds of MCP servers defaulting to `0.0.0.0` binding, making them reachable from adjacent network hosts and exposing command injection paths — a supply-chain risk even for locally-installed components. The MCP spec's 2025-11-25 revision formalized governance (working groups, SDK tiering, formal governance structure) but still does not mandate integrity verification, leaving enforcement entirely to implementers and gateway providers. As of March 2026, lockfile-based integrity verification is emerging as a practical intermediate layer: MCPShield's SHA-512-hashed `mcp.lock.json` and MCPTrust's capability-pinned lockfiles provide deterministic snapshots that CI gates can enforce, catching drift before production — though neither is a substitute for cryptographic attestation of the server binary itself. The Claude DXT zero-click RCE (February 2026, CVSS 10.0) exposed a fundamental architectural gap: Claude Desktop Extensions run unsandboxed with full system privileges, allowing even benign-seeming connectors (e.g., Google Calendar) to be chained into arbitrary code execution via prompt injection — Anthropic declined to fix the issue, stating it "falls outside our current threat model," underscoring that supply-chain integrity cannot rely on host-level sandboxing alone. The TeamPCP supply chain campaign (March 19–24, 2026) demonstrated cascading compromise across five ecosystems: attackers weaponized the Trivy security scanner via stolen CI/CD secrets, then pivoted to npm packages, Docker Hub (44 Aqua Security repositories defaced), Checkmarx GitHub Actions, and LiteLLM on PyPI (95 million monthly downloads, present in 36% of monitored cloud environments). LiteLLM is a common transitive dependency in MCP servers and agent frameworks, meaning compromised versions propagated silently into environments that never explicitly installed it. Mandiant estimated 1,000+ SaaS environments impacted, with projections reaching 5,000–10,000. The attack underscores that MCP supply chain integrity must extend beyond direct dependencies to transitive dependency chains — particularly AI infrastructure libraries like LiteLLM that store API keys for 100+ LLM providers. A March 2026 audit of 194 MCP server packages (via agentaudit.dev) found 118 security findings: 5 critical (4.2%), 9 high (7.6%), 63 medium (53.4%), with unsanitized shell command inputs and environment variable leakage as the most prevalent vulnerability patterns. AgentSeal's April 2026 scan of 1,808 MCP servers found 66% had at least one finding — 43% shell/command injection, 20% tooling infrastructure, 13% authentication bypass, 10% path traversal — confirming that the underlying vulnerability distribution has not shifted materially since the early-2026 CVE surge despite a growing scanner ecosystem. The MCP security scanning landscape is rapidly expanding — Cisco MCP Scanner, ToolTrust, MCPShield, mcpscan.ai, Snyk agent-scan, and AgentSeal's [awesome-mcp-security](https://github.com/AgentSeal/awesome-mcp-security) (daily-updated scores for 800+ servers via nine analyzers) now provide overlapping but complementary coverage — though no single tool covers the full attack surface from source through runtime. The most sobering development is Ox Security's April 15, 2026 disclosure of a systemic flaw in the official MCP SDKs (Python, TypeScript, Java, Rust): the STDIO transport executes launch commands even when the child process fails to start, with no toolchain-level sanitization warning, enabling arbitrary command execution on any system that connects to a malicious or compromised MCP server configuration. Ox estimates 150 million cumulative downloads, 7,000+ publicly accessible servers, and up to 200,000 vulnerable instances; Anthropic declined to patch, stating the STDIO model is a "secure default" and input sanitization is the developer's responsibility. The implication for supply-chain verification is that integrity attestation of the server binary is necessary but not sufficient — even a cryptographically verified server inherits this transport-level exposure from the SDK it was built with, and allowlisting must therefore pin SDK versions as well as server versions. The downstream-mitigation pattern is now visible in production code: LiteLLM's CVE-2026-30623 patch (v1.83.7-stable, April 21, 2026) introduces an `MCP_STDIO_ALLOWED_COMMANDS` constant restricted to seven launcher binaries with extension via `LITELLM_MCP_STDIO_EXTRA_COMMANDS`, defended at both request validation and client instantiation, and locks test/preview endpoints behind a `PROXY_ADMIN` role — confirming that the workable defense at the host layer is a hardcoded launcher allowlist rather than runtime sanitization, because attacker-supplied command strings cannot reliably be made safe by escaping. The same pattern is being formalized at the configuration layer: the OpenAI Codex SHA-256 proposal (issue #15814, March 26, 2026) adds an optional `sha256` field to `requirements.toml` allowlist entries so the host computes and verifies binary hashes before process spawn, with OpenTelemetry audit logging of verification results — closing the gap between command-name allowlisting and verified binary identity. As of May 2026, neither pattern is mandated by the MCP spec, but both are reasonable verification expectations for any production deployment. The late-April 2026 supply-chain wave reinforced this — CanisterSprawl, xinference, the Bitwarden CLI cascade, and PyTorch Lightning 2.6.2/2.6.3 (the last with on-import payload execution that runs even before any user-visible code path) collectively demonstrated that auto-updating dependencies, postinstall hooks, and import-time side effects make scan-after-install structurally insufficient; pre-install hash pinning, command-launcher allowlisting, and SDK-version pinning together form the minimum credible posture. The Mini Shai-Hulud campaign that closed out the month (April 29-30, 2026, hitting SAP CAP packages and `intercom-client` ~7.0.4/7.0.5) crossed a final architectural line: rather than merely stealing MCP credentials, the payload writes `.claude/settings.json` with a `SessionStart` hook and `.vscode/tasks.json` with `"runOn": "folderOpen"` so that future developer sessions silently re-execute attacker code. This collapses the historically separate categories of "supply-chain compromise" and "MCP configuration tampering" into a single attack chain — meaning component-integrity verification must now extend to monitoring the agent's own configuration directory for unauthorized writes, not just to verifying the integrity of installed packages. Microsoft's vscode issue [#309406](https://github.com/microsoft/vscode/issues/309406) (filed sixteen days before the Mini Shai-Hulud campaign) flagged exactly this `tasks.json` `folderOpen` trigger as an unaddressed attack surface, and the campaign exploited it within the disclosure window — illustrating that for IDE-resident MCP infrastructure, even pre-disclosed editor-level vectors can be operationalized faster than mitigations land. |
| **10.1.2** | **Verify that** only allowlisted MCP server identifiers (name, version, and registry) are permitted in production and that the runtime rejects connections to unlisted or unregistered servers at load time. | 1 | **Credential exposure via configuration leakage.** MCP configurations (e.g., `claude_desktop_config.json`, `.mcp.json`) commonly embed API keys and database connection strings in plaintext. GitGuardian's 2026 State of Secrets Sprawl report found 24,008 unique secrets exposed in MCP-related configuration files across public GitHub, including 2,117 valid credentials (8.8% validity rate). Overall, 28.65 million new hardcoded secrets were pushed to public repos in 2025 — a 34% year-over-year increase. MCP credentials can also persist in agent context memory, creating "contextual secret leakage" where the protocol layer itself becomes an unintentional secret repository. CVE-2026-26118 (March 2026, CVSS 8.8) demonstrated a related vector: an SSRF vulnerability in Azure MCP Server allowed authenticated attackers to coerce the server into making requests to attacker-controlled URLs, capturing the server's managed identity token and gaining the full privileges of that identity across Azure subscriptions. [CVE-2026-32211](https://cvefeed.io/vuln/detail/CVE-2026-32211) (April 3, 2026, CVSS 9.1) extended the pattern: Microsoft's own `@azure-devops/mcp` npm package shipped with no authentication at all, exposing configuration, API keys, and project metadata to unauthenticated callers — and the package's `preinstall` script overwrote the npm registry configuration on the installing machine, a classic supply-chain pivot vector. The package was published from a personal npm account without GitHub Actions trusted publishing, leaving no verifiable link between the source repository and the published artifact — the same missing-provenance condition that let the axios compromise proceed undetected four days earlier. Allowlisting that only checks the name and version of this package would still have admitted it to production; only a policy requiring authentication-declared metadata and provenance attestation catches it. Ranked MCP01 in the [OWASP MCP Top 10](https://owasp.org/www-project-mcp-top-10/2025/MCP01-2025-Token-Mismanagement-and-Secret-Exposure). | Scan configuration files for plaintext secrets using `trufflehog`, `gitleaks`, or `detect-secrets`. As of March 2026, the [GitHub MCP Server](https://github.blog/changelog/2026-03-17-secret-scanning-in-ai-coding-agents-via-the-github-mcp-server/) can scan code changes for exposed secrets before commit, integrating directly with MCP-compatible IDEs. Verify production deployments use secret managers (HashiCorp Vault, AWS Secrets Manager, Doppler, 1Password CLI) with runtime injection — tools like `doppler run` can inject secrets into MCP server processes at startup with automated rotation and audit trails. Check that `.gitignore` excludes MCP config files. Verify credentials are rotated every 30–90 days and that OAuth 2.1 short-lived tokens are used where supported (only ~8.5% of MCP implementations currently use OAuth). Where OAuth 2.1 is used, confirm Resource Indicators (RFC 8707) bind tokens to specific MCP servers — preventing tokens issued for one server from being replayed against another. Verify least-privilege scopes are enforced per tool/capability rather than catch-all scopes. The MCP 2025-11-25 spec adds incremental scope consent via `WWW-Authenticate` headers and Client ID Metadata Documents (CIMD) for registration without static secrets — verify implementations have adopted these mechanisms. Confirm token audience validation (`aud` claim) is enforced per-server to prevent confused deputy attacks. Check that OAuth token passthrough between MCP servers is prohibited (the spec explicitly forbids reusing tokens across services). Ensure secrets are never baked into Docker images and that credentials are separated across dev/staging/production environments with no overlap. For enterprise-scale discovery of MCP servers that may harbor credential issues, [Qualys TotalAI](https://blog.qualys.com/product-tech/2026/03/19/mcp-servers-shadow-it-ai-qualys-totalai-2026) can detect MCP servers at the network, host, and supply-chain levels — including localhost-bound servers on random high ports that evade traditional security tools — and extract tool catalogs, resource URIs, and filesystem access scopes for audit. Implement dual-credential rotation strategies — maintain overlapping validity windows for old and new credentials during rotation cycles to avoid downtime while eliminating long-lived static secrets (53% of MCP deployments still rely on long-lived static credentials as of early 2026). Forward all MCP credential access events to a SIEM platform (Datadog, Splunk) and alert on anomalous patterns: off-hours access, repeated 401/403 errors, API call volume spikes, and connections from unknown server identifiers. | This is the most pervasive problem in the MCP ecosystem — approximately 48% of reviewed MCP servers recommend storing sensitive credentials in plaintext files. The MCP spec does not define a standard secret-reference mechanism (e.g., `${vault:secret/path}`), so each implementation handles this differently. The OWASP MCP Top 10 ranks this as MCP01, emphasizing that attack scenarios include prompt recall exposure (crafted prompts retrieve stored API keys from model memory), log scraping (raw MCP payloads containing tokens in debug logs), and context poisoning (malicious meta-instructions trigger secret leakage). The MCP 2025-11-25 specification now mandates PKCE (S256) for all OAuth flows and aligns Protected Resource Metadata discovery with RFC 9728, but adoption lags — only ~8.5% of MCP implementations use OAuth at all. Two proposals under review (SEP-1932 for DPoP and SEP-1933 for Workload Identity Federation) would further strengthen credential security by binding tokens to specific client instances and enabling managed identity flows, but neither is ratified. Anthropic's own MCP server directory now requires recognized CA certificates and privacy policy disclosures before listing remote servers, setting a baseline for supply-chain credential hygiene. |
| **10.1.3** | **Verify that** all MCP tool and resource schemas include cryptographically verifiable provenance metadata — including author, timestamp, version hash, signature, and approved‑by fields. | 2 | **Schema manipulation, post-approval mutation, and rug-pull attacks.** Attackers inject malicious instructions into tool descriptions or parameter schemas — invisible to human users but processed by AI models — either at registration time or after initial approval. The formal threat taxonomy in 2604.05969 (2026) identifies three schema-level threats scoring Critical in DREAD analysis: TV2 (Schema Manipulation — hidden side-effect fields injected into parameter schemas), TV4 (Tool Shadowing — servers registering identically-named tools to intercept invocations), and TV5 (Post-Approval Mutation — tools silently modifying descriptions or behavior after the user has already granted approval, converting legitimate access into unauthorized capabilities). Real-world tool-poisoning prevalence is documented: Invariant Labs found 5.5% of production MCP servers exhibit tool-poisoning characteristics; the MCPTox benchmark confirms malicious tool definitions pass into agent contexts with high frequency; and a 2026 survey of 2,614 MCP implementations found code injection risks present in two-thirds of servers. MITRE ATLAS added a new technique in February 2026 — "Publish Poisoned AI Agent Tool" — capturing exactly this threat class. Without signed provenance metadata pinning schema content to an approved version, there is no reliable mechanism for a client or orchestrator to detect that a tool definition has been modified since approval, either by a supply-chain compromise upstream or by the server operator after deployment. Relates to MITRE ATLAS AML.T0054 (Prompt Injection) and AML.T0010 (ML Supply Chain Compromise). | The primary verification framework is the **Tool Attestation Record (TAR)** model proposed in arxiv 2604.05969 (2026): each MCP server publishes cryptographically signed attestations that incorporate (1) a collision-resistant hash of the full tool definition, (2) monotonically increasing version numbers with regression prevention, (3) a trusted timestamp, and (4) a dependency-tree hash covering all schema dependencies. Before any tool invocation, the client verifies the signature, compares the current definition hash against the approved baseline stored locally, confirms version number has not decreased, and validates dependency hashes. This enforces **Property 1 (Tool Integrity)**: tool definitions at invocation time must cryptographically match approved versions. The [Secure Tool Manifest framework](https://arxiv.org/html/2601.23132v1) (arxiv 2601.23132, January 2026) operationalizes this using ECDSA P-256 signatures stored in Hardware Security Modules with append-only Merkle-tree transparency logs, achieving ~80% verification success rate with R²=0.998 linear scalability across 50,000 manifest instances. [MCPS (MCP Secure)](https://mcp-secure.dev/) wraps every JSON-RPC message — including `tools/list` responses — in a signed envelope with nonce-based replay protection, providing author identity (ECDSA P-256 "Agent Passport"), timestamp, and signature at the message layer (L1–L4 trust levels). For compliance verification: review MCP server implementations for signed tool definition responses; test that `tools/list` responses include a `provenance` object with `author`, `timestamp`, `version_hash`, `signature`, and `approved_by` fields; verify that the client re-validates signatures on every `notifications/tools/list_changed` event per AISVS 10.4.5; confirm `approved_by` records the auditable identity of the administrator or automated pipeline that approved the definition for production; use [MCPTrust](https://github.com/mcptrust/mcptrust) drift detection (categorizes changes as critical/moderate/informational) to detect post-approval mutations; and run [Snyk agent-scan](https://github.com/snyk/agent-scan) to flag tool-poisoning patterns. NIST's AI Agent Standards Initiative (February 2026) explicitly identifies non-repudiation — "accountability that links agent actions to the human authority that sanctioned them" — as a required property, covering approved-by metadata. For container-based deployments, [ToolHive](https://toolhive.dev/) extracts container image fingerprints and validates GitHub Attestations proving source code, workflow, build environment, and signing identity, which serves as build-provenance evidence for the server binary hosting the schemas. | As of April 2026, no standardized schema provenance format is defined in the MCP specification itself — the `tools/list` response schema contains no mandatory `provenance` field, leaving implementers to add it optionally. TAR is a research/proposal stage concept (arxiv 2604.05969) without a reference implementation or standard adoption path. MCPS message signing addresses transport-layer provenance but does not define the content of approved-by governance metadata. The MCP spec's `notifications/tools/list_changed` mechanism (AISVS 10.4.5) signals that a definition changed but carries no cryptographic proof of what changed or whether the new version was authorized. The [PipeLab State of MCP Security 2026 report](https://pipelab.org/blog/state-of-mcp-security-2026/) explicitly documents "no schema provenance tracking across sessions or fleet-wide tool description drift" and "version-diff alerting missing" as active gaps. The Secure Tool Manifest paper notes that key rotation and temporal randomization are proposed mitigations but not yet integrated solutions, and that long-term resilience under adversarial load remains partially unexplored. NIST's AI Agent Standards Initiative is collecting input through 2026 and may yield a standard for agent action provenance that would cover tool schema attestation, but no timeline has been announced. The asymmetric-key dominance finding (fairness index 𝒦=0.42) in the Secure Tool Manifest evaluation suggests that under real-world load distributions, some tool definitions receive disproportionate verification overhead — a practical obstacle to universal enforcement. As of April 2026, the best achievable posture is defense-in-depth: lockfile-based hashing (MCPShield SHA-512 `mcp.lock.json`) for static drift detection, MCPS message signing for transport-layer integrity, TAR/Secure Tool Manifest for environments willing to adopt experimental frameworks, and MCPTrust behavioral drift detection for runtime monitoring — with the explicit acknowledgment that none of these is a ratified standard. |

---

## Real-World Incidents & Research (2025-2026)

- **WhatsApp MCP Tool Poisoning (April 2025):** A malicious MCP server silently exfiltrated a user's entire WhatsApp chat history through tool poisoning, exposing personal conversations and business data. One of the earliest documented cases of MCP-specific data exfiltration.
- **GitHub MCP Prompt Injection (May 2025):** A prompt-injection attack against a GitHub MCP integration enabled theft of private repository contents, internal project details, and personal financial information. Overly broad API tokens amplified the blast radius.
- **Asana MCP Cross-Tenant Leak (June 2025):** An access control flaw in an Asana MCP integration permitted cross-tenant data visibility between different customer organizations.
- **Anthropic MCP Inspector RCE (June 2025):** An unauthenticated remote code execution vulnerability in the MCP Inspector development tool exposed developer workstations' filesystems, API keys, and environment secrets.
- **CVE-2025-6514 — mcp-remote Command Injection (July 2025):** Critical command-injection bug (CVSS 10.0) in the `mcp-remote` npm package affected 437,000+ downloads. The package's OAuth proxy passed malicious endpoints directly to system shell handlers (PowerShell on Windows, shell commands on Unix), enabling credential theft, Git repository exfiltration, and full system compromise. The first documented case of full system compromise achieved through MCP infrastructure.
- **Anthropic Filesystem MCP Sandbox Escape (August 2025):** Sandbox escape vulnerabilities allowed arbitrary file access through symlink bypass and containment flaws in the official filesystem MCP server.
- **Postmark MCP Supply Chain Breach (September 2025):** Attackers injected a single line of malicious code into an npm package used for sending transactional emails through Postmark in AI automation pipelines. Compromised MCP servers silently BCC'd every outgoing email to the attackers.
- **Smithery Registry Path Traversal (October 2025):** A path-traversal bug in the Smithery.ai build configuration exposed Docker credentials controlling 3,000+ hosted MCP servers and their API tokens.
- **Figma MCP Command Injection (October 2025):** A command-injection flaw via unsanitized input in the Figma/Framelink MCP integration enabled shell execution and design data access.
- **Supabase Cursor Agent Exploit (mid-2025):** Supabase's Cursor agent, running with privileged service-role access, processed support tickets containing user-supplied input as commands. Attackers embedded SQL instructions to exfiltrate sensitive integration tokens by leaking them into a public support thread.
- **CVE-2026-21852 — Claude Code Project File RCE (January 2026):** Critical vulnerability allowing remote code execution and API credential theft through malicious project configuration files.
- **MCP CVE Surge (early 2026):** 30+ CVEs filed against MCP implementations in a 60-day period, covering SSRF, command injection, path traversal, and authentication bypass. Over 40% of public MCP servers tested were susceptible to command injection.
- **8,000+ Exposed MCP Servers (2026):** Research identified over 8,000 publicly exposed MCP servers, with 492 lacking basic authentication or encryption.
- **GitGuardian Secrets Report (2026):** 24,008 unique secrets found in MCP-related configuration files across public GitHub, including 2,117 valid credentials. AI coding tools correlated with a doubling of secret leak rates.
- **mcp-server-git CVE Chain (CVE-2025-68145/68143/68144):** Three chained vulnerabilities in Anthropic's own `mcp-server-git` package combined with the Filesystem MCP server to achieve full remote code execution via malicious `.git/config` files. Demonstrated that even vendor-maintained MCP components can harbor critical security flaws.
- **CoSAI MCP Threat Analysis (early 2026):** The Coalition for Secure AI identified 40+ distinct MCP threat categories. Most organizations were addressing fewer than two categories, suggesting widespread underestimation of MCP attack surface.
- **Tool Poisoning Prevalence (2026):** Invariant Labs research found 5.5% of MCP servers in the wild exhibit tool poisoning characteristics — malicious instructions hidden in tool descriptions that are invisible to human users but processed by AI models.
- **Shadow MCP Servers (2026):** Developers increasingly connect MCP servers to IDEs without IT oversight, granting file system and shell access outside traditional security monitoring perimeters. These "shadow MCP" deployments bypass organizational security controls entirely.
- **MCPTox Benchmark (2025-2026):** Research benchmark evaluating how often malicious or manipulated tool definitions pass into AI agent contexts, finding tool poisoning attacks frequently result in unauthorized execution or data leakage.
- **CVE-2026-30856 — WeKnora Tool Execution Hijacking (2026):** A naming collision flaw (CWE-706) in Tencent's WeKnora framework allowed attackers to register malicious MCP servers with tool identifiers that overwrote legitimate tool references in memory, enabling indirect prompt injection and data exfiltration. Fixed in v0.3.0 with UUID-based tool identifiers and "first-wins" registration policy.
- **Unit 42 MCP Sampling Attack Vectors (2026):** Palo Alto Networks' Unit 42 research identified three novel attack vectors through MCP's sampling feature: resource theft via hidden prompts (servers inject covert instructions causing LLMs to generate unauthorized content), persistent conversation hijacking (injected directives alter AI behavior across multiple turns), and covert tool invocation (servers manipulate prompts to trigger unauthorized file system operations). These attacks exploit the bidirectional control reversal inherent in MCP sampling, where servers become active requesters of LLM completions with minimal oversight.
- **CVE-2026-23744 — MCPJam Inspector RCE (March 2026):** Critical RCE vulnerability (CVSS 9.8) in the MCPJam Inspector development platform (versions ≤1.4.2). The service bound to `0.0.0.0` instead of `127.0.0.1`, allowing remote attackers to send crafted HTTP requests that triggered MCP server installation and arbitrary code execution without authentication. Fixed in v1.4.3.
- **CVE-2026-26118 — Azure MCP Server SSRF (March 2026):** An SSRF vulnerability (CVSS 8.8) in Azure MCP Server allowed authenticated attackers to submit malicious URLs as tool parameters, coercing the server into making outbound requests that exposed its managed identity token. Captured tokens granted the attacker the full privileges of the MCP Server's Azure identity. Patched in Microsoft's March 2026 Patch Tuesday.
- **Qualys TotalAI Shadow MCP Discovery (March 2026):** Qualys research found MCP servers frequently evade traditional security tools by binding to localhost, using random high ports, or hiding behind proxies. Over 10,000 public MCP servers appeared within one year of the protocol's introduction, with many operating outside IT governance perimeters.
- **NeighborJack MCP Server Exposure (2026):** Hundreds of MCP servers found defaulting to `0.0.0.0` binding instead of `127.0.0.1`, exposing command injection paths to adjacent network hosts and enabling complete host system compromise when internet-accessible — even for servers intended to be local-only.
- **Linux Foundation Agentic AI Foundation (December 2025):** The Linux Foundation launched open governance over MCP standard development, signaling the protocol's transition from a single-vendor project to a community-governed standard with formal working groups and SDK tiering.
- **Proofpoint Agent Integrity Framework (March 2026):** Proofpoint published a five-phase maturity model for AI agent integrity — from initial discovery through runtime enforcement — defining what it means for agents (including their MCP tool integrations) to operate within authorized boundaries.
- **Claude DXT Zero-Click RCE (February 2026):** LayerX researchers discovered a zero-click remote code execution vulnerability (CVSS 10.0) in Claude Desktop Extensions (DXT) affecting 10,000+ active users and 50 extensions. Unlike traditional browser extensions, DXT runs unsandboxed with full system privileges, allowing attackers to embed malicious code in benign data sources (e.g., a Google Calendar event) that Claude autonomously chains into arbitrary code execution. Anthropic declined to fix the issue, stating it "falls outside our current threat model" — a decision that highlights the gap between MCP component trust assumptions and actual attack surface.
- **TeamPCP Supply Chain Campaign (March 19–24, 2026):** A coordinated multi-ecosystem supply chain attack by a threat actor group linked to LAPSUS$. Initial access exploited a misconfigured `pull_request_target` GitHub Actions trigger in Aqua Security's Trivy repository. The attackers weaponized Trivy (one of the most trusted cloud-native security scanners), force-pushing malicious binaries to 76 of 77 version tags starting with v0.69.4. The campaign then cascaded: a self-propagating worm ("CanisterWorm") spread via stolen npm tokens to 66+ packages on March 20; Docker Hub images were poisoned on March 22 (44 Aqua Security repositories defaced); Checkmarx KICS and GitHub Actions were hijacked on March 23; and LiteLLM was compromised on PyPI on March 24 (versions 1.82.7 and 1.82.8). LiteLLM — an LLM API gateway with ~95 million monthly downloads present in 36% of monitored cloud environments — stores API keys for 100+ LLM providers, making it a high-value target. The credential-stealing payload harvested SSH keys, cloud credentials, Kubernetes secrets, and database credentials. Mandiant estimated 1,000+ SaaS environments impacted with projections of 5,000–10,000; approximately 300 GB of stolen credentials were reported with active extortion underway. PyPI quarantined the malicious LiteLLM packages within ~3 hours. The incident is particularly relevant to MCP security because LiteLLM is a common transitive dependency in MCP server implementations and agent orchestration frameworks.
- **JFrog Universal MCP Registry Launch (March 2026):** JFrog announced a Universal MCP Registry as part of its AI Catalog, providing enterprise-grade governance for MCP servers — treating them as managed artifacts with security blocking of malicious or non-compliant servers before execution. The first major DevOps platform vendor to integrate MCP server governance into an existing software supply chain management product.
- **MACH Alliance MCP Registry (2026):** The MACH Alliance launched a vendor-neutral MCP registry providing standardized metadata, automated verification, and governance policy enforcement for MCP server discovery and registration — supporting both public and internal-only enterprise entries.
- **MCP Spec 2025-11-25 Security Enhancements (November 2025):** The anniversary spec revision introduced OIDC Discovery support, incremental scope consent via WWW-Authenticate, Client ID Metadata Documents for registration without static secrets, tool naming guidance, and formal governance structures — the most significant security-oriented spec update since MCP's launch.
- **Axios npm Supply Chain Compromise (March 31, 2026):** Attackers hijacked a maintainer account and published axios 1.14.1 and 0.30.4 to npm — axios sees over 100 million weekly installs, making it one of the most downloaded JavaScript libraries and a transitive dependency in a large fraction of MCP server implementations. The malicious versions added a hidden `plain-crypto-js` dependency carrying the WAVESHAPER.V2 cross-platform RAT, which provided recon, arbitrary command execution, in-memory PE injection on Windows, and filesystem enumeration. The payload self-erased from `node_modules` after execution, so `npm audit` returned clean. Google Threat Intelligence attributed the operation to the North Korea–nexus Sapphire Sleet cluster; the attack window lasted 179 minutes before the packages were yanked. Notably, for agentic workflows where Claude Code or Cursor run `npm install` autonomously, the traditional detection signal — a developer watching terminal output — is absent, making pre-install provenance gating the only reliable defense. [Huntress](https://www.huntress.com/blog/supply-chain-compromise-axios-npm-package), [Wiz](https://www.wiz.io/blog/axios-npm-compromised-in-supply-chain-attack), and [Microsoft](https://www.microsoft.com/en-us/security/blog/2026/04/01/mitigating-the-axios-npm-supply-chain-compromise/) published mitigation guidance within 24 hours.
- **CVE-2026-32211 — Azure DevOps MCP Server Missing Authentication (April 3, 2026):** Microsoft disclosed an information disclosure vulnerability (CVSS 9.1) in the `@azure-devops/mcp` npm package. The server lacked any authentication mechanism, exposing work items, repositories, pipelines, pull requests, API keys, and project metadata to unauthenticated callers. No patch was available at disclosure — Microsoft provided only mitigation guidance (restrict network access, use firewalls/reverse proxies). Independent monitoring flagged two pre-incident supply-chain risk signals: a `preinstall` script that rewrites the npm registry configuration on the installing host (the same vector abused in the axios campaign days earlier) and publication from a personal npm account without GitHub Actions trusted publishing, leaving no verifiable source-to-artifact provenance link. The incident underscores that allowlisting must evaluate both malware presence and architectural security controls — "does this server implement the security controls it should?" not only "does this package contain malware?"
- **Ox Security MCP STDIO Systemic Flaw (April 15, 2026):** Ox Security disclosed a critical architectural vulnerability in the official MCP SDKs (Python, TypeScript, Java, Rust): the STDIO transport executes launch commands even when the child process fails to start, providing no toolchain-level sanitization warning, and enabling arbitrary command execution on any system that connects to a malicious or compromised MCP server configuration. Ox estimated 150 million cumulative downloads across the affected SDKs, 7,000+ publicly accessible servers, and up to 200,000 vulnerable instances, spanning 200+ open-source projects. Ox issued 30+ responsible disclosures and identified 10+ high/critical CVEs in individual downstream projects. Anthropic declined to modify the protocol, characterizing STDIO as a "secure default" where input sanitization is the developer's responsibility. The implication for C10.1 is that cryptographic attestation of the server binary is necessary but not sufficient — every server built on an affected SDK inherits the transport-level exposure, so allowlisting must pin SDK versions in addition to server versions, and organizations operating MCP fleets should track SDK-level CVEs as a first-class dependency.
- **AgentSeal Scan of 1,808 MCP Servers (April 2026):** AgentSeal performed what is, to date, the largest systematic security audit of the MCP ecosystem — scanning 1,808 servers discovered through GitHub repositories, npm and PyPI packages, and public MCP registries including Smithery and MCP.run. 66% had at least one security finding: 43% shell or command injection, 20% tooling infrastructure issues, 13% authentication bypasses, and 10% path traversal. The detection pipeline combined regex signatures, deobfuscation, semantic embedding-based comparison against known attack patterns, and Claude Opus–based classification for context-aware severity. The result confirms that the distribution of MCP server vulnerabilities has not materially shifted from the early-2026 CVE surge despite the emergence of multiple scanner ecosystems.
- **CVE-2026-30623 — LiteLLM MCP STDIO Command Injection Patch (April 8–21, 2026):** LiteLLM tracked the systemic Anthropic MCP STDIO flaw as CVE-2026-30623 and shipped the first concrete downstream fix. [PR #25343](https://github.com/BerriAI/litellm/pull/25343) (merged April 8, 2026; v1.83.7-stable on April 21) introduced an `MCP_STDIO_ALLOWED_COMMANDS` constant restricting STDIO launches to seven binaries (`npx`, `uvx`, `python`, `python3`, `node`, `docker`, `deno`), with extension via `LITELLM_MCP_STDIO_EXTRA_COMMANDS`. The patch enforces the allowlist at both `NewMCPServerRequest`/`UpdateMCPServerRequest` validation and at `_create_mcp_client` instantiation, and locks `/mcp-rest/test/*` endpoints behind a `PROXY_ADMIN` role. Eleven new unit tests accompanied the change. The fix is now the de-facto reference design for any MCP host that accepts STDIO server configurations from anywhere outside the operator's direct control.
- **OpenAI Codex SHA-256 MCP Allowlist Proposal (March 26, 2026):** [Codex issue #15814](https://github.com/openai/codex/issues/15814) proposes adding an optional `sha256` field to the `identity` section of `requirements.toml`, so MCP server allowlist entries pin the verified binary hash before execution. The proposal is fully backward-compatible (optional field), computes the hash before process spawn, emits OpenTelemetry audit events on verification, and produces clear error messages on mismatch. It is the first public proposal from a major AI tooling vendor to extend allowlisting from command-name matching to binary integrity verification, closing PATH-based typosquatting and unauthorized-update gaps that command-name allowlisting cannot reach.
- **CanisterSprawl npm Worm (April 21, 2026):** A self-propagating supply-chain worm — distinct from but technically overlapping with TeamPCP's CanisterWorm — was identified in 16+ malicious package versions across `@automagik/genie` (an AI agent-orchestration CLI), `pgserve`, `@fairwords`, and `@openwebconcept`. The worm executes via npm postinstall hook, harvests roughly 40 categories of credentials (npm publish tokens, GitHub PATs, AWS/Azure/GCP keys, browser/wallet data) via regex sweep, and exfiltrates to a dual-channel C2 that includes an Internet Computer Protocol (ICP) canister for resilience. If a PyPI publish token is found on the infected host, the worm jumps ecosystems and republishes itself there. In-code references to a "TeamPCP/LiteLLM method" and `.pth` file injection technique link it to prior TeamPCP tooling without formal attribution. The campaign is materially relevant to MCP because `@automagik/genie` targets AI agent orchestration — exactly the layer where MCP servers are wired in — and because the cross-ecosystem propagation pattern means an MCP server author's npm token can be silently used to push poisoned versions of their unrelated PyPI MCP packages. [Socket](https://socket.dev/blog/namastex-npm-packages-compromised-canisterworm) and [StepSecurity](https://www.stepsecurity.io/blog/pgserve-compromised-on-npm-malicious-versions-harvest-credentials) tracked the campaign in real time.
- **Bitwarden CLI v2026.4.0 Cascade Compromise (April 22, 2026):** TeamPCP attackers pushed malicious Docker images overwriting five existing Checkmarx KICS tags and creating two new ones; Bitwarden's Dependabot automation pulled the trojanized KICS image during the v2026.4.0 build pipeline, poisoning the released CLI before removal. Approximately 334 downloads occurred before the package was pulled. This was the first documented cascade compromise through trusted automation in the TeamPCP campaign, demonstrating that MCP component integrity verification cannot stop at direct dependencies — the trust boundary extends to every CI/CD container that touches the build, including security scanners. Defensive guidance: audit any KICS usage between April 22 14:17–15:41 UTC, review Dependabot-triggered CI/CD pulls for that window, and revoke any GitHub/npm/cloud tokens that may have been resident in build environments.
- **xinference PyPI Compromise (April 22, 2026):** Three consecutive xinference releases on PyPI carried base64-encoded payloads harvesting credentials and exfiltrating to attacker infrastructure. Notably, the payload sent a plain `tar.gz` directly to the C2 server — a departure from prior TeamPCP encryption tradecraft, and the payload contained the string `# hacked by teampcp` despite TeamPCP denying involvement, suggesting copycat activity or operational deception. xinference is a model-inference framework with cross-pollination into agent stacks, making it a credible MCP-adjacent transitive dependency.
- **PyTorch Lightning 2.6.2 / 2.6.3 PyPI Compromise (April 30, 2026):** Lightning versions 2.6.2 and 2.6.3 were published with a malicious payload that runs **on import** — no postinstall hook required. A daemon thread downloads the Bun JavaScript runtime, executes an 11 MB heavily obfuscated payload with suppressed output, and harvests GitHub tokens, npm credentials, AWS/Azure/GCP cloud keys, and 20+ CI platform environment variables. The execution chain runs automatically the first time `lightning` is imported by any code path. Socket's AI scanner flagged the threat eighteen minutes after publication — a useful detection latency for monitoring tools but an inadequate defense for any developer who installed during that window. The incident is materially relevant to MCP component integrity because (1) Lightning is a transitive dependency in many AI agent and MCP-server stacks, (2) the on-import execution pattern bypasses the traditional `--ignore-scripts` mitigation entirely, and (3) agentic coding assistants that run `pip install` autonomously have no human in the loop to notice the workspace becoming hostile — making pre-install provenance gating, not post-facto scanning, the only structurally adequate defense. Version 2.6.1 (January 30, 2026) is clean. [Socket](https://socket.dev/blog/lightning-pypi-package-compromised) published the technical analysis.
- **Mini Shai-Hulud Campaign (April 29-30, 2026):** A coordinated multi-package supply-chain campaign that explicitly targeted MCP infrastructure as both harvest target and persistence channel. Compromised packages spanned npm and PyPI: SAP Cloud Application Programming dependencies (`mbt@1.2.48`, `@cap-js/sqlite@2.2.2`, `@cap-js/postgres@v2.2.2`, `@cap-js/db-service@v2.10.1` — combined ~572K weekly downloads), the `intercom-client` npm SDK (v7.0.4 published April 30 at 14:41 UTC via a hijacked GitHub Actions OIDC token, plus v7.0.5 per Wiz attribution), the `intercom/intercom-php` Composer package (v5.0.2), and PyTorch Lightning 2.6.2/2.6.3. SecurityWeek tracked roughly 1,800 victim repositories. The payload used a `package.json` `preinstall` hook to invoke `setup.mjs`, which loaded the Bun JavaScript runtime to execute an obfuscated credential stealer harvesting GitHub tokens (validated and weaponized for worm propagation), npm tokens, SSH keys, cloud credentials, Kubernetes secrets, Vault contents, Docker configs, and `.env` files. What made the campaign materially new for MCP component integrity is the persistence step: the malware writes `.claude/settings.json` containing a `SessionStart` hook (re-executes every time the developer launches Claude Code), `.vscode/tasks.json` with `"runOn": "folderOpen"` (re-executes on project open in VS Code), and harvests AI-tooling-specific paths including `~/.claude.json`, `~/.claude/mcp.json`, and `~/.kiro/settings/mcp.json` for embedded MCP server credentials and database connection strings. For repositories where the worm has workflow permissions, it creates a typosquatted `dependabout/github_actions/format/setup-formatter` branch with `.github/workflows/format-check.yml` that dumps `${{ toJSON(secrets) }}` on push. Microsoft's vscode issue [#309406](https://github.com/microsoft/vscode/issues/309406) flagged the `tasks.json folderOpen` attack surface sixteen days before this campaign — the window between disclosure and operationalization underscores that pre-install gating, agent-config integrity monitoring, and aggressive token revocation are the only defenses that match attacker velocity. The campaign brands itself "Mini Shai-Hulud" via dead-drop description strings; it shares five technical TTPs with the TeamPCP campaigns tracked since March 2026 and follows the September/November 2025 Shai-Hulud worm lineage, with researchers attributing the convergence to multiple cooperating actors rather than a single group. Exfiltration to public GitHub repositories named with Dune-universe terminology, plus a `zero.masscan.cloud` C2 channel with dynamic fallback. [Aikido](https://www.aikido.dev/blog/mini-shai-hulud-has-appeared), [Sophos](https://www.sophos.com/en-us/blog/-mini-shai-hulud-supply-chain-attack-targets-sap-npm-packages), [Wiz](https://www.wiz.io/blog/mini-shai-hulud-supply-chain-sap-npm), [Phoenix Security](https://phoenix.security/mini-shai-hulud-sap-cap-mbt-npm-supply-chain-bun-credential-stealer/), [StepSecurity](https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared), and [Onapsis](https://onapsis.com/blog/sap-cap-mini-shai-hulud-supply-chain-attack/) published technical analyses within 48 hours.
- **Intercom-client npm Compromise (April 30, 2026):** Independently tracked from but operationally connected to Mini Shai-Hulud, `intercom-client@7.0.4` (Socket attribution) and `@7.0.5` (Wiz attribution) were published to npm via a hijacked GitHub Actions OIDC token at 14:41 UTC. The Intercom engineering team did not publish these versions — the CI/CD pipeline was used against them. The compromise traced back to a local install of the poisoned `pyannote-audio` package, which carried Lightning as a transitive dependency, demonstrating that the Lightning→Intercom propagation crossed both ecosystem (PyPI→npm) and runtime boundaries on a single developer machine. Combined Lightning+Intercom monthly downloads approached 10 million. The package is a high-value target for MCP-adjacent stacks because Intercom integrations frequently appear in agent-driven customer-support automation, and the SDK is commonly installed in backend services and CI/CD pipelines that integrate with Intercom's API. [Upwind](https://www.upwind.io/feed/intercom-client-7-0-4-supply-chain-attack) and [The Hacker News](https://thehackernews.com/2026/04/pytorch-lightning-compromised-in-pypi.html) tracked attribution and timeline.
- **MCP Gateway Tool Landscape Expansion (Q2 2026):** The MCP gateway market consolidated around four enterprise-oriented entrants in addition to the existing Lasso/Docker/MCP-Gateway-Registry ecosystem. [MCP Manager](https://mcpmanager.ai/) provides RBAC, SSO, SCIM, and end-to-end logging across MCP traffic with deployment and observability primitives. [Golf.dev](https://golf.dev/) targets organizations deploying their own MCP servers, centralizing through an authentication-gated master MCP server with prompt-injection mitigation and MCP-specialized rate limiting. [Obot MCP Gateway](https://obot.ai/) ships project sharing, power-user roles, enhanced permissions, and governance primitives for enterprise MCP fleets. [TrueFoundry MCP Gateway](https://www.truefoundry.com/mcp-gateway) layers federated login via Okta and Azure AD, RBAC policies, and OAuth 2.0 token brokering. [MintMCP](https://www.mintmcp.com/) — already SOC 2 Type II certified — added malicious-command blocking, file-access restrictions, and LLM monitoring. The proliferation suggests the gateway-as-PEP pattern has become the dominant enterprise architecture for MCP enforcement, but the absence of a standard gateway interface means each implementation enforces a different policy vocabulary; teams running multi-vendor agent stacks must currently choose between gateway lock-in and per-runtime enforcement.
- **Stacklok ToolHive Registry Update (April 13, 2026):** ToolHive's Registry Server added claim-based authorization: read, write, and admin operations are gated on JWT claims so only entries the caller's identity is authorized for are visible (unauthorized resources are hidden entirely rather than surfaced as permission errors). Per-entry access control for Kubernetes sources allows individual MCP server and skill entries to carry their own authorization claims, and immutable entry visibility prevents subsequent versions from broadening access. A `/me` endpoint exposes authenticated identity and roles in the pattern of GitHub and Microsoft Graph. MCPServers in Kubernetes now enforce per-user, per-tool call rate limiting on `tools/call` (shared, per-user, or hybrid) backed by Redis, returning HTTP 429 with `Retry-After` on exceeded limits and failing open if Redis is unreachable. The virtual MCP Server gained an embedded OAuth authorization server with Dynamic Client Registration and upstream identity-provider support (GitHub, Google, Okta). This release materially closes the gap between registry-level allowlisting and runtime tool-invocation enforcement.

## Related Standards & References

- [OWASP MCP Top 10 — MCP01: Token Mismanagement & Secret Exposure](https://owasp.org/www-project-mcp-top-10/2025/MCP01-2025-Token-Mismanagement-and-Secret-Exposure) — top-ranked MCP vulnerability class
- [CoSAI: Model Context Protocol Security](https://github.com/cosai-oasis/ws4-secure-design-agentic-systems/blob/main/model-context-protocol-security.md) — Coalition for Secure AI guidance on MCP security design
- [MCPS (MCP Secure)](https://mcp-secure.dev/) — cryptographic identity, message signing, and trust verification for MCP (ECDSA P-256, L0–L4 trust levels)
- [SLSA (Supply-chain Levels for Software Artifacts)](https://slsa.dev/) — framework for supply-chain integrity
- [Sigstore](https://www.sigstore.dev/) — keyless signing for open-source packages
- [npm Provenance](https://docs.npmjs.com/generating-provenance-statements) — build provenance for npm packages
- [OWASP Dependency-Check](https://owasp.org/www-project-dependency-check/) — known-vulnerability scanning
- [CIS Software Supply Chain Security Guide](https://www.cisecurity.org/benchmark/software_supply_chain_security)
- [GitHub MCP Registry & Allowlist Controls](https://docs.github.com/en/copilot/how-tos/administer-copilot/manage-mcp-usage/configure-mcp-server-access) — enterprise MCP server governance for Copilot
- [GitHub MCP Server Secret Scanning](https://github.blog/changelog/2026-03-17-secret-scanning-in-ai-coding-agents-via-the-github-mcp-server/) — pre-commit secret detection for MCP-compatible IDEs
- [MCP Gateway & Registry](https://agentic-community.github.io/mcp-gateway-registry/) — open-source centralized MCP governance with OAuth and Keycloak/Entra
- [Doppler: MCP Credential Security Best Practices](https://www.doppler.com/blog/mcp-server-credential-security-best-practices) — runtime injection, rotation, and OAuth 2.1 for MCP
- [Invariant Labs: MCP Tool Poisoning Research](https://invariantlabs.ai/blog/mcp-security-notification-tool-poisoning-attacks) — tool poisoning attack analysis
- [AuthZed: Timeline of MCP Breaches](https://authzed.com/blog/timeline-mcp-breaches) — chronological MCP security incident database
- [Vulnerable MCP Project](https://vulnerablemcp.info/) — comprehensive MCP security vulnerability database
- [MCP Security 2026: 30 CVEs in 60 Days](https://www.heyuan110.com/posts/ai/2026-03-10-mcp-security-2026/) — CVE analysis for MCP ecosystem
- [GitGuardian: Breaking MCP Server Hosting](https://blog.gitguardian.com/breaking-mcp-server-hosting/) — path traversal to supply chain compromise analysis
- [Incredibuild: Why SBOMs for MCP Servers Matter Now](https://www.incredibuild.com/blog/the-hidden-supply-chain-in-your-ai-agent-why-sboms-for-mcp-servers-matter-now) — SBOM generation for MCP dependency chains using Syft, Trivy, and BuildGuard
- [Amazon Q Developer: MCP Governance](https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/mcp-governance.html) — registry-based allowlisting with automatic enforcement for AWS environments
- [Kiro: Enterprise MCP & Model Governance](https://kiro.dev/blog/enterprise-governance-mcp-and-models/) — enterprise MCP registry controls with deterministic policy enforcement
- [GitHub MCP Allowlist Enforcement](https://docs.github.com/en/copilot/reference/mcp-allowlist-enforcement) — technical reference for Copilot MCP allowlist matching rules
- [Aembit: Ultimate Guide to MCP Security Vulnerabilities](https://aembit.io/blog/the-ultimate-guide-to-mcp-security-vulnerabilities/) — comprehensive 2026 vulnerability taxonomy
- [Docker: MCP Horror Stories — The Supply Chain Attack](https://www.docker.com/blog/mcp-horror-stories-the-supply-chain-attack/) — detailed supply chain attack walkthrough
- [Credence: Pre-Install MCP Trust Verification](https://credence.securingthesingularity.com/) — adversarial AI deliberation, static analysis, and cryptographic attestation for MCP servers
- [Snyk agent-scan](https://github.com/snyk/agent-scan) — security scanner for AI agents and MCP servers (prompt injection, tool poisoning, tool shadowing, toxic flow)
- [Qualys TotalAI: MCP Shadow IT Detection](https://blog.qualys.com/product-tech/2026/03/19/mcp-servers-shadow-it-ai-qualys-totalai-2026) — network/host/supply-chain discovery of MCP servers
- [Unit 42: MCP Sampling Attack Vectors](https://unit42.paloaltonetworks.com/model-context-protocol-attack-vectors/) — novel prompt injection through MCP sampling feature
- [NIST AI Agent Standards Initiative](https://www.nist.gov/news-events/news/2026/02/announcing-ai-agent-standards-initiative-interoperable-and-secure) — federal initiative on AI agent security, identity, and interoperability (February 2026)
- [MCP Tool Shadowing Attack Matrix](https://modelcontextprotocol-security.io/ttps/tool-poisoning/tool-shadowing/) — tool namespace collision attack patterns and mitigations
- [SlowMist MCP Security Checklist](https://github.com/slowmist/MCP-Security-Checklist) — comprehensive security checklist for MCP-based AI tools
- [ToolHive: Secure MCP Server Platform](https://toolhive.dev/) — container-based MCP server deployment with Sigstore/GitHub Attestation verification (Stacklok)
- [Salesforce Agentforce MCP Governance](https://www.startuphub.ai/ai-news/ai-research/2026/securing-the-model-context-protocol-agentforce-adds-enterprise-governance/) — trusted gateway with mandatory allowlisting and tool vetting
- [Microsoft MCP Security & Governance](https://www.microsoft.com/insidetrack/blog/protecting-ai-conversations-at-microsoft-with-model-context-protocol-security-and-governance/) — secure-by-default architecture with policy-as-code allowlists
- [Secure Tool Manifest & Digital Signing for MCP](https://arxiv.org/html/2601.23132v1) — ECDSA-signed manifests with Merkle-tree transparency logs (academic)
- [MCP Tool Registration Workflow](https://blog.christianposta.com/prevent-mcp-tool-poisoning-attacks-with-a-registration-workflow/) — five-step registration pattern for enterprise MCP governance
- [Practical DevSecOps: MCP Security Vulnerabilities](https://www.practical-devsecops.com/mcp-security-vulnerabilities/) — prompt injection and tool poisoning prevention guide
- [MCP Specification 2025-11-25 Changelog](https://modelcontextprotocol.io/specification/2025-11-25/changelog) — formal security-related spec changes including OIDC Discovery, incremental scope consent, and governance
- [MCP 2026 Roadmap](http://blog.modelcontextprotocol.io/posts/2026-mcp-roadmap/) — roadmap including DPoP (SEP-1932) and Workload Identity Federation (SEP-1933) proposals
- [CoSAI: Practical Guide to MCP Security](https://www.coalitionforsecureai.org/securing-the-ai-agent-revolution-a-practical-guide-to-mcp-security/) — Coalition for Secure AI practical implementation guide
- [Proofpoint Agent Integrity Framework](https://www.helpnetsecurity.com/2026/03/17/proofpoint-ai-security/) — five-phase maturity model for AI agent integrity verification
- [MintMCP Gateway](https://www.mintmcp.com/) — first SOC 2 Type II certified MCP platform with RBAC and compliance audit logging
- [Lasso Security: MCP Server Reputation Scoring](https://www.lassosecurity.com/) — real-time reputation scoring and automatic blocking for MCP servers
- [Stack Overflow: MCP Authentication & Authorization](https://stackoverflow.blog/2026/01/21/is-that-allowed-authentication-and-authorization-in-model-context-protocol/) — enterprise MCP auth requirements analysis
- [Red Hat: MCP Security Risks and Controls](https://www.redhat.com/en/blog/model-context-protocol-mcp-understanding-security-risks-and-controls) — comprehensive MCP security risk analysis
- [MCPShield](https://github.com/mcpshield/mcpshield) — supply chain security scanner for MCP servers with SHA-512 lockfile verification, typosquatting detection, and policy enforcement
- [MCPTrust](https://github.com/mcptrust/mcptrust) — deny-by-default runtime security proxy with lockfile enforcement, drift detection, Ed25519/Sigstore signing, CEL policy, and OpenTelemetry tracing
- [JFrog Universal MCP Registry](https://jfrog.com/press-room/jfrog-unveils-universal-mcp-registry-for-ai-software-supply-chain/) — enterprise MCP server governance as part of JFrog AI Catalog (March 2026)
- [MACH Alliance MCP Registry](https://machalliance.org/mach-alliance-mcp-registry) — vendor-neutral MCP server directory with standardized metadata and governance policies
- [LayerX: Claude DXT Zero-Click RCE](https://layerxsecurity.com/blog/claude-desktop-extensions-rce/) — unsandboxed extension chaining vulnerability analysis (CVSS 10.0)
- [MCP Registry Setup & Allowlist Enforcement](https://dxrf.com/blog/2026/02/26/mcp-registry-setup-and-enforcement/) — practical guide to IDE-level MCP policy enforcement across VS Code, JetBrains, and Visual Studio
- [Security Boulevard: MCP Credential Security Best Practices](https://securityboulevard.com/2026/03/best-practices-for-securing-credentials-in-mcp-servers/) — dual-credential rotation, SIEM integration, and post-breach protocols for MCP
- [Cisco MCP Scanner](https://github.com/cisco-ai-defense/mcp-scanner) — open-source three-engine MCP vulnerability scanner (YARA, LLM-as-judge, Cisco AI Defense)
- [ToolTrust by AgentSafe](https://glama.ai/mcp/servers/AgentSafe-AI/tooltrust-scanner) — MCP server security scanner with CI/CD integration and TeamPCP exploit detection
- [mcpscan.ai](https://mcpscan.ai/) — MCP security scanning platform with LLM-based tool poisoning classification
- [SANS: TeamPCP Supply Chain Campaign Analysis](https://www.sans.org/blog/when-security-scanner-became-weapon-inside-teampcp-supply-chain-campaign) — comprehensive timeline of the March 2026 multi-ecosystem supply chain attack
- [Trend Micro: Inside the LiteLLM Supply Chain Compromise](https://www.trendmicro.com/en_us/research/26/c/inside-litellm-supply-chain-compromise.html) — technical analysis of LiteLLM backdoor payload
- [Endor Labs: TeamPCP Hits LiteLLM](https://www.endorlabs.com/learn/teampcp-isnt-done) — analysis of cascading supply chain pivot from CI/CD to production
- [Arctic Wolf: TeamPCP Campaign Advisory](https://arcticwolf.com/resources/blog/teampcp-supply-chain-attack-campaign-targets-trivy-checkmarx-kics-and-litellm-potential-downstream-impact-to-additional-projects/) — enterprise impact assessment and downstream dependency analysis
- [AgentAudit: MCP Server Security Study](https://dev.to/ecap0/the-state-of-mcp-server-security-in-2026-118-findings-across-68-packages-4fkd) — 194-package audit with 118 security findings across severity tiers
- [AgentSeal: We Scanned 1,808 MCP Servers](https://agentseal.org/blog/mcp-server-security-findings) — April 2026 ecosystem-wide scan; 66% had findings (43% command injection, 13% auth bypass)
- [AgentSeal awesome-mcp-security](https://github.com/AgentSeal/awesome-mcp-security) — daily-updated security scores for 800+ MCP servers using nine analyzers
- [Ox Security: Systemic MCP STDIO Flaw](https://www.infosecurity-magazine.com/news/systemic-flaw-mcp-expose-150/) — April 15, 2026 disclosure of architectural vulnerability across Python, TypeScript, Java, and Rust SDKs
- [Huntress: Axios npm Supply Chain Compromise](https://www.huntress.com/blog/supply-chain-compromise-axios-npm-package) — technical analysis of the WAVESHAPER.V2 RAT payload
- [Wiz: Axios NPM Distribution Compromised](https://www.wiz.io/blog/axios-npm-compromised-in-supply-chain-attack) — supply chain attack analysis and incident timeline
- [Microsoft: Mitigating the Axios npm Supply Chain Compromise](https://www.microsoft.com/en-us/security/blog/2026/04/01/mitigating-the-axios-npm-supply-chain-compromise/) — vendor guidance on detection and response
- [Google Threat Intelligence: Sapphire Sleet / Axios Attribution](https://cloud.google.com/blog/topics/threat-intelligence/north-korea-threat-actor-targets-axios-npm-package) — North Korea–nexus threat actor attribution for the Axios campaign
- [CVE-2026-32211: Azure MCP Server Information Disclosure](https://cvefeed.io/vuln/detail/CVE-2026-32211) — CVSS 9.1 missing authentication flaw in `@azure-devops/mcp`
- [ToolHive April 2026 Updates](https://docs.stacklok.com/toolhive/updates/2026/04/13/updates) — claim-based authorization, per-user/per-tool rate limiting, `/me` endpoint
- [Microsoft MCP Azure Security Guide: Supply Chain Attacks & Dependency Tampering](https://deepwiki.com/microsoft/mcp-azure-security-guide/4.2.2-mcp04:-supply-chain-attacks-and-dependency-tampering) — MCP04 threat category analysis (OWASP-aligned)
- [New MCP Authorization Specification: OAuth 2.1 & Resource Indicators](https://dasroot.net/posts/2026/04/mcp-authorization-specification-oauth-2-1-resource-indicators/) — April 2026 breakdown of the authorization spec evolution
- [LiteLLM CVE-2026-30623 Security Update](https://docs.litellm.ai/blog/mcp-stdio-command-injection-april-2026) — STDIO command-injection patch with `MCP_STDIO_ALLOWED_COMMANDS` allowlist (April 21, 2026)
- [LiteLLM PR #25343: Block Arbitrary Command Execution via STDIO Transport](https://github.com/BerriAI/litellm/pull/25343) — reference implementation of the launcher allowlist pattern
- [OpenAI Codex Issue #15814: SHA-256 MCP Allowlist Proposal](https://github.com/openai/codex/issues/15814) — pre-execution binary hash verification for `requirements.toml` allowlist entries
- [Socket: Lightning PyPI Supply Chain Compromise](https://socket.dev/blog/lightning-pypi-package-compromised) — on-import execution of obfuscated Bun-runtime credential stealer (April 30, 2026)
- [Socket: Namastex.ai npm Packages Hit with CanisterWorm](https://socket.dev/blog/namastex-npm-packages-compromised-canisterworm) — `@automagik/genie` and `pgserve` AI-orchestration package compromise (April 22, 2026)
- [StepSecurity: pgserve npm Compromise (CanisterSprawl)](https://www.stepsecurity.io/blog/pgserve-compromised-on-npm-malicious-versions-harvest-credentials) — self-propagating npm worm with ICP-canister C2
- [GitGuardian: Three Supply Chain Campaigns in 48 Hours](https://blog.gitguardian.com/three-supply-chain-campaigns-hit-npm-pypi-and-docker-hub-in-48-hours/) — pgserve, xinference, and Bitwarden CLI cascade timeline (April 21–22, 2026)
- [SANS / IBVL: TeamPCP Update 008](https://blog.ibvl.in/index.php/2026/04/27/teampcp-supply-chain-campaign-update-008-26-day-pause-ends-with-three-concurrent-compromises-checkmarx-kics-bitwarden-cli-cascade-xinference-pypi-canistersprawl-npm-worm-identified-and-tier-1/) — concurrent compromises of Checkmarx KICS, Bitwarden CLI, xinference, and CanisterSprawl npm worm
- [The Hacker News: Anthropic MCP Design Vulnerability Enables RCE](https://thehackernews.com/2026/04/anthropic-mcp-design-vulnerability.html) — coverage of the systemic STDIO architectural flaw and downstream impact
- [The Register: MCP Design Flaw Puts 200k Servers at Risk](https://www.theregister.com/2026/04/16/anthropic_mcp_design_flaw/) — independent corroboration of Ox Security's STDIO disclosure
- [Cloud Security Alliance: MCP by Design — RCE Across the AI Agent Ecosystem](https://labs.cloudsecurityalliance.org/research/csa-research-note-mcp-by-design-rce-ox-security-20260420-csa/) — CSA research note on the architectural RCE class
- [Aikido: Mini Shai-Hulud Has Appeared](https://www.aikido.dev/blog/mini-shai-hulud-has-appeared) — Bun-based credential stealer hitting SAP CAP packages
- [Sophos: Mini Shai-Hulud Supply Chain Attack Targets SAP npm Packages](https://www.sophos.com/en-us/blog/-mini-shai-hulud-supply-chain-attack-targets-sap-npm-packages) — payload analysis and `setup.mjs` Bun loader pattern
- [Wiz: Mini Shai-Hulud Supply Chain on SAP npm Packages](https://www.wiz.io/blog/mini-shai-hulud-supply-chain-sap-npm) — package-by-package compromise inventory and intercom-client v7.0.5 attribution
- [Phoenix Security: Mini Shai-Hulud SAP CAP and mbt npm Supply Chain](https://phoenix.security/mini-shai-hulud-sap-cap-mbt-npm-supply-chain-bun-credential-stealer/) — SessionStart hook persistence, `runOn: folderOpen` task injection, and dependabout-branch secret-dump workflow
- [StepSecurity: A Mini Shai-Hulud Has Appeared](https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared) — obfuscated Bun-runtime payload technical analysis
- [Onapsis: SAP CAP Mini Shai-Hulud Supply Chain Attack](https://onapsis.com/blog/sap-cap-mini-shai-hulud-supply-chain-attack/) — SAP-ecosystem-specific impact assessment
- [SecurityWeek: 1,800 Hit in Mini Shai-Hulud Attack on SAP, Lightning, Intercom](https://www.securityweek.com/1800-hit-in-mini-shai-hulud-attack-on-sap-lightning-intercom/amp/) — victim count, exfiltration repository telemetry
- [Upwind: intercom-client 7.0.4 Supply Chain Attack](https://www.upwind.io/feed/intercom-client-7-0-4-supply-chain-attack) — npm package compromise and CI/CD OIDC-token hijack
- [The Hacker News: PyTorch Lightning and Intercom-client Hit in Supply Chain Attacks](https://thehackernews.com/2026/04/pytorch-lightning-compromised-in-pypi.html) — Lightning→Intercom transitive cascade and 10M combined monthly downloads
- [Microsoft vscode issue #309406: tasks.json folderOpen Attack Surface](https://github.com/microsoft/vscode/issues/309406) — pre-disclosed editor-level vector exploited 16 days later by Mini Shai-Hulud
- [MCP Manager](https://mcpmanager.ai/) — gateway with RBAC, SSO, SCIM, and end-to-end MCP traffic logging
- [Golf.dev](https://golf.dev/) — master-server MCP deployment platform with authentication, prompt-injection mitigation, and MCP-specific rate limiting
- [Obot MCP Gateway](https://obot.ai/) — enterprise MCP gateway with project sharing, power-user roles, and governance upgrades
- [TrueFoundry MCP Gateway](https://www.truefoundry.com/mcp-gateway) — federated login (Okta, Azure AD), RBAC, and OAuth 2.0 token brokering

---

## Open Research Questions

- [ ] Should the MCP ecosystem adopt a standardized package-signing mechanism? MCPS proposes ECDSA P-256 "Agent Passports" with five trust levels, but adoption is early-stage — is this the right foundation, or should the MCP spec itself incorporate signing requirements?
- [ ] How should MCP configuration files handle secret references in a cross-platform, cross-SDK way? Only ~8.5% of MCP implementations currently use OAuth 2.1 — what would it take to make secretless access the default?
- [ ] Is there a viable model for an MCP server registry with enforced integrity metadata? GitHub, AWS Q Developer, and Kiro now offer registry-based enforcement, but all rely on name-based matching rather than cryptographic attestation — can the ecosystem converge on a standard that ties server identifiers to verified binaries?
- [ ] How do you verify integrity of MCP servers that are dynamically discovered at runtime rather than pre-configured? MCPS's signed tool definitions are one approach, but runtime discovery introduces trust bootstrapping challenges.
- [ ] Can MCPTox-style automated benchmarking be integrated into CI/CD pipelines to detect tool poisoning before deployment?
- [ ] The MCP gateway debate: centralized gateways offer unified enforcement but create single points of failure. Should per-server enforcement (signed envelopes, per-connection authentication) replace or complement gateway architectures?
- [ ] How should organizations handle the "contextual secret leakage" problem, where credentials persist in agent context memory across sessions?
- [ ] Should AI-BOM (AI Bill of Materials) standards extend to MCP server dependencies? Traditional SBOMs miss model versions, training data lineage, and agentic dependency chains — is CycloneDX/SPDX sufficient, or does the MCP ecosystem need purpose-built transparency formats?
- [ ] How should organizations detect and govern "shadow MCP" deployments where developers connect servers to IDEs without IT oversight, bypassing security monitoring? Qualys TotalAI's three-layer detection (network/host/supply-chain) is one approach — are there lighter-weight alternatives?
- [ ] MCP sampling creates a bidirectional control reversal where servers can request LLM completions — should the protocol require explicit user consent for sampling requests, or can capability declarations and prompt sandboxing suffice?
- [ ] Tool shadowing via namespace collisions (demonstrated by CVE-2026-30856) is a gap in the MCP spec. Should tool identifiers be globally unique (UUID-based) by default, or is registry-backed namespace isolation sufficient?
- [ ] NIST's AI Agent Standards Initiative (February 2026) is collecting input on agent security and identity — should the MCP community align with NIST's forthcoming framework for AI agent identity and authorization, or develop MCP-specific standards independently?
- [ ] Enterprise platforms (GitHub Copilot, AWS Q, Kiro, Agentforce, Microsoft) each implement MCP governance differently. Can the ecosystem converge on a standard registration and verification workflow — identity verification, automated scanning, human review, cryptographic signing — or will fragmentation persist?
- [ ] ToolHive demonstrates container-based provenance verification using Sigstore, but most MCP servers are distributed as npm/PyPI packages rather than container images. How should provenance verification extend to non-containerized MCP components?
- [ ] The MCP gateway market is fragmenting (MintMCP, Kong, Bifrost, Lunar.dev, Docker, Lasso) with different enforcement models. Should the MCP spec define a standard gateway API or enforcement interface, or will market consolidation handle this?
- [ ] Lockfile-based integrity verification (MCPShield's SHA-512 `mcp.lock.json`, MCPTrust's capability-pinned lockfiles) provides deterministic snapshots enforceable in CI — but lockfiles verify capability contracts, not binary integrity. Can lockfile verification be composed with cryptographic attestation to close the gap, or are these fundamentally different trust layers?
- [ ] The Claude DXT architecture runs extensions unsandboxed with full system privileges, and Anthropic's position that this "falls outside our current threat model" leaves users exposed. Should MCP hosts be required to sandbox MCP server extensions, or is this an acceptable tradeoff for local development tooling? How should this be reflected in verification standards?
- [ ] DPoP (SEP-1932) and Workload Identity Federation (SEP-1933) are under review for the MCP spec. If ratified, these could eliminate static client secrets and bind tokens to specific client instances — but adoption requires coordinated changes across all MCP SDKs. What is the realistic timeline for this?
- [ ] The Proofpoint Agent Integrity Framework proposes a five-phase maturity model for agent verification. Should MCP server registries require attestation of a minimum maturity level before listing, similar to how app stores require security reviews?
- [ ] The TeamPCP campaign demonstrated that transitive dependencies (e.g., LiteLLM as an AI gateway library) can propagate supply chain compromises silently into MCP servers and agent frameworks. Should MCP integrity verification require recursive dependency scanning, and how deep should the scan go? Current SBOM tooling captures direct dependencies but often misses build-time and runtime transitive chains.
- [ ] The MCP security scanning landscape now includes at least five major tools (Cisco MCP Scanner, ToolTrust, MCPShield, mcpscan.ai, Snyk agent-scan) with overlapping but non-identical coverage. Should the ecosystem converge on a common vulnerability taxonomy and reporting format for MCP-specific findings, analogous to CWE for traditional software?
- [ ] The Ox Security April 2026 STDIO disclosure demonstrates that vulnerabilities baked into the official MCP SDKs propagate to every downstream server built on them. Should allowlisting and component-integrity verification require SDK version pinning as a first-class check alongside server name and version — and should MCP registries track SDK-level CVE exposure per listed server, analogous to how container registries track base-image CVEs?
- [ ] When a vendor declines to patch a disclosed architectural vulnerability (as Anthropic did for both Claude DXT and the STDIO STDIO-execution flaw), what is the correct posture for a verification standard? Should AISVS treat vendor-affirmed architectural risks as implementer-level requirements (e.g., mandate sandboxing at the host layer), accept them as residual risk disclosed in threat models, or recommend replacement of the affected component entirely?
- [ ] The Axios compromise affected transitive dependencies that most MCP server authors never explicitly declared. Traditional SBOM and provenance checks capture direct dependencies well, but transitive chains — particularly for autonomous agent workflows that run `npm install` without human review — remain a blind spot. Is pre-install provenance gating (SLSA Level 3+ verification at install time, not scan time) a realistic requirement for MCP fleets, or is the tooling not yet mature enough to enforce it without breaking developer workflows?
- [ ] The LiteLLM CVE-2026-30623 patch demonstrates that downstream hosts can credibly mitigate the Anthropic STDIO flaw through a hardcoded launcher allowlist (`MCP_STDIO_ALLOWED_COMMANDS`). Should AISVS treat this pattern as a normative requirement for STDIO-launching MCP hosts, or only as guidance until the MCP spec catches up? And if normative, what is the right minimum allowlist — should it be defined per-language-runtime (Node, Python, container runtime) or per-deployment-context (developer machine, CI, production)?
- [ ] The OpenAI Codex SHA-256 allowlist proposal (issue #15814) extends `requirements.toml` from command-name matching to verified binary hash pinning. If this becomes a de-facto standard across IDEs and agent runtimes, how should hash rotation be managed when servers update? Lockfile-style locked-version-with-hash, signed update channels, or operator-mediated rotation? Each has different ergonomics for autonomous agent workflows where updates may happen without human review.
- [ ] The PyTorch Lightning April 2026 compromise executed its payload **on import**, not via postinstall — bypassing the `--ignore-scripts` mitigation that has been the default defensive posture for npm. Should MCP component integrity verification require evidence that a Python package's import path performs no network or filesystem side effects, and how should this be tested at scale across PyPI's hundreds of thousands of packages? Existing static analysis tools struggle with dynamic import-time behavior, particularly when payloads are obfuscated or fetched at runtime.
- [ ] The Mini Shai-Hulud campaign (April 29-30, 2026) collapsed the historically separate categories of "supply-chain compromise" and "MCP configuration tampering" into one attack chain by writing `.claude/settings.json` `SessionStart` hooks and `.vscode/tasks.json` `folderOpen` tasks that re-execute attacker code on every developer session. Should AISVS C10.1 require file-integrity monitoring on the agent's configuration directory (`~/.claude/`, `~/.kiro/`, `~/.cursor/`, project-local `.vscode/`, `.claude/`) as a normative control alongside package-level integrity verification, or treat it as part of C13 monitoring? And should IDE vendors be expected to require explicit user approval before honoring newly-introduced `folderOpen` or `SessionStart` hooks that arrive via `git pull` rather than user action?

---

## Related Pages

- [C06-05 AI-BOM & Model Artifacts](C06-05-AI-BOM-Model-Artifacts) — the AI-BOM analogue at the model layer; CycloneDX ML-BOM and SPDX AI profiles complement MCP server SBOMs and share the same Sigstore/signing tooling.
- [C03-01 Model Authorization & Integrity](C03-01-Model-Authorization-Integrity) — model-registry signing, deployment-time signature verification, and provenance lineage for the model side of the same trust boundary MCP servers operate behind.
- [C01-05 Data Lineage & Traceability](C01-05-Data-Lineage-Traceability) — dataset-side provenance and signed lineage records using OpenLineage, NVIDIA OMS, and C2PA — the data analogue of the tool-attestation work in 10.1.3.
- [C01-01 Training-Data Origin Traceability](C01-01-Training-Data-Origin-Traceability) — supply-chain hygiene for training data inventory and source provenance, sharing many of the same compliance drivers (EU AI Act, AB 2013) that motivate MCP allowlisting.
- [Appendix C: AI Secure Coding](Appendix-C-AI-Secure-Coding) — broader SSDLC, tool qualification, IaC/pipeline artifacts, and autonomous-agent change-control practices that frame how MCP components are introduced and approved.
