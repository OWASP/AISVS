# C3.2: Model Validation & Testing

> [Back to C03 Index](C03-Model-Lifecycle-Management.md)
> **Last Researched:** 2026-06-09 (refreshed)

## Purpose

Models must pass defined security and safety validations before deployment. This section covers pre-deployment testing gates, safety thresholds, audit logging of model changes, versioned evaluation harnesses, and the re-evaluation requirements for models that undergo post-training compression (quantization, pruning, distillation).

Pre-deployment testing is the primary defense against deploying models vulnerable to prompt injection, jailbreaks, or models that produce unsafe outputs. Despite this, Cleanlab's 2025 production survey found only 5% of AI agents in production have mature monitoring -- underscoring the critical need for automated testing gates before models ever reach production. Post-training compression techniques, increasingly common for edge deployment and cost reduction, can silently degrade safety alignment and adversarial robustness, requiring distinct re-evaluation of compressed artifacts. As of May 2026, the tooling ecosystem has matured considerably: Promptfoo's current red-team docs describe 50+ vulnerability categories, CI/CD quality gates, and MCP-specific tests; Giskard Hub exposes scan tags aligned to the OWASP LLM Top 10 (2025); MLCommons AILuminate provides safety and jailbreak benchmarks across 12 hazard categories; and the OpenSSF Model Signing project provides a concrete artifact-integrity pattern for binding validation evidence to the model actually deployed. The EU AI Act's conformity assessment provisions now make documented validation evidence a regulatory concern for high-risk systems; on May 7, 2026, the European Commission announced a Digital Omnibus political agreement moving high-risk AI rules for specified areas to 2 December 2027 and product-integrated high-risk systems to 2 August 2028, while Article 43 still requires a new conformity assessment after substantial modification.

The Continuous Automated Red Teaming (CART) approach has gained traction in 2026, with platforms like Mindgard (recognized in the OWASP LLM Security Solutions Landscape Guide and winner of the 2025 Cybersecurity Excellence Award) integrating directly into CI/CD pipelines via GitHub Actions or CLI, running adversarial tests on every commit. Open-source alternatives have also matured -- Garak (prompt injection, data leakage, adversarial prompt testing), PyRIT (Microsoft's Python Risk Identification Tool), and Promptfoo all support pipeline integration. Promptfoo's docs, last updated May 2, 2026, describe local scans against HTTP APIs, browser targets, direct model access, RAG flows, and agent workflows; its MCP security testing guide adds targeted checks for tool poisoning, side-channel data exfiltration, authentication hijacking, tool shadowing, indirect prompt injection, and cross-server agent attacks. On [March 9, 2026, OpenAI announced its acquisition of Promptfoo](https://openai.com/index/openai-to-acquire-promptfoo/), integrating its red-teaming capabilities directly into OpenAI Frontier while committing to keep the MIT-licensed CLI open source -- a notable signal that automated pre-deployment testing is now table stakes for frontier model vendors. As of May 5, 2026, [NIST's CAISI announced expanded agreements](https://www.nist.gov/news-events/news/2026/05/caisi-signs-agreements-regarding-frontier-ai-national-security-testing) with Google DeepMind, Microsoft, and xAI for pre-deployment frontier model evaluations, reinforcing the expectation that high-impact model releases should be tested before public availability. The SecureAI-Flow framework (preprint, 2025) introduces a multi-agent CI/CD architecture where specialized agents handle static code analysis, adversarial robustness checks, container signing, and behavioral anomaly detection -- treating security policies and defense heuristics as version-controlled code under DevSecOps principles. IBM's 2025 Cost of a Data Breach Report found 63% of breached organizations lacked AI governance policies at the time of their breach, and shadow AI usage (unauthorized tools deployed by ~20% of employees) cost organizations an average of $670,000 more per breach than other incidents. As of April 7, 2026, NIST released a concept note for an AI RMF Profile on Trustworthy AI in Critical Infrastructure, extending model validation requirements to AI-enabled capabilities in IT, OT, and Industrial Control Systems -- the Measure function specifically mandates pre-deployment testing and formalized reporting for trustworthiness and reliability. Singapore's IMDA is publishing Version 1.0 of its Starter Kit for Testing LLM-Based Applications for Safety and Reliability in 2026, providing government-backed reference test plans aimed specifically at the pre-deployment testing phase -- useful for teams that want an external benchmark for their internal gates.

## Current Research Notes

As of May 11, 2026, independent evaluation has become a concrete release-gate pattern for frontier systems. NIST CAISI's May 5, 2026 agreements with Google DeepMind, Microsoft, and xAI explicitly support pre-deployment evaluations, post-deployment assessment, and targeted research, and NIST says CAISI has completed more than 40 evaluations, including on unreleased models. For 3.2.3 and 3.2.4, auditors should ask for retained eval suites, model identifiers, threshold configs, raw failure evidence, and sign-off records rather than only a model card summary: https://www.nist.gov/news-events/news/2026/05/caisi-signs-agreements-regarding-frontier-ai-national-security-testing

Agent and MCP validation should be exercised with real tool calls, not just static prompt lists. Promptfoo's MCP security testing guide, updated May 8, 2026, calls out tool poisoning, side-channel exfiltration, authentication hijacking, rug pulls, tool shadowing, indirect prompt injection, and cross-server attacks; its examples test a trusted client, a multi-server environment with a malicious server, direct MCP protocol calls, and CI/CD fail conditions. Promptfoo's MCP plugin now breaks MCP coverage into function discovery, parameter injection, function-call manipulation, recursive tool use, system-information leakage, tool-output manipulation, tool-metadata injection, and unauthorized tool invocation. Giskard Hub exposes OWASP LLM Top 10-aligned scan tags with A-D or N/A grades, while Garak remains useful for broad prompt injection, leakage, hallucination, toxicity, and jailbreak probes: https://www.promptfoo.dev/docs/red-team/mcp-security-testing/ and https://www.promptfoo.dev/docs/red-team/plugins/mcp/ and https://docs.giskard.ai/hub/sdk/guides/scans and https://github.com/NVIDIA/garak

Recent agent advisories reinforce why the evaluation harness in 3.2.5 has to include repository, file-system, and configuration side effects. The October 2025 Kilo Code advisory documented CVE-2025-11445, where prompt injection could rewrite VS Code extension settings to allow git commands and then poison an upstream repository. INCIBE's entry for CVE-2025-59536 documents Claude Code versions before 1.0.111 executing project code before the trust dialog completed. These are not only model-behavior failures; they are release-blocking workflow failures when an assistant can write files, change allowlists, run shell commands, or push code: https://mcpsec.dev/advisories/2025-10-02-kilo-code-ai-agent-supply-chain-attack/ and https://www.incibe.es/en/incibe-cert/early-warning/vulnerabilities/cve-2025-59536

April 2026 NVD records show the same pattern across MCP servers and agent platforms. CVE-2026-39884 in mcp-server-kubernetes allowed prompt-injection-assisted argument injection in a `kubectl port_forward` wrapper; CVE-2026-30624 in Agent Zero accepted external MCP server JSON that could execute attacker-controlled commands; and CVE-2026-27826 in MCP Atlassian exposed SSRF through HTTP middleware rather than a tool handler, making it invisible to tool-level analysis alone. For 3.2.5, the harness should include malicious MCP manifests, command/argument injection attempts, SSRF probes against metadata endpoints, and tests that inspect the adapter and transport layer around each tool: https://nvd.nist.gov/vuln/detail/CVE-2026-39884 and https://nvd.nist.gov/vuln/detail/CVE-2026-30624 and https://nvd.nist.gov/vuln/detail/CVE-2026-27826

AI workflow builders and evaluation harness infrastructure now need the same treatment as the model under test. CISA added Langflow CVE-2026-33017 to the Known Exploited Vulnerabilities catalog on March 25, 2026, after exploitation of an unauthenticated public-flow code-injection path; Cloud Security Alliance's March 30, 2026 analysis says attackers were observed within roughly 20 hours of disclosure and were harvesting environment files, databases, and AI provider credentials. Flowise CVE-2025-59528 shows the same pattern around MCP configuration: the GitHub advisory rates the CustomMCP issue as CVSS 10.0 because user-supplied configuration reached JavaScript execution with Node.js privileges, and BleepingComputer reported active exploitation on April 7, 2026 with 12,000-15,000 internet-exposed Flowise instances. For 3.2.4 and 3.2.5, a passing model eval is not enough if the agent builder, workflow canvas, or red-team harness can be exploited before the gate runs: https://nvd.nist.gov/vuln/detail/CVE-2026-33017 and https://labs.cloudsecurityalliance.org/research/csa-research-note-langflow-cve-2026-33017-rce-20260330-csa-s/ and https://github.com/advisories/GHSA-3gcm-f6qx-ff7p and https://www.bleepingcomputer.com/news/security/max-severity-flowise-rce-vulnerability-now-exploited-in-attacks/amp/

As of May 13, 2026, the MCP attack surface has enough repeated cases that validation harnesses should treat command execution, not only model refusal behavior, as a release-blocking category. OX Security's April 15, 2026 advisory describes four MCP STDIO command-injection families across AI workflow builders, IDE assistants, and hidden transport-type switches, including unauthenticated configuration paths, hardening bypasses through allowed commands such as `npm`/`npx`, prompt-injected local MCP JSON changes, and network requests that re-enable hidden STDIO configuration. NVD records add concrete regression cases: GitHub Copilot CLI CVE-2026-29783 let prompt-influenced shell text use bash parameter expansion to bypass a "read-only" safety classifier; Windsurf CVE-2026-30615 let attacker-controlled HTML prompt injection alter local MCP configuration and register a malicious STDIO server; and aws-mcp-server CVE-2026-5059 received a CNA CVSS 9.8 score for command injection through the allowed-commands list. For 3.2.5, the harness should replay malicious repository files, hostile MCP server responses, crafted MCP JSON, command allowlist bypasses, and shell-expansion payloads before approving any agent release: https://www.ox.security/blog/mcp-supply-chain-advisory-rce-vulnerabilities-across-the-ai-ecosystem/ and https://nvd.nist.gov/vuln/detail/CVE-2026-29783 and https://nvd.nist.gov/vuln/detail/CVE-2026-30615 and https://nvd.nist.gov/vuln/detail/CVE-2026-5059

As of May 13, 2026, NVD records add another validation-harness regression target: CVE-2026-30617 in LangChain-ChatChat 0.3.1 allowed a remote attacker to configure an MCP STDIO server with attacker-controlled commands through a public management interface, with agent activity later triggering command execution. That case belongs in pre-release tests as a platform-level control, not only an agent prompt-safety control: evaluators should attempt unauthenticated MCP management access, malicious server registration, command/argument injection, and checks that deployment blocks when an eval harness or workflow builder exposes command execution. The same evidence packet should include patch status for harness dependencies and an explicit failed-gate sample: https://nvd.nist.gov/vuln/detail/CVE-2026-30617

Model loaders themselves are now a release-gate concern, not only the model artifact. vLLM CVE-2026-22807 (fixed in v0.14.0) loaded Hugging Face `auto_map` dynamic modules during model resolution without gating on `--trust-remote-code`, so an attacker who controlled a model repo or path could execute arbitrary Python on the inference host at server startup -- before any request handling and before any safety eval ran. CVE-2026-27893 (fixed in v0.18.0) is the third trust-bypass in vLLM (after CVE-2025-66448 and CVE-2026-22807): two model implementation files for NemotronVL and Kimi-K25 hardcoded `trust_remote_code=True` and silently overrode operators who had explicitly set `--trust-remote-code=False`. CVE-2026-27893 carries a CNA CVSS of 8.8. For 3.2.5 and 3.2.6, harnesses should attempt to load a hostile model repo against the deployed serving stack with remote-code trust disabled and confirm both the loader version and the patch level are recorded alongside the model digest; for 3.2.7, the same regression check belongs in the compressed-artifact gate so a quantized or distilled model swapped in for size or cost reasons does not silently re-introduce a loader bypass: https://github.com/vllm-project/vllm/security/advisories/GHSA-2pc9-4j83-qjmr and https://github.com/vllm-project/vllm/security/advisories/GHSA-7972-pg2x-xr59 and https://www.esecurityplanet.com/artificial-intelligence/critical-vllm-flaw-puts-ai-systems-at-risk-of-remote-code-execution/

The MCP STDIO RCE pattern keeps producing concrete cases that need to be in the regression suite. CVE-2025-49596 in Anthropic's MCP Inspector (CVSS 9.4) chained DNS rebinding with the local Inspector dev tool so a developer who merely visited a malicious site could be compromised -- a release-gate concern for any team where the Inspector or a similar local agent UI runs alongside the eval pipeline. CVE-2026-22252 in LibreChat versions before v0.8.2-rc2 accepted arbitrary commands through the MCP stdio transport, letting an authenticated user execute shell commands as root inside the container in a single API call. CVE-2025-54994 in `@akoskm/create-mcp-server-stdio` (npm, fixed in 0.0.13) used Node.js `exec` with concatenated user input in its `which-app-on-port` tool. AgentSeal's scan of 1,808 publicly accessible MCP servers reported 66% with security findings and ~36.7% SSRF exposure on URL-accepting endpoints; PipeLab and Endor Labs reach similar conclusions, with the recurring theme that "2010-era web vulnerabilities" (SSRF, path traversal, command injection, unsafe deserialization) are now landing inside the AI tool layer. For 3.2.5, that means every MCP harness should include both the Anthropic-disputed STDIO command-field case and a generic webapp regression battery (SSRF to 169.254.169.254 and DNS-rebinding targets, path-traversal on file-name parameters, deserialization on `application/json` and pickle-style transports, and command injection through any tool argument that touches `exec`/`spawn`): https://www.oligo.security/blog/critical-rce-vulnerability-in-anthropic-mcp-inspector-cve-2025-49596 and https://www.sentinelone.com/vulnerability-database/cve-2026-22252/ and https://github.com/advisories/GHSA-3ch2-jxxc-v4xf and https://agentseal.org/blog/mcp-server-security-findings

The LLM application framework layer itself is now a validation target, not only the model. On March 27, 2026, Cyera disclosed three CVEs in widely deployed LLM frameworks: [CVE-2025-68664](https://github.com/advisories/GHSA-r399-636x-v7f6) (LangChain Core "LangGrinch," CVSS 9.3, fixed by escaping `lc` keys during free-form dictionary serialization) used `dumps()`/`dumpd()` against LLM response fields such as `additional_kwargs` and `response_metadata` so prompt-injected output flowed into serialized state that downstream streaming and persistence consumers trusted; [CVE-2026-34070](https://medium.com/@imr1ck/cve-2026-34070-how-i-discovered-a-high-severity-path-traversal-in-langchain-core-38b0828a34d8) (CVSS 7.5, fixed in langchain-core 1.2.22) opened arbitrary files through `open(path)` against unsanitized config-dictionary values in the prompt-loading API; and [CVE-2025-67644](https://thehackernews.com/2026/03/langchain-langgraph-flaws-expose-files.html) (CVSS 7.3) added SQL injection through LangGraph's SQLite checkpoint metadata filter, letting attacker-controlled metadata change checkpoint queries. For 3.2.4 and 3.2.5, that means a release gate has to cover the LLM application framework -- prompt-injection-induced serialization payloads, path-traversal probes against any prompt or template loader, and SQL injection against LangGraph or equivalent checkpoint stores -- not only model behavior and MCP transports.

[CVE-2026-33032](https://thehackernews.com/2026/04/critical-nginx-ui-vulnerability-cve.html) ("MCPwn") added another concrete release-gate failure mode for MCP-exposed infrastructure tools: nginx-ui shipped two paired endpoints, `/mcp` (with `AuthRequired()` plus IP allowlist middleware) and `/mcp_message` (only the IP allowlist, with a default-empty list treated as "allow all"), so any network-adjacent attacker could invoke 12 privileged MCP tools, including `nginx_config_add` with auto-reload, in roughly two unauthenticated HTTP requests and take over the web server end to end. The flaw carries a CVSS v3.1 base score of 9.8 (CWE-306), was fixed in nginx-ui 2.3.4 on March 15, 2026, and was named by Recorded Future's Insikt Group as one of 31 high-impact CVEs actively exploited in March 2026 (Risk Score 94/100), with [Shodan showing ~2,689 publicly reachable instances](https://www.sentinelone.com/vulnerability-database/cve-2026-33032/). For 3.2.5, the harness should treat MCP transport authentication as a release-gate check (not a tool-content check) -- replay unauthenticated tool invocations against every MCP-exposed admin interface, fail closed when a paired endpoint diverges on middleware, and confirm that destructive tools (config writes, reload, restart) require explicit authentication independent of the read path: https://www.darkreading.com/application-security/critical-mcp-integration-flaw-nginx-risk and https://www.infosecurity-magazine.com/news/nginx-ui-mcp-flaw-actively/

Microsoft's [May 7, 2026 disclosure of two Semantic Kernel RCE chains](https://www.microsoft.com/en-us/security/blog/2026/05/07/prompts-become-shells-rce-vulnerabilities-ai-agent-frameworks/) is a clean illustration that the agent framework -- not only the model or the MCP transport -- is now a release-gate concern. [CVE-2026-25592](https://nvd.nist.gov/vuln/detail/CVE-2026-25592) (CVSS 10.0, fixed in Semantic Kernel .NET 1.71.0) was a sandbox escape where `DownloadFileAsync` had been accidentally tagged `[KernelFunction]` and exposed to the LLM with no path validation, letting a prompt-injected agent write arbitrary files into the Azure Container Apps Python sandbox host, including Windows Startup-folder writes for persistence. [CVE-2026-26030](https://nvd.nist.gov/vuln/detail/CVE-2026-26030) (CVSS 9.8, fixed in `semantic-kernel` Python 1.39.4) abused unsafe string interpolation in `InMemoryVectorStore` filter expressions: an AST-based blocklist was bypassed through class-hierarchy traversal so attacker-controlled filter text reached `eval()`. For 3.2.5, that argues for two specific harness checks before any agent release: enumerate every method tagged as a kernel/tool function and confirm none is an internal helper exposed by mistake, and exercise every filter, query, or expression parameter the model can populate with payloads that try to escape an AST allowlist via dunder traversal. The Microsoft blog's framing is worth copying into the evidence packet -- "your LLM is not a security boundary" -- so the release gate enforces deterministic validation independent of the model's refusal behavior.

The model-serving framework layer keeps producing the same pattern. [CVE-2026-3059](https://feedly.com/cve/CVE-2026-3059) and [CVE-2026-3060](https://www.sentinelone.com/vulnerability-database/cve-2026-3060/) in SGLang (CVSS 9.8, affecting 0.5.5-0.5.9, unpatched at disclosure per [CERT/CC VU#665416](https://kb.cert.org/vuls/id/665416)) showed a ZMQ broker binding to `tcp://*` with no authentication and calling `pickle.loads()` on every received payload, so any network-reachable attacker could land unauthenticated RCE against a deployment that enabled multimodal generation (3059) or encoder parallel disaggregation (3060). vLLM's video pipeline added another multimodal regression case: [CVE-2026-22778](https://github.com/vllm-project/vllm/security/advisories/GHSA-4r2x-xpjr-7cvv) (CVSS 9.8, fixed in v0.14.1) chained a PIL error-message heap-address leak with a JPEG2000 cdef-box heap overflow in OpenCV/FFmpeg, so a single malicious video URL submitted to a vLLM multimodal endpoint produced RCE on the inference host. For 3.2.5 and 3.2.7, that means the multimodal eval suite has to include hostile media uploads against the actual serving stack -- not only text prompts against a chat surface -- and the harness should record the OpenCV, FFmpeg, and serving-framework patch level alongside the model digest; compressed or modality-extended artifacts that swap in a different decoder path need their own re-evaluation rather than inheriting the base-model verdict.

The May 6-15, 2026 window added several more concrete MCP and agent-builder regression cases. [CVE-2026-44284](https://github.com/labring/FastGPT/security/advisories/GHSA-cxxj-99f7-f5wq) (FastGPT, fixed in 4.14.17, May 6, 2026) is a stored-SSRF where the MCP tool create/update endpoints did not reject internal URLs even though the live preview/run path did, so `http://localhost:3000/mcp` could be persisted and later resolved by the workflow runner without revalidating the destination; [CVE-2026-39974](https://nvd.nist.gov/vuln/detail/CVE-2026-39974) (n8n-MCP, fixed in 2.47.4) let an authenticated user in multi-tenant HTTP mode inject arbitrary destination URLs through MCP headers and exfiltrate the response body via JSON-RPC, including cloud IMDS replies; [CVE-2026-7220](https://vuldb.com/vuln/359820) (`fastly-mcp-server`) passed user input into `child_process.exec` against a PowerShell command line, so prompt-derived strings escaped through `&`/`|`/`>` despite single-quote wrapping; and [CVE-2026-5603](https://dev.to/armor1ai/how-to-check-your-mcp-server-for-cve-2026-5603s-vulnerability-pattern-and-why-shellquote-isnt-gdn) (`@elgentos/magento2-dev-mcp`) showed that `shell-quote`-style escaping designed for Bourne shells fails closed on Windows `cmd.exe`, where metacharacters retain command-separator semantics. For 3.2.5, the harness should include a stored-vs-runtime SSRF differential (preview validates, persist path does not), a multi-tenant header SSRF probe against IMDS and loopback, and a cross-platform command-injection regression that tests every MCP tool on at least one POSIX and one Windows runner so a shell-quote bypass cannot reach production unnoticed.

May 13, 2026 disclosures extend the MCP regression surface from STDIO transports into database adapters. A single bug hunter published three distinct database-MCP issues the same day: Apache Doris MCP CVE-2025-66335 (SQL injection through an unvalidated `db_name` parameter in `exec_query`, fixed in v0.6.1), Apache Pinot MCP v1.1.0 (HTTP transport with no authentication, letting remote callers invoke SQL-execution tools), and Alibaba RDS MCP (information disclosure that Alibaba declined to fix). For 3.2.5, the harness should treat database, search, and data-platform MCP servers the same way as workflow-builder MCPs: replay parameterized SQL through every tool field, attempt unauthenticated tool invocation against the transport, and confirm both upstream fixes and vendor non-fixes are reflected in the deployed allowlist: https://www.theregister.com/security/2026/05/13/bug-hunter-tracks-down-three-serious-mcp-database-flaws-one-left-unpatched/5238916

The May 11-20, 2026 window added three more release-gate signals that argue for binding patch evidence to the release record, not only behavioral evals. [PraisonAI CVE-2026-44338](https://github.com/advisories/GHSA-6rmh-7xcm-cpxj) (CVSS 7.3, affecting 2.5.6-4.6.33, fixed in 4.6.34) shipped a legacy Flask API server with `AUTH_ENABLED = False` hardcoded so `/agents` enumeration and `/chat` workflow execution failed open by design; [Sysdig](https://www.sysdig.com/blog/cve-2026-44338-praisonai-authentication-bypass-in-under-4-hours-and-the-growing-trend-of-rapid-exploitation) and [The Hacker News](https://thehackernews.com/2026/05/praisonai-cve-2026-44338-auth-bypass.html) measured 3 hours 44 minutes 39 seconds from advisory publication (May 11, 2026 13:56 UTC) to the first targeted scan against the exact vulnerable path, with the scanner self-identifying as `CVE-Detector/1.0` — a concrete time-to-exploit data point that any release gate built around "patch within 24 hours" should treat as load-bearing. [Ollama Bleeding Llama CVE-2026-7482](https://www.cyera.com/research/bleeding-llama-critical-unauthenticated-memory-leak-in-ollama) (CVSS 9.1, disclosed May 5, 2026 by Cyera) is a heap out-of-bounds read in the GGUF model loader that lets an unauthenticated caller exfiltrate Ollama's process heap in three API calls with no log entries; [The Hacker News reporting](https://thehackernews.com/2026/05/ollama-out-of-bounds-read-vulnerability.html) and [Cybersecurity News](https://cybersecuritynews.com/ollama-vulnerability-exposes-servers/) note ~300,000 internet-reachable Ollama servers and that the leaked heap regularly contained system prompts, other users' chat fragments, environment variables (often holding OpenAI/Anthropic tokens, database credentials, and cloud secrets), and any PII or PHI flowing through inference jobs. The patch shipped silently in v0.17.1 on Feb 25, 2026 without a security label, so a release gate that only consumes "security-flagged" release notes from upstream missed this for three months. The same Ollama codebase produced a paired Windows auto-updater chain: [CVE-2026-42248](https://www.helpnetsecurity.com/2026/05/05/ollama-windows-vulnerabilities-cve-2026-42248-cve-2026-42249/) (signature verification function called but no-op so any downloaded payload runs) and [CVE-2026-42249](https://github.com/advisories/GHSA-7mp6-jmch-f3fh) (path traversal in update local-file path constructed from attacker-controlled HTTP headers), both CVSS 4.0 7.7 on Ollama for Windows 0.12.10-0.17.5, where [CERT Polska](https://cert.pl/en/posts/2026/04/CVE-2026-42248/) took over coordination after the vendor's documented security address stopped responding. For 3.2.6, that argues for binding the inference container's auto-update path and signature verification posture into the same release record as the model digest, since a silently-updated serving runtime is now a category of regression separate from the model artifact itself.

The same window pulled multimodal media and MCP-adapter SSRF/RCE into the regression surface. [Spring AI CVE-2026-22742](https://spring.io/security/cve-2026-22742/) (CVSS 8.6, fixed in 1.0.5 / 1.1.4, March 27 2026) is a SSRF in `BedrockProxyChatModel` when multimodal messages reference user-supplied media URLs: the URL scheme, host, and destination were not validated before server-side fetch, so an attacker could point the multimodal handler at `169.254.169.254/latest/meta-data/` or other internal endpoints; the fix added an allowlist that blocks RFC 1918, loopback, and metadata destinations. [mcp-atlassian CVE-2026-27825](https://arcticwolf.com/resources/blog-uk/cve-2026-27825-critical-unauthenticated-rce-and-ssrf-in-mcp-atlassian/) (CVSS 9.1, fixed in v0.17.0, Feb 24, 2026) chains directly with the previously-listed CVE-2026-27826: the `download_attachment` and `download_content_attachments` tools accepted attacker-supplied target paths with no base-directory or symlink check, so a network-adjacent unauthenticated caller against an HTTP-transport instance bound to `0.0.0.0` could overwrite `~/.ssh/authorized_keys` or `~/.bashrc` in two requests, with the [Pluto Security](https://pluto.security/blog/mcpwnfluence-cve-2026-27825-critical/) writeup citing 4.4K stars and 4M downloads at disclosure. [mcp-browser-agent CVE-2026-5607](https://www.sentinelone.com/vulnerability-database/cve-2026-5607/) is the recurring "vendor non-response" case for the regression suite: `request.params.name` and `request.params.arguments` flowed into URL handlers in `src/handlers.ts` and `src/executor.ts` with no allowlist or SSRF guard, the issue was publicly disclosed, and the vendor never responded — so deployment evidence has to either pin a non-vulnerable version or document compensating egress controls before the harness signs off. For 3.2.5, the practical takeaway is that multimodal SSRF, MCP-adapter path traversal, and unpatched-vendor cases all belong in the same suite as STDIO command injection — and the harness should re-run them on every MCP image refresh, not only on the model change.

On the benchmark side, [MLCommons AILuminate Jailbreak v0.7](https://mlcommons.org/2026/02/jailbreak-0-7/) (Feb 2026, [methodology paper](https://mlcommons.org/wp-content/uploads/2026/02/MLCommons___Security___Jailbreak_0_7_Paper_Collaborative.pdf)) is the most usable upgrade to the threshold-setting story since v0.5. The release shifts to a mechanism-first taxonomy for single-turn, inference-time jailbreaks and explicitly separates *attack mechanism* (how the bypass is constructed) from *hazard class* (what unsafe behavior it elicits), so a single test attempt can be scored against multiple hazards and a single hazard is sampled across multiple attack families. For 3.2.3, that maps cleanly onto release-gate evidence: store per-mechanism *and* per-hazard pass rates rather than a single aggregate, and require the harness to cover each mechanism family it claims to test rather than relying on whatever attack distribution the underlying prompt set happened to ship with. MLCommons states that v1.0 is on the Q1 2026 roadmap with regular post-1.0 updates for new attack techniques.

An additional release-gate signal landed on March 30, 2026 with [CVE-2026-5023](https://www.sentinelone.com/vulnerability-database/cve-2026-5023/) in the `codebase-mcp` server: the `getCodebase`, `getRemoteCodebase`, and `saveCodebase` functions in `src/tools/codebase.ts` concatenated user-supplied repository paths into shell commands without escaping metacharacters, and the entry remained unpatched at disclosure time. Treat unpatched but enumerated MCP CVEs as load-bearing harness inputs -- the regression suite should fail closed when the deployed image still ships an unpatched advisory ID, not only when a tool call returns a measurable bad outcome.

Microsoft's [May 12, 2026 disclosure of MDASH](https://www.microsoft.com/en-us/security/blog/2026/05/12/defense-at-ai-speed-microsofts-new-multi-model-agentic-security-system-tops-leading-industry-benchmark/) (Microsoft Security multi-model agentic scanning harness) is the first large-scale public example of an agentic evaluation harness used for pre-deployment vulnerability discovery, and the architecture is worth borrowing for 3.2.5 evidence design. MDASH orchestrates more than 100 specialized AI agents across an ensemble of frontier and distilled models in three explicit stages: a `Prepare` stage that ingests source and threat models, a `Scan` stage where auditor agents emit findings with hypotheses, and a `Prove` stage that constructs and executes inputs to demonstrate exploitability end-to-end. Microsoft reports MDASH scored 88.45% on the public CyberGym benchmark (topping the leaderboard above Anthropic's Claude Mythos Preview), 100% recall on `tcpip.sys` MSRC cases and 96% on `clfs.sys`, and zero false positives on a 21-case private test driver; the harness contributed 16 vulnerabilities to May 2026 Patch Tuesday including four Critical RCE issues (one double-free in `ikeext.dll`). For 3.2.3, that is a useful template for documenting harness coverage as benchmark performance plus retrospective recall against an internal vulnerability corpus; for 3.2.5, the Prepare/Scan/Prove split is a practical pattern for separating model-behavior tests from agent-level exploit construction so a passing eval includes a constructed working input, not only a refusal or classifier verdict: https://thehackernews.com/2026/05/microsofts-mdash-ai-system-finds-16.html

The same May 2026 window highlighted a gap between model evaluation and release-pipeline coverage. [VentureBeat's May 14, 2026 analysis](https://venturebeat.com/security/supply-chain-incidents-openai-anthropic-meta-release-surface-vendor-questionnaire-matrix) tracked four AI supply-chain incidents in 50 days against OpenAI, Anthropic, and Meta: none targeted the model, and all four exposed CI, dependency, runner, or packaging gaps that no published system card or red-team evaluation had scoped. Two concrete cases sit inside the 3.2 evidence path: Anthropic shipped Claude Code v2.1.88 to npm with a 59.8 MB source map that pointed at a Cloudflare R2 zip containing 513,000 lines of unobfuscated TypeScript across 1,906 files -- a release artifact that an immutable model audit (3.2.6) was not designed to catch -- and on May 11, 2026 a self-propagating worm called Mini Shai-Hulud published 84 malicious versions across 42 `@tanstack/*` npm packages in roughly six minutes by chaining a `pull_request_target` misconfiguration, GitHub Actions cache poisoning, and OIDC token extraction from runner memory, then cascading into Mercor (a $10B AI training-data supplier to Meta, OpenAI, and Anthropic) and exfiltrating ~4 TB of data including proprietary training-methodology references. For 3.2.4 and 3.2.6, that argues for binding the model digest, signature, harness version, and the runner/packaging attestation set into the same release record -- and for blocking deployment when the release pipeline itself fails a hardening check, even if every model benchmark passes.

Hugging Face's [Open-OSS/privacy-filter typosquat (May 7, 2026)](https://www.hiddenlayer.com/research/malware-found-in-trending-hugging-face-repository-open-oss-privacy-filter) added a pre-deployment vetting case for the model loader path: a malicious repository copied OpenAI's legitimate Privacy Filter model card nearly verbatim, climbed to the #1 trending spot with roughly 244K downloads in under 18 hours, and shipped a `loader.py` that fetched and executed a Rust-based infostealer harvesting browser credentials, session tokens, cryptocurrency wallets, and SSH keys. For 3.2.5, this is a reminder that even an entirely benign-looking model card cannot substitute for sandboxed loader execution, network egress controls during model resolution, and a vetting check against known-good signatures before any inference container is allowed to pull a third-party artifact.

The community-maintained [Vulnerable MCP Project](https://vulnerablemcp.info/) (`vulnerablemcp.info`, also mirrored at https://github.com/vineethsai/vulnerablemcp) is now a usable input for 3.2.5 harness coverage: it indexes prompt-injection, tool-poisoning, input-validation, authentication, and session-management findings across MCP servers and includes a vulnerability taxonomy worth aligning regression tests to. As an example, the tracker documents response leakage across client boundaries in MCP TypeScript SDK 1.10.0-1.25.3 -- the kind of cross-tenant evaluation case that is easy to miss with single-client tests. Treat it as a reference index, not an authoritative SBOM: confirm each finding against NVD, GHSA, or the upstream patch before promoting it into a release-gate test.

Benchmark coverage has also widened past 2025-era static suites. PandaGuard / PandaBench (arXiv:2505.13862) is a standardized harness across ~3B tokens and 49 open and closed-source LLMs under combinations of jailbreak attacks and defenses, useful as a reference distribution when teams set their own pass/fail thresholds. JBDistill -- "Jailbreak Distillation: Renewable Safety Benchmarking" (arXiv:2505.22037, EMNLP 2025 Findings) -- distills jailbreak prompts into a renewable benchmark that retained ~81.8% attack effectiveness and generalized across 13 evaluation models, including proprietary and reasoning LLMs; for 3.2.3, that lets teams refresh thresholds without re-running an entire human red-team campaign every release. SafeDialBench (arXiv:2502.11090) is a fine-grained multi-turn dialogue safety benchmark with diverse jailbreak attacks, which complements the single-turn vs multi-turn Promptfoo data point already used to size 3.2.1 thresholds. LITMUS (arXiv:2605.10779, May 2026) introduces "behavior jailbreak" as a distinct category from content-jailbreak: an adversary induces an OS-level agent into dangerous or irreversible host operations, and the benchmark adds semantic-physical dual verification and state rollback so test cases do not contaminate each other. AgentHarm (arXiv:2410.09024) and OS-Harm (arXiv:2506.14866) extend the same idea to computer-use agents. For 3.2.5, those are the benchmarks worth replaying against deployed agents and MCP wrappers, not only the model under test: https://arxiv.org/pdf/2505.13862 and https://arxiv.org/pdf/2505.22037 and https://arxiv.org/pdf/2502.11090 and https://arxiv.org/html/2605.10779v1 and https://arxiv.org/pdf/2410.09024 and https://arxiv.org/pdf/2506.14866

The OWASP Top 10 for Agentic Applications 2026 is now a stable reference (peer-reviewed by 100+ industry contributors) covering goal misalignment, tool misuse, delegated trust, inter-agent communication, persistent memory, and emergent autonomous behavior. Confident AI's [DeepTeam](https://www.trydeepteam.com/docs/frameworks-owasp-top-10-for-agentic-applications) ships an `OWASP_ASI_2026()` framework call that runs a full risk assessment with code or in the Confident AI platform, and Palo Alto Networks, Aikido, and SecurityWall have published mappings from the 2026 list to concrete CI/CD evaluation cases (prompt injection, agent tool misuse, privilege escalation, cloud-metadata exposure, workflow manipulation). For 3.2.5, treat OWASP_ASI_2026 mappings as the minimum coverage matrix for the agent harness, especially when the harness already builds on top of LangSmith traces, Giskard scans, or Mindgard CART: https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/ and https://www.trydeepteam.com/docs/frameworks-owasp-top-10-for-agentic-applications

Independent evaluation partnerships continue to multiply alongside CAISI. The UK AI Security Institute's [May 6, 2026 partnership with Microsoft](https://www.aisi.gov.uk/blog/partnering-with-microsoft-to-strengthen-frontier-ai-safety) covers three workstreams that directly map to 3.2.3 and 3.2.5 evidence: method development for evaluating high-risk capabilities, evaluation of safeguard effectiveness rather than only base capability, and research on conversational AI behavior in sensitive contexts. AISI states it has conducted evaluations of over 30 state-of-the-art models since November 2023, including cyber and biological capability tracking that has moved from ~10% apprentice-task success in early 2024 to ~50% by 2026, with the first expert-level cyber capability observed in 2025; for release gates, that argues for keeping safeguard-effectiveness tests distinct from capability benchmarks in the evidence packet, not bundling them into a single aggregate score.

The EU AI Act's [Code of Practice on General-Purpose AI](https://digital-strategy.ec.europa.eu/en/policies/contents-code-gpai) now provides a concrete pre-enforcement compliance path. The Second Draft was published March 3, 2026, the Commission's enforcement powers enter into application on August 2, 2026, and GPAI models already on the market have a two-year transition window to August 2, 2027, during which signatories can demonstrate compliance through Code adherence ahead of European standards. Even non-signatories should treat the Code's evaluation, documentation, and transparency commitments as the de facto evidence template for 3.2.3 and 3.2.6 -- the AI Office has stated it will consider Code commitments when calibrating fines under Article 99, which makes the Code's evidence pattern (model-behavior assessment, systemic-risk mitigation, post-market monitoring, and incident reporting) the cheapest baseline to organize release-gate evidence against.

For audit alignment, [unified SOC 2 / ISO 42001 / EU AI Act control mappings](https://www.augmentcode.com/guides/ai-governance-framework-for-soc-2-and-iso-42001-compliance) are becoming the standard way to bind a single evidence pipeline to multiple frameworks. A model-versioning control simultaneously satisfies SOC 2 CC8 (change management), ISO/IEC 42001 clause 8 (operation), and AI Act Annex IV technical documentation; a prompt and completion audit trail satisfies SOC 2 CC7 (system monitoring), ISO/IEC 42001 clause 9 (performance evaluation), and AI Act Article 12 (record-keeping for high-risk systems). For 3.2.6, that argues for one canonical evidence schema covering model version, training-data provenance, inference logging with PII redaction policy, drift detection thresholds, rollback procedure, prompt/completion retention, and upstream-provider contract coverage -- duplicate evidence pipelines per framework remain a frequent audit-failure root cause.

CAISI's first public model evaluation is now a useful template, not only a policy signal. NIST's May 3, 2026 [CAISI Evaluation of DeepSeek V4 Pro](https://www.nist.gov/news-events/news/2026/05/caisi-evaluation-deepseek-v4-pro) ran 9 benchmarks across 5 domains -- cybersecurity, software engineering, natural sciences, abstract reasoning, and mathematics -- and reported concrete capability gaps (32% on cybersecurity, 46% on abstract reasoning, 44% on agentic software engineering against frontier US models). For 3.2.3, the format is worth copying: name the suite, name the benchmark version, store per-domain pass rates rather than a single aggregate, and bind each result to the model identifier and date. The May 5, 2026 [CAISI agreements with Google DeepMind, Microsoft, and xAI](https://www.nist.gov/news-events/news/2026/05/caisi-signs-agreements-regarding-frontier-ai-national-security-testing) make this format the de facto reference for high-impact model releases.

Promptfoo's December 11, 2025 GPT-5.2 trust-and-safety assessment is a useful concrete data point for 3.2.1 and 3.2.3 threshold design. Independent red-team evaluation of GPT-5.2 lifted attack success from a 4.3% single-turn baseline to 61.0% with single-turn jailbreak strategies and 78.5% with multi-turn Hydra-style strategies in roughly 30 minutes of automated testing, with impersonation, graphic content, harassment, disinformation, hate speech, and self-harm categories scoring worst. The implication for release gates: thresholds set against single-prompt benchmarks systematically underestimate exposure once an attacker can chain turns, so a passing gate should include both single-turn and multi-turn adversarial suites, and store per-category pass rates rather than a single aggregate: https://www.promptfoo.dev/blog/gpt-5.2-trust-safety-assessment/

The April 15, 2026 OX Security disclosure also tracked the root issue as CVE-2026-30623 in Anthropic's MCP SDK STDIO transport: `StdioServerParameters` runs whatever command field it is handed, and the same configuration-to-command path exists across the official Python, TypeScript, Java, and Rust SDKs. OX estimates roughly 200,000 publicly accessible server instances and 150 million SDK downloads in scope, and Anthropic has characterized the behavior as a documented design choice rather than a flaw, leaving sanitization to downstream implementers. Downstream patches have been pushed quickly where vendors disagree: LiteLLM landed the fix in commit 7b7f304 (PR #25343), included it in v1.83.6-nightly and v1.83.7-stable, and published vendor guidance for callers that previously trusted untrusted `command`/`args`. For 3.2.5, evaluation harnesses should test every MCP wrapper -- web UI, code path, configuration file, automation route -- and confirm allowlists, argument validation, and sandboxing block both the direct command field and argument-injection bypasses such as `npx -c`. For 3.2.6, deployment evidence should record the exact MCP SDK version and the downstream patch level used at release time, not only the model digest: https://docs.litellm.ai/blog/mcp-stdio-command-injection-april-2026 and https://thehackernews.com/2026/04/anthropic-mcp-design-vulnerability.html

The same window added a separate cloud-platform target. Microsoft's March 10, 2026 Patch Tuesday addressed CVE-2026-26118 in Azure MCP Server Tools as a CVSS 8.x server-side request forgery and elevation-of-privilege issue: an authenticated user could submit a malicious URL where a standard Azure resource identifier was expected, and the MCP server made an outbound HTTP request -- including its managed identity token -- to the attacker-controlled host. That converts an agent-tool invocation into IMDS-token theft, so 3.2.5 harnesses should treat URL/resource parameters in tool schemas as untrusted, replay loopback and 169.254.169.254 probes through every MCP adapter, and confirm that egress controls and identity scope limits prevent metadata-service callouts during evaluation runs: https://nvd.nist.gov/vuln/detail/CVE-2026-26118 and https://www.sentinelone.com/vulnerability-database/cve-2026-26118/

For workflow-builder governance, CISA continues to drive aggressive remediation timelines. Sysdig's review of Langflow CVE-2026-33017 puts active scanning within roughly 20 hours of disclosure, Python-script exploitation within 21 hours, and `.env`/`.db` harvesting after 24 hours; the Binding Operational Directive 22-01 deadline for federal agencies was April 8, 2026. Patched releases land in Langflow 1.9.0 and Flowise 3.1.1 (or 3.0.6 as a minimum), and NVD entries CVE-2026-41268 (Flowise parameter-override bypass) and CVE-2026-41679 (Paperclip unauthenticated RCE), both rated critical on April 23, 2026, show the agent-builder attack surface keeps producing fresh release-gate failures. ModelScope's MS-Agent CVE-2026-2256 (NVD March 2, 2026) is an instructive 3.2.5 case: its Shell tool let prompt-derived input flow into the operating system command path -- exactly the agent-tool chain that 3.2.5 calls out -- and the published advisory recommends sanitization plus disabling shell access in untrusted contexts: https://www.sysdig.com/blog/cve-2026-33017-how-attackers-compromised-langflow-ai-pipelines-in-20-hours and https://thehackernews.com/2026/04/flowise-ai-agent-builder-under-active.html and https://github.com/advisories/GHSA-4gc2-344q-r2rw

The May 18-20, 2026 window added three more concrete MCP regression cases worth pinning into the harness. [CVE-2026-32211](https://cvefeed.io/vuln/detail/CVE-2026-32211) in `@azure-devops/mcp` (CVSS 9.1, disclosed April 3, 2026, CWE-306) is a missing-authentication-for-critical-function flaw distinct from the previously listed Azure MCP SSRF (CVE-2026-26118): an unauthenticated, network-adjacent caller can reach work-item, repo, pipeline, and pull-request tools and read configuration details, API keys, authentication tokens, and project data with no patch available at disclosure -- so deployment evidence has to record the reverse-proxy or firewall mitigation, not only the model digest. [CVE-2026-22688](https://github.com/Tencent/WeKnora/security/advisories/GHSA-78h3-63c4-5fqc) in Tencent/WeKnora (CVSS 9.9, fixed in v0.2.5) lets an authenticated user inject `stdio_config.command`/`args` into the `/test` execution flow with no allowlist, fixed binary, or admin-only check, demonstrating again that "service configuration data" is treated as an execution sink in MCP wrappers and has to be tested with a non-admin Bearer token, not only with anonymous calls. [CVE-2026-20205](https://www.sentinelone.com/vulnerability-database/cve-2026-20205/) in Splunk MCP Server app (CVSS 7.2, CWE-532, fixed in v1.0.3, see [Splunk advisory SVD-2026-0407](https://advisory.splunk.com/advisories/SVD-2026-0407)) writes session and authorization tokens to the `_internal` index in clear text, so any privileged user or anyone with local log access can replay them for session hijacking; this argues for grepping every MCP tool's log output for bearer tokens, OAuth secrets, and cookie material during the regression run, not only verifying that the tool returns the expected result. Two additional 2026 benchmarks fill a gap on the agent-evaluation side: [OpenAgentSafety](https://arxiv.org/abs/2507.06134) (ICLR 2026, Vijayvargiya et al., revised February 16, 2026) is a modular evaluation framework that exercises 350+ executable, multi-turn tasks across eight risk categories on top of the OpenHands agentic scaffold in a sandboxed Docker environment with users exhibiting benign, ambiguous, or adversarial intent; [DoomArena](https://arxiv.org/abs/2504.14064) (Boisvert et al., revised October 7, 2025) is a plug-in security evaluation framework that lets operators configure threat models and inject modular attacks across web and tool-calling agent environments. Both are useful additions to 3.2.5 harness coverage where AgentDojo, LITMUS, AgentHarm, and OS-Harm together still leave gaps in long-horizon, multi-tool, multi-intent test scenarios.

Tool surfaces are also expanding. Mindgard added a reconnaissance module in March 2026 that maps AI guardrails, system prompts, exposed tools, integrations, and external services in production agents -- useful for 3.2.5 harness coverage so evaluators replay against the actual deployed attack surface, not only a static prompt list. Giskard Hub's December 16, 2025 release introduced ChatInject (OWASP LLM01 prompt injection through native chat templates) and CoT Forgery (appending realistic reasoning traces to harmful requests) probes, both mapped to the OWASP LLM Top 10 categories used in 3.2.1 and 3.2.5 scans, and remains useful as a CI gate because scans expose A-D grades plus an insufficient-data result. For the post-training compression line in 3.2.7, the [UniComp](https://arxiv.org/abs/2602.09130) framework (arXiv:2602.09130, 2026) evaluates pruning, quantization, and distillation jointly across capability and safety benchmarks and finds a consistent "knowledge bias" -- factual recall is preserved while multi-step reasoning, multilingual, and instruction-following ability degrade -- and a decoupling between performance and reliability that argues for retaining safety re-evaluation even when a compressed model still hits accuracy targets: https://mindgard.ai/blog/continuous-automated-red-teaming and https://docs.giskard.ai/hub/ui/release_notes/2025-12-16.html

The May 7, 2026 EU Digital Omnibus political agreement made the timeline shift concrete: Annex III (use-based) high-risk obligations are deferred from August 2, 2026 to December 2, 2027 (a 16-month deferral), and Annex I (product-regulated) high-risk obligations are deferred from August 2, 2027 to August 2, 2028. Article 43 still requires a new conformity assessment after substantial modification, so model swaps, fine-tunes, safety-model changes, and compression artifacts each need a distinct evaluation record bound to their digest. OpenSSF Model Signing (OMS) is increasingly the supply-chain pattern operators reach for here: the OMS specification follows the Sigstore Bundle Format, is PKI-agnostic (bare keys, certificate chains, or keyless Sigstore identities), and is now actively adopted by NVIDIA's NGC catalog and Google's Kaggle hub for ML artifacts: https://www.consilium.europa.eu/en/press/press-releases/2026/05/07/artificial-intelligence-council-and-parliament-agree-to-simplify-and-streamline-rules/ and https://github.com/ossf/model-signing-spec

For orchestration-platform validation, auditors should ask for:
- A current inventory of AI workflow builders, agent runtimes, MCP gateways, eval runners, and red-team harness dependencies.
- Evidence that Langflow, Flowise, MCP servers, browser drivers, and custom eval providers are patched before release gates execute.
- Tests proving public-flow endpoints, CustomMCP-style configuration inputs, hidden transport switches, and user-supplied tool definitions cannot reach `eval`, `exec`, shell, filesystem, or network primitives without sandboxing.
- SBOM, lockfile, or artifact attestation evidence for the eval runner itself, not only the model artifact being evaluated.
- A malicious-configuration regression test that exercises project files, MCP manifests, tool descriptions, shell-safety classifiers, command allowlists, and environment overrides before approving agent releases.
- Adapter-layer tests for MCP servers that proxy Kubernetes, Atlassian, databases, and external command runners, including SSRF, DNS rebinding, argument injection, and command-injection cases.
- Outbound egress controls on eval and agent-builder hosts so a failed test cannot exfiltrate API keys, retrieved documents, prompts, or model artifacts.
- A failed-release sample where a critical KEV or CVSS 9+ issue in the validation stack blocks deployment independently of the model's benchmark score.
- Separate production, staging, and red-team credentials so a compromised harness cannot reuse secrets from the runtime environment it is meant to assess.
- Post-exposure evidence: credential rotation, outbound callback review, and replay of affected evals when a validation tool or AI workflow platform was vulnerable.

Compliance evidence needs to bind validation results to the exact artifact that was evaluated. On May 7, 2026, the European Commission announced a Digital Omnibus political agreement setting 2 December 2027 for high-risk systems in areas such as biometrics, critical infrastructure, education, employment, migration, asylum, and border control, and 2 August 2028 for product-integrated high-risk systems. Article 43 still requires a new conformity assessment after substantial modification, so model compression, fine-tuning, safety-model swaps, and agent tool changes should each have distinct evaluation records. OpenSSF's MLSecOps whitepaper and Model Signing project are useful evidence patterns here: sign the model bundle, store the digest in the audit record, and verify the same digest at deployment time: https://digital-strategy.ec.europa.eu/en/news/eu-agrees-simplify-ai-rules-boost-innovation-and-ban-nudification-apps-protect-citizens and https://ai-act-service-desk.ec.europa.eu/en/ai-act/article-43 and https://openssf.org/projects/model-signing/

For this page, the highest-yield verifier checks are:
- Confirm every safety threshold is tied to a model version or digest, an eval suite version, and a deployment target.
- Trigger a failing Promptfoo, Giskard, or custom eval and verify the release stops before registry promotion or traffic shift.
- Run at least one agent test that uses a malicious tool response, not only malicious user input.
- Run at least one MCP adapter test that tries to modify command arguments, issue loopback or metadata-service requests, and inject a malicious external-server configuration.
- Include a model-loader regression that attempts to load a hostile Hugging Face-style repository with `--trust-remote-code=False` and confirms the serving stack (e.g., vLLM v0.18.0+, Triton, or equivalent) refuses to execute `auto_map` modules or vendor-specific overrides such as the NemotronVL/Kimi-K25 hardcoded `trust_remote_code=True` path (CVE-2026-22807, CVE-2026-27893).
- Replay the Anthropic MCP SDK STDIO command-field case (CVE-2026-30623) against every wrapper layer -- web UI, code path, configuration file, automation route -- and confirm allowlists, argument validation, and sandboxing block both direct command execution and argument-injection bypasses such as `npx -c`.
- Include an Azure MCP SSRF regression test (CVE-2026-26118) that points URL/resource parameters at 169.254.169.254 and other internal endpoints, and confirm egress controls and identity scope limits prevent managed-identity token exposure during evaluation runs.
- Attempt MCP server registration through every public, admin, project, and workflow-builder endpoint and confirm unauthenticated or low-privilege users cannot add STDIO transports.
- Replay the nginx-ui MCPwn case (CVE-2026-33032) against every MCP-exposed admin or infra tool: send unauthenticated requests to each paired endpoint, fail closed when one side enforces authentication and the other side relies only on an IP allowlist with a default-empty list, and confirm destructive tools (config writes, reloads, restarts) require explicit authentication independent of the read path.
- Include LangChain-ChatChat-style regression tests where a public MCP management path registers a malicious command and waits for later agent activity to execute it.
- Enumerate every method exposed to the model as a kernel/tool function and confirm no internal helper has been tagged by mistake (Semantic Kernel CVE-2026-25592 `[KernelFunction] DownloadFileAsync` pattern); fail closed if a high-risk primitive such as file write, command execution, or path resolution is reachable from a model-callable surface without an explicit allowlist.
- Replay AST-bypass payloads against every filter, query, and expression parameter the model can populate, including dunder-traversal payloads that would reach `eval()` or equivalent (Semantic Kernel `InMemoryVectorStore` CVE-2026-26030 pattern); pin the framework version (`semantic-kernel` >= 1.39.4 Python, Semantic Kernel .NET >= 1.71.0) in the release evidence record.
- Probe any model-serving broker that exposes a ZMQ, gRPC, or HTTP control channel with unauthenticated pickle and JSON payloads (SGLang CVE-2026-3059/CVE-2026-3060 pattern); fail closed when the deployment image ships an affected SGLang 0.5.5-0.5.9 release without the broker bound to loopback or wrapped in authenticated proxy.
- Include hostile multimodal media against the actual serving stack -- malicious video URLs, JPEG2000 streams, and crafted images -- and confirm the deployed vLLM, Triton, or equivalent server is at or above the patch level addressing the OpenCV/FFmpeg JPEG2000 decoder issue (vLLM >= 0.14.1 for CVE-2026-22778); store the decoder, serving-framework, and model digest together so a compressed or modality-extended artifact triggers its own re-evaluation.
- Cover database-MCP adapters (Apache Doris CVE-2025-66335, Apache Pinot v1.1.0 unauthenticated transport, Alibaba RDS information disclosure) with parameterized-SQL fuzzing and unauthenticated-tool-invocation checks; pull additional regression candidates from the [Vulnerable MCP Project](https://vulnerablemcp.info/) tracker after confirming each against NVD or upstream patch notes.
- Include LLM application framework regression tests: a LangChain serialization probe (CVE-2025-68664) that drives prompt-injected `additional_kwargs`/`response_metadata` through any `dumps()`/`dumpd()` path, a prompt-template path-traversal probe (CVE-2026-34070) against every loader that accepts a config-supplied path, and a LangGraph checkpoint SQL injection probe (CVE-2025-67644) against metadata-filter keys, confirming the deployed `langchain-core` version is at or above 1.2.22.
- Bind the model digest, signature, harness version, runner attestation, and packaging-pipeline scan results into the same release record so a missing or stale source map, leaked artifact, or hijacked release pipeline (Anthropic Claude Code v2.1.88 source-map disclosure, May 11, 2026 Mini Shai-Hulud `@tanstack/*` worm) blocks deployment even when model benchmarks pass.
- Include an unsigned-or-typosquat model-loader regression test (Hugging Face Open-OSS/privacy-filter, May 7, 2026): confirm the inference container refuses to fetch or execute unsigned weights, that network egress during model resolution is denied by default, and that a signature mismatch fails closed before any `loader.py` or `auto_map` module runs.
- Include both single-turn and multi-turn (Hydra-style) jailbreak suites and store per-category pass rates so an aggregate score cannot mask a category collapse.
- Run at least one coding-agent test where hostile repository content, issue text, or MCP output attempts to change local MCP JSON, smuggle shell expansion through a read-only classifier, or bypass a command allowlist.
- Keep MCP server allowlists, OAuth scopes, and tool-call audit logs in the same evidence packet as test results.
- Block releases when the evaluation harness or agent workflow builder has an active-exploited critical vulnerability, even if model benchmark thresholds pass.
- Require a dependency-patch gate for the validation harness itself, including workflow builders, browser drivers, MCP gateways, model-serving adapters, and custom eval providers.
- Store one negative-control evidence packet showing that a critical finding in the harness stack blocks deployment independently of the model's benchmark score.
- Confirm the validation stack cannot read unrelated production secrets during tests and that outbound callbacks from eval hosts are denied by default.
- Re-run the same suite against compressed, fine-tuned, or adapter-composed artifacts and store separate results for each digest.
- Rehearse a rapid-exploitation timeline against the validation stack itself: pick a recent agent-framework auth-bypass advisory (PraisonAI CVE-2026-44338 is a concrete sub-4-hour disclosure-to-scan case) and confirm the release pipeline can ingest the advisory, patch the harness or wrapper, and re-run the gated evaluation inside the measured time-to-exploit window rather than the team's preferred "patch within 24 hours" SLA.
- Send unauthenticated probes against every model-serving HTTP endpoint that loads attacker-supplied artifacts (GGUF, ONNX, SafeTensors, multimodal media) and confirm the deployed server refuses or returns a deterministic error rather than echoing process heap (Ollama Bleeding Llama CVE-2026-7482 pattern); pin the serving image to a known-good version even when upstream release notes do not flag the patch as a security fix.
- Include a signed-update regression for the inference container's auto-updater path: serve a tampered payload from a man-in-the-middle position, an unsigned binary, and HTTP headers that try to traverse outside the update directory (Ollama Windows auto-updater CVE-2026-42248 / CVE-2026-42249 pattern), and fail closed if the updater either accepts the payload or writes outside the expected directory.
- Send unauthenticated probes against every cloud-platform MCP wrapper -- not only the previously listed Azure MCP SSRF (CVE-2026-26118) but the distinct missing-authentication case in `@azure-devops/mcp` (CVE-2026-32211) -- and confirm a reverse proxy, firewall rule, or explicit auth middleware blocks the call before the tool reaches work items, repos, pipelines, or pull-request data.
- Run the MCP STDIO regression with a non-admin Bearer token, not only with anonymous calls, so authenticated configuration-data execution paths (WeKnora CVE-2026-22688 `stdio_config.command`/`args` injection through `/test`) fail closed before a low-privilege role can reach a subprocess sink.
- Grep every MCP tool's log output and the model-serving runtime's internal index for bearer tokens, OAuth secrets, session cookies, and authorization headers (Splunk MCP CVE-2026-20205 `_internal` index clear-text logging pattern); deployment evidence should bind both the MCP app version and the redaction policy in effect at release time.
- Add OpenAgentSafety (ICLR 2026, OpenHands scaffold with 350+ multi-turn tasks across eight risk categories in a sandboxed Docker environment) and DoomArena (configurable threat-model plug-in framework for web and tool-calling agents) to the 3.2.5 harness rotation when AgentDojo, LITMUS, AgentHarm, and OS-Harm still leave coverage gaps in long-horizon, multi-tool, multi-intent scenarios.
- Treat the LLM gateway itself as a release-gate target, not only the model behind it: replay a full stdio server configuration (`command`/`args`/`env`) against every MCP test or preview route -- with and without credentials -- and confirm the deployed image is patched (LiteLLM CVE-2026-42271 / Starlette CVE-2026-48710 unauthenticated-RCE chain, on the CISA KEV catalog as of June 8, 2026); fail closed when the gateway version is in the affected range even if the model benchmarks pass.
- Extend the model-loader regression beyond the serving framework to the base library: attempt to load a hostile repository whose `config.json` carries underscore-prefixed kernel or implementation keys (e.g., `_attn_implementation_internal`) with remote-code trust disabled, confirm the deployed `transformers` is at or above 5.3.0 (Hugging Face Transformers CVE-2026-4372), and force the agent-framework Docker-sandbox fail-open path (CrewAI CVE-2026-2287/CVE-2026-2275) plus a poisoned GGUF `tokenizer.chat_template` against any rerank/serving endpoint (SGLang CVE-2026-5760).

The June 2026 window opened with the clearest example yet that the validation/serving stack -- not the model -- is the live attack surface. [LiteLLM CVE-2026-42271](https://thehackernews.com/2026/06/litellm-flaw-cve-2026-42271-exploited.html) (CVSS 8.7, affecting 1.74.2 through 1.83.6, fixed in 1.83.7) is a command injection in the MCP preview endpoints `POST /mcp-rest/test/connection` and `POST /mcp-rest/test/tools/list`: both accepted a full stdio server configuration -- `command`, `args`, and `env` -- in the request body and spawned it as a subprocess with no validation. Horizon3.ai chained it with [CVE-2026-48710](https://cybelangel.com/blog/itellm-vulnerability-cve-2026-42271/) (CVSS 6.5), a Starlette host-header validation bypass, to convert the authenticated command-injection into unauthenticated RCE (combined CVSS 10.0), and CISA added CVE-2026-42271 to the Known Exploited Vulnerabilities catalog on June 8, 2026. This is the same configuration-data-as-execution-sink pattern as the WeKnora and Anthropic SDK STDIO cases, but it lands inside an LLM gateway that many teams run as part of the eval and serving path itself -- so for 3.2.4 and 3.2.5 the harness should treat the gateway's own MCP test/preview routes as a release-gate target, replay the stdio-config body against them with and without credentials, and fail closed when the deployed gateway image still ships an affected LiteLLM build.

Model-loader trust bypasses continue to surface above the serving framework, now in the most widely used library. [Hugging Face Transformers CVE-2026-4372](https://www.csoonline.com/article/4181094/hugging-face-transformers-rce-flaw-enables-stealthy-compromise-via-ai-model-configs.html) (disclosed June 4, 2026 by Pluto Security, affecting 4.56.0 through 5.2.x, fixed in 5.3.0) abuses the config loader's use of `setattr` over every key in a model's `config.json`, including underscore-prefixed fields that look like internal implementation details. An attacker injects `_attn_implementation_internal`, which slips past `trust_remote_code=False`; `hub_kernels.py` then treats the value as an owner/repo reference, downloads attacker code, and executes it with "no sandboxing, no code signing, no integrity verification, no user prompt" and no runtime warning. This is the same class as the vLLM `auto_map`/`trust_remote_code` cases (CVE-2026-22807, CVE-2026-27893) but at the base-library layer, so for 3.2.5 and 3.2.7 the loader regression test should attempt to load a hostile repo whose `config.json` carries underscore-prefixed kernel/implementation keys with remote-code trust disabled, and the release record should pin the exact `transformers` version (>= 5.3.0) alongside the model digest -- a compressed or fine-tuned artifact swapped in later cannot inherit the base verdict if it re-resolves config through an older library.

Two more agent-framework cases reinforce that "sandboxed" tool execution fails open under prompt injection. CrewAI (CERT/CC [VU#221883](https://kb.cert.org/vuls/id/221883), [ThaiCERT writeup](https://www.thaicert.or.th/en/2026/04/02/multiple-vulnerabilities-in-crewai-allow-sandbox-escape-and-remote-code-execution-via-prompt-injection/), April 2, 2026) bundles four flaws reachable via direct or indirect prompt injection against an agent using the Code Interpreter Tool: CVE-2026-2287/CVE-2026-2275 let the interpreter silently fall back to an insecure in-process mode if Docker becomes unavailable mid-session, allowing arbitrary native calls via `ctypes`; CVE-2026-2286 is SSRF through RAG search tools that do not validate runtime URLs; and CVE-2026-2285 is arbitrary local file read in the JSON loader tool. SGLang [CVE-2026-5760](https://kb.cert.org/vuls/id/915947) (CERT/CC VU#915947, CVSS 9.8, public PoC against 0.5.9, unpatched at coordination close) is a Jinja2 server-side template injection in the reranker: `getjinjaenv()` builds a plain `jinja2.Environment()` instead of an `ImmutableSandboxedEnvironment`, so a poisoned `tokenizer.chat_template` embedded in a GGUF model renders unsandboxed and runs arbitrary Python when a request hits `/v1/rerank`. For 3.2.5, both argue for harness checks that force the Docker-sandbox fail-open path and that submit a malicious GGUF chat template through the actual rerank/serving endpoint; for 3.2.7, the SGLang case is a concrete reason the compressed-artifact gate has to exercise the GGUF template path, not only accuracy and refusal metrics.

The first publicly documented case of an autonomous LLM agent running the post-exploitation stage landed on May 10, 2026. Sysdig observed an in-the-wild intrusion that exploited an internet-exposed Marimo notebook server ([CVE-2026-39987](https://thehackernews.com/2026/05/attackers-use-llm-agent-for-post.html), pre-auth RCE affecting Marimo <= 0.20.4, patched in 0.23.0) and then handed post-exploitation to an LLM agent: it harvested two cloud credentials, pulled an SSH key from AWS Secrets Manager, opened eight parallel SSH sessions to a bastion, and exfiltrated a full PostgreSQL schema in under two minutes, with agentic tells including adaptive recon, `---` command delimiters with stderr suppression, and a leaked non-English comment. For 3.2.5, the takeaway is that an AI-adjacent notebook or eval surface left exposed is now a launch point for agent-driven lateral movement, so the harness should treat the eval/notebook hosts themselves as in-scope and verify they fail closed on unauthenticated code execution.

On the tooling side, NVIDIA shipped [garak v0.15.0 (May 1, 2026) and v0.15.1 (June 5, 2026)](https://github.com/NVIDIA/garak/releases). v0.15.0 adds a multi-turn GOAT probe, an "Agent breaker" probe that targets the tools exposed to an LLM agent, a system-prompt-extraction probe, NeMo Guardrails server support, and mTLS client-cert auth for the REST generator; v0.15.1 adds ProPILE PII-leakage probes, `simonw/llm` generator support, and JSON/file output modes for CI consumption. (v0.14.1 earlier added bootstrap confidence intervals on attack-success rates and a ModernBERT refusal detector.) For 3.2.1, 3.2.2, and 3.2.5, the Agent breaker and GOAT probes are worth adding to the gated suite, and the confidence-interval output gives a defensible way to set a threshold on attack-success rate rather than a single-run number. MITRE also moved ATLAS to a monthly cadence: the [atlas-data v2026.05 release](https://github.com/mitre-atlas/atlas-data/releases) (May 27, 2026, following the May 6 Secure AI v2 expansion that added 45+ techniques and 20+ case studies) tags every technique with platform labels including a distinct Agentic AI platform and moves to data format v6.0.0 with Pydantic schemas and a REST API -- so a versioned harness can programmatically pin the ATLAS data version it tested against and scope agent/MCP/RAG coverage per platform for 3.2.5 evidence.

Fresh benchmark and harness-audit work continued to fill the agent-evaluation gap. [HarnessAudit / HarnessAudit-Bench](https://arxiv.org/abs/2605.14271) (arXiv:2605.14271, May 14, 2026) audits full agent execution trajectories -- not only final outputs -- across boundary compliance, execution fidelity, and system stability, with 210 tasks over eight domains in single- and multi-agent configurations; evaluating 10 harness configurations, the authors find task completion does not guarantee safe execution and that violations compound as trajectories lengthen, with resource access and inter-agent information transfer as the primary failure areas. The UK AISI / Arcadia Impact [Inspect Evals](https://github.com/UKGovernmentBEIS/inspect_evals) collection added agentic-security evals -- AgentThreatBench (resilience to OWASP agentic threats such as memory poisoning and autonomy hijacking, scoring both utility and security), CodeIPI (indirect prompt injection embedded in software artifacts for coding agents), and the b3 "Backbone Breaker Benchmark" -- and from May 8, 2026 moved community contributions to an automated, bot-validated `/register/` submission flow, a concrete versioned-eval-harness registry pattern for 3.2.3 and 3.2.5. MLCommons also operationalized third-party gating with its [AILuminate Global Assurance Program](https://mlcommons.org/2026/02/ailuminate-global-assurance/) (announced Feb 19, 2026, backed by a coalition including KPMG, Google, Microsoft, and Qualcomm), which runs private, non-saturated safety benchmarks as "practice" and "official" testing and emits an AILuminate Risk Label -- a benchmarking-as-a-service pass/fail pattern for 3.2.3 and 3.2.4 -- alongside an [Agentic Product Maturity Ladder](https://mlcommons.org/ailuminate/agentic/) in early release-candidate form.

The compression-safety evidence for 3.2.7 also strengthened. [Quantization Undoes Alignment](https://arxiv.org/abs/2605.15208) (arXiv:2605.15208, May 2, 2026) tests Qwen2.5-7B, Mistral-7B, and Phi-3.5-mini from BF16 down to 3-bit and finds that even when perplexity rises less than 0.5% at 8-bit and under 3% at 4-bit, 2.5-5.6% of items develop new stereotypical or biased behavior at 4-bit and 6-21% at 3-bit, with "unknown"-answer selection dropping 17.4% -- a dose-response relationship confirmed by logistic regression and direct evidence that an accuracy-preserving quantization can still erode safety. [Aligned but Fragile](https://arxiv.org/abs/2605.29396) (arXiv:2605.29396, May 28, 2026) shows safety alignment degrades under lightweight parameter- and activation-level perturbations, more severely under W4A4 quantization, and proposes a zeroth-order-optimization training pass to harden alignment against such perturbations. Both reinforce that a passing accuracy re-eval is not a substitute for re-running the safety and bias suite on the exact compressed digest.

On the standards side, the EU Digital Omnibus on AI moved from the May 7 political agreement toward formal adoption: Coreper endorsed the provisional agreement on May 13, 2026 and the IMCO/LIBE committees adopted it 93-4-15 on June 2, 2026, ahead of a plenary vote in the June session, with the new high-risk compliance dates (Annex III stand-alone systems 2 December 2027, Annex I embedded systems 2 August 2028, AI-content marking 2 December 2026) now the deadlines release gates for high-risk AI must be planned against ([European Parliament legislative train](https://www.europarl.europa.eu/legislative-train/package-digital-package/file-digital-omnibus-on-ai)). The standards that will carry presumption of conformity also reached concrete stage-gates: per the [CEN-CENELEC newsletter](https://www.cencenelec.eu/news-events/news/2026/newsletter/ots-73-etuc/) (May 27, 2026), prEN 18286 (Quality Management System for EU AI Act purposes) entered Formal Vote and prEN 18228 (AI Risk Management, supporting Article 9) went to Public Enquiry through end of July 2026, with prEN 18283 (Managing Bias in AI) still in drafting -- these are the QMS, risk-management, and documented-testing standards that will define the validation and audit evidence high-risk providers must produce for 3.2.3, 3.2.4, and 3.2.6.

---

## Requirements

| # | Requirement | Level | Threat Mitigated | Verification Approach | Gaps / Notes |
|---|-------------|:-----:|-----------------|----------------------|--------------|
| **3.2.1** | **Verify that** models undergo automated input validation testing before deployment. | 1 | Deploying models vulnerable to prompt injection, jailbreaks, or producing unsafe/toxic outputs; regressions in safety behavior between versions. MITRE ATLAS [AML.T0051](https://atlas.mitre.org/techniques/AML.T0051) (LLM Prompt Injection), [AML.T0043](https://atlas.mitre.org/techniques/AML.T0043) (Craft Adversarial Data). Promptfoo's [GPT-5.2 trust-and-safety assessment](https://www.promptfoo.dev/blog/gpt-5.2-trust-safety-assessment/) (Dec 11, 2025) showed jailbreak attack success rising from a 4.3% single-prompt baseline to 61.0% with single-turn strategies and 78.5% with multi-turn Hydra-style chains in ~30 minutes of automated red teaming on a current frontier model -- evidence that input-validation gates built only against single-prompt suites underestimate real exposure. | Review CI/CD pipeline for automated test stages. Inspect test suites for input fuzzing, output content filtering, and safety red-teaming using tools like [Giskard Hub scans](https://docs.giskard.ai/hub/sdk/guides/scans) (OWASP LLM Top 10 tags, prompt-injection and hallucination categories, A-D scan grades), Deepchecks (tabular/NLP/vision validation with CI/CD hooks), Palo Alto Prisma AIRS (scans 35+ model formats for tampering and deserialization attacks), or Mindgard CART (continuous automated red teaming with GitHub Actions integration, tests on every commit). Open-source options include [Garak](https://github.com/NVIDIA/garak) (prompt injection, data leakage, hallucination, misinformation, toxicity, and jailbreak probes), [PyRIT](https://www.microsoft.com/en-us/security/blog/2024/02/22/announcing-microsofts-open-automation-framework-to-red-team-generative-ai-systems/) (Microsoft's risk identification framework with single-turn and multi-turn attack strategies), Johns Hopkins [Jailbreak Distillation (JBDistill)](https://arxiv.org/pdf/2505.22037) for renewable distilled-prompt benchmarks that retain ~81.8% attack effectiveness and generalize across 13 evaluation models, [PandaGuard / PandaBench](https://arxiv.org/pdf/2505.13862) for standardized attack/defense matrices across 49 LLMs, [SafeDialBench](https://arxiv.org/pdf/2502.11090) for fine-grained multi-turn dialogue safety, and Promptfoo (declarative test configs, automated scanning across 50+ vulnerability classes including jailbreaks, injections, RAG poisoning and policy violations; now backed by OpenAI after the [March 2026 acquisition](https://openai.com/index/openai-to-acquire-promptfoo/) but remains MIT-licensed). Include agent-tool prompt-injection coverage with [AgentDojo](https://arxiv.org/html/2406.13352v3) (97 user tasks and 629 security cases across banking, Slack, travel, and workspace domains, tracking benign utility, utility under attack, and attack success rate) so injection tests exercise tool chains, not just classifier inputs; recent results show learning-based attacks like AutoInject reaching ~77.96% ASR on Gemini-2.5-Flash while a dedicated preprocessor such as [PromptArmor](https://arxiv.org/abs/2507.15219) (ICLR 2026) drives the same workload below 1% ASR with under 1% false-positive and false-negative rates using GPT-4o, GPT-4.1, or o4-mini -- useful as a baseline defense to run alongside the gated test suite rather than a substitute for it. Pair functional input tests with mutation-style coverage: Meta's [Automated Compliance Hardening (ACH)](https://engineering.fb.com/2025/09/30/security/llms-are-the-key-to-mutation-testing-and-better-compliance/) system uses LLMs to synthesize privacy-relevant mutants and unit tests -- in production it has processed 10,795 Android Kotlin classes across 7 platforms, generating 9,095 mutants and 571 privacy-hardening test cases that engineers accepted at a 73% rate, of which 36% were judged privacy-relevant. Confirm pass/fail gates exist with defined thresholds, that failed tests block deployment, and that both single-turn and multi-turn jailbreak suites are exercised so a single aggregate score does not hide a category collapse. | Defining universal pass/fail thresholds for safety remains an open challenge -- thresholds are highly application-specific. [OWASP LLM01:2025](https://genai.owasp.org/llmrisk/llm01-prompt-injection/) notes that prompt injection can be direct, indirect, multimodal, obfuscated, or payload-split, so input tests need coverage beyond simple jailbreak strings. The OpenSSF [MLSecOps whitepaper](https://openssf.org/resources/visualizing-secure-mlops-mlsecops-a-practical-guide-for-building-robust-ai-ml-pipeline-security/) provides a practical reference architecture for data validation gates, secure artifact validation, model signing, and anomaly detection on incoming training batches. The CART approach (Continuous Automated Red Teaming) is emerging as best practice -- tools like Mindgard reduce testing timelines from months to minutes using curated attack libraries. Mutation-guided test generation (arXiv:2501.12862, accepted at FSE 2025 Industry track) is an emerging technique for systematically closing compliance gaps rather than relying on engineer-authored tests alone. Tooling for the gated suite keeps advancing: NVIDIA's [garak v0.15.0 (May 1, 2026) and v0.15.1 (June 5, 2026)](https://github.com/NVIDIA/garak/releases) add a multi-turn GOAT probe, an "Agent breaker" probe that targets the tools exposed to an agent, system-prompt-extraction and ProPILE PII-leakage probes, and JSON/file output for CI, while the earlier v0.14.1 added bootstrap confidence intervals on attack-success rates -- a more defensible basis for setting an input-validation threshold than a single-run number. *(Updated 2026-06)* |
| **3.2.2** | **Verify that** models undergo automated output sanitization testing before deployment. | 1 | Unsafe or toxic model outputs reaching end users; indirect prompt injection via crafted outputs; data exfiltration through output channels; downstream XSS, SQL injection, command injection, SSRF, privilege escalation, or remote code execution when model output is trusted by another component. OWASP LLM Top 10 maps this to [LLM05:2025 Improper Output Handling](https://genai.owasp.org/llmrisk/llm052025-improper-output-handling/). | Inspect CI/CD pipeline for output sanitization test stages -- verify tests cover toxic content detection, PII leakage, schema/format adherence, Markdown/HTML rendering, code execution, SQL generation, shell-command generation, and tool-call arguments. Use Giskard scan tags for hallucination, privacy exfiltration, harmful content, and prompt injection; Promptfoo security scans with structured assertions and JSON/HTML reports; and deterministic validators that reject outputs failing schemas, allowlists, output encoding, or policy checks before reaching downstream APIs. Confirm that output validation thresholds are defined, versioned, and enforced as deployment gates, and that red-team tests include payloads that turn model output into browser script, SQL, shell, API, or filesystem operations. | Output sanitization testing is more mature than input testing for text-only models, but multimodal output validation (image/audio/video generation) still lacks robust automated tooling. Treating model output as trusted application data remains a common design flaw; OWASP's 2025 guidance emphasizes treating model output like untrusted user input and validating before downstream use. The boundary between content filtering and censorship creates domain-specific tension that complicates universal thresholds, so teams should store policy-specific test cases, failed-output samples, and approval decisions as audit evidence. *(Updated 2026-05)* |
| **3.2.3** | **Verify that** models undergo safety evaluations with defined pass/fail thresholds before deployment. | 1 | Deploying models that fail safety benchmarks; silent regression in safety behavior between model versions; compliance violations under EU AI Act conformity assessment requirements. | Review safety evaluation pipeline for defined benchmarks -- look for SafetyBench (11,435 multiple-choice questions across 7 safety categories), TruthfulQA (817 questions measuring hallucination tendencies), [HarmBench](https://github.com/centerforaisafety/HarmBench) from the Center for AI Safety (a standardized red-teaming framework covering 400+ harmful behaviors across four functional categories -- standard, contextual, copyright, and multimodal -- with automated attack-success-rate classifiers; now the de facto benchmark for reproducible LLM safety comparisons), or domain-specific safety rubrics. Add [MLCommons AILuminate](https://mlcommons.org/benchmarks/ailuminate/) when general-purpose chat safety or jailbreak resistance is in scope: Safety v1.0 uses 12 hazard categories and over 24,000 prompts per language, while [AILuminate Jailbreak](https://mlcommons.org/ailuminate/jailbreak/) progressed from the v0.5 draft (text-to-text and text-plus-image-to-text adversarial coverage) to the [Feb 2026 v0.7 mechanism-first taxonomy](https://mlcommons.org/2026/02/jailbreak-0-7/) that separates *attack mechanism* from *hazard class* and lets a single attempt score against multiple hazards -- a clean basis for storing per-mechanism and per-hazard pass rates in the release record rather than a single aggregate. Promptfoo ships a ready-to-run [HarmBench integration guide](https://www.promptfoo.dev/docs/guides/evaling-with-harmbench/) so HarmBench prompts can be dropped into CI evaluation configs. Verify pass/fail thresholds are documented, versioned, and enforced as CI/CD gates. Confirm evaluation results are stored with model version identifiers. Inspect historical evaluation records to verify consistency across releases. | NIST AI RMF's MEASURE function covers bias testing, performance evaluation, and drift detection but does not prescribe specific safety thresholds. NIST's [Generative AI Profile, AI 600-1](https://www.nist.gov/publications/artificial-intelligence-risk-management-framework-generative-artificial-intelligence) was updated April 8, 2026 and remains a useful cross-sector profile for evaluation planning; NIST also released a concept note for an [AI RMF Profile on Trustworthy AI in Critical Infrastructure](https://www.nist.gov/itl/ai-risk-management-framework) on April 7, 2026. [ISO/IEC 42001:2023](https://www.iso.org/standard/42001) aligns management-system requirements with documented risk treatment, traceability, transparency, and reliability evidence. On May 7, 2026, the European Commission announced a Digital Omnibus political agreement moving high-risk AI rules in specified areas to 2 December 2027 and product-integrated high-risk systems to 2 August 2028; [Article 43](https://ai-act-service-desk.ec.europa.eu/en/ai-act/article-43) still requires a new conformity assessment after substantial modification. No single benchmark covers all safety dimensions; MLCommons' April 2026 [continuous prompt stewardship](https://mlcommons.org/2026/04/continuous-prompt-stewardship/) note is a useful reminder that benchmarks degrade as models improve and test data becomes exposed. NIST's May 3, 2026 [CAISI Evaluation of DeepSeek V4 Pro](https://www.nist.gov/news-events/news/2026/05/caisi-evaluation-deepseek-v4-pro) is a workable reference format for release-gate evidence: 9 benchmarks across 5 domains, with per-domain pass rates (e.g., 32% cybersecurity vs 71% baseline, 44% agentic software engineering vs 78%) rather than a single aggregate score. NIST CAISI's March 2026 monitoring paper also warns that pre-deployment evaluations happen in controlled environments, so release thresholds should hand off to post-deployment monitoring rather than standing alone. Third-party gating patterns are maturing alongside the regulatory deadlines: MLCommons' [AILuminate Global Assurance Program](https://mlcommons.org/2026/02/ailuminate-global-assurance/) runs private, non-saturated safety benchmarks as "practice" and "official" testing and emits an AILuminate Risk Label, and the UK AISI / Arcadia Impact [Inspect Evals](https://github.com/UKGovernmentBEIS/inspect_evals) collection added agentic-security evals (AgentThreatBench, CodeIPI, the b3 Backbone Breaker Benchmark) with a May 8, 2026 automated registry flow that is a workable versioned-harness pattern. On the regulatory side, the EU Digital Omnibus on AI advanced from political agreement toward adoption (Coreper May 13, 2026; IMCO/LIBE committees 93-4-15 on June 2, 2026), fixing the high-risk conformity-assessment deadlines (Annex III stand-alone 2 December 2027, Annex I embedded 2 August 2028) that release gates must plan against ([European Parliament legislative train](https://www.europarl.europa.eu/legislative-train/package-digital-package/file-digital-omnibus-on-ai)). *(Updated 2026-06)* |
| **3.2.4** | **Verify that** deployment is automatically blocked when safety evaluation results do not meet the thresholds defined for the model being deployed. | 1 | Failed or ignored safety gates allowing a known-unsafe model into production; prompt injection, jailbreak, harmful-content, hallucination, and output-handling regressions becoming user-facing defects; emergency releases that normalize bypassing validation. This maps to MITRE ATLAS [AML.T0051](https://atlas.mitre.org/techniques/AML.T0051) (LLM Prompt Injection), [AML.T0043](https://atlas.mitre.org/techniques/AML.T0043) (Craft Adversarial Data), and OWASP [LLM01:2025 Prompt Injection](https://genai.owasp.org/llmrisk/llm01-prompt-injection/) / [LLM05:2025 Improper Output Handling](https://genai.owasp.org/llmrisk/llm052025-improper-output-handling/). The [UK NCSC's December 2025 prompt-injection guidance](https://www.ncsc.gov.uk/blog-post/prompt-injection-is-not-sql-injection) is especially relevant: it warns that LLM prompts do not preserve a hard security boundary between instructions and data, so a passing gate needs deterministic deployment controls rather than trust in model self-restraint. Time-to-exploit pressure on the gate itself keeps shrinking: [PraisonAI CVE-2026-44338](https://github.com/advisories/GHSA-6rmh-7xcm-cpxj) (auth bypass in the legacy Flask API, fixed in 4.6.34) was first targeted by a `CVE-Detector/1.0` scanner within [3 hours 44 minutes of advisory publication](https://www.sysdig.com/blog/cve-2026-44338-praisonai-authentication-bypass-in-under-4-hours-and-the-growing-trend-of-rapid-exploitation) on May 11, 2026, so a "deploy is blocked on failed gate" policy must include the pipeline's own dependencies and re-run the gate inside the measured time-to-exploit window for the harness, not only when the model changes. | Trigger a deliberate failing eval and confirm the release job stops before registry promotion, endpoint rollout, traffic shifting, or model card publication. Inspect pipeline policy for explicit thresholds tied to the model identifier and deployment target: Promptfoo supports `--fail-on-error`, JSON result parsing, JUnit reports, and pass-rate quality gates in CI; [Giskard Hub scans](https://docs.giskard.ai/hub/sdk/guides/scans) expose A-D scan grades plus an insufficient-data result and show CI examples that exit non-zero when the grade is outside the allowed set; LangSmith's CI/CD example uses offline evaluations and quality-gated production releases for agent deployments. For Kubernetes or model-serving platforms, verify that admission policy (OPA Gatekeeper/Kyverno), registry metadata, or deployment controller checks require a passing eval attestation containing the model version or digest, eval suite version, threshold config, timestamp, and reviewer/approver identity for any approved exception. | The hard part is threshold governance, not the pipeline mechanics. Pass/fail criteria need to be versioned per use case, threat model, language, modality, and deployment context; a single aggregate score can hide a critical failure category. LLM-as-judge evaluations are useful for triage but can be nondeterministic, so blocking decisions should combine deterministic assertions, curated adversarial regression tests, and retained raw evidence. False positives can pressure teams to weaken gates, while false negatives can create misplaced confidence. As of May 2026, [CAISI's pre-deployment evaluation agreements](https://www.nist.gov/news-events/news/2026/05/caisi-signs-agreements-regarding-frontier-ai-national-security-testing) show increasing expectation for independent evaluation of frontier models, but most enterprise application teams still need to build their own enforceable release gates around application-specific risks. *(Updated 2026-05)* |
| **3.2.5** | **Verify that** security testing covers agent workflows, tool and MCP integrations, RAG and memory interactions, multimodal inputs, and guardrails (safety models or detection services) using a versioned evaluation harness. At minimum, tests demonstrate rejection of prompt injection probes from a maintained corpus and correct sanitization of tool outputs before model context inclusion. | 2 | Exploits through untested agent tool chains; RAG poisoning via unvalidated retrieval contexts; MCP server compromise; multimodal bypass of text-only guardrails; MITRE ATLAS [AML.T0015](https://atlas.mitre.org/techniques/AML.T0015) (Evade ML Model). The [OWASP Top 10 for Agentic Applications](https://genai.owasp.org/2025/12/09/owasp-top-10-for-agentic-applications-the-benchmark-for-agentic-security-in-the-age-of-autonomous-ai/) identifies agent goal hijack, tool misuse, identity abuse, agentic supply-chain vulnerabilities, unexpected code execution, memory/context poisoning, insecure inter-agent communication, cascading failures, human-agent trust exploitation, and rogue agents as first-class risks. The OpenClaw incident (early 2026) -- an open-source AI agent with 135,000+ GitHub stars exposing 21,000+ instances through malicious marketplace exploits -- underscores the real-world risk of untested agent integrations. CVE-2025-59536 and CVE-2026-21852 (patched in Claude Code 2.0.65+, disclosed Q1 2026) covered MCP command-injection paths, and a Cursor advisory documented similar RCE risk when an OAuth-authenticated client connects to an untrusted MCP server. The same STDIO design pattern produced [CVE-2025-49596](https://www.oligo.security/blog/critical-rce-vulnerability-in-anthropic-mcp-inspector-cve-2025-49596) in Anthropic's MCP Inspector (CVSS 9.4 RCE via DNS rebinding through the local dev tool), [CVE-2026-22252](https://www.sentinelone.com/vulnerability-database/cve-2026-22252/) in LibreChat (arbitrary commands accepted by the MCP stdio transport, fixed in v0.8.2-rc2), and [CVE-2025-54994](https://github.com/advisories/GHSA-3ch2-jxxc-v4xf) in `@akoskm/create-mcp-server-stdio` (Node.js `exec` concatenation in the `which-app-on-port` tool, fixed in 0.0.13). Independent scans paint the same picture at scale: [AgentSeal's review of 1,808 publicly accessible MCP servers](https://agentseal.org/blog/mcp-server-security-findings) found 66% with security findings and ~36.7% SSRF exposure on URL-accepting endpoints. Model-loader code paths are now in scope as well: [CVE-2026-22807](https://github.com/vllm-project/vllm/security/advisories/GHSA-2pc9-4j83-qjmr) in vLLM loaded Hugging Face `auto_map` dynamic modules during model resolution without checking `--trust-remote-code` (fixed in v0.14.0), and [CVE-2026-27893](https://github.com/vllm-project/vllm/security/advisories/GHSA-7972-pg2x-xr59) (CVSS 8.8, fixed in v0.18.0) hardcoded `trust_remote_code=True` in NemotronVL and Kimi-K25 implementations, silently overriding an operator opt-out. NVD records from March-April 2026 add concrete adapter-layer cases: [CVE-2026-39884](https://nvd.nist.gov/vuln/detail/CVE-2026-39884) exposed Kubernetes port-forward argument injection through an MCP server, [CVE-2026-30624](https://nvd.nist.gov/vuln/detail/CVE-2026-30624) allowed Agent Zero external MCP server configuration to execute commands, and [CVE-2026-27826](https://nvd.nist.gov/vuln/detail/CVE-2026-27826) let MCP Atlassian make unauthenticated SSRF requests outside the tool handler path. April-May 2026 disclosures add [CVE-2026-30623](https://docs.litellm.ai/blog/mcp-stdio-command-injection-april-2026) in the Anthropic MCP SDK STDIO transport (command-field execution across Python/TypeScript/Java/Rust SDKs, roughly 200,000 server instances in scope) and [CVE-2026-26118](https://nvd.nist.gov/vuln/detail/CVE-2026-26118) in Azure MCP Server Tools (authenticated SSRF that exfiltrates managed-identity tokens through the IMDS endpoint), with ModelScope's [CVE-2026-2256](https://github.com/advisories/GHSA-4gc2-344q-r2rw) showing the same pattern at the agent-framework Shell tool. On May 13, 2026, a researcher disclosed three database-MCP issues the same day -- Apache Doris MCP SQL injection (CVE-2025-66335, fixed in v0.6.1), Apache Pinot MCP v1.1.0 unauthenticated HTTP transport, and an Alibaba RDS MCP information disclosure that the vendor declined to fix -- showing that the MCP regression surface now extends into database, search, and data-platform adapters, not only STDIO and workflow-builder cases. [CVE-2026-4192](https://www.sentinelone.com/vulnerability-database/cve-2026-4192/) (quip-mcp-server), [CVE-2026-5023](https://www.sentinelone.com/vulnerability-database/cve-2026-5023/) (codebase-mcp `getCodebase`/`getRemoteCodebase`/`saveCodebase` OS command injection, unpatched at disclosure), and the figma-developer-mcp issue tracked as [CVE-2025-53967](https://github.com/GLips/Figma-Context-MCP/security/advisories/GHSA-gxw4-4fc5-9gr5) (unsanitized `child_process.exec` in `get_figma_data`, fixed in v0.6.3) are useful additional regression cases for SaaS-integration MCPs. The LLM application framework layer itself is now in scope: [CVE-2025-68664](https://github.com/advisories/GHSA-r399-636x-v7f6) (LangChain Core "LangGrinch," CVSS 9.3) lets prompt-injected `additional_kwargs`/`response_metadata` survive `dumps()`/`dumpd()` serialization and reach downstream consumers; [CVE-2026-34070](https://medium.com/@imr1ck/cve-2026-34070-how-i-discovered-a-high-severity-path-traversal-in-langchain-core-38b0828a34d8) (path traversal in the prompt-loading API, fixed in `langchain-core` 1.2.22) opens arbitrary files through unsanitized config paths; and [CVE-2025-67644](https://thehackernews.com/2026/03/langchain-langgraph-flaws-expose-files.html) (LangGraph SQLite checkpoint SQL injection) lets attacker-controlled metadata change checkpoint queries. Microsoft's [May 7, 2026 Semantic Kernel disclosures](https://www.microsoft.com/en-us/security/blog/2026/05/07/prompts-become-shells-rce-vulnerabilities-ai-agent-frameworks/) add [CVE-2026-25592](https://nvd.nist.gov/vuln/detail/CVE-2026-25592) (CVSS 10.0, .NET SDK sandbox escape through an accidentally exposed `[KernelFunction]` file-write helper, fixed in 1.71.0) and [CVE-2026-26030](https://nvd.nist.gov/vuln/detail/CVE-2026-26030) (CVSS 9.8, Python `InMemoryVectorStore` filter expressions reaching `eval()` through an AST-blocklist bypass, fixed in 1.39.4) -- direct examples that the agent framework's exposed-function surface and any filter/query expression the model can populate need their own deterministic validation independent of the model's refusal behavior. The model-serving framework is in scope too: SGLang [CVE-2026-3059](https://feedly.com/cve/CVE-2026-3059) and [CVE-2026-3060](https://www.sentinelone.com/vulnerability-database/cve-2026-3060/) (CVSS 9.8, unpatched at disclosure per [CERT/CC VU#665416](https://kb.cert.org/vuls/id/665416)) ship a ZMQ broker that calls `pickle.loads()` on unauthenticated payloads in multimodal generation and encoder parallel disaggregation modes, and vLLM [CVE-2026-22778](https://github.com/vllm-project/vllm/security/advisories/GHSA-4r2x-xpjr-7cvv) (CVSS 9.8, fixed in v0.14.1) chains a PIL heap-address leak with a JPEG2000 heap overflow in OpenCV/FFmpeg so a single malicious video URL submitted to a vLLM multimodal endpoint reaches RCE on the inference host. SaaS agent-builder and MCP adapters add more regression-suite inputs: [CVE-2026-44284](https://github.com/labring/FastGPT/security/advisories/GHSA-cxxj-99f7-f5wq) (FastGPT stored MCP-URL SSRF, fixed in 4.14.17, May 6, 2026), [CVE-2026-39974](https://nvd.nist.gov/vuln/detail/CVE-2026-39974) (n8n-MCP multi-tenant HTTP-header SSRF, fixed in 2.47.4), [CVE-2026-7220](https://vuldb.com/vuln/359820) (`fastly-mcp-server` PowerShell command injection via `child_process.exec`), and [CVE-2026-5603](https://dev.to/armor1ai/how-to-check-your-mcp-server-for-cve-2026-5603s-vulnerability-pattern-and-why-shellquote-isnt-gdn) (`@elgentos/magento2-dev-mcp` `shell-quote` bypass on Windows `cmd.exe`). The May 11-20, 2026 window pulled three more cases into the harness scope: [Spring AI CVE-2026-22742](https://spring.io/security/cve-2026-22742/) (CVSS 8.6, fixed in 1.0.5/1.1.4) is a multimodal-media SSRF in `BedrockProxyChatModel` where user-supplied media URLs reached server-side fetch without scheme/host validation, hitting AWS IMDS at `169.254.169.254` and other internal endpoints; [mcp-atlassian CVE-2026-27825](https://arcticwolf.com/resources/blog-uk/cve-2026-27825-critical-unauthenticated-rce-and-ssrf-in-mcp-atlassian/) (CVSS 9.1, fixed in v0.17.0, Feb 24, 2026) chains with the already-listed CVE-2026-27826: the `download_attachment` and `download_content_attachments` tools accepted attacker-supplied paths with no base-dir or symlink check, so a network-adjacent unauthenticated caller against an HTTP-transport instance bound to `0.0.0.0` could overwrite `~/.ssh/authorized_keys` or `~/.bashrc` in two requests; and [mcp-browser-agent CVE-2026-5607](https://www.sentinelone.com/vulnerability-database/cve-2026-5607/) (SSRF via `request.params.name`/`request.params.arguments` in `src/handlers.ts` and `src/executor.ts`, no allowlist or guard, vendor non-response) is the recurring "unpatched vendor" case that has to be reflected in the deployed allowlist rather than waiting for an upstream fix. The model-serving runtime itself is now in scope: [Ollama Bleeding Llama CVE-2026-7482](https://www.cyera.com/research/bleeding-llama-critical-unauthenticated-memory-leak-in-ollama) (CVSS 9.1, heap out-of-bounds read in the GGUF loader, ~300,000 internet-reachable instances, fixed silently in v0.17.1 on Feb 25, 2026) lets unauthenticated callers exfiltrate the Ollama process heap in three API calls -- system prompts, other users' chat fragments, environment variables holding API keys, and any PII flowing through inference -- with no error logs. Cloud-platform MCP wrappers and authenticated-only configuration sinks keep producing fresh release-gate cases too: [CVE-2026-32211](https://cvefeed.io/vuln/detail/CVE-2026-32211) in `@azure-devops/mcp` (CVSS 9.1, missing authentication for critical function, unpatched at disclosure) is distinct from the Azure MCP SSRF case (CVE-2026-26118); [CVE-2026-22688](https://github.com/Tencent/WeKnora/security/advisories/GHSA-78h3-63c4-5fqc) in Tencent/WeKnora (CVSS 9.9, fixed in v0.2.5) lets an authenticated non-admin user inject `stdio_config.command`/`args` through the `/test` flow; and [CVE-2026-20205](https://www.sentinelone.com/vulnerability-database/cve-2026-20205/) in Splunk MCP Server app (CVSS 7.2, CWE-532, fixed in v1.0.3, [SVD-2026-0407](https://advisory.splunk.com/advisories/SVD-2026-0407)) writes session and authorization tokens to the `_internal` index in clear text. | Inspect the versioned evaluation harness for test coverage across: (1) agent tool-call chains with adversarial tool responses, (2) RAG retrieval with poisoned documents, (3) MCP server interaction fuzz testing, including crafted OAuth flows, command/argument injection payloads, malicious external-server JSON, allowlist-bypass attempts such as `npx -c`, SSRF probes against cloud metadata endpoints (169.254.169.254, loopback, and DNS-rebinding targets), parameterized SQL through database-MCP tools (e.g., Apache Doris/Pinot/Alibaba RDS-style adapters), unauthenticated HTTP transport checks, LLM application framework regression probes (prompt-injection-induced LangChain serialization payloads against `dumps()`/`dumpd()`, path-traversal payloads against prompt-template and config loaders, LangGraph SQLite metadata-filter SQL injection), an enumeration of every method registered as a kernel/tool/`[KernelFunction]` so accidentally exposed internal helpers cannot reach the model (Semantic Kernel `DownloadFileAsync` pattern in CVE-2026-25592), AST-bypass payloads against every filter/query/expression parameter the model can populate (Semantic Kernel `InMemoryVectorStore` pattern in CVE-2026-26030 reaching `eval()` through dunder traversal), unauthenticated pickle-deserialization probes against any model-serving broker that exposes ZMQ or similar transports (SGLang CVE-2026-3059/CVE-2026-3060), and hostile multimodal media uploads against the actual serving stack including malicious video URLs through OpenCV/FFmpeg decoders (vLLM CVE-2026-22778); stored-vs-runtime SSRF differential checks against agent-builder MCP tool configuration (FastGPT CVE-2026-44284), multi-tenant HTTP-header SSRF (n8n-MCP CVE-2026-39974), multimodal-media URL SSRF against any LLM SDK that fetches user-supplied media server-side (Spring AI Bedrock Converse CVE-2026-22742), path-traversal RCE through MCP attachment/download tools bound to network interfaces (mcp-atlassian CVE-2026-27825 chained with the previously-listed CVE-2026-27826), unguarded URL parameters in browser-automation MCPs where the vendor has not patched (mcp-browser-agent CVE-2026-5607), unauthenticated heap-leak probes against the model-serving HTTP endpoint that loads attacker-supplied artifacts (Ollama Bleeding Llama CVE-2026-7482), and cross-platform command-injection runs against every MCP tool on at least one POSIX and one Windows runner so a `shell-quote`-style bypass fails closed (fastly-mcp-server CVE-2026-7220, `@elgentos/magento2-dev-mcp` CVE-2026-5603); reference indexes such as the [Vulnerable MCP Project](https://vulnerablemcp.info/) are useful for keeping coverage current, but each finding should be validated against NVD or the upstream patch before promotion into a release-gate test, (4) multimodal inputs (adversarial images triggering unsafe text), (5) guardrail bypass attempts, including both single-turn and multi-turn Hydra-style jailbreak chains given that current frontier evaluations see attack success climb several-fold between single-prompt and multi-turn strategies. Verify harness is in source control and tests run in CI. Map coverage to the [OWASP Top 10 for Agentic Applications 2026](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/) (the 2026 list covers goal misalignment, tool misuse, delegated trust, inter-agent communication, persistent memory, and emergent autonomous behavior); Confident AI's [DeepTeam](https://www.trydeepteam.com/docs/frameworks-owasp-top-10-for-agentic-applications) ships an `OWASP_ASI_2026()` framework call that runs the 2026 risk assessment in code or in the Confident AI platform. Use [AgentDojo](https://arxiv.org/html/2406.13352v3) as a standardized harness for tool-using agents (97 user tasks, 629 security cases across banking, Slack, travel, and workspace domains, reporting benign utility, utility under attack, and attack success rate). Add behavior-jailbreak coverage with [LITMUS](https://arxiv.org/html/2605.10779v1) (semantic-physical dual verification plus OS-state rollback for irreversible host operations), [AgentHarm](https://arxiv.org/pdf/2410.09024), and [OS-Harm](https://arxiv.org/pdf/2506.14866) for computer-use agents. Round out long-horizon agent coverage with [OpenAgentSafety](https://arxiv.org/abs/2507.06134) (ICLR 2026, 350+ multi-turn executable tasks across eight risk categories on the OpenHands agentic scaffold in a sandboxed Docker environment with benign, ambiguous, and adversarial users) and [DoomArena](https://arxiv.org/abs/2504.14064) (configurable threat-model plug-in framework for web and tool-calling agents) so the harness exercises multi-tool, multi-intent scenarios alongside single-turn jailbreak suites. Use Giskard Hub for agent vulnerability scans mapped to OWASP categories (its [December 2025 release](https://docs.giskard.ai/hub/ui/release_notes/2025-12-16.html) added ChatInject and CoT Forgery probes for LLM01 prompt injection through native chat templates and synthetic reasoning traces), Mindgard CART for continuous adversarial testing with thousands of threat scenarios -- its March 2026 [reconnaissance module](https://mindgard.ai/blog/continuous-automated-red-teaming) discovers guardrails, system prompts, tools, integrations, and external services so harnesses run against the actual deployed surface, not a static prompt list -- FuzzyAI for bias/toxicity/data leakage testing at enterprise scale, or custom harnesses with Promptfoo. Promptfoo's [MCP security testing guide](https://www.promptfoo.dev/docs/red-team/mcp-security-testing/) explicitly tests tool poisoning, side-channel data exfiltration, authentication hijacking, rug pulls, tool shadowing, indirect prompt injection, and cross-server attacks by exercising real agent behavior against benign and malicious MCP servers. DeepScan AI maps test results to MITRE ATLAS and OWASP frameworks for compliance reporting. LangSmith, the observability companion to LangChain, has become a common choice for tracing every step in agentic pipelines and running structured evaluation suites that tie individual agent decisions to prompt and model versions. | MCP security testing remains nascent -- few established frameworks exist for systematically testing MCP server interactions in adversarial conditions, though the emergence of MCP gateways and dedicated MCP vulnerability guidance through 2026 is starting to close that gap. Tool-level tests are not enough when vulnerabilities live in HTTP middleware, stdio transport configuration, command wrappers, or adapter code around the tool. Anthropic's stance on [CVE-2026-30623](https://thehackernews.com/2026/04/anthropic-mcp-design-vulnerability.html) -- that STDIO command execution is by-design -- means downstream patching responsibility falls on every integrator, so harness evidence should record the exact SDK and wrapper version at release. The May 7, 2026 Semantic Kernel disclosures show the same lesson at the agent framework layer: a single accidentally exposed `[KernelFunction]` or an unsafe filter-expression path can convert prompt injection into host RCE without ever touching MCP, so the harness has to enumerate the actual exposed-function surface rather than rely on the model's refusal behavior. Multimodal attack testing (e.g., adversarial images triggering unsafe text outputs) requires specialized tooling like SafeBench (multimodal LLM safety framework); HarmBench's multimodal category is another partial option. OWASP's agentic work is useful for structuring harness coverage because it separates model behavior, tool authority, delegated identity, memory, inter-agent messaging, and human approval risks. The SecureAI-Flow framework proposes multi-agent pipeline security where specialized agents autonomously perform adversarial robustness checks and behavioral anomaly detection at each CI/CD stage, though it remains a preprint and has not yet seen wide production adoption. The June 2026 disclosures show the gateway and base-library layers are now squarely in scope: [LiteLLM CVE-2026-42271](https://thehackernews.com/2026/06/litellm-flaw-cve-2026-42271-exploited.html) (CVSS 8.7, fixed in 1.83.7) is a command injection in the MCP preview routes `POST /mcp-rest/test/connection` and `POST /mcp-rest/test/tools/list` that accepted full stdio server config in the body, chained with the Starlette host-header bypass [CVE-2026-48710](https://cybelangel.com/blog/itellm-vulnerability-cve-2026-42271/) to reach unauthenticated RCE and added to the CISA KEV catalog on June 8, 2026; [Hugging Face Transformers CVE-2026-4372](https://www.csoonline.com/article/4181094/hugging-face-transformers-rce-flaw-enables-stealthy-compromise-via-ai-model-configs.html) (fixed in 5.3.0) bypasses `trust_remote_code=False` through an underscore-prefixed `_attn_implementation_internal` config key that `hub_kernels.py` resolves to attacker code; CrewAI (CERT/CC [VU#221883](https://kb.cert.org/vuls/id/221883)) silently falls back from a Docker sandbox to in-process `ctypes` execution under prompt injection; and SGLang [CVE-2026-5760](https://kb.cert.org/vuls/id/915947) (CVSS 9.8, unpatched at coordination close) renders a poisoned GGUF `tokenizer.chat_template` through an unsandboxed Jinja2 environment on `/v1/rerank`. The first in-the-wild case of an autonomous agent running post-exploitation (Sysdig, May 10, 2026, against an exposed Marimo notebook, [CVE-2026-39987](https://thehackernews.com/2026/05/attackers-use-llm-agent-for-post.html)) underscores that the eval and notebook hosts around the harness are themselves attack surface. MITRE's [ATLAS atlas-data v2026.05](https://github.com/mitre-atlas/atlas-data/releases) now tags techniques with a distinct Agentic AI platform and ships a machine-readable v6.0.0 schema, so a versioned harness can pin the threat-data version it tested against. *(Updated 2026-06)* |
| **3.2.6** | **Verify that** all model changes (deployment, configuration, retirement) generate immutable audit records. Each record includes a timestamp, an authenticated actor identity, a change type, before/after states, trace metadata (environment and consuming services/agents), and a model identifier (version/digest/signature). | 2 | Unauthorized model changes going undetected; inability to reconstruct incident timelines; compliance failures under EU AI Act conformity assessment; repudiation of destructive actions (e.g., unauthorized model retirement). IBM's 2025 Cost of a Data Breach Report found 63% of breached organizations lacked AI governance policies -- missing audit trails directly contribute to delayed detection and higher breach costs. | Review audit log storage for immutability guarantees (append-only logs, WORM storage, or blockchain-anchored). Verify logs contain all required fields (timestamp, actor, change type, before/after states, environment, model version/digest) by inspecting sample entries. Test immutability by attempting to modify or delete a log entry. Confirm logs capture all change types: deployment, configuration change, retirement, and rollback. Tools: AWS CloudTrail, GCP Cloud Audit Logs, Azure Monitor, MLflow Model Registry (webhook-based event logging with Unity Catalog for centralized governance and cross-workspace lineage tracking), or Databricks Model Registry. Every model version in the registry should be immutable once registered -- overwriting artifacts destroys the audit trail and makes rollback impossible. Where model signing is in scope, inspect Sigstore or equivalent signature verification records and require deployment logs to bind the model digest, signature, runtime environment, and consuming service or agent. The [OpenSSF Model Signing (OMS) specification](https://github.com/ossf/model-signing-spec) follows the Sigstore Bundle Format and is PKI-agnostic (bare keys, certificate chains, or keyless Sigstore identities); NVIDIA NGC and Google Kaggle have begun adopting OMS for ML artifacts, so registry audit logs should record the OMS bundle reference alongside the model version. | Immutable audit logging for infrastructure events is well-supported by cloud providers, but capturing all model lifecycle events -- including fine-tuning runs, configuration changes, and agent-level model swaps -- with the required metadata fields remains a gap in most MLOps setups. MLflow's Model Registry supports stage transitions, lineage tracking, and version-level annotations; Databricks extends this via Unity Catalog integration for centralized governance with fine-grained access control and cross-workspace lineage tracking. Each registered model version links to the originating MLflow run, enabling full reproducibility -- deleting versions should be avoided to maintain a complete immutable history. The EU AI Act Articles 11-12 require automatic logging sufficient for traceability over the system's lifetime, including vulnerability existence at each release, remediation history, and machine-readable SARIF-format scan results; high-risk system records must be retained for 10 years after the system is taken off the market (financial services may require 7 years). Immutability can be enforced via SHA-256 hash chains, digital signatures, Merkle trees, or WORM storage. ISO/IEC 42001 aligns quality management system requirements with audit trail expectations. The OpenSSF MLSecOps whitepaper recommends applying Sigstore, SLSA, GUAC, and model-signing patterns to ML artifacts so audit logs can be tied to verifiable provenance rather than only registry metadata. Loader-bypass cases such as vLLM [CVE-2026-22807](https://github.com/vllm-project/vllm/security/advisories/GHSA-2pc9-4j83-qjmr) and [CVE-2026-27893](https://github.com/vllm-project/vllm/security/advisories/GHSA-7972-pg2x-xr59) reinforce that audit evidence should bind the model digest to the exact serving-stack version and the `trust_remote_code` configuration in effect at load time, not only the registry stage and approver identity. The Ollama Windows auto-updater chain ([CVE-2026-42248](https://www.helpnetsecurity.com/2026/05/05/ollama-windows-vulnerabilities-cve-2026-42248-cve-2026-42249/) signature-verification no-op plus [CVE-2026-42249](https://github.com/advisories/GHSA-7mp6-jmch-f3fh) path traversal in update headers, both CVSS 4.0 7.7, affecting 0.12.10-0.17.5) is a useful illustration that an auto-updated inference container changes the "what is actually running" answer without a corresponding registry event -- audit records should capture the serving binary's signature posture and updater configuration alongside the model digest, and a silent serving-runtime version change should appear in the same immutable audit stream as a model swap. Ollama Bleeding Llama [CVE-2026-7482](https://www.cyera.com/research/bleeding-llama-critical-unauthenticated-memory-leak-in-ollama) reinforces the same lesson at the release-note layer: the patch shipped in v0.17.1 on Feb 25, 2026 without a security label, so audit pipelines that only consume "security-flagged" upstream notes missed a critical heap-leak fix for three months. Policy-as-code approaches (OPA Gatekeeper, automated compliance checks before stage transitions) are emerging as the recommended pattern for enforcing governance rules programmatically. Recent supply-chain incidents argue for binding release-artifact attestations into the same record as the model audit log: Anthropic shipped Claude Code v2.1.88 to npm with an unintended 59.8 MB source map that pointed at a Cloudflare R2 zip containing 513,000 lines of TypeScript, and the May 11, 2026 Mini Shai-Hulud worm hijacked the TanStack `release.yml` to publish 84 malicious npm versions in roughly six minutes, cascading into Mercor and exfiltrating ~4 TB of AI training-data references; an immutable model-level audit that does not include the release-pipeline scan or packaging attestation cannot catch either case. Practical unified-evidence mappings are emerging here: a single model-versioning control satisfies SOC 2 CC8, ISO/IEC 42001 clause 8, and AI Act Annex IV; a prompt and completion audit trail satisfies SOC 2 CC7, ISO/IEC 42001 clause 9, and AI Act Article 12 -- adopting one canonical evidence schema is generally cheaper than maintaining parallel pipelines per framework. The June 2026 [Hugging Face Transformers CVE-2026-4372](https://www.csoonline.com/article/4181094/hugging-face-transformers-rce-flaw-enables-stealthy-compromise-via-ai-model-configs.html) (config-key `trust_remote_code` bypass, fixed in 5.3.0) reinforces that the audit record has to bind the model digest to the exact base-library version and config-trust posture in effect at load time, not only the registry stage and serving-framework version. The EU conformity-standards picture is also firming up: per the [CEN-CENELEC newsletter](https://www.cencenelec.eu/news-events/news/2026/newsletter/ots-73-etuc/) (May 27, 2026), prEN 18286 (Quality Management System for EU AI Act purposes) is in Formal Vote and prEN 18228 (AI Risk Management) is in Public Enquiry, so the documented-evidence and traceability expectations these standards encode will shape what high-risk audit records must contain. *(Updated 2026-06)* |
| **3.2.7** | **Verify that** models subjected to post-training quantization, pruning, or distillation are re-evaluated against the same safety and alignment test suite on the compressed artifact before deployment. Evaluation results are retained as distinct records linked to the compressed artifact's version or digest. | 2 | Safety alignment degradation after compression; adversarial robustness loss in quantized/pruned models; deploying compressed models that no longer satisfy safety thresholds established for the uncompressed version. MITRE ATLAS [AML.T0043](https://atlas.mitre.org/techniques/AML.T0043) (Craft Adversarial Data) -- compressed models may be more susceptible to adversarial examples. Research from January 2026 ([arXiv:2601.12033](https://arxiv.org/abs/2601.12033)) demonstrates that non-English safety is particularly vulnerable to quantization, with Korean and Arabic showing substantial safety declines. | Run the full safety/alignment test suite against the compressed artifact and compare results to the uncompressed baseline. Specifically evaluate with SafetyBench (multiple-choice safety questions), TruthfulQA, Do-Not-Answer, HEx-PHI (attack success rates), MultiJail (multilingual safety across English, Korean, Arabic), OpenSafetyMini (1,067-question benchmark for distinguishing quantization-method safety impacts), and [HarmBench](https://github.com/centerforaisafety/HarmBench) (standard, contextual, copyright, and multimodal categories; use its automated classifiers to compute ASR before and after compression so regressions are directly comparable). For fairness evaluation, include StereoSet, CrowS-Pair, Jigsaw, and MBBQ (4-language bias benchmark). Verify that evaluation records are stored with a distinct version/digest identifier for the compressed model. Confirm CI/CD gates enforce re-evaluation before compressed models reach production. Check that hybrid compression pipelines (prune-then-quantize) trigger re-evaluation at each compression stage. | Research from February 2025 (arXiv:2502.15799) shows 4-bit quantization generally maintains safety, but 2-bit precision introduces pronounced safety declines -- particularly for certain architectures. A Nature study (2026) on phase transitions in LLM compression found that performance degradation follows a pattern of gradual decline followed by abrupt catastrophic collapse once a critical compression threshold is reached -- 4-bit achieves 43.1% size reduction with only 0.51% perplexity loss, but 2-bit causes a steep 7.94% accuracy drop. Dynamic quantization methods (FP8, LLM.int8()) demonstrate the best safety/fairness preservation, while static methods (GPTQ, AWQ) lack consistency. Critical Weight Protection ([arXiv:2601.12033](https://arxiv.org/abs/2601.12033)) identifies safety-critical weights via sensitivity scoring and preserves them in FP16 while quantizing the remainder to INT4 -- avoiding expensive retraining. [Q-Resafe](https://arxiv.org/abs/2506.20251) (arXiv:2506.20251, ICLR 2026 submission) provides a quantization-aware safety patching pipeline that builds a safety-patching dataset from the pre-quantization model, applies DPO to realign the quantized model with its pre-quantization safety behavior, and selectively updates safety-critical weights -- on Llama-2-7B-Chat INT4 with challenging data the authors report attack success rates dropping from over 70-80% on naive quantization back to ~13-14% while preserving utility, which is a useful concrete data point that "we re-ran safety tests and they passed" should not be confused with "we applied a safety patch where the quantized model degraded." Alignment-Aware Quantization (AAQ) ([arXiv:2511.07842](https://arxiv.org/abs/2511.07842)) takes a complementary approach: a contrastive objective leveraging both the safe fine-tuned model and the unsafe pre-trained model applies an alignment-aware transformation before quantization, ensuring the compressed model retains safety refusal behavior; tested across Llama 2/3, Qwen 2, and Mistral 7B, AAQ significantly outperforms naive quantization on safety benchmarks while preserving task accuracy. Larger models (70B+) exhibit more consistent behavior across quantization methods; smaller models show greater divergence in safety performance. Vector quantization methods (AQLM) demonstrate superior stability compared to scalar approaches at extreme compression. The [UniComp evaluation framework](https://arxiv.org/abs/2602.09130) (arXiv:2602.09130, 2026) evaluates pruning, quantization, and distillation jointly and finds a consistent knowledge bias -- factual recall is largely preserved while multi-step reasoning, multilingual ability, and instruction-following degrade -- plus a decoupling between performance and reliability that argues against trusting accuracy-only re-evaluation. The NSA/CISA advisory (March 2026) recommends regular testing as a key mitigation. Two May 2026 studies sharpen the case for mandatory re-evaluation: [Quantization Undoes Alignment](https://arxiv.org/abs/2605.15208) (arXiv:2605.15208) finds that across Qwen2.5-7B, Mistral-7B, and Phi-3.5-mini, even quantization that keeps perplexity loss under 0.5% at 8-bit and under 3% at 4-bit introduces new biased behavior in 2.5-5.6% of items at 4-bit and 6-21% at 3-bit -- a dose-response confirmed by logistic regression -- so an accuracy-only re-eval can miss a real safety regression; [Aligned but Fragile](https://arxiv.org/abs/2605.29396) (arXiv:2605.29396) shows alignment degrades under W4A4 quantization and proposes a zeroth-order-optimization hardening pass. The compressed-artifact gate also has to exercise the loader and serving path, not just the weights: SGLang [CVE-2026-5760](https://kb.cert.org/vuls/id/915947) (CVSS 9.8) shows a poisoned GGUF `tokenizer.chat_template` reaching unsandboxed Jinja2 execution, and [Hugging Face Transformers CVE-2026-4372](https://www.csoonline.com/article/4181094/hugging-face-transformers-rce-flaw-enables-stealthy-compromise-via-ai-model-configs.html) (fixed in 5.3.0) shows a swapped-in artifact re-resolving config through an older `transformers` can re-introduce a `trust_remote_code` bypass, so the re-evaluation record should pin the serving-framework and library versions alongside the compressed digest. Edge-specific and modality-aware safety benchmarks remain gaps -- most benchmarks focus on text-only LLMs. *(Updated 2026-06)* |

## Related Pages

- [C06.1: Pretrained Model Vetting](../C06-Supply-Chain/C06-01-Pretrained-Model-Vetting.md) complements validation gates with model-origin, signing, quarantine, and inherited-backdoor checks before a model reaches deployment.
- [C02.1: Prompt Injection Defense](../C02-User-Input-Validation/C02-01-Prompt-Injection-Defense.md) supplies prompt-injection, tool-output, and MCP attack scenarios that should be replayed before model promotion.
- [C06.2: Trusted Source Enforcement](../C06-Supply-Chain/C06-02-Trusted-Source-Enforcement.md) ties model-validation evidence to approved registries, signing keys, source pinning, and trusted artifact acquisition.
- [C06.4: Supply Chain Attack Monitoring](../C06-Supply-Chain/C06-04-Supply-Chain-Attack-Monitoring.md) helps decide when active exploitation in model hubs, packages, or workflow builders should block a release gate.
- [C06.5: AI BOM & Model Artifacts](../C06-Supply-Chain/C06-05-AI-BOM-Model-Artifacts.md) extends the evidence packet with AI-BOM fields, artifact digests, signatures, and deploy-time verification records.

---
