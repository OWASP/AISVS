# C8.1: Access Controls on Memory & RAG Indices

> [Back to C08 Index](C08-Memory-and-Embeddings.md)
> **Last Researched:** 2026-06-09

## Purpose

Enforce fine-grained access controls and query-time scope enforcement for every memory store, vector collection, retrieval index, and semantic cache. A RAG system should never retrieve content merely because it is semantically close to the question; it should retrieve content only when the caller, agent, service principal, and current task are authorized to see it.

The recurring failure mode is permission stripping during ingestion. Connectors for SharePoint, Confluence, file shares, ticket systems, and object stores often convert source documents into chunks and embeddings while dropping the source ACL, writer identity, sensitivity label, or revocation state. Once that happens, the vector store has no reliable basis for enforcing access at query time, and the application is left to guess or filter after retrieval. That is why C8.1 treats provenance, immutable metadata, retrieval logging, canary records, and anomaly detection as part of access control rather than as separate observability concerns.

As of May 2026, the practical implementation pattern is clearer than it was a year ago: store authorization metadata with every chunk, inject the caller's scope server-side, push filters into the vector/search engine whenever possible, and keep post-retrieval checks as a backup rather than the primary control. Azure AI Search now documents native document-level access control patterns using Microsoft Entra tokens, POSIX-like ACL/RBAC scopes, SharePoint ACLs, and Purview sensitivity labels. The 2025-05-01-preview REST API introduced native ACL/RBAC enforcement, and 2025-11-01-preview added the explicit `x-ms-elevated-read` header so that troubleshooting paths must be opted into instead of accidentally bypassing ACLs. Databricks Mosaic AI Vector Search is governed through Unity Catalog and explicit vector-index privileges, but native row-level and column-level permissions are still not supported on vector indexes, so multi-tenant deployments have to enforce per-row scope through the filter API in the application path. Pinecone namespaces, Weaviate tenants, and Qdrant payload indexes are still useful isolation primitives, but they need server-side enforcement and regression tests so a missing filter cannot silently become a cross-tenant leak.

The threat landscape also keeps pushing access control toward provenance and monitoring. OWASP LLM08:2025 calls out unauthorized vector access, cross-context leakage, poisoning, and weak retrieval monitoring. MITRE ATLAS v5.6.0 separates RAG Poisoning (AML.T0070) from RAG database collection (AML.T0085.000), which is a useful reminder that defenders need both write-path integrity controls and query-path data leakage detection. PoisonedRAG showed that five malicious texts can drive a 90% attack success rate in large corpora; the May 2025 One-Shot Dominance / AuthChain work pushed that down to a single document with high stealth against existing RAG defenses. The April 2026 Black-Hole Attack showed centroid-positioned vectors appearing in up to 99.85% of top-10 results. Semantic caches add another retrieval surface: the January 2026 Key Collision Attack showed that embedding-based cache keys behave like fuzzy hashes and can be collided for response hijacking. Access controls therefore need to cover insert, update, delete, query, retrieval logging, cache hit paths, restricted-index canaries, and incident replay. The detection side finally has packaged tooling: in February 2026 Cisco's AI Defense team released HubScan, an open-source scanner that finds adversarial hubs — vectors engineered to appear in top-k results for a disproportionate share of queries — across FAISS, Pinecone, Qdrant, and Weaviate indexes.

The connector and copilot incidents over the past year keep validating these priorities. Microsoft's January-February 2026 Copilot Chat bug (CW1226324) processed emails marked `Confidential` and `Highly Confidential` from Sent Items and Drafts despite Purview sensitivity labels and DLP policies being correctly configured. It was the second sensitivity-label bypass in eight months, after the June 2025 EchoLeak (CVE-2025-32711) zero-click exfiltration. Concentric AI's 2026 data risk reporting still shows roughly 16% of business-critical data overshared, averaging around 800,000 at-risk files per organization, which makes connector-stage ACL propagation and document-level filters the most leveraged controls in any enterprise RAG rollout.

---

## Requirements

| # | Requirement | Level | Threat Mitigated | Verification Approach | Gaps / Notes |
|---|-------------|:-----:|-----------------|----------------------|--------------|
| **8.1.1** | **Verify that** vector insert, update, delete, and query operations are enforced with namespace/collection/document-tag scope controls (e.g., tenant ID, user ID, data classification labels) with default-deny. | 1 | Cross-tenant retrieval, unauthorized embedding reads, and write-path poisoning where a caller can insert or mutate vectors outside its tenant or data classification. This maps directly to OWASP LLM08:2025 Vector and Embedding Weaknesses and also supports MITRE ATLAS RAG Poisoning and RAG Credential Harvesting scenarios. The Microsoft Copilot CW1226324 incident (January 2026) showed that even when sensitivity labels and DLP policies are correctly applied, a single retrieval code path with the wrong scope can surface `Confidential` and `Highly Confidential` messages from Sent Items and Drafts. | Review the vector/search query path and confirm tenant, user, role, data-classification, and source-system ACL filters are injected by trusted server code, not accepted from the client. Attempt unauthenticated, wrong-tenant, wrong-role, and omitted-filter queries and verify they fail closed. Test the troubleshooting and elevated-read paths separately because misconfigured Azure AI Search clients can bypass ACLs by silently sending `x-ms-elevated-read` from the 2025-11-01-preview API. Seed canary chunks in each tenant and confirm no other tenant can retrieve, update, delete, cache-hit, or cite them. Inspect authorization decision logs for the policy version, caller identity, resource IDs, and denied matches. | Native controls vary by platform. Pinecone recommends one namespace per tenant for serverless multitenancy; Weaviate tenants are isolated in separate shards; Qdrant recommends payload partitioning with `is_tenant=true` indexes and tiered multitenancy for large tenants. Azure AI Search can enforce Microsoft Entra-backed document permissions at query time with `x-ms-query-source-authorization` (REST 2025-05-01-preview and later) and explicit elevated-read for break-glass scenarios. Databricks Mosaic AI Vector Search inherits Unity Catalog `USE CATALOG`/`USE SCHEMA`/`SELECT` privileges but does not yet provide native row- or column-level vector permissions, so per-document scope must be enforced through the filter API. Zanzibar-style systems such as SpiceDB, OpenFGA, and Cerbos are increasingly used to convert relationship-based authorization into vector metadata filters or post-retrieval permission checks. Application-only filters remain fragile: one missing `tenant_id` predicate or one overbroad service principal can expose a shared index. Milvus operators should patch CVE-2025-64513 (vulnerable proxy path with unauthenticated administrative access) and CVE-2026-26190 (metrics-port authentication bypass exposing REST API and sensitive operations on weakly isolated deployments). |
| **8.1.2** | **Verify that** every ingested document is tagged at write time with source, writer identity (authenticated user or system principal), timestamp, batch ID, and embedding model version. | 2 | Data provenance attacks, false RAG entries, and "ghost documents" inserted by compromised ingestion jobs. Without source, writer, timestamp, batch, and embedding-model metadata, responders cannot determine which poisoned vectors to quarantine or which model version needs re-embedding. The April 2026 RAGShield work showed that adding C2PA-inspired cryptographic attestations and trust-weighted retrieval can drive attack success rates to 0% on the evaluated benchmarks, but only when every chunk preserves verifiable signer identity and source chain. | Inspect ingestion code and stored vector metadata for required fields on every chunk. Query a sample from each connector and verify the source-system object ID, ACL snapshot or policy reference, writer principal, ingestion job ID, timestamp, content hash, embedding model name/version, and chunking strategy are present. Attempt direct writes that omit provenance fields and verify rejection. Reconcile batch logs against vector counts so missing or extra chunks are visible. For high-trust corpora, validate that ingestion signs each chunk and that retrieval rejects or downgrades chunks whose signatures or provenance chains do not validate. | Provenance has to be captured before chunking and embedding, not reconstructed afterward. RAGForensics and similar traceback approaches depend on retrieval records and source metadata to identify poisoned texts. EU AI Act record-keeping expectations for high-risk systems and NIST AI RMF evidence practices both push teams toward durable provenance records. For regulated deployments, keep source ACL snapshots or stable policy references with each chunk so permission changes can be replayed during incident review. Cryptographic document attestation (C2PA-style) and trust-weighted retrieval are still research-grade for general RAG, but the RAGShield evaluation is a useful reference for government and other high-trust deployments planning end-to-end signing. |
| **8.1.3** | **Verify that** document metadata tags applied at ingestion are immutable after the initial write and cannot be changed by later pipeline stages or user operations. | 2 | Metadata tampering after ingestion, where an attacker re-labels poisoned or restricted content as trusted, public, or owned by another tenant. This undermines quarantine, revocation, audit replay, and source-based authorization. | Try to overwrite source, owner, tenant, classification, timestamp, batch ID, content hash, and embedding model fields through the vector DB API, ingestion API, SDK upsert path, and bulk re-index job. Verify rejection or append-only versioning. Review storage design for WORM object storage, immutable Delta/lake commits, signed manifests, or hash-chain records that let auditors detect metadata changes. Confirm metadata-change attempts are logged as security events. | Many vector databases treat metadata as mutable payload by design. Pinecone upserts, Qdrant payload updates, Weaviate property updates, and Milvus field updates can be safe only when wrapped by an application policy that distinguishes content refresh from provenance mutation. Practical compensating controls include append-only metadata stores, signed ingestion manifests, content hashes stored outside the vector DB, and periodic reconciliation between source systems, object storage, and vector indexes. |
| **8.1.4** | **Verify that** RAG pipeline retrieval events log the query issued, the documents or chunks retrieved, similarity scores, the knowledge source, and whether retrieved content passed prompt injection scanning before it was added to the model context. | 2 | Undetected RAG poisoning, indirect prompt injection through retrieved content, unauthorized source exposure, and failed incident reconstruction. If retrieval events are incomplete, teams cannot prove which source influenced a response or whether an authorization filter failed. | Issue controlled queries and confirm logs include the normalized query, caller identity, authorization scope, vector index, cache-hit status, retrieved chunk IDs, source-system IDs, similarity/rerank scores, authorization decision, prompt-injection scan result, and downstream context inclusion decision. Replay sampled events from logs and verify the same chunks would still be allowed for the caller. Ship logs to a SIEM with retention and access controls appropriate for sensitive prompts and retrieved text. | Logging full prompts and chunks can create a secondary data store containing regulated content, so many teams log identifiers, hashes, classifications, scores, and scan decisions while storing raw text only in restricted forensic stores. Azure AI Search's document-level permission model and EU AI Act Article 12 both reinforce the need for traceable query-time decisions. RevPRAG and RAGForensics show why retrieval records matter: detection and traceback are much weaker when the system cannot link output behavior back to specific retrieved chunks. |
| **8.1.5** | **Verify that** restricted retrieval indices include uniquely marked canary records that contain no real sensitive content, with markers that survive retrieval, embedding, and context-assembly pipelines. | 2 | Silent cross-scope retrieval, broad RAG database probing, accidental inclusion of restricted chunks, and poisoning campaigns that push decoy records into ordinary result sets. The May 2025 One-Shot Dominance (AuthChain) work showed that a single optimized poisoned document can dominate retrieval for multi-hop queries against six common LLMs, so canary triggers are often the first observable signal that an attacker is probing or has already seeded a corpus. | Seed each restricted tenant, role, source system, sensitivity label, and semantic-cache namespace with synthetic canary chunks that look realistic to retrieval systems but contain no production secrets. Run scheduled probes from unauthorized users and service principals, then verify that any canary match, citation, cache hit, prompt-context inclusion, or tool-visible retrieval raises a high-severity alert with caller, query, index, chunk ID, policy version, and source scope. Include canary chunks tuned to look like "high-authority" documents so they trigger when AuthChain-style attackers attempt to inject trusted sources. Confirm alerts route to the SIEM and that analysts can quarantine the affected namespace or batch. | Canary records are detection controls, not authorization controls. They must be clearly marked in security-owned metadata, excluded from analytics and model training, and tuned so normal users do not see them. Thinkst Canarytokens-style tripwires are useful for document and URL access, but vector canaries also need retrieval-layer checks because similarity matches may never trigger an embedded URL or file-open event. Avoid placing real credentials, real personal data, or operational secrets in canaries. |
| **8.1.6** | **Verify that** a high-severity security alert is generated whenever a canary record is selected by retrieval, matched by similarity search, or passed to the model as context. | 2 | Silent canary hits. Without a reliable alert path the canary records from 8.1.5 are inert data: attackers probing restricted indices (MITRE ATLAS RAG database collection, AML.T0085.000), overbroad service principals, missing scope filters, and AuthChain-style poisoned "high-authority" documents all go unnoticed, and detection latency turns a contained probe into a prolonged exfiltration. The Copilot CW1226324 incident ran for weeks partly because nothing in the retrieval path treated label-violating retrievals as a security event. | Trigger every canary deliberately — from an unauthorized user, an overbroad service principal, the semantic-cache hit path, and the context-assembly path — and measure the full chain: retrieval event, detection rule, SIEM alert, paging. Confirm the alert carries caller identity, normalized query, index, chunk ID, policy version, and source scope, and that severity pages an analyst rather than landing on a dashboard. Verify the retrieval service raises the alert on a similarity match alone, before any URL is opened or document rendered, because embedded-URL tripwires never fire on vector matches. Review the detection-rule inventory against MITRE ATLAS techniques; Splunk's MITRE ATLAS AI Threat Detection app (Splunkbase 8527) ships ATLAS-mapped saved searches for prompt injection, exfiltration, and poisoning that teams extend with canary-hit rules over retrieval logs. Confirm the runbook links each alert to quarantine of the affected namespace or batch. | No major vector database emits native canary-hit alerts, so the retrieval service has to be instrumented — which means the alert path silently breaks when someone adds a new retrieval entrypoint, exactly the failure mode behind connector incidents. Canarytokens-style webhooks cover document-open and URL paths but not similarity-only matches. canari-llm (PyPI) generates deterministic fake secrets useful for prompt-injection canaries with near-zero false positives. Alert fatigue appears when legitimate broad-scope batch jobs touch canary namespaces; fix the job scopes rather than downgrading severity. MITRE ATLAS AI Telemetry Logging (AML.M0024) and Memory Hardening (AML.M0031) give auditors the framework hooks for this control. |
| **8.1.7** | **Verify that** retrieval anomaly detection identifies embedding density outliers, repeated dominance of specific documents in similarity results, and sudden shifts in retrieval bias distribution that may indicate vector database poisoning. | 3 | Poisoning that never breaks an ACL but manipulates what authorized users retrieve: adversarial hubs and centroid-positioned vectors (Black-Hole Attack, up to 99.85% of top-10 results), single-document dominance (One-Shot Dominance, May 2025), near-duplicate poisoned clusters, semantic-cache collisions, and slow retrieval-bias drift from coordinated campaigns. Maps to MITRE ATLAS RAG Poisoning (AML.T0070); OWASP LLM08:2025 lists weak retrieval monitoring as a core vector and embedding weakness. | Run an embedding-space scanner on a schedule and after every re-index. Cisco AI Defense's HubScan (open source, Apache 2.0, February 2026) detects adversarial hubs with median/MAD-based z-scores, cluster spread analysis, and query-perturbation stability testing across FAISS, Pinecone, Qdrant, and Weaviate, reporting 90% recall at a 0.2% alert budget on its benchmarks. Log similarity-score distributions and alert when a top-1 score sits two to three standard deviations above the corpus baseline or one document dominates top-k across unrelated queries. Track per-document retrieval-frequency histograms, per-tenant embedding density, centroid proximity, and stable benchmark-query drift over time. Red-team the detectors with synthetic poisoning — centroid-positioned vectors, single-document fixtures, near-duplicate clusters — and confirm detection fires, quarantine works by source/batch ID, and clean re-indexing restores baseline retrieval. | This is the least mature layer of C8.1. HubScan is the first packaged scanner, but it needs a representative query workload and per-corpus threshold tuning, and statistical detectors remain benchmark-validated; corpus-dependent attacks designed to blend into the target distribution (Semantic Chameleon, March 2026) are explicitly built to evade them. Robust-retrieval research reduces impact but is not detection: ReliabilityRAG (NeurIPS 2025) filters contradicting documents via maximum-independent-set analysis with provable robustness under bounded corruption, and RAGPart/RAGMask and ProGRank harden the retriever itself. RevPRAG and RAGForensics cover generation-time detection and post-incident traceback. No major vector database ships native poisoning analytics, so plan on offline scans plus SIEM rules over retrieval telemetry (ATLAS AML.M0024). |

---

## Implementation Notes

**Default-deny retrieval path.** Treat vector retrieval as a privileged database read. The caller's tenant, role, data classification clearance, source-system permissions, and task scope should be computed server-side and injected into the query. The client should never be allowed to choose its own authorization filter.

**Source ACL propagation.** For connector-based ingestion, preserve the source object's stable ID, ACL or policy reference, sensitivity label, owner, and revocation state before chunking. If the connector cannot provide reliable ACLs, route that content to a separate low-trust index rather than mixing it into the enterprise retrieval pool.

**Pre-filter and post-filter tradeoff.** Pre-filtering keeps unauthorized content out of vector ranking and model context, but it depends on accurate metadata and filter support. Post-filtering is useful as a defense-in-depth check, but over-fetching can still reveal side channels through counts, scores, latency, and empty-answer patterns. Log both allowed and denied candidate counts.

**Restricted-index canaries.** Place synthetic canary chunks in every restricted retrieval scope and treat any retrieval, citation, cache hit, or prompt-context inclusion as a high-confidence incident. The canary should identify its owning scope through security metadata, not through real secrets in the text. Exercise canaries from denied users, overbroad service principals, and cache-hit paths so the alert path is tested as often as the authorization path.

**Regression test set.** Maintain tenant canary documents, revoked documents, high-sensitivity documents, poisoned test chunks, semantic-cache collision probes, and stable benchmark queries. Run the set after connector changes, vector DB upgrades, embedding model changes, policy changes, and cache tuning.

**Incident response.** Quarantine must operate by source ID, batch ID, embedding model version, and content hash, not only by vector ID. During a poisoning incident, responders need to remove or suppress every chunk derived from the bad source and then replay retrieval logs to identify affected users and responses.

---

## Notable Incidents and Research

- January-February 2026: Microsoft's [Copilot Chat CW1226324 advisory](https://venturebeat.com/security/microsoft-copilot-ignoring-sensitivity-labels-dlp-cant-stop-ai-trust-failures) covered a retrieval-path bug that pulled emails from Sent Items and Drafts into the assistant's context even when the messages carried Purview `Confidential` or `Highly Confidential` labels and matching DLP policies. It was the second sensitivity-label bypass in eight months, after June 2025 [EchoLeak (CVE-2025-32711)](https://msrc.microsoft.com/update-guide/vulnerability/CVE-2025-32711), and confirms that document-level ACLs and DLP have to be enforced by trusted server code at every retrieval entrypoint, not relied on as a single perimeter.
- February 2026: [Milvus CVE-2026-26190](https://github.com/milvus-io/milvus/security/advisories) (critical, fixed in 2.5.27+ / 2.6.10+) and the earlier [CVE-2025-64513](https://nvd.nist.gov/vuln/detail/CVE-2025-64513) proxy-path bypass are reminders that vector-database control planes are themselves an authorization surface. Operators should pin patched versions, monitor advisories, and audit metrics/REST exposure on internal networks.
- February 2026: Cisco AI Defense released [HubScan](https://github.com/cisco-ai-defense/adversarial-hubness-detector), an Apache-2.0 scanner for adversarial hubs in RAG vector indexes, with the accompanying [hubness-poisoning paper](https://arxiv.org/abs/2602.22427) reporting 90% recall at a 0.2% alert budget and full recovery of domain-targeted attacks that escaped global detection across Food-101, MS-COCO, FiQA, and 1M MS MARCO web documents. First packaged tooling for the C8.1.7 control.
- March 2026: [Semantic Chameleon](https://arxiv.org/abs/2603.18034) demonstrated corpus-dependent poisoning crafted to match the statistical profile of the target corpus, directly targeting the perplexity, duplication, and outlier signals most anomaly-filtering defenses rely on. A reminder that 8.1.7 detectors need adversarial re-testing, not one-time validation.
- April 2026: [RAGShield](https://arxiv.org/abs/2604.00387) proposed a five-layer defense-in-depth framework that treats RAG knowledge bases as a software supply chain: C2PA-style cryptographic document attestation, trust-weighted retrieval, a formal taint lattice with contradiction detection, provenance-aware generation with auditable citations, and a NIST SP 800-53 mapping across 15 control families. Reported 0% attack-success rate against tested poisoning tiers (including adaptive attacks) on the evaluated benchmark.
- April 2026: the [Securing RAG taxonomy survey](https://arxiv.org/abs/2604.08304) organized known attacks and defenses across six workflow stages and three trust boundaries, and concluded that current defenses are "largely reactive and fragmented." Useful as a structured checklist when reviewing existing pipelines for missing controls at ingestion, retrieval, generation, and post-retrieval stages.
- December 2025: [RAGPart and RAGMask](https://arxiv.org/abs/2512.24268) introduced retrieval-stage defenses that fragment documents and aggregate across multiple retrievers (RAGPart) or use targeted token masking to flag suspicious similarity shifts (RAGMask). Both operate on the retriever, are computationally lightweight, and consistently reduce attack-success rates across four poisoning strategies and four retrievers without modifying the generation model.
- September 2025 (NeurIPS 2025): [ReliabilityRAG](https://arxiv.org/abs/2509.23519) introduced a graph-theoretic defense that runs maximum-independent-set analysis over a contradiction graph of retrieved documents, with provable robustness guarantees under bounded adversarial corruption — one of the few defenses with formal guarantees rather than benchmark-only results.
- May 2025: [One-Shot Dominance (AuthChain)](https://arxiv.org/abs/2505.11548) demonstrated that a single poisoned document can dominate retrieval and bypass common RAG defenses across six LLMs, including multi-hop queries. Lifts the urgency of single-document poisoning tests in any C8.1 verification fixture.
- 2026 enterprise data: Concentric AI's [2026 Data Risk Report](https://concentric.ai/too-much-access-microsoft-copilot-data-risks-explained/) and the [EPC Group Copilot oversharing guidance](https://www.epcgroup.net/copilot-sharepoint-permissions-oversharing-fix-2026) report roughly 16% of business-critical data overshared and around 800,000 at-risk files per organization. Connector-stage ACL propagation, SharePoint site cleanup, and sensitivity-label remediation should run before any Copilot or RAG production cutover.
- Earlier 2025-2026 research that remains directly relevant: [PoisonedRAG](https://www.usenix.org/conference/usenixsecurity25/presentation/zou-poisonedrag) (five documents, 90% ASR), [Black-Hole Attack](https://arxiv.org/abs/2604.05480) (centroid-positioned vectors in up to 99.85% of top-10 results), the [Key Collision Attack on semantic caching](https://arxiv.org/abs/2601.23088), [RevPRAG](https://aclanthology.org/2025.findings-emnlp.698/) for activation-based poisoned-response detection, and [RAGForensics](https://doi.org/10.1145/3696410.3714756) for traceback.

---

## Implementation Maturity

- Mature: tenant isolation at the namespace, collection, or sharded-tenant level is well supported by Pinecone, Weaviate, Qdrant, Milvus, and pgvector. The hard part is the application contract that injects scope filters and prevents client-supplied authorization.
- Mature: query-time document-level ACL enforcement on managed search platforms. Azure AI Search 2025-05-01-preview and later, Elastic's document-level security, and OpenSearch fine-grained access control are production-ready, with elevated-read break-glass paths that auditors can review.
- Developing: vector-database governance with row- and column-level permissions. Databricks Mosaic AI Vector Search inherits Unity Catalog object-level permissions but still requires application-level filter expressions for per-document scope.
- Developing: provenance and immutable metadata. Append-only metadata stores, signed manifests, content hashes, and per-chunk source-ACL snapshots are achievable today, but cryptographic document attestation (RAGShield, C2PA-style) is still research-grade for general RAG.
- Developing: relationship-based authorization for retrieval. SpiceDB (Authzed), OpenFGA, and Cerbos have documented LangChain/Pinecone integration paths and increasing production deployment, but most teams still hand-build the SpiceDB-to-vector filter bridge.
- Early: retrieval-anomaly detection for poisoning. HubScan (February 2026) is the first packaged open-source scanner and covers FAISS, Pinecone, Qdrant, and Weaviate, but it is an offline/scheduled scan rather than a runtime control. RAGPart, RAGMask, ProGRank, ReliabilityRAG, RevPRAG, and RAGForensics round out retrieval- and generation-stage defenses, yet all remain benchmark-validated rather than turnkey, and adaptive corpus-matched attacks already target their detection signals.
- Early: canary-hit alerting. No vector database emits native canary alerts; Canarytokens and canari-llm cover document/URL and prompt-injection canaries, and Splunk's MITRE ATLAS app provides ATLAS-mapped SIEM rules, but the similarity-match alert path still has to be hand-built into every retrieval entrypoint.
- Early: connector ACL propagation for SharePoint, Confluence, Drive, ticket systems, and email. The 2026 Microsoft Copilot incidents show that even well-resourced vendors still ship retrieval paths that bypass labels, so verification has to test each connector independently rather than relying on platform claims.

---

## Cross-Chapter Links

- [C05-03 Query-Time Security Enforcement](../C05-Access-Control/C05-03-Query-Time-Security-Enforcement.md) — mandatory server-side filters and fail-closed query behavior that C8.1 retrieval depends on.
- [C05-06 Multi-Tenant Isolation](../C05-Access-Control/C05-06-Multi-Tenant-Isolation.md) — the tenant-scoping primitives behind vector-store namespaces, payload partitions, and source-system ACL propagation.
- [C08-02 Embedding Sanitization & Validation](C08-02-Embedding-Sanitization-Validation.md) — write-path validation, loader-stage poisoning fixtures, and memory-write authorization that complement C8.1's read-path access controls.
- [C08-03 Memory Expiry, Revocation & Deletion](C08-03-Memory-Expiry-Revocation-Deletion.md) — TTL and revocation semantics that keep authorization metadata aligned with source-system permission changes.
- [C08-05 Scope Enforcement for User-Specific Memory](C08-05-Scope-Enforcement-User-Memory.md) — user and agent memory scope, namespace uniqueness, and cross-user leakage tests.
- [C01-02 Training Data Security & Integrity](../C01-Training-Data/C01-02-Training-Data-Security-Integrity.md) — provenance, integrity, and rollback evidence patterns that mirror RAG ingestion controls.
- [C13-01 Request and Response Logging](../C13-Monitoring-and-Logging/C13-01-Request-Response-Logging.md) — tamper-evident logging requirements that the retrieval-decision logs in C8.1.4 inherit.

---

## References

* [OWASP LLM08:2025 Vector and Embedding Weaknesses](https://genai.owasp.org/llmrisk/llm082025-vector-and-embedding-weaknesses/)
* [MITRE ATLAS data repository](https://github.com/mitre-atlas/atlas-data)
* [MITRE ATLAS v5.6.0 data file](https://raw.githubusercontent.com/mitre-atlas/atlas-data/main/dist/ATLAS.yaml)
* [PoisonedRAG: Knowledge Corruption Attacks to Retrieval-Augmented Generation of Large Language Models](https://www.usenix.org/conference/usenixsecurity25/presentation/zou-poisonedrag)
* [RevPRAG: Revealing Poisoning Attacks in Retrieval-Augmented Generation through LLM Activation Analysis](https://aclanthology.org/2025.findings-emnlp.698/)
* [Traceback of Poisoning Attacks to Retrieval-Augmented Generation](https://doi.org/10.1145/3696410.3714756)
* [Can You Trust the Vectors in Your Vector Database? Black-Hole Attack from Embedding Space Defects](https://arxiv.org/abs/2604.05480)
* [From Similarity to Vulnerability: Key Collision Attack on LLM Semantic Caching](https://arxiv.org/abs/2601.23088)
* [Azure AI Search - Document-level access control](https://learn.microsoft.com/en-us/azure/search/search-document-level-access-overview)
* [Databricks Mosaic AI Vector Search](https://docs.databricks.com/aws/en/vector-search/vector-search)
* [Databricks Apps - Add a vector search index resource](https://docs.databricks.com/aws/en/dev-tools/databricks-apps/vector-search)
* [Pinecone - Implement multitenancy](https://docs.pinecone.io/guides/get-started/implement-multitenancy)
* [Weaviate - Multi-tenancy operations](https://weaviate.io/developers/weaviate/manage-data/multi-tenancy)
* [Qdrant - Configure multitenancy](https://qdrant.tech/documentation/guides/multitenancy/)
* [Authzed - Access Control in RAG with Pinecone and SpiceDB](https://authzed.com/docs/spicedb/integrations/pinecone)
* [Authzed - Use SpiceDB with LangChain and LangGraph for RAG authorization](https://authzed.com/docs/spicedb/integrations/langchain-spicedb)
* [Cerbos - Authorize RAG retrieval with dynamic filters](https://www.cerbos.dev/ecosystem/rag)
* [OpenFGA - Introduction to fine-grained authorization](https://openfga.dev/docs/fga)
* [Canarytokens documentation](https://docs.canarytokens.org/)
* [Milvus release notes - v2.6.5 security update](https://milvus.io/docs/id/release_notes.md)
* [NVD - CVE-2025-64513](https://nvd.nist.gov/vuln/detail/CVE-2025-64513)
* [Azure AI Search - Query-time ACL and RBAC enforcement](https://learn.microsoft.com/en-us/azure/search/search-query-access-control-rbac-enforcement)
* [Azure AI Foundry - Microsoft Entra-based document-level security in Azure AI Search](https://techcommunity.microsoft.com/blog/azure-ai-foundry-blog/announcing-enterprise-grade-microsoft-entra-based-document-level-security-in-azu/4418584)
* [RAGShield: Provenance-Verified Defense-in-Depth Against Knowledge Base Poisoning](https://arxiv.org/abs/2604.00387)
* [RAGPart & RAGMask: Retrieval-Stage Defenses Against Corpus Poisoning in RAG](https://arxiv.org/abs/2512.24268)
* [Securing Retrieval-Augmented Generation: A Taxonomy of Attacks, Defenses, and Future Directions](https://arxiv.org/abs/2604.08304)
* [One Shot Dominance: Knowledge Poisoning Attack on RAG Systems (AuthChain)](https://arxiv.org/abs/2505.11548)
* [Microsoft Copilot sensitivity-label bypass (CW1226324) - VentureBeat](https://venturebeat.com/security/microsoft-copilot-ignoring-sensitivity-labels-dlp-cant-stop-ai-trust-failures)
* [MSRC - EchoLeak CVE-2025-32711](https://msrc.microsoft.com/update-guide/vulnerability/CVE-2025-32711)
* [Concentric AI - 2026 Copilot data risk report](https://concentric.ai/too-much-access-microsoft-copilot-data-risks-explained/)
* [EPC Group - Copilot SharePoint oversharing fix guide (2026)](https://www.epcgroup.net/copilot-sharepoint-permissions-oversharing-fix-2026)
* [HubScan: Detecting Hubness Poisoning in Retrieval-Augmented Generation Systems](https://arxiv.org/abs/2602.22427)
* [Cisco AI Defense - adversarial-hubness-detector (HubScan)](https://github.com/cisco-ai-defense/adversarial-hubness-detector)
* [Cisco blog - Adversarial Hubness in RAG Systems](https://blogs.cisco.com/ai/your-models-memory-has-been-compromised-adversarial-hubness-in-rag-systems)
* [ReliabilityRAG: Effective and Provably Robust Defense for RAG-based Web-Search](https://arxiv.org/abs/2509.23519)
* [Semantic Chameleon: Corpus-Dependent Poisoning Attacks and Defenses in RAG Systems](https://arxiv.org/abs/2603.18034)
* [ProGRank: Probe-Gradient Reranking to Defend Dense-Retriever RAG from Corpus Poisoning](https://arxiv.org/abs/2603.22934)
* [MITRE ATLAS AI Threat Detection for Splunk](https://splunkbase.splunk.com/app/8527)
* [canari-llm - deterministic canary secrets for LLM pipelines](https://pypi.org/project/canari-llm/)
* [NIST AI RMF Generative AI Profile](https://www.nist.gov/publications/artificial-intelligence-risk-management-framework-generative-artificial-intelligence)
* [Regulation (EU) 2024/1689 - Artificial Intelligence Act](https://eur-lex.europa.eu/eli/reg/2024/1689/oj?locale=en)

---

## Related Pages

- [C05-03: Query-Time Security Enforcement](../C05-Access-Control/C05-03-Query-Time-Security-Enforcement.md) - Covers mandatory security filters and fail-closed query behavior that C8.1 relies on for vector and search retrieval.
- [C08-05: Scope Enforcement for User-Specific Memory](C08-05-Scope-Enforcement-User-Memory.md) - Extends these controls to user and agent memory, including namespace uniqueness and cross-user leakage tests.
- [C08: Memory, Embeddings & Vector Databases](C08-Memory-and-Embeddings.md) - Frames this page alongside embedding sanitization, retention, inversion defense, and multi-tenant scope enforcement.
- [C01-02: Training Data Security & Integrity](../C01-Training-Data/C01-02-Training-Data-Security-Integrity.md) - Links retrieval provenance back to the integrity controls, logging, and rollback evidence needed when indexed source data changes.
- [C01-03: Data Labeling & Annotation Security](../C01-Training-Data/C01-03-Data-Labeling-Annotation-Security.md) - Connects provenance, label integrity, and annotator audit trails to the metadata that C8.1 preserves during ingestion.

---

## Community Notes

_Space for contributor observations, discussion, and context that doesn't fit elsewhere._

---
